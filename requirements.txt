# ATLAS engine dependencies (Python 3.14 venv).
# Deterministic spine (M1) needs only the light stack below - indicators are pure-numpy
# (per docs/08: "Implement support/resistance, gaps, volume-anomaly, relative-strength as
# your own small unit-tested functions" - better reproducibility than any TA library).
numpy
pandas
pydantic>=2
PyYAML
jsonschema

# Data plane: httpx is the HTTP client behind the Tradier + news collectors; pyarrow backs the
# parquet round-trips in the daily-append path; alpaca-py is the optional secondary bar feed
# (imported at module scope by the news collector, so it is required for a clean import).
httpx
pyarrow
alpaca-py

# Test
pytest

# Scout vision (--scout modality: vision, screenshot source). After install run:
#   python -m playwright install chromium
# Not needed for the text scout path or the core engine.
playwright

# --- Added in later milestones (kept here for reference, install when reached) ---
# finnhub-python     # M3 news / earnings calendar
# instructor         # M2 constrained-decoding retry wrapper
# outlines           # M2 grammar-constrained JSON
# keeks              # M1+ Kelly ceiling (we use fixed-fractional as the binding rule)
# sentence-transformers  # M4 local embeddings for retrieval relevance
# huggingface_hub[hf_xet]  # model download (already installed)
