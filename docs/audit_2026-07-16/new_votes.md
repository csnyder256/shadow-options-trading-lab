### MATH-CORE-1 [high] p_regain 0DTE double-decays the clock: zero_dte_effective_T is fed the horizon-END model time, violating its own derivation
- UPHELD(degrades-edge): Finding survives every refutation attempt. (1) Derivation check: zero_dte_effective_T's own docstring derives T_eff(end)=T_now*exp(-2I) from the CURRENT time value (TV proportional to sqrt(T)); math.py:299 instead feeds it the horizon-END model time, so coded t_minus = (T_now-dt_h)*exp(-2I) = model decay x empirical decay for the same interval - an exact double charge (sqrt-te~
- UPHELD(degrades-edge): Verified, not refuted. Numeric repro reproduces exactly (I=0.1793; coded t_minus 41.9min vs derivation-intent 125.8min vs plain model 60min; p_regain 0.533 coded / 0.826 intent / 0.598 plain-model). The call at math.py:299 feeds horizon-END model time into zero_dte_effective_T, whose own derivation (math.py:264-269), exit_engine.py:237-238 ("REPLACES the model clock"), and doc~
### MATH-CORE-2 [high] p_regain systematically understates true regain probability 2-3x and its 'conservative-toward-holding' label is backwards
- REFUTED(none): The finding's benchmark is degenerate: c_ref is TODAY'S mid (rule 18), so the value process starts ON the barrier and the auditor's 'TRUE' event (value >= c_ref at any mark) tends to probability 1 for every profitable position - my MC reproduces their exact numbers (0.913/0.850/0.853) at 1-minute discrete marks, but the same quantity is 0.97 at 5-second marks and 0.9998 bridge~
- REFUTED(cosmetic): The math reproduces exactly (shipped 0.530/0.330/0.328 match; true anytime-regain 0.85-0.91 on the auditor's grid measure; lower-bound direction confirmed), but the finding fails the reachability/materiality lens. (1) Gate order: rule (h) ev_hold<ev_sell precedes (i*) in decide_exit (exit_engine.py:385-398), and in a 3,560-state scan of the flagged region every shipped-p<0.25 ~
### TRAJECTORY-1 [critical] Exit rules h/i* are a noise clock: 20-min annualized drift noise (SE ~ 14) vs a ~0.016 decision threshold guarantees a forced sale within ~~
- UPHELD(blocks-goal): Independent re-derivation with the repo's own modules reproduces every claimed number exactly: mu_flip=+0.0156 (bisected on om.ev_hold for the ATM SPY example), SE=14.020, t_flip=-0.412 giving P(h fires per mark|H0)=0.340, and the 0.19-0.37 per-mark band across mu_thesis 0.5-9. A full-pipeline Monte Carlo (1-min GBM bars through underlying_state -> mu_blend -> ev_hold, 5-min m~
- UPHELD(blocks-goal): Every number reproduces with the repo's own code: mu_flip=+0.01566 (claimed +0.0156), t_flip=-0.412, per-mark fire P=0.340 under zero drift; SE=14.02. The path is live and unfiltered: run_options_shadow.py:1058-1073 feeds the 20-min mu_hat/t_stat into decide_exit every 5-min mark, and rule h has no debounce or grace period. End-to-end Monte Carlo through the repo's own underly~
### TRAJECTORY-2 [high] w = t^2/(1+t^2) is a scale-free gate that overweights a +/-1400%-annualized-noise estimate ~20-40x relative to any calibrated Bayes weight
- UPHELD(degrades-edge): Every number reproduces independently: SE=iv/sqrt(T_K)=14.02 annualized (iv=0.20, 20-min window); under pure noise E[w]=0.344, P(w>0.5)=0.317, std(w*mu_hat)=9.57; conjugate w*=0.020 (tau=2)/0.0051 (tau=1) so live w(t=1)=0.5 is 25-98x; blend outputs -4.79/-9.97/+18.71 match the claimed -4.76/-9.90/+18.7, verified end-to-end through underlying_state(). Live-path: mu_eff is the d~
- REFUTED(none): The sim arithmetic reproduces exactly (E[w]=0.344, P(w>0.5)=0.317, std=9.56), but the claim fails on its own calibration procedure and on materiality. (1) The "20-40x vs any calibrated Bayes weight" benchmark uses tau in {1,2} justified by "the lane's own 100% thesis" - factually wrong: the only live entry ever recorded has mu_thesis=-8.218, and lanes.py's _mu_from_target gene~
### TRAJECTORY-3 [high] Endpoint-only estimator is maximally microstructure-sensitive: one 10bp stale print at a window edge moves t by 0.35 sigma - alone comparab~
- REFUTED(cosmetic): The delta_t arithmetic checks out (verified: 0.35 sigma per 10bp at iv=0.20), but the finding's conclusion fails independent re-derivation at the decision layer and is unreachable on the live path. (1) No "0.41 sigma h-fire threshold" exists in code - t enters only via w=t^2/(1+t^2) shrinkage blended against mu_thesis, then an EV comparison; under pure GBM on a CLEAN tape |t|>~
- REFUTED(cosmetic): Arithmetic reproduces (0.35 sigma per 10bp at iv=0.20) but the claim dies on reachability: (1) the "0.41 sigma rule-h threshold" does not exist - h is ev_hold(mu_eff)<ev_sell (exit_engine.py:385) and at every h evaluation thesis_valid=True (rule b at :357 sells first), so mu_prior=mu_thesis (+1.4 to +14/yr from _mu_from_target), never the weak prior the sensitivity assumes; me~
### TRAJECTORY-4 [high] Optional-stopping cadence dependence: the first-match ladder re-samples the noisy t every mark, so the probability of a noise exit compound~
- REFUTED(cosmetic): Re-derived the statistics and re-ran the repro with the production code (atlas/options/exit_engine.py, trajectory.py, scripts/run_options_shadow.py). The code description is accurate (h/i* single-mark, g debounced, 300/120/60s cadence), but every load-bearing quantitative and mechanistic claim fails. (1) The compounding model is wrong: the rolling t is a continuous process, so~
- REFUTED(cosmetic): The claim's centerpiece is unreachable and its measurable effect is nil. (1) The 60s last-30-min cadence window (minute>=930, dte==0) can never evaluate h/i*: first-match rule (a) a_zero_dte_clock (exit_engine.py:354, sell_clock<=930) preempts on the first mark - verified by calling the real decide_exit with maximally hostile inputs at minute 935. The 'final half hour' is alre~
### LANES-1 [critical] Zero-hysteresis, single-tick, growing-barrier invalidation makes rule (b) a near-guaranteed minutes-scale exit
- REFUTED(cosmetic): Descriptive core reproduces but the defect claim dies. Replayed the ACTUAL lane code over 1,024 real entries (500+ cached sessions x IWM/SPY/QQQ, live 300s mark cadence, causal VWAP, trailing-14d profiles): median hold 10 min, 37.5% cut <=5 min, 74.8% <=30 min - the claimed rates are roughly right and are the DOCUMENTED design (rules 12/20 'sell winners too'). But (1) the cuts~
- REFUTED(cosmetic): Mechanics confirmed (single-tick 300s-polled zero-buffer invalidation; live 07-13 trade entered 1.07bps past the band and was b-cut in one 306s reval; 505-entry replay over ~260 real sessions shows 92% eventual cut, median 10 min, 34% at first reval) - but the defect claim fails on materiality. Paired A/B on the same 505 entries of the finding's own proposed fix (0.8x frozen b~
### LANES-2 [high] One-per-side daily latch burns at EMISSION, so selector rejections (the observed death mode) permanently disarm the lane for the day
- REFUTED(cosmetic): Mechanical kernel is accurate (latch burns at emission, lanes.py:252/:426; no release on no_pick, run_options_shadow.py:745; no TTL retry) but the finding's causal and numeric content fails re-derivation. (1) The latch cannot be a 'direct mechanical cause of 0.14 fires/day': fires are counted at emission, upstream of the latch; it only suppresses repeats, resets daily (_roll_d~
- REFUTED(cosmetic): The mechanical reading is correct (lanes.py:252/426 arm _fired before return; runner handles each signal once, no release), but the causal claim fails on the live path. (1) The headline scenario never happened: the only open-cluster index fire (IWM call 09:35 on 07-13, move 0.207% vs noise 0.191%) burned a side that could never re-fire - max move-from-open afterward was +0.17%~
### LANES-3 [high] range_percentile_14d gate is scale-invariant: it deletes a fixed ~43% of trading days and cannot detect a vol regime by construction
- UPHELD(degrades-edge): Finding survives independent re-derivation and a 2-year empirical replay on the live caches. Mechanics confirmed: lanes.py:203 self-inclusive rank, lanes.py:235 <50 stand-down, live defaults range_percentile_min=50/lookback=14 with no hunter.yaml override, n_days=14 on every rolled day (the n=5 branch never occurs live). Analytic 42.9% stand-down confirmed; empirical stand-dow~
- UPHELD(degrades-edge): CONFIRMED, not refuted - my replays reproduced the finding's numbers on real data. (1) Math: lanes.py:203 pct is a pure within-window rank (scale-invariant; self-inclusion floors it at 7.1); pass = rank>=7/14 = 57.1% under exchangeability. (2) Reachable: DEFAULTS range_percentile_min=50 (run_options_shadow.py:73; no hunter.yaml override, cfg hash 6c1dc2a4e1a7), first check in ~
### LANES-4 [high] Last30's 15%-of-move target cannot arithmetically clear the selector's EV floors at p_thesis=0.5 except on ~2-sigma days
- UPHELD(degrades-edge): Independent re-derivation with the real code confirms the core claim, and the live ledger confirms it empirically. Running select_contract/ev_hold_thesis on synthetic 1DTE chains at 15:30 (p_thesis=0.5, default SelectorParams, DTE>=1-only chain as the runner builds it): the lane's target=max(0.15|ret|,10bps) first clears the floors at |ret|~1.75% / target ~26bps with 1DTE IV>=~
- REFUTED(degrades-edge): The finding's headline claim ("cannot clear EV floors except ~2-sigma days; designed-in no_pick generator, pure starvation") fails on re-execution with the real code. (1) The auditor's numeric repro is unreachable on the live path: their SPY contract (delta 0.6, mid $2.10, S=630) has lambda=180 and is rejected at lambda_band (15-90) BEFORE the EV stage - verified against selec~
### LANES-5 [high] IndexTrend noise band is the MEAN |move|, which pure noise exceeds 42.5% of the time at any boundary
- UPHELD(degrades-edge): Independent re-derivation CONFIRMS the finding on every axis. (1) Math exact: P(|X|>E|X|)=0.4249 for Gaussian X. (2) Real live-path replay (rolling out-of-sample 14-session profiles via the actual build_noise_profile on the actual intraday cache, exact trigger semantics incl. 5-min boundary closes, VWAP side, pre-open bar filter, percentile>=50 arming, per-side latch): per-bou~
- UPHELD(degrades-edge): Core claim CONFIRMED by independent replay of 496-515 cached sessions/symbol through the actual build_noise_profile + IndexTrendLane code with live-path gates (trailing-14 profiles, range-percentile>=50, 5-min boundary, VWAP side, per-side latch; defaults rule since config/hunter.yaml has no options_shadow block): boundary exceedance on armed days = 0.44-0.45 with the VWAP sid~
### LANES-6 [high] No target-vs-remaining-time feasibility check: late-session fires request full-day-scale moves in minutes and pass the selector as priced l~
- UPHELD(degrades-edge): Independent re-derivation CONFIRMS the finding end-to-end with repo code. (1) lanes.py:254: the 2x-noise target floor grows ~sqrt(elapsed) (0.19% at 10:00 -> 0.54% at 15:49, synthetic GBM at daily sigma 0.756%) while 0.35x remaining_range shrinks, so the floor dominates from midday and a 15:50 IndexTrend fire emits target 1.086% over a 10-min horizon, mu_thesis=106/yr. (2) mat~
- REFUTED(cosmetic): The arithmetic is right but the severity chain is wrong on the live path. Verified with real cache data: a 15:50 index fire does produce mu_thesis~91/yr (7.6 sigma) and mixture p_profit 0.74 passes the 0.40 floor. However: (1) the 'priced lottery' framing is false - selector gates (zero_dte_after_1400, delta 0.40-0.80, lotto ban, lambda<=90, spread) reject every cheap short-da~
### SELECTOR-1 [high] Lambda band (<=90) x delta gate (0.40-0.80) is jointly EMPTY for index options at current IV - structural starvation; the registered 0DTE c~
- REFUTED(degrades-edge): Independent re-derivation with the repo's own pricing stack (bs_price/implied_vol/greeks/trading_T, the finding's own S=630/$1-strike/T_cal=(dte+0.27)/365 conventions) disproves the central numeric claim: DTE-2 is NOT empty at cal IV 0.13 - K=625 passes both gates (delta 0.786, lam 83.6) even on the finding's late-day clock, and at a morning clock DTE-2 passes at 0.12 (2 strik~
- REFUTED(cosmetic): The claim's central assertions fail on the repo's own math, real chain data, and the live record. (1) Its "DTE-2 empty at calIV 12-13%" repro only scanned CALLS: re-running the selector's exact pipeline (bs_price -> implied_vol under trading_T -> greeks, selector.py:201-213) with both types shows PUTS pass at calIV 0.12 (P635 lam 87.3), 0.13 (3 strikes), 0.14 (5 incl. calls) - ~
### SELECTOR-2 [high] hold<=1/3-of-life gate bans ALL DTE-1 contracts before 12:45 ET for rest-of-day signals; combined with SELECTOR-1 the morning index funnel ~
- UPHELD(degrades-edge): Core claim survives independent re-derivation exactly: with hold_max_frac_of_life=1/3 (selector.py:68) and only dte==0 exempt (selector.py:176), a rest-of-day horizon (lanes.py:105-109; 0.9872 td at 09:35) exceeds DTE-1's T/3 (0.6624 td via trading_T) until the selector-call minute reaches 765 = 12:45 ET - verified by executing the actual code, matching the repro to 4 decimals~
- REFUTED(none): The arithmetic is right (DTE-1 is hold-gate-banned before 12:45 for rest-of-day signals; live 55+110=165 rejections confirmed in runtime/options_shadow_journal.jsonl) but the finding's causal and materiality claims fail. Counterfactual replay of BOTH live morning events through the repo's own select_contract - at the actual archived ATM IVs (options_iv.db: SPY DTE-1 12.5%, IWM~
### SELECTOR-3 [high] EV stage measures target ambition, not edge: flat p_thesis=0.5 x a drift that attains the target by construction makes ev_pct ~linear in ta~
- UPHELD(degrades-edge): Independent re-derivation confirms the finding on every axis; my best kill attempts failed. (1) Premises verified in code: math.py:209 sets mu_t=ln(1+target)/horizon (mean-attains-target by construction; E_thesis[value]=3.92 >= BSM-at-target 3.79 via Jensen), p_thesis is the flat runner config 0.5 (run_options_shadow.py:72) passed unmodified to all four lanes (:440-449) and st~
- UPHELD(degrades-edge): Every cited number reproduces exactly against the repo's own modules (IWM 2026-07-13: solved_iv .1803, ev_pct 53.31, p_profit .686, p_touch .086, mu=0 branch -1.18%/.432, 1.71-sigma/140-min target), and the defect is fully on the live path: run_options_shadow.py:72 hardcodes p_thesis=0.5 into all four lanes and _enter() feeds it straight into select_contract, whose ev_pct_floo~
### SELECTOR-7 [high] Non-index spread gates (<=10% AND <=$0.30) are unpassable for lane-2 catalyst names - the lane has never produced a pick
- REFUTED(none): Falsified by live evidence dated the morning after the audit window: runtime/options_shadow_entries.jsonl line 2 shows a lane-2 (inplay_orb) shadow_entry on 2026-07-16 at 09:36 ET - NVDA 260720 P210, catalyst=true (kind=product, first5_rvol 9.63), bid/ask 3.80/3.95, spread $0.15 <= $0.30 abs cap, spread_pct 3.87% <= 10% cap, non-index so both gates at selector.py:183-186 appli~
- REFUTED(none): Live counterexample kills the core claim: on 2026-07-16 09:36 ET (6 min after the open, inside the finding's own 'first 15 minutes' window), lane-2 inplay_orb entered NVDA260720P00210000 (catalyst_kind=product, first5_rvol 9.63) under the same config_hash 6c1dc2a4e1a7 - spread $0.15 / 3.87% of mid passed both gates (caps $0.30 / 10%), and the row cleared OI, volume, delta (−0.~
### SELECTOR-6 [high] OI/volume floors use mismatched clocks: prior-day OI + full-day volume bar judged 5-15 minutes into the session
- UPHELD(degrades-edge): Finding survives independent re-derivation. Repro is exact: journal shows 9 no_picks; IWM 07-13 fire rejections {OI:101,no_quote:66,hold:55,spread:10,lotto:2,lambda:1,volume:2}=237=all 474/2 call rows, 0 rows reached EV; cross-journal OI=473 vs volume=4. Mechanism confirmed in code: floors at selector.py:55-57, OI-before-volume at :188-192, live params via SelectorParams.for_c~
### EXIT-ENGINE-1 [critical] Rule (b) thesis_invalid is a structurally guaranteed insta-cut for marginal lane-1 entries: no debounce, tick-sampled, against a rising bar~
- REFUTED(none): Mechanism confirmed (rule (b) is first-match, undebounced, tick-sampled vs a growing band; the IWM exit was a band re-entry at the first 300s mark, arithmetic reproduces exactly) - but the claim fails on materiality and generalization. (1) The only live exercise was truth-validated CORRECT by the platform's own 07-15 retro-grading: IWM reversed up during the hold and the put e~
### SHADOW-LEDGER-2 [high] Rebuild fails open on an unreadable exits file (mass position resurrection) and append has no torn-line guard; the grader never dedupes exi~
- UPHELD(degrades-edge): Failed to refute. Independent re-derivation confirmed every load-bearing prong with live code execution: (1) a Windows share-mode-0 lock really does raise PermissionError->OSError on read_text, making read_jsonl (shadow.py:253-256) return [] - the fail-open trigger is reachable on this box; (2) with exits unreadable, ShadowLedger.open_positions_all() resurrected the already-ex~
- UPHELD(degrades-edge): Core claim survives execution-verified attack. Confirmed by running the real builders + grader in a temp dir (venv, read-only on repo): (1) read_jsonl returns [] on OSError (shadow.py:253-256), so _open_positions (:333) resurrects an exited unexpired position - reproduced deterministically; (2) the runner's day-roll (run_options_shadow.py:461-473) re-tracks it and its exit pat~