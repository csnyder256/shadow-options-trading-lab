# PART 2 DIGEST
## SEAM probability-measure
VERDICT: NO - there is no single probability measure end-to-end; there are at least FIVE unreconciled measures for the same target event across one position's life. The live IWM 293P trade carried its target-attainment probability simultaneously as 0.042 (market risk-neutral, computed from the trade's own solved IV), 0.086 (the selector's own logged p_touch_target - computed, written to the ledger, gated on nothing), 0.5 (the hardcoded entry mixture), ~1.0 at exit (p_target_thesis: p_touch under the FULL mu_thesis extrapolated over the option's whole 2.3-day life instead of the 140-min horizon that defined it), and then 0.0 the instant rule (b) tripped. The claimed selector edge (ev_pct 10-53%) is not a market-mispricing claim: the market-implied mixture weight for the live trade is -0.004 (RN EV = -1.61% of ask, i.e. exactly transaction costs), so 100% of the +53.31% is manufactured by the p_thesis=0.5 prior on a drift that attains the target by construction. ev_pct is monotone in target ambition (6.5% at a -0.20% target to 176% at -3.0% on the same contract) while true attainability collapses to zero - the EV stage prices ambition, not edge. All numbers reproduced with the repo's own cod~
- [critical] PROBABILITY-MEASURE-1: The selector's ev_pct (10-53%) is manufactured by the prior, not a market-mispricing claim: mu_thesis = ln(1+target)/horizon makes the thesis branch's forward attain the target price EXACTLY (reproduced: 289.8370 = 289.8370), so at fixed p_thesis=0.5 the mixture EV is ~linear in target size regardless of attainability. Market-implied mixture weight for the live IWM trade = -0.004; RN terminal attainment = 0.042, RN touch = 0.085; RN EV = -1.61% of ask (pure costs). Same cont~
    FIX: Make ev_pct a genuine mispricing claim: require the physical-measure probability to exceed the market's, p_cal >= p_mkt + margin, where p_mkt = p_touch(S, target, solved_iv, horizon, mu=r) is one line with existing code (the selector already computes ~this at selector.py:225 and ignores it). Immedi~
- [critical] PROBABILITY-MEASURE-2: Five unreconciled probability measures for the SAME event coexist in the system's own ledger rows: (1) market RN 0.042/0.085; (2) selector p_touch_target = 0.086 under mu=0, sigma=max(iv,hv20) - logged, never gated; (3) entry mixture p=0.5 (claimed p_profit 0.686 vs RN ~0.43); (4) exit p_target_thesis = 1.0 - p_touch under the full mu_thesis (-8.22) extrapolated over the option's remaining LIFE T=2.3 days instead of the 140-min horizon that defined mu_thesis (exit_engine.py:~
    FIX: Define ONE object - p_t = P(touch target within the REMAINING thesis horizon | evidence at t) - and make every consumer read it: selector gates on p_0 vs p_mkt; exit prior mu_prior scales with p_t; p_target columns computed over min(T, remaining_horizon) not T. The thesis's claim must expire with i~
- [high] PROBABILITY-MEASURE-3: Entry-exit prior incoherence quantified for the live trade (extends TRAJECTORY-6): entry prices the thesis at 50%, exit holds at 100% (exit_engine.py:242 mu_prior = pv.mu_thesis, weight 1.0; p_thesis stored on the position at run_options_shadow.py:826 but has ZERO occurrences in exit_engine.py). At the same instant with identical inputs: EV_hold = +7.2999/share under the full prior vs +3.0985 entry-consistent (0.5x) vs +0.0216 (mu=0), against ev_sell = -0.015. The h-rule fli~
    FIX: Ship as one changeset with the TRAJECTORY-1/2 blend rescale (either alone moves the noise-exit rate in an unintended direction): mu_prior = p_t * mu_thesis with p_t the shared calibrated probability, and w *= tau^2/(tau^2+SE^2) with SE = iv/sqrt(T_K) (already computed in trajectory.py). Replace the~
- [high] PROBABILITY-MEASURE-4: At p_thesis=0.5 the EV gates cannot bind on probability - payoff asymmetry does all the work. Live trade: thesis branch EV = +108.6% of ask (convexity harvests the full distribution around the target forward), mu=0 branch = -0.39%, so break-even mixture weight = 0.011 and the ev_pct>=10 gate needs only p = 0.103 - the hardcoded 0.5 is 5x the gate level and 46x break-even. The p_profit>=0.40 floor is structurally unbinding near ATM: 0.5 x thesis-branch p_profit (0.939) = 0.47~
    FIX: Feasibility-cap the thesis drift so the branch cannot assert super-market moves: require |ln(1+target)| <= k * iv * sqrt(horizon_T) (k~2, i.e. p_touch >= ~5%) at signal build or as a selector gate - kills the priced-lottery class. Then re-derive the ev_pct floor per-horizon so honest 25-min targets~
- [medium] PROBABILITY-MEASURE-5: The mixture's semantics are undefined and its complement branch contradicts the lane's own beliefs. (a) p_thesis multiplies a branch where the target is attained only in EXPECTATION - P(S_T <= target | thesis branch) = 0.5014 - so 'p_thesis=0.5' is neither P(touch) nor P(terminal attainment) nor P(direction); no empirical quantity could ever calibrate it as written. (b) The (1-p) branch is mu=0 (driftless), but the entry conditions on a band-break (momentum) and the lane's O~
    FIX: Define p_thesis as P(touch within horizon) and price the trade as p*EV(touch-conditioned exit at target) + (1-p)*EV(failure-conditioned drift mu_fail < 0 for the position, estimated from the lane's replayed failure population), not a driftless complement. Convert IV to a physical sigma via a regist~
- [medium] PROBABILITY-MEASURE-6: Calibration sources already exist in the system and are all dead-ended: p_thesis is one global config constant (run_options_shadow.py:72) fanned to all four lanes (:440-449) regardless of evidence quality (an RVOL-8 catalyst break and a 2bp macro drift get the identical 0.5); catalyst memory computes kind_hist_p_pos2d - an actual empirical base-rate probability - and it is written to the entry ledger as an inert covariate (null on the live record, no selector/lane consumer);~
    FIX: Build the per-lane p_thesis table offline this week: replay each lane's fire condition over the intraday cache and measure the realized target-touch rate within horizon (index_trend/last30/ORB separately; condition on the covariates already logged: range_percentile, RVOL bucket, catalyst kind via k~
RECS: 1. (Design North Star) One calibrated probability object end-to-end: p_t = P(touch target within remaining horizon | evidence). Entry: mixture EV with p_0 from the per-lane calibrated table; gate on edge vs the market (~ | 2. (Ship this week, no re-architecture) Selector coherence guard: reject/flag entries where the market-implied probability contradicts the manufactured EV - require p_touch_target >= 0.25 (it is already computed and log~ | 3. (Ship this week) Feasibility cap on mu_thesis at signal build: |ln(1+target)| <= 2 * iv * sqrt(horizon_T) (needs an iv estimate in the lane - pass yesterday's ATM IV from the iv_archive snapshotter or a fixed per-sym~ | 4. (Next) Calibrate per-lane p_thesis offline from the 500-session intraday cache replay (the pure lane predicates make this a one-day job the LANES-1 refuters already prototyped); register in sweep_ledger; thread throu~ | 5. (Same changeset as TRAJECTORY-1/2 fixes - explicitly coupled) Entry-consistent exit prior (mu_prior = p_thesis*mu_thesis) + variance-scaled blend weight (w *= tau^2/(tau^2+SE^2), SE = iv/sqrt(T_K) already computed in~ | 6. (Instrumentation, immediate) Log the measure disagreement per position: entry row already carries p_thesis and p_touch_target; add p_mkt (RN touch) and, at exit, the horizon-consistent p_target beside the current lif~

## SEAM funnel-throughput
VERDICT: The funnel is not starved by bad luck but by a compound mis-specification that leaves morning index signals exactly ONE marginal expiration to shop. Measured over 6 sessions (journal + ledgers): ~3 lane fires/day -> 33% lost pre-selector to staleness/stand-downs -> 11 signals reached a chain -> 2 entries (18% conversion, 0.33 entries/day; lane-2's first-ever pick landed this morning on NVDA). Inside the selector, 1,487 same-direction rows died first-fail at OI 32%/no_quote 25%/spread 19%/third-of-life 11%; only 0.4% ever reached the EV stage and none passed it except the two entries. Root mechanism (verified on real stored Tradier chains with the repo's own pricer): max_chain_expirations=3 truncates daily-expiry indexes to DTE{0,1,2}; 0DTE is lambda-infeasible below ~16% IV (the registered 0DTE carve-out has gated zero rows - unreachable) or banned after 14:00; the 1/3-of-life gate bans DTE1 before ~12:45; DTE2 passes with minutes of slack - so SELECTOR-2's mechanism is confirmed (corrected: DTE0 exempt, DTE2 marginal, DTE1 banned) while SELECTOR-1's 'jointly empty' is narrowed (QQQ/IWM DTE2 pass 13/3 real rows; SPY effectively 0; 0DTE empty for all at current IV). Lane 2's spread~
- [critical] FUNNEL-THROUGHPUT-1: The platform is an evidence machine: nightly grader issues lane verdicts at N>=25 per lane (go-live talk at 50) across a 5-lane roster, implying verdicts on an actionable timescale.
    FIX: Adopt the throughput package below (R1-R5): target ~1 entry/day for lanes 1 and 2 (N=25 in 4-6 weeks each), add a pooled cross-lane grader row so portfolio-level evidence accrues at ~2/day, and either fix or retire last30/pre_earnings so the roster only contains lanes that can mathematically accumu~
- [high] FUNNEL-THROUGHPUT-2: Selector 'scans DTE 0-5' (selector.py:8, max_dte=5 at :64) so the funnel prices the full short-tenor menu.
    FIX: Raise max_chain_expirations 3->6 (one config default; +3 chain requests per signal, trivially inside the 60/min self-cap). This restores the declared DTE 0-5 scan, gives morning index signals DTE3-5 tenors that pass 1/3-of-life with >=1.6x slack and carry lambda 60-90 at current IV. Register the tw~
- [high] FUNNEL-THROUGHPUT-3: SELECTOR-1 as filed: lambda<=90 x delta>=0.40 is 'jointly EMPTY for index options at current IV' and the registered 0DTE carve-out is unreachable.
    FIX: Decide the 0DTE question on merits (open post-retraction): if 0DTE stays in scope, make the lambda cap tenor-aware - lambda scales ~1/(sigma*sqrt(T)), so a flat 90 is a de-facto 0DTE ban at index IV; per-DTE caps around {0: 200, 1: 120, >=2: 90} keep the leverage intent while making the carve-out r~
- [high] FUNNEL-THROUGHPUT-4: Lane 2 (inplay_orb) trades the AI crew's catalyst names; its liquidity gates are protective filters, not a universe veto (SELECTOR-7: 'the lane has never produced a pick').
    FIX: Replace the flat $0.30 absolute cap with a premium-tiered cap: spread_abs <= max($0.30, 5% of mid), keeping spread_pct<=10% as the binding relative gate and the OI>=300/volume floors as junk protection. Microcaps (UNTY/NXTC class) still die - correctly. Expected lane-2 conversion on large/mid-cap c~
- [medium] FUNNEL-THROUGHPUT-5: OI>=1000 (index) and volume>=100 floors (selector.py:56-57, gates :188-191) measure liquidity of the tenors the funnel actually needs.
    FIX: Two clock-consistent floors: (a) time-scale volume: require volume >= 100 * min(1, minutes_elapsed/390); (b) for index DTE<=1 daily-expiry series accept OI>=1000 OR same-day volume>=500 - day-of volume is the honest liquidity signal for a series listed yesterday. Keep the non-index OI>=300 floor un~
- [medium] FUNNEL-THROUGHPUT-6: Lane 1b last30 is a functioning lane: 15%-of-move continuation target, 'refit at N=25' (lanes.py:317-318).
    FIX: Either (a) re-derive the target from remaining-range like lane 1 (target = max(0.35*remaining_range_at(930), 0.15*|move|)) - honest if the continuation thesis is believed, though note SELECTOR-3: ev_pct is ~linear in target, so any target raise must be pre-registered, not tuned to pass; or (b) pre-~
- [medium] FUNNEL-THROUGHPUT-7: One signal per side per day per underlying (lanes.py:216-217) is a dedup guard; the selector then decides.
    FIX: Burn the latch at ENTRY, not emission: on no_pick, re-arm the (symbol, side) after a 30-min cooldown if the lane predicate still holds (move still beyond noise band, same VWAP side / still outside OR). No new thesis class is admitted - the same break gets 3-5 selector attempts against fresh books i~
- [low] FUNNEL-THROUGHPUT-8: The funnel's stage-0 (lane arming and fires) is observable from the journal, so throughput diagnosis is possible from telemetry.
    FIX: Add a once-per-day 'lane_armed' journal row per lane with the operative stats (range_percentile_14d value, armed/stood-down, lane-2 armed-name count), dedupe re-journaled expired signals by (day, lane, underlying, direction, expires_minute), and retry the lane-2 average_volume backfill hourly inste~
RECS: R1 (config, register as tweak): raise max_chain_expirations 3->6 in run_options_shadow.py DEFAULTS (:79). Restores the selector's declared DTE 0-5 scan; morning index signals gain DTE3-5 tenors that clear 1/3-of-life wi~ | R2 (lanes.py): move the one-per-side daily latch from emission to entry with a 30-min re-arm while the lane predicate still holds (IndexTrendLane :249-252, InPlayORBLane :423-426). Same thesis retried against fresh book~ | R3 (selector.py:54): premium-tiered absolute spread cap for non-index - spread_abs <= max($0.30, 0.05*mid) - keeping spread_pct<=10% binding and OI>=300/volume floors intact. Unblocks the large/mid-cap catalyst cohort t~ | R4 (selector.py:56-57,188-191): clock-consistent liquidity floors - volume >= 100*min(1, elapsed/390), and for index DTE<=1 daily-expiry series OI>=1000 OR same-day volume>=500 (prior-day OI is structurally low on 1-day~ | R5 (runner): retry the lane-2 average_volume backfill hourly instead of single-shot at day-roll (21 lane2_rvol_baseline_missing stand-downs = ~40% of the universe never armed). Roughly doubles lane-2 armed candidate-day~ | R6 (decision, not code): settle 0DTE on merits - if in scope, per-DTE lambda caps ~{0:200, 1:120, >=2:90} (lambda scales ~1/(sigma*sqrt(T)); flat 90 is a de-facto 0DTE ban at index IV <16%); if out, delete the unreachab~ | R7 (last30): fix or retire - re-derive target from remaining-range (pre-registered, not tuned to pass the EV floor per SELECTOR-3's target-ambition warning) or drop the lane; as built it emits arithmetically un-passable~ | R8 (grader): add a pooled cross-lane row (GRADER-2's missing aggregate) so portfolio-level N=25 lands in ~2-3 weeks at post-fix throughput (~1.5-2.5 entries/day pooled), and add per-day lane-arming telemetry (range_perc~ | Sequencing: R1+R2 first (they compound: more tenors x more attempts), R5+R3 second (lane-2 universe then gates), R4 third, R6/R7 as registered design decisions. Do NOT loosen: spread_pct 10%, lotto_delta_ban, crossed/no~

## SEAM cost-and-edge
VERDICT: The strategy class survives its own costs ONLY on index/mega-cap penny books held to horizon - and the system as configured occupies neither condition. Measured from stored NBBOs (runtime/options_iv.db iv_surface, 3 days, delta-gate band 0.40-0.80): WORST round-trip = one full spread = 0.87% (SPY 2-DTE) / 1.27% (QQQ) / 2.14% (IWM) of premium on index, but 13.9%-108% (median per name: GS 13.9, WFC 18.4, MS 22, JNJ 24, BLK 29, CTAS 53, PNC 50, MTB 76, UNTY 104, GSHD 108) on the lane-2 catalyst universe - 5-25x any plausible modest edge, so that universe can never pass a WORST ledger. Break-even edge per trade: spread-only (gamma pays theta back, RV=IV) needs a 0.01-0.06-sigma favorable move per hold - attainable; full-cost (theta unrecovered) needs 0.11-0.19 sigma (IWM actual: 9.6bp/140min; NVDA live 07-16: 42bp/day; SPY 2-DTE: 4.7bp) - elite-forecaster territory; and the realized behavior (5-minute rule-b insta-cut) is the worst point on the curve: full spread paid over a 5-min drift window = 0.17 sigma / ~430%/yr annualized required just to break even. The selector EV stage and the grading ledger are NOT measuring the same thing: reproduced the live IWM record exactly - ev_pct=53.~
- [high] COST-AND-EDGE-1: The configured lane-2 catalyst universe is unpriceable at WORST fills: real stored NBBOs show median quoted spreads of 13.9%-108% of premium in the selector's own delta band, so the round-trip cost alone is 5-25x any plausible modest edge - while index/mega-cap books cost 0.9-2.1% and are survivable. The strategy class and its hunting ground are mismatched.
    FIX: Re-scope lane 2's universe to books the WORST ledger can price: gate the hunt list on live spread_pct <= 5% at the 0.5-delta strike (mega-cap class), and drop the small/mid-cap catalyst names entirely - no exit-engine or grader change can rescue a 20%-of-premium toll.
- [high] COST-AND-EDGE-2: Break-even required edge per trade, quantified: the system's REALIZED behavior (5-min rule-b insta-cut) sits at the worst point of the cost curve - full spread paid over a near-zero drift window - needing a 0.17-sigma (IWM, ~430%/yr annualized) to 0.52-sigma (NVDA) favorable move per 5-min hold; held to the planned horizon the bar drops to 0.11-0.19 sigma full-cost, or 0.01-0.06 sigma if gamma earns theta back (RV~IV). Long-premium intraday directional is only viable in the ~
    FIX: Treat exit-ladder repair (TRAJECTORY-1/EXIT-ENGINE-1/LANES-1 debounce) as a PREREQUISITE of the cost-and-edge verdict, not a separate bug: no lane can be judged until holds reach horizons; then measure realized RV-vs-IV over holds so the system knows whether its true toll is the 0.01-0.06-sigma spr~
- [critical] COST-AND-EDGE-3: The selector EV stage and the grading ledger measure different quantities, so the funnel's 'edge' arithmetic never touches what the grader scores: ev_pct is a target-ambition meter (hardcoded p_thesis=0.5 x a drift that attains the lane's requested target by construction, hold-to-horizon), while the grader scores realized WORST P&L over insta-cut holds. The costs are shared (approximately); the edge, horizon, and hold policy are not. Score to date: selector claimed +$184.08 ~
    FIX: Make the EV stage price evidence: floor the entry decision on the mu=0 branch plus a per-lane p_thesis calibrated from realized target-touch rates (the journal/ledger accumulates exactly this), journal ev_mu0 and p_touch beside ev_pct, and alarm when ev_pct > 5x p_touch-implied EV; simultaneously a~
- [high] COST-AND-EDGE-4: The perma-shadow trap is real with a precise shape: (a) on the lane-2 single-name universe no modest true edge can EVER pass the WORST bar (P(PASS@N=25)=0.000 for a +3%-of-premium mid edge under the measured ~20% haircut); (b) on index books the WORST bar itself is passable (same +3% edge held to horizon: P(PASS)=0.72) - the trap there is the exit ladder converting the edge into pure spread bleed (P(PASS)=0.017) times throughput (per-lane N=25 is 4-12 months at 2 entries per~
    FIX: Ranked unblocking: (1) fix exits so edge can express; (2) restrict lane 2 to <=5%-spread books so WORST mean>0 is arithmetically reachable; (3) add a pooled cross-lane %-of-premium ledger row with an N-scaled test (bootstrap lower-CB>0 at pre-registered looks) so a modest index edge is detectable w~
- [medium] COST-AND-EDGE-5: Where genuine edge could live in this architecture - and both candidates are currently strangled upstream of the EV stage: (i) macro_reaction post-print drift on SPY/QQQ 1-2 DTE, the only venue where the measured cost (0.87-1.3% RT) is small enough and print-day realized vol funds the theta; but the 07-14 CPI SPY signal died with 110 hold_exceeds_third_of_life + 122 open_interest rejections (SELECTOR-2/6) - the lane with the best-documented underlying anomaly has never reach~
    FIX: Point the funnel where the cost math allows an edge: extend the 0DTE third-of-life carve-out to DTE-1 morning signals (same registered logic as selector.py:69-74) and fix the OI/volume clock mismatch so macro_reaction can be priced at all; promote catalyst-kind memory from covariate to lane input (~
- [medium] COST-AND-EDGE-6: Theta cost accounting spans three inconsistent conventions, so the system cannot locate its own true cost between the spread-only floor and the full-theta ceiling - a 5-10%-of-premium band that decides whether the whole class is viable: vendor per-calendar-day theta is multiplied by trading-day hold counts (understates true trading-time decay by 31%: IWM planned theta_paid $10.14 -> $14.69 true; NVDA $34.29 -> $49.66, pushing full cost from 12.5% to 16.4% of premium), theta ~
    FIX: Standardize on trading-time theta (annual/252 per trading day) across selector/ledger/exit-engine in one registered change, add a realized-variance-over-hold field to the exit record (computable from the mark stream already stored), and report per-lane mean (IV_entry^2 - RV_hold^2) so the grader ca~
RECS: 1. PREREQUISITE, not parallel work: debounce/repair the exit ladder (rule b hysteresis, h/i* noise floor - TRAJECTORY-1, EXIT-ENGINE-1, LANES-1) before drawing ANY cost-and-edge conclusion from the ledgers. Every break-~ | 2. Re-scope lane 2's universe by measured spread, not by catalyst: require live spread_pct <= 5% at the 0.5-delta strike for hunt-list admission (keeps NVDA/BABA/RIVN-class, drops UNTY/GSHD/CTAS-class whose 13-108% book~ | 3. Make the selector EV stage price evidence instead of ambition: floor entry on the mu=0 branch (currently -1.19% on the live example while headline ev_pct said +53.31), calibrate per-lane p_thesis from realized target~ | 4. Unblock the one lane with a plausible mechanism: extend the registered 0DTE third-of-life carve-out to DTE-1 morning signals and fix the OI/volume clock mismatch (SELECTOR-2/6) so macro_reaction on SPY/QQQ 1-2 DTE - ~ | 5. Fix the evidence machine's timescale and power together: add a pooled cross-lane %-of-premium ledger row, replace mean>0 + PF>=1.2 with an N-scaled test (one-sided bootstrap lower confidence bound > 0 at pre-register~ | 6. Add realized-vol-vs-IV-over-hold to the exit record and grader card (computable from stored marks), and standardize theta to trading-time in one registered change. This locates each lane's true cost between the sprea~ | 7. Promote the catalyst-kind memory from passive covariate to a directional lane input on the <=5%-spread universe (direction = measured fade/continuation sign, gated on kind_hist_n >= 50 and |p_pos2d - 0.5| >= 0.08): i~

## SEAM timescale-stack
VERDICT: The stack is incoherent at its two most consequential joints. (1) Decisions are sampled far below the evidence timescale that admitted them: entries require a committed 5-min boundary close beyond the noise band, but invalidation reads a raw 10-second tick against the same zero-hysteresis, sqrt(t)-growing band at every mark - this decided the platform's only live trade (IWM 293P: entered 13:40:02 on a 1.07bp margin, cut at the first 300s mark on a +8.5bp tick while the engine's own state logged ev_hold=+0.021 > ev_sell=-0.01). (2) The exit ladder's fire probability depends on how often you look, not on the path: fresh Monte Carlo through the repo's own mu_blend shows pure-noise h-fires 77% within 30 min at 300s cadence and 83% at 60s (median 10 min), and the count-based debounce the engine already uses for rule (g) is NOT cadence-invariant (noise-fire 56% vs 76% at 30 min across 300s->60s) while a time-based 20-min persistence IS (12.2% vs 10.1%; 47.2% vs 46.9% at 2h) - so the runner's cadence escalation to 120/60s and its news/price-shock forced re-marks, documented as 'WHEN we mark, never WHAT', mathematically change WHAT. Layered on top: a 20-min two-endpoint drift estimate (1-~
- [critical] TIMESCALE-STACK-1: Entry and exit consume evidence on different grids: entry admission requires a committed 5-min boundary close beyond the noise band, but thesis invalidation is evaluated at every mark on a raw 10-second poll tick against the same band - zero hysteresis, band grown wider since entry, and mixed-clock inputs (tick-fresh S vs bar-stale svwap vs wall-minute band lookup). Exits therefore sample noise at ~30x the entry resolution and fire before the rest-of-day thesis horizon can e~
    FIX: Evaluate invalidation only on committed bar closes on the entry's own 5-min grid (pass ctx.close and the last committed minute into _thesis_valid, not tick S / wall minute), freeze the invalidation band at its entry-time width, and require 2 consecutive boundary closes inside (or a 0.8x-band hyster~
- [critical] TIMESCALE-STACK-2: The exit ladder is cadence-dependent by construction: rules b/h/i*/f/d2 are memoryless per mark and re-sample heavily-overlapping statistics (a 300s mark shares 15/20 of the mu window; 60s shares 19/20), so P(noise exit) compounds with the number of looks. The runner escalates to 120s/60s exactly when positions are most vulnerable (0DTE afternoon, last 30 min) and forces EXTRA draws on news/price shocks - the documented invariant 'WHEN we mark, never WHAT the exit engine dec~
    FIX: Convert evidence-rule debounces to TIME-based persistence: b/h/i* fire only when the breach has persisted continuously for a registered duration (sweep {5, 10, 20} min; 20 min = one non-overlapping mu window makes h approximately cadence-invariant per the MC). Keep clock rules (a, print-window, pla~
- [high] TIMESCALE-STACK-3: Window/horizon mismatch in the mu stack: a 20-minute drift estimated from exactly TWO closes (maximally endpoint-sensitive) is extrapolated to rest-of-day/1-day EV horizons at full blend weight. 1-sigma pure noise on mu_hat over 20 min = 12.6/yr annualized (6.3 after the w=0.5 blend at t=1) against decision thresholds of ~0.016; a horizon-consistent shrinkage T_K/(T_K+dt_1day) = 0.049 says the current weighting is ~20x too heavy. A single 10bp stale print at either window en~
    FIX: Two independent changes, both already in the registered weight-family sweep lane (opts-calib-mu-blend-weight-v1): (1) robust endpoints - median of the first/last 3 window closes (kills single-print sensitivity ~3x); (2) horizon-scaled weight w' = w * T_K/(T_K + dt_decision), or equivalently compute~
- [high] TIMESCALE-STACK-4: Session-scale state is built across the premarket/RTH regime boundary: the process polls from 08:30 ET with a close-only gate, LiveBarBuilder commits premarket bars (including prevclose-echo bars from the last<=0 fallback) permanently into cum_pv/cum_v (session VWAP), HOD, and the session arrays; the RTH filter exists only in front of lane dispatch. The 20-min mu window therefore straddles 09:30 for the first ~20 minutes of RTH - exactly when ORB and macro positions are ente~
    FIX: Gate builder commits to OPEN_MIN <= minute < close (drop premarket ticks in on_tick or filter in seed/_commit), or reset builder state at 09:30. Structural and cohort-neutral (not in DEFAULTS, no config-hash split). VALIDATION: recompute svwap and mu_hat over the 529-session intraday cache with and~
- [high] TIMESCALE-STACK-9: The timescale compression at exit starves and corrupts the stack's slowest layer: noise exits collapse holds to minutes (the one live trade: 306 seconds against a rest-of-day horizon; refuter replay: 41% of lane-1 entries cut at the first mark, 76% within 30 min), so each WORST-graded sample the months-scale grader accumulates (N>=25/lane at ~0.14 fires/day = 6 months to years per verdict) measures spread + sampling noise, not thesis quality. The grading layer cannot converg~
    FIX: No separate mechanism - this is the payoff metric for fixing TIMESCALE-STACK-1/2/3: after bar-aligned debounced exits, median hold should approach the lane horizon scale. VALIDATION: track median hold-time / horizon_T ratio and the share of exits by rule (b/h/i* vs clocks) as first-class replay + s~
- [high] TIMESCALE-STACK-5: The lane-1 admission band is sized on the wrong statistic for its grid: mean |move-from-open| is a ~P58 threshold that a driftless walk closes beyond at ~42% of 5-min boundaries (empirical fire rates at the 09:35 boundary: SPY 38% / QQQ 42% / IWM 44% on the real cache), and the band grows ~sqrt(t) intraday so the invalidation barrier chases the position while the entry margin is a single boundary-close overshoot (~9bp IWM). Entries are therefore admitted on noise and born ~0~
    FIX: Sweep band variants on the exact harness the refuters built (529-session cache, real build_noise_profile + lane code): {mean (current), k*mean for k in 1.25/1.5/2.0, per-minute p80/p85} x {band frozen at entry / growing}. Decision metrics per variant: boundary fire rate, conditional forward return ~
- [medium] TIMESCALE-STACK-6: No staleness bound ties the evidence clock to the decision clock: the trajectory window anchors at the last committed bar's minute (not wall clock), so an unpolled or halted tape serves the last drift as current indefinitely; a frozen tape reads r=0 -> t=0 -> w=0 -> mu_eff = full mu_thesis, i.e. a halt is scored as 'thesis fully intact' (h cannot fire, g's p_tgt_eff stays above floor) with only a journal entry (possible_halt) as defense; and a 44-minute-old carried IV counts~
    FIX: Pass the mark's wall minute into underlying_state and return no_view when mins[-1] is more than ~3 min stale; on a possible_halt/symbol_state hit for a HELD underlying, force mu_prior=0 (degrade to the no-view measure, not the most bullish one) and journal the degradation; tighten the IV freshness ~
- [medium] TIMESCALE-STACK-7: Signal-time vs entry-time clock skew: (1) may_overnight compares horizon_T measured from the BAR's close minute against rest_of_day measured at the entry WALL minute with only a 0.1% tolerance, so a single >=1-minute bar-completion delay (routine for sparse lane-2 names whose bars only close when the next tick arrives) silently flips an intraday signal to 'overnight' and rejects all DTE<2 contracts; (2) backfill-era signals inside the 10-min TTL proceed to entry with a stale~
    FIX: Compute may_overnight at signal creation (compare horizon to close_min - expires-anchor) or allow one-bar (2-min) tolerance; at _enter, refresh S from a fresh poll and re-evaluate the lane's own predicate (the invalidated() function exists and is pure) before committing an entry older than ~2 min. ~
- [low] TIMESCALE-STACK-8: Sparse-tick bar fidelity is a tolerable but correctable bias floor: ~6 last-trade samples/min understate true 1-min extremes (expected sampled range ~0.71R), so lane-2's opening range is ~10-20% low - target = 2x OR inherits the understatement while the 'close beyond OR' break loosens (a double EV penalty in the same direction); and receive-time stamping (ts_epoch=now) lets a stale print on a thin name form fresh-looking echo bars. Tolerable for index lanes at 10s polling; m~
    FIX: Carry Tradier's trade timestamp into Tick and skip echo bars whose last is stale; require a minimum tick count per OR bar; widen the OR by a registered sampling-correction factor (sweep {1.0, 1.15, 1.3} on the paired replay once lane 2 produces picks - currently unreachable behind SELECTOR-7's spre~
RECS: PREREQUISITE (enables validating everything else): fix REPLAY-LAB-1 - keep marking/capturing quotes for exited positions until their thesis horizon (rest-of-day for intraday lanes), so hold-longer variants (debounce, hy~ | 1. Align rule-(b) invalidation to the entry's evidence grid (fixes TIMESCALE-STACK-1): evaluate on committed 5-min boundary closes (pass ctx.close + last committed minute into _thesis_valid at run_options_shadow.py:871-~ | 2. Make the exit ladder cadence-invariant (fixes TIMESCALE-STACK-2): TIME-based persistence for b/h/i* (breach must persist a registered duration, sweep {5,10,20} min - MC shows 20 min = one non-overlapping mu window co~ | 3. Robust endpoints + horizon-scaled blend (fixes TIMESCALE-STACK-3): median-of-3 window endpoints in trajectory.py:106 (kills the 0.35-sigma-per-10bp single-print sensitivity), and w' = w * T_K/(T_K + dt_decision) (fac~ | 4. RTH-gate LiveBarBuilder (fixes TIMESCALE-STACK-4): drop pre-open commits (or reset builder state at 09:30) in feed.py on_tick/_commit - structural, cohort-neutral, also reconciles restart-vs-continuous svwap (backfil~ | 5. Band recalibration, validate-first (TIMESCALE-STACK-5 - split refuter verdict, so measure before shipping): sweep {mean, 1.25x, 1.5x, 2.0x mean, per-minute p85} x {frozen, growing} on the 529-session harness with bou~ | 6. Staleness ties (TIMESCALE-STACK-6): wall-minute into underlying_state -> no_view beyond 3 min of bar silence; possible_halt/symbol_state forces mu_prior=0 for held names; IV freshness bounded to one mark cadence with~ | 7. Small clock-skew fixes (TIMESCALE-STACK-7/8): may_overnight computed at signal creation or with one-bar tolerance; refresh S + re-run the lane's pure invalidated() predicate at _enter for signals older than ~2 min; c~ | SEQUENCING NOTE: 1-3 interact (the inflated exit prior TRAJECTORY-6 currently brakes the noise clock; the growing band feeds both entry and kill) - ship as one registered exit-engine version with the full column set rat~

## SEAM dead-layers - which pieces of the 105-agent stack do not belong: per-layer KEEP/PARK/DELETE/FIX-CHEAPLY audit of iv_rank/IV-archive, news+catmem covariates, mention tap, C5/news-shock mark-accel, symbol-state halt gate, day_briefing.json, replay lab, and watch_hub equity panels
VERDICT: The owner's suspicion is confirmed and quantifiable: ~5,000 lines of auxiliary stack (6 session processes + 2 scheduled tasks launched every trading day) currently influence ZERO decisions. Journal tally across all sessions: 0 reval_trigger events (C5 and news-shock accel never fired), 0 halted-underlying rejections, 1 entry whose 9 news/catmem covariate fields are all null/false/0 and whose iv_rank is null. The lab report proves the replay lab has zero discriminating power on 100% of history (grid n_replayed=0; all 5 AB variants byte-identical at {n:1, -16.0, b_thesis_invalid}; exit_efficiency null). The deep pattern is not "bad pieces" but stage-mismatch: stage-2+ instrumentation (covariates, IV rank, mention baselines, catmem base rates) was bolted onto a stage-0 funnel that produces ~1 entry per 3-4 sessions, all in the one lane (index_trend) for which every one of those covariates is structurally null - so their promotion-ladder payoff horizon is years-to-never at N=25, regardless of code quality. Verdicts: DELETE the iv_rank score penalty (provably order-invariant no-op) and the mark-accels (their only causal channel today is extra noise-exit draws against the broken ladder)~
- [critical] DEAD-LAYERS-1: REPLAY LAB - FIX-CHEAPLY (do not park, do not trust as-is): it is the promotion ladder's mandatory stage-4 toll gate (PROMOTION_LADDER.md rules 2, 6, 7 - every exit-ladder change and every p_thesis/EV-input signal must pass through a paired-replay variant), yet it has zero discriminating power on 100% of recorded history and structurally cannot answer the owner's #1 open question (was the rule-b insta-cut right?). Deleting it is unconstitutional under the platform's own law;~
    FIX: Three registered, cheap fixes convert it from dead to load-bearing: (1) keep polling an exited contract's NBBO at the same cadence until EOD (0DTE) or next-day close (DTE>=1), rows flagged post_exit:true; (2) store per-lane thesis INPUTS (or verdicts) in ext so variant thesis policies (e.g. 2-conse~
- [high] DEAD-LAYERS-2: NEWS + CATMEM COVARIATES - PARK: 9 of the 13 stage-2 covariate fields (catalyst_kind, news_shock_15m, news_kind_recent, news_direction_align, headline_age_min, news_count_60m, kind_hist_n/ret2d_med/p_pos2d) are structurally null on the ONLY lane that has ever entered. catalyst_kind and the three catmem fields require a lane-2 catalyst entry - lane 2 has 0 lifetime picks because its spread gates are unpassable (SELECTOR-7); the news fields require fresh symbol-tagged news on ~
    FIX: Park the C6 classifier (news_flag_tap launcher block), the premarket catalyst_context build, and lab job C's catmem append until trailing-20-session entries >=10 (an explicit re-arm condition - ladder rule 3 makes demotion free). KEEP raw news_tap collection (cheap HTTP polls, its stream compounds)~
- [high] DEAD-LAYERS-3: IV_RANK / IV ARCHIVE - DELETE the consumer, conditionally KEEP the collector: the selector's iv_rank penalty is a provable no-op even after warm-up - a per-call scalar subtracted identically from every candidate's score, where score is consumed solely by a within-call sort + top_n slice with no absolute threshold; identical ordering, identical picks, for every possible iv_rank value (order-invariance of x -> x-c). It is also None until at least 2026-07-24 (4 in-band sessions~
    FIX: Delete the penalty line and the hv20 term (both provably decision-neutral -> cohort-neutral, config_hash-safe) and the FRED-fallback docstring claim. Keep scripts/snapshot_iv.py ONLY with (a) a heartbeat + alert_watch page on a missed session (IV-ARCHIVE-7) and (b) write-time trading-time normaliza~
- [high] DEAD-LAYERS-4: WATCH_HUB - FIX-CHEAPLY: the platform's only human RTH surface (1,190 lines, auto-opened in the browser by the launcher) renders 7 equity-era tabs (Decisions/Scans/Funnel/Positions/Services/Activity/Risk) fed by archived systems - guardian heartbeats, RH equity positions, discovery scans, slippage telemetry, $0 pills - while the options-shadow view is computed server-side every 2 seconds (_options_view: per-lane verdicts, N-progress, quarantines, entry/exit counts) and then ~
    FIX: Ship the pending render: add an Options tab as the DEFAULT tab consuming snap.options (verdicts, N-progress, today's entries/exits, quarantine counts, funnel rejection tally), and delete or collapse the seven equity tabs plus their feeders (parse_rh_mcp, _guardian_view, _slippage_view, scans state)~
- [medium] DEAD-LAYERS-5: C5 PRICE-SHOCK + NEWS-SHOCK MARK-ACCEL - DELETE (re-register after the exit ladder is re-founded): both have fired exactly 0 times in all recorded sessions, and their design ('WHEN we mark, never WHAT we do') is not neutral under the CURRENT exit ladder - every extra mark is another draw of the noisy exit ladder (TRAJECTORY-4: the first-match ladder re-samples a ±1400%-annualized-noise drift estimate every mark, so P(noise exit) compounds with polling frequency). An accelera~
    FIX: Remove both blocks (or set reval_shock_mult unreachable in config) and journal the removal in the sweep ledger; the news-shock accel's registration (opts-news-shock-accel-v1) can be re-entered verbatim AFTER the exit ladder's noise-clock defects (TRAJECTORY-1/2, EXIT-ENGINE-1/2/3) are fixed, at whi~
- [medium] DEAD-LAYERS-6: SYMBOL-STATE HALT GATE - KEEP the 12-line entry gate, FIX-CHEAPLY the poller, and note the inversion: it protects the path that never occurs while leaving the path that hurts unguarded. Entry-side: 0 halted/suspended rejections lifetime - index ETFs (the only entering lane) essentially never halt, and lane-2 single names (which do halt) never pass the selector. Exit-side: nothing reads symbol_state for HELD positions, and a halted tape reads as 'no evidence' -> full-thesis H~
    FIX: Three cheap moves: (1) --poll-seconds 90 in launch_options_day.ps1:160 and the script default (freshness bound is 180s - still 2x margin); (2) add a symbol_state read in _reval_positions that journals halted_while_held and forces an immediate mark attempt (observability-only, same C5 idiom - no lad~
- [medium] DEAD-LAYERS-7: DAY_BRIEFING.JSON - FIX-CHEAPLY, with a scope correction: the artifact has zero consumers (grep: writer + tests only - the runner re-derives session shape and events, and reads hunt_list.json directly), so its earnings enrichment (up to 25 Finnhub calls/day), scorecard digest, opex/witching flags, and permanently-null vix field are dead outputs; it is not even the stage-1 ('humans see it') artifact the ladder implies, because no hub panel or report renders it. BUT its writer~
    FIX: Shrink build_day_briefing to what it actually is - the premarket crew orchestrator: keep the crew invocation (with the timeout raised above the crew's worst case, per PREMARKET-CREW-2), drop the earnings/scorecard/vix enrichment, and repurpose the freed budget for the layer's real missing piece: st~
- [low] DEAD-LAYERS-8: MENTION TAP - PARK: a self-aware stage-0 collector ('NOTHING consumes these files yet' in its own docstring) whose designed future consumer is a direction-agnostic in-play detector for lane-2 single names - the lane the selector has structurally closed (0 lifetime picks). 847 count rows banked, mention_flags.jsonl not yet born (z-flags need >=5 sessions of per-bucket baseline), alert_watch never reads its heartbeat (silent death), and the baseline is cheap to rebuild (~1 wee~
    FIX: Remove the launcher block (keep the code and the 847 banked rows - restart costs one week of baseline); write the re-arm condition into the sweep ledger alongside the park: 're-launch when lane-2 selector gates are re-registered AND trailing-20-session lane-2 picks >= 5'. This makes the park a ladd~
RECS: 1. (unblocks everything else) Fix the replay lab's three capture defects and register them as one sweep row: post-exit NBBO polling to a defined horizon (EOD for 0DTE, next-day close for DTE>=1, rows flagged post_exit:t~ | 2. Ship the watch_hub options tab (render the already-computed snap.options as the default tab) and delete the seven equity tabs and their feeders (parse_rh_mcp, _guardian_view, _slippage_view, scans). Pure observabilit~ | 3. Delete the iv_rank score penalty and the dead hv20 term from selector.py Stage 3 (provably order-invariant within the sort -> decision-neutral -> cohort-neutral), and delete the unbuilt FRED-fallback claim from iv_ar~ | 4. Remove the C5 price-shock and news-shock mark-accel blocks (0 fires lifetime; under the current noise-clock ladder their only causal effect is extra noise-exit draws) and journal the removal; re-register them verbati~ | 5. Park the stage-2 covariate stack that cannot grade: news_flag_tap (C6 classifier), the premarket catalyst_context build, and lab job C's catmem append - each with an explicit re-arm condition in the sweep ledger (tra~ | 6. Park mention_tap (drop the launcher block, keep code + 847 banked rows; ~1 week of baseline is the whole restart cost) with the same explicit re-arm condition tied to lane-2 selector re-registration. | 7. Shrink build_day_briefing.py to crew orchestration: raise the 300s crew kill window above the crew's measured worst case, drop the 25 Finnhub earnings calls / scorecard digest / null vix, and add the hunt-list freshn~ | 8. Keep the symbol-state entry halt gate but slow the poller to 90s (respects the 1/min courtesy limit with 2x freshness margin), add its heartbeat to alert_watch, and add an exit-side halted_while_held journal in _reva~ | 9. Adopt the stage-mismatch rule going forward: no new stage-2+ instrumentation while the funnel is below ~2 entries/week; every PARK carries a written re-arm condition so demotion stays free and reversible (ladder rule~

## SEAM evidence-machine
VERDICT: The current evidence machine is a fixed-threshold filter wearing a lab coat: at its own N=25 floor it false-PASSes a zero-edge lane ~33% of the time (my MC, confirming GRADER-1), ~69% once the nightly all-history re-grade peeks from N=25 to 100, and ~87% familywise across 5 lanes - while the PF>=1.2 gate is analytically equivalent to t=0.36, i.e. no test at all. Meanwhile it is also too SLOW where it matters: with no pooled row and 0.14 fires/day, the one-year minimum detectable edge (~17% of premium) exceeds the selector's own 10% entry floor, so the machine cannot confirm even its own design target on any actionable timescale. The replacement that fits this problem exactly is a betting e-process on %-of-premium WORST returns (bounded below by -1 by construction of a long option), which makes nightly re-grading legitimately anytime-valid, is immune to the post-premium-cap dollar-dominance problem, needs no variance estimate, and - fitting the casino posture - literally IS a bankroll: pooled wealth 5:1 unlocks a tuition-capped 1-lot live pilot (which simultaneously measures real fills and settles the WORST-vs-BASE question empirically), 20:1 unlocks quarter-Kelly-capped sized live~
- [critical] EVIDENCE-MACHINE-1: The PASS bar (grade_options_shadow.py:135-136) is a fixed-effect-size filter with near-zero discriminating power, and the nightly all-history re-grade (line 73, load_exits(day=None)) is unbounded peeking. Independently verified by Monte Carlo: under a zero-NET-edge null with a realistic post-cap premium mix, P(PASS) at N=25 single-look = 0.333 (0.259 under a skewed null), P(ever PASS) while peeking nightly from N=25 to 100 = 0.688, familywise across 5 lanes single-look = 0.8~
    FIX: Replace mean>0 + PF>=1.2 with an anytime-valid betting e-process on %-of-premium WORST returns (see recommendations 1-2). Nightly re-grading then carries zero peeking penalty by Ville's inequality: MC type-I under CONTINUOUS monitoring to N=1000 = 0.020 (normal) / 0.017 (skewed) at wealth threshold~
- [critical] EVIDENCE-MACHINE-2: No statistical redesign can make the machine conclusive at the observed funnel rate - the honest minimum-detectable-effect must be stated jointly with the fire rate. At 0.14 fires/day (~35 trades/yr even if every fire lands in one pooled row), the one-year MDE at 80% power / one-sided alpha=0.05 with per-trade sd ~40% of premium is ~16.8% of premium per trade - larger than the selector's own 10%-of-ask EV entry floor (selector.py:75), i.e. the machine cannot even verify the ~
    FIX: Add the pooled ALL row (deduped by position_id) as the PRIMARY verdict unit; publish the MDE-vs-fire-rate table in the scorecard so every night states what the machine can and cannot yet see; treat funnel telemetry (fires/day, per-gate rejection counts with SELECTOR-14 order-correction) as first-cl~
- [high] EVIDENCE-MACHINE-3: All-USD statistics after the premium-cap removal (sweep_ledger.jsonl:52, opts-tweak-remove-premium-cap-v1) let one expensive contract decide a lane: a single $2-3k QQQ 0.55-0.75-delta contract among 24 ~$150 contracts carries ~94% of the lane's dollar variance (600^2/(600^2+24*30^2)=0.943). My MC used this mix and the false-PASS numbers in EVIDENCE-MACHINE-1 already include it. The correct per-trade statistic exists in every exit row today: R_i = net_pnl_usd / (100 * ledgers~
    FIX: Grade everything on %-of-premium returns; keep USD columns as descriptive detail. This simultaneously fixes premium-mix dependence of lane comparisons (SELECTOR-11).
- [high] EVIDENCE-MACHINE-4: WORST-vs-BASE, quantified: WORST pays 100% of quoted spread round-trip, BASE pays 35% (shadow.py:31,41-54), so the gap is 0.65x quoted spread - 1.0% of premium on the one real trade (1.6% quoted spread), <=3.3% on 'clean' contracts (spread_clean_pct=0.05, selector.py:52), <=6.5% at the 10% spread cap (selector.py:53). Against the selector's 10%-of-ask entry EV demand, WORST is NOT so conservative that a true edge cannot pass on clean index contracts - the binding problems ar~
    FIX: Keep WORST as the go-live falsification bar (its null is 'edge exceeds full quoted spread' - exactly the registered intent); run the identical e-process on BASE in parallel for lane ALLOCATION ranking; calibrate a third EFFECTIVE ledger from real pilot fills (recommendation 3) and register the meas~
- [high] EVIDENCE-MACHINE-5: The concentration gate is both drifted from registration and statistically harmful: the registered bar says 'not concentrated in <20% of days' (sweep_ledger.jsonl:32) but the code implements max|day|/sum|day|<0.5 (grade:133-136), which GRADER-3's repro shows false-FAILs ~42% of genuinely profitable-but-clustered lanes (25 trades over 4 days) while passing a 2-day/90%-concentrated sample. Under the e-process with per-bet return capping (bet on min(R_i, 2.0)) a single lucky da~
    FIX: Demote concentration to a reported diagnostic (top-20%-of-days profit share, per the registration); remove it from the verdict; the capped e-process carries the anti-fluke protection.
- [medium] EVIDENCE-MACHINE-6: Mechanical integrity holes make any statistic garbage-in and become MORE dangerous under a sequential test whose wealth persists across nights: no dedup of exit rows by position_id (double-counted trades pump wealth, GRADER-7); merged multi-lane exits fully double-counted per lane (GRADER-8); day/lanes/decomposition fail soft to '?'/epoch-strings/0.0 (GRADER-5); expiry==today 0DTE orphans invisible on the only night anyone looks (GRADER-6, grade:175-176); config_hash stamped~
    FIX: In the same change as the statistical replacement: quarantine duplicate position_ids loudly; pooled ALL row dedupes merges (full trade counted once); extend _malformed to day/lanes/decomposition; expiry==today counts as expired on the nightly run; key every e-process by (scope, config_hash cohort) ~
- [medium] EVIDENCE-MACHINE-7: Multiple-comparisons leakage from the replay lab into the evidence machine: nightly grid/AB sweeps name best variants with no N floor (REPLAY-LAB-4) and any promoted variant would inherit the incumbent's accumulated history under the current all-history grader - a selection-then-confirmation loop that the ~87% familywise false-PASS already shows the system cannot afford. A variant chosen BECAUSE it looked best on the sample cannot be validated on that same sample.
    FIX: Rule: a promoted variant starts a fresh e-process under its new config_hash at wealth 1.0; replay results are hypothesis GENERATORS only, never evidence; at most one live exit-engine cohort accrues evidence at a time.
RECS: 1. REPLACE THE TEST (grade_options_shadow.py:135-136): score each exit as R_i = net_pnl_usd/(100*ledgers.worst.entry_fill) (bounded below by -1), process exits in exit-timestamp order, and maintain a betting e-process W~ | 2. TWO-TIER HIERARCHY: pooled ALL row (deduped by position_id, merges counted once) is the PRIMARY existence-of-edge verdict at wealth>=20 (alpha=0.05); per-lane e-processes at wealth>=10 (alpha=0.10) are for ALLOCATION~ | 3. GRADUATED GO-LIVE REPLACING BINARY PASS (the casino posture done safely): wealth>=5 (odds 5:1, P<=20% under null) unlocks a PILOT - 1 contract, premium <= min($150, 5% of account), <=1 trade/day; zero-edge tuition is~ | 4. KEEP WORST AS THE GO-LIVE LEDGER, BASE FOR RANKING: WORST costs only 0.65x quoted spread vs BASE (1.0% of premium on the observed contract, <=3.3% on clean <=5%-spread picks) - small against the 10%-of-ask entry EV d~ | 5. FIX MECHANICAL PREREQUISITES IN THE SAME CHANGE: dedup exits and entries by position_id with loud quarantine (GRADER-7); pooled row dedupes merged-lane trades (GRADER-8); extend _malformed to day/lanes/decomposition ~ | 6. PUBLISH THE HONEST MDE TABLE NIGHTLY: the scorecard should print trades/yr at the trailing 20-session fire rate and the corresponding 80%-power MDE (35/yr -> 16.8% of premium; 250/yr -> 6.3%; 750/yr -> 3.6%) so nobod~ | 7. VARIANT DISCIPLINE: replay-lab winners are hypothesis generators; promotion = new config_hash cohort = fresh e-process at wealth 1.0; never grade a variant on the sample that selected it.

## RESEARCH Empirical intraday time-value decay of 0DTE and 1-2 DTE index options (SPY/QQQ): what is the measured intraday decay cu~
- [high] Intraday S&P 500 volatility has a pronounced U-shaped deterministic periodic component - per-minute variance is highest after the open and before the close and lowest midday - and this U-shape is confirmed OPTION-IMPLIED, not just from returns: Todorov & Zhang (2024) estimate it nonparametrically from 0DTE/1DTE SPX option quotes (CBOE Da~ (Todorov & Zhang, 'Intraday Volatility Patterns from Short-Dated Options', Journal of Econ~)
- [medium] Because the close carries above-average variance, 0DTE quoted (annualized, calendar-time) implied volatility follows a mechanical intraday pattern: elevated at the open, compressed through midday, expanding into the close (the 'IV ramp'). This is the calendar-time signature of the market pricing remaining VARIANCE rather than remaining c~ (Practitioner: https://volatilitybox.com/research/0dte-options-volatility-day-trading-guid~)
- [medium] Measured 0DTE value-decay curve shape (practitioner data): Option Alpha sampled SPX 5-wide verticals every market minute for 30 calendar days (Jul-Aug 2024) and found decay is 'gradual in the morning, accelerates in the afternoon, and sharply drops off toward the end of day' - an inverse-sigmoid in VALUE space - with the single largest p~ (https://optionalpha.com/blog/0dte-options-time-decay + https://optionalpha.com/videos/0dt~)
- [high] Under Black-Scholes with calendar time, the fractional decay rate of remaining ATM time value is 1/(2*T_rem) per unit time (TV proportional to sqrt(T_rem)): for a 16:00-expiry 0DTE that is 8.3%/hr at 10:00, 12.5%/hr at 12:00, 25%/hr at 14:00, 50%/hr at 15:00, 100%/hr at 15:30, diverging to infinity at the bell. This is arithmetic on the ~ (Standard BSM ATM approximation (Brenner-Subrahmanyam); derivation verified in this audit)
- [medium] Direction of BSM sqrt(T) bias for 0DTE afternoons: constant-IV calendar-clock BSM OVERSTATES proportional decay from roughly midday to ~15:15 ET (because the variance-rich close props up remaining value - the market marks IV up into the afternoon rather than letting premium bleed at the calendar rate) and UNDERSTATES the terminal collaps~ (Computed from Todorov-Zhang periodic-vol estimates + BSM ATM approximation; consistent wi~)
- [high] BSM is a poor pricing model for 0DTE generally: Bandi, Fusari & Reno (2024, Journal of Finance version circulating; intraday SPX option data Jan 2014-May 2023) show that even BSM with unrestricted time-varying spot volatility misprices 0DTEs, and that local-in-time Edgeworth expansions adding skew (leverage) and kurtosis (vol-of-vol) cor~ (Bandi/Fusari/Reno '0DTE Option Pricing' (https://papers.ssrn.com/sol3/papers.cfm?abstract~)
- [high] Long premium 0DTE is negative-EV on average for takers: Beckmeyer, Branger & Gayda (SSRN 4404704; CBOE Open/Close data Feb 2021-Sep 2023) find retail lost >$125M total (>$90M of it transaction costs, i.e., ~60% of losses are spread/fees), average single-leg put trades return -5.6% and calls -9.4% of posted margin, median returns near zer~ (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=4404704 (PDF via https://wp.lancs.ac.~)
- [medium] 0DTE options embed a large intraday variance risk premium and are relatively mispriced in exploitable ways: Almeida, Freire & Hizmeri ('0DTE Asset Pricing', SSRN 4701401) find investors demand high compensation to bear intraday variance risk (mainly upside risk), the 0DTE VRP predicts intraday market returns, and 0DTE options violate sto~ (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=4701401; https://www.paris-december.e~)
- [high] The ATLAS hardcoded curve rate(m)=0.02+0.14/(1+exp(-(m-840)/45)) (sigmoid centered 14:00 ET, ~2%/hr at 9:30, 9%/hr at 14:00, capping ~15%/hr at 16:00) integrates to only ~0.417 over the 6.5h session, i.e., it leaves e^-0.417 ≈ 66% of morning time value still on the books at the closing bell. For a 0DTE this understates true afternoon dec~ (This audit's arithmetic on the stated model vs BSM/variance-clock benchmarks (verified nu~)
- [high] Practitioner folklore numbers circulating for 0DTE decay (e.g., '$0.15-0.25/hr at open, $0.40-0.55/hr midday, $0.80-1.20/hr final 90 min on a $5 ATM option', 'loses 50% of premium in first two hours', '4-5x morning rate by 3:30pm') are UNCITED - no methodology, sample, or period is ever given - and are internally contradictory across sit~ (https://volatilitybox.com/research/0dte-options-volatility-day-trading-guide/, https://ma~)
- [low] For 1-2 DTE marking, overnight (close-to-open) variance is a material fraction of 24h variance for US equity indices (commonly estimated in the ~20-40% range, period-dependent) and must be assigned its own weight in any variance clock; the academic literature treats overnight separately from intraday realized variance because doing so im~ (HAR-with-overnight literature, e.g., ScienceDirect S0169207013000472 ('Overnight stock re~)
- [high] Event days break any average intraday decay curve: scheduled macro releases (FOMC 14:00 ET, CPI 8:30 ET pre-open) concentrate variance at known clock times, and 0DTE trading/pricing responds to these - Bandi-Fusari-Reno document 0DTE use for macro-announcement bets and Todorov-Zhang's periodic component shifts across regimes (2020 vs 201~ (Bandi/Fusari/Reno Section 5 (announcement evidence); Todorov & Zhang Figure 7 (regime var~)
VERDICT: The folklore shape - morning gradual, afternoon acceleration, collapse concentrated around/after 15:30 ET - is real and multiply confirmed: measured by practitioner minute-data studies (Option Alpha 30-day SPX sample; a 40-day independent replication) and explained by the academically solid fact that intraday variance is U-shaped with the close carrying ~1.5-3x average variance (Todorov-Zhang, option-implied, Journal of Econometrics). But ATLAS's hardcoded sigmoid gets the magnitudes badly wrong in both directions: capping at ~15%/hr it understates true 0DTE afternoon decay (25-100+%/hr of remaining time value after 14:00) by 3-6x and leaves ~66% of morning time value on the books at a 0DTE's expiry (truth: zero), while simultaneously overstating 1-2 DTE afternoon decay ~5-7x (truth: ~1-2%/hr). The root defect is structural, not parametric: fractional decay depends on remaining variance~
IMPLICATIONS: STRUCTURAL FIX (highest priority): replace the clock-time-only sigmoid with a variance-clock hazard that depends on time-to-expiry: rate(t, expiry) = eta(t) / (2 * RemainingVariance(t, expiry)), where eta(t) is a normalized intraday variance profile (U-shape:~ | MAGNITUDE CORRECTIONS if keeping a parametric stopgap: for 0DTE the current curve understates post-14:00 decay 3-6x (true fractional decay of remaining TV: ~25%/hr at 14:00, ~50%/hr at 15:00, 100%+/hr after 15:30, with the single biggest measured drop around ~ | ENTRY GATES / p_thesis: long-premium 0DTE entries after ~14:00 ET are paying 25-100%/hr of remaining time value; the thesis's expected move must clear the remaining-variance-priced move in under 2 hours. Either (a) raise the p_thesis/edge bar for 0DTE entries~ | EXIT ENGINE: with the current model the engine believes a held 0DTE still has ~66% of its morning time value at the close - it will systematically hold losers into the 15:30 collapse because the modeled cost of waiting is 3-6x too low. Under the owner's 26-rules la~ | EVENT-DAY OVERLAY: on FOMC (14:00), CPI-day opens, and other scheduled-release days the unconditional curve is wrong in both directions - premium holds up (decay near zero) into the event and then gaps down (IV crush) after it. Gate: suppress decay-based exit~ | VALIDATION / REFIT PROTOCOL for the decay model (fits the existing N>=25 policy): (1) from ATLAS's own quote snapshots build per-bucket empirical decay: bucket by DTE (0/1/2) x 30-min time-of-day x event-day flag; (2) strip direction by regressing delta-ln(ex~ | GRADING BAR (/eodreport Phase 3b): grade afternoon holds and exits against a decay-adjusted counterfactual - the WORST-fill benchmark for 'should have exited at t' must charge the variance-clock decay between t and actual exit, otherwise every afternoon 0DTE ~ | POSTURE SANITY CHECK: the best academic evidence (Beckmeyer et al.) says undifferentiated long-premium 0DTE is negative-EV with ~60% of taker losses coming from spreads/fees - ATLAS's contract-selection gates (spread caps, OI, no_quote rejects) that currently~ | SANITY NOTE ON THE MODEL'S CLOCK: this audit read m as minutes-since-midnight ET (840 = 14:00, giving 9:30 = 570). If the code actually feeds minutes-since-open (open = 0), the sigmoid midpoint sits 14 hours after the open, the afternoon term never activates ~

## RESEARCH Published, cost-adjusted evidence for (1) intraday continuation after morning range breaks on index ETFs, (2) ORB on ca~
- [medium] ORB on QQQ (Zarattini & Aziz 2023, 2016-2023): 5-min opening range breakout, net of commissions, ~33%/yr annualized alpha and ~675% total return vs 169% buy-and-hold QQQ; optimized variant (stop = 5% of 14-day ATR, hold to EoD) shows 93%/yr alpha and +0.18R average per trade but models NO slippage (authors' caveat) and is a low-win-rate/~ (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=4416622 ; caveats: https://retailtrad~)
- [medium] SPY intraday momentum with VWAP/noise-area bands (Zarattini, Aziz & Barbon 2024 'Beat the Market'): trade only when price exits a 14-day noise area, exit at close or on VWAP/boundary re-cross; ~19.6%/yr, Sharpe 1.33, total ~1,985%; win rate ~36-43%, payoff ratio ~2.09; on ES futures WITH realistic costs expectancy ≈ +2 basis points per t~ (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=4824172 ; https://quantmacro.substack~)
- [medium] ORB on stocks-in-play (Zarattini, Barbon & Aziz 2024; 7,000+ US stocks 2016-2023): 5-min ORB restricted to top-20 relative-volume names yields Sharpe ~2.4, beta ~0, ~33%/yr alpha net of commissions. Per-trade expectancy flips sign at the RVOL boundary: −0.02R when opening-range relative volume <100% of normal vs +0.08R when >100%. QuantC~ (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=4729284 ; https://www.quantconnect.co~)
- [high] Market intraday momentum (Gao, Han, Li & Zhou, JFE 2018, peer-reviewed; SPY 1993-2013): first half-hour return predicts last half-hour return, R²=1.6% (2.6% adding 12th half-hour). Sign-timing the last 30 min earns 6.67%/yr (in market 30 min/day), Sharpe 1.08, success rate 54.37% vs 50.42% always-long - the best-documented index intraday~ (https://www.sciencedirect.com/science/article/abs/pii/S0304405X18301351 ; https://papers.~)
- [high] Mechanism and decay risk of last-30-min momentum (Baltussen, Da, Lammers & Martens, JFE 2021): last-30-min return is positively predicted by rest-of-day return across 60+ futures (equities/bonds/commodities/FX), 1974-2020; attributed to gamma-hedging flows of option market makers and leveraged-ETF rebalancing; the move REVERTS over subse~ (https://www.sciencedirect.com/science/article/abs/pii/S0304405X21001598 ; https://www3.nd~)
- [medium] End-of-day CROSS-SECTIONAL reversal in single stocks (Baltussen, Da & Soebhag, 1993-2019): ranking on return through 3:30pm, biggest intraday losers beat biggest winners by ~0.24% on average during 3:30-4:00pm (>20%/yr gross long-short alpha), driven by closing-auction institutional flows. Index momentum into the close and single-name re~ (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=5039009 ; https://alphaarchitect.com/~)
- [high] Overnight/intraday split: essentially the entire SPY equity premium since 1993 is earned close-to-open; across major ETFs 1993-2017 overnight returns averaged +14.78%/yr vs intraday −7.14%/yr (microstructure inflates overnight ~6%/yr). Unconditional P(last half-hour up) = 50.42%. There is NO unconditional intraday drift tailwind for long~ (https://www.sciencedirect.com/science/article/abs/pii/S138641812030032X ; https://elmweal~)
- [high] Lucca & Moench (Journal of Finance 2015; NY Fed SR512): S&P 500 rose on average +49bp in the 24 HOURS BEFORE scheduled FOMC announcements (1994-2011), ~80% of the annual equity premium; AFTER the 2pm release the market is on average roughly FLAT in the following hours and next day - the published FOMC anomaly is pre-announcement; there i~ (https://www.newyorkfed.org/medialibrary/media/research/staff_reports/sr512.pdf)
- [high] The pre-FOMC drift largely DISAPPEARED after 2015, documented independently twice (Finance Research Letters 2020 'The disappearing pre-FOMC announcement drift'; Applied Economics 2024). (https://pmc.ncbi.nlm.nih.gov/articles/PMC7525326/ ; https://www.tandfonline.com/doi/full/~)
- [medium] Since 2020, stocks and bonds systematically move OPPOSITE to their initial 2:00pm FOMC statement reaction during the 2:30pm press conference, with reversal direction linked to the Chair's spoken tone (Gorodnichenko et al., CEPR/VoxEU). The robust post-FOMC intraday pattern is partial REVERSAL risk at 2:30pm ET, not continuation. (https://cepr.org/voxeu/columns/market-impact-fed-press-conference)
- [medium] 'Monetary momentum' (Neuhierl & Weber, NBER w24748): returns drift in the direction of the policy SURPRISE on a MULTI-DAY horizon - cumulative expansionary-minus-contractionary return difference grows from ~2.5% at the decision to >4% by 15 days after. Supports multi-day directional bias after genuine surprises (relevant to 2-5 DTE), not~ (https://www.nber.org/system/files/working_papers/w24748/w24748.pdf)
- [high] Macro announcement-day premium (Savor & Wilson 2013, 1958-2009): average equity excess return ~8.3-11.4bp on CPI/employment/FOMC days vs ~1.1-1.4bp on other days - real but ~0.1x of a typical daily range, far below a 0.3-1x range target. Post-release price discovery for CPI/employment completes within minutes; measurable pre-release drif~ (https://jacobslevycenter.wharton.upenn.edu/wp-content/uploads/2019/04/The-Macroeconomic-A~)
- [medium] 0DTE friction and realized retail outcomes (Beckmeyer, Branger & Gayda, Feb 2021-Sep 2023): >75% of retail S&P-complex option trades are 0DTE; retail lost ~$350k/day on average since daily expirations (May 2022); mean return of 0DTE trades ≈ −4.6% per trade; retail effective spreads ≈ 6.1% of premium (calls) / 4.9% (puts). Bogousslavsky ~ (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=4404704 ; https://www.lsu.edu/busines~)
- [medium] Quoted SPY 0DTE spreads are ~$0.01-0.03 near the money - ~0.5-3% of premium ATM (e.g., $0.01 on a $2.15 ATM call ≈ 0.5%) but ~13% of premium for cheap (~$0.15) OTM contracts. In underlying terms an ATM SPY option spread is <1bp of spot per side, so documented underlying edges (+2-8bp/trade) CAN survive ATM index option friction but CANNO~ (https://marketxls.com/blog/how-to-trade-0dte-spy-options-expert-insights ; https://fattai~)
- [high] Folklore vs fact: 'breakouts continue 50%+ of the time / trend days are common' is folklore. Measured win rates of breakout-continuation systems to meaningful targets: ~17% (stocks-in-play ORB, ATR stop, EoD exit), low-20s% (QQQ ORB with large R-multiple targets), 36-43% (SPY noise-area, EoD exit, VWAP trail). Measured directional sign e~ (Cross-source synthesis: SSRN 4416622, SSRN 4729284, SSRN 4824172, JFE 2018 (Gao et al.), ~)
VERDICT: The four literatures are real but they support convexity, not coin-flip continuation to big targets. Cost-surviving measured facts: index morning-break continuation systems win 36-43% of trades (SPY noise-area/VWAP, ~+2bp/trade on futures with costs, 19.6%/yr, Sharpe 1.33); catalyst/high-RVOL stock ORB wins ~17-25% with +0.08R expectancy above the RVOL=100% gate and ~33%/yr alpha net of commissions (practitioner working papers - medium confidence, overfit/slippage caveats); last-30-min index momentum is peer-reviewed and survives measured bid-ask costs (54.4% directional hit vs 50.4% base, 4.46%/yr net, 3-4x stronger on macro days, gamma-flow-driven, reverts next day), while single-name intraday winners REVERSE in the final half hour; post-FOMC/CPI intraday drift is NOT supported - the pre-FOMC drift (+49bp) died post-2015, post-print hours average flat with 2:30pm press-conference reve~
IMPLICATIONS: p_thesis = 0.5 is defensible ONLY at the near edge of the target band: a 0.3x daily-range target with end-of-day horizon, RVOL-confirmed breakout, and no tight stop. It is NOT defensible for a 1x daily-range target - literature-implied hit rates there are ~0.~ | Keep the RVOL gate hard and quantitative: the stocks-in-play paper measures the expectancy sign flip exactly at opening-range RVOL = 100% of normal (−0.02R below, +0.08R above). Catalyst-lane entries should require RVOL > 1.0 minimum (>1.5 preferred). The fun~ | Grade lanes by R-expectancy at N≥25, never by win rate: published intraday edges run 17-43% win rates with ~2:1+ payoffs and per-trade expectancy of only +0.02R to +0.18R on the underlying (+2bp/trade on futures with costs). A lane at 30% wins with positive R~ | Last-30-minutes lane shape: go WITH the rest-of-day direction on SPY/QQQ/IWM into the close (54.4% hit, net-of-spread positive per Gao JFE 2018; gamma-flow mechanism per Baltussen JFE 2021), upweight on high-vol/high-volume/macro-news days (effect 3-4x strong~ | For catalyst SINGLE NAMES the last half hour is the opposite trade: big intraday winners fade 3:30-4:00pm (losers beat winners by ~24bp in that window, closing-auction flows). Treat 3:30pm ET as a scheduled EV headwind for long calls on names already up big i~ | FOMC/CPI: there is NO published intraday post-print continuation edge justifying p_thesis=0.5 toward range-scale targets after 2pm FOMC or 8:30am CPI. Average post-FOMC hours are flat; the robust modern pattern is partial REVERSAL during the 2:30pm press conf~ | Contract selection: stay near-ATM on SPY/QQQ/IWM where spread is 0.5-3% of premium; reject contracts with round-trip spread >~5% of premium or premium <~$0.50 (cheap OTM spreads run ~13%). The literature's losing retail category is exactly 'single-leg long pr~ | No unconditional intraday drift exists: all of SPY's premium since 1993 is overnight; intraday averages ~0 to negative. Every long-call entry must be funded by the conditional signal (RVOL breakout, rest-of-day momentum into close, post-surprise multi-day dri~ | Decay-model calibration: treat thesis validity as flow-driven and intraday-mortal - breakout theses die when price re-enters the noise band or crosses VWAP against the position (the published stop that made the SPY strategy work); last-30-min momentum theses ~

## RESEARCH US options microstructure facts to calibrate ATLAS shadow-trader gates: (1) NBBO spreads for SPY/QQQ/IWM 0-5 DTE near-t~
- [high] SPY 0DTE near-ATM (delta 0.31-0.69, premium $0.85-3.00): quoted NBBO spread $0.01-0.03 = 0.5-1.2% of mid; ATM 753C $0.01 on $1.145 mid (0.87%). Measured live Thu 2026-07-16 at 09:35:27 ET (SPY spot 752.80; 18 SPY contracts sampled). Single-morning sample, but ATM quotes are pinned at the $0.01 minimum tick so this generalizes to normal R~ (Live Robinhood NBBO snapshot via get_option_quotes, 2026-07-16T13:35Z)
- [high] SPY deep-ITM 0DTE (delta 0.80-0.93): spread $0.04-0.25 = 0.9-3.4% of mid; SPY 5DTE (07-21) ATM $0.03 (0.9%), 5DTE 0.86-delta $0.26 (2.0%). ITM spreads are 3-10x wider in absolute terms than ATM at the same expiry. (Live Robinhood NBBO snapshot 2026-07-16 09:35 ET)
- [high] QQQ 0DTE ATM spread $0.01-0.05 (0.45-1.5% of mid; 0DTE IV 36-38% this morning); IWM 0DTE/1DTE ATM $0.01-0.03 (1.4-2.4%; IV 19-27%); NVDA 1DTE ATM $0.03 (1.4%, IV ~49%), NVDA 0.76-delta $0.10 (1.9%). NVDA tick regime: $0.01 below $3.00 premium, $0.05 above; SPY/QQQ/IWM are $0.01 at all prices (min_ticks confirmed via API). (Live Robinhood NBBO snapshot 2026-07-16 09:35 ET)
- [medium] Intraday pattern: option bid-ask spreads are widest at the open, fall rapidly, then remain roughly flat through the day - unlike the U-shape in stock spreads (canonical CBOE-options finding in the intraday-spread literature). By 09:35 index ATM spreads were already at the 1-tick floor, so open-widening persists mainly in ITM strikes and ~ (https://www.sciencedirect.com/science/article/abs/pii/S0304405X99000227 (Chan/Chung/Johns~)
- [high] Population baselines: retail-favored weekly equity options averaged 12.6% quoted relative spread (OPRA, Nov 2019-Jun 2021, all moneyness - dominated by cheap OTM contracts; ATM index dailies are ~10x tighter). S&P 500 stock-option quoted spreads average as high as ~17% of option value. (Bryzgalova-Pavlova-Sikorskaya, J. Finance 2023, https://onlinelibrary.wiley.com/doi/full/~)
- [high] At 09:35 ET, ATM/near-OTM index strikes pass the proposed gates by an order of magnitude: prior-day OI 3,500-27,000 and day volume already 385-16,038 (SPY 753C vol 14,148; QQQ 710P vol 13,578; IWM 0DTE 296C OI 3,533 / vol 4,876). OI>=1000 and vol>=100 are not binding for the ATM index case. (Live Robinhood snapshot 2026-07-16 09:35 ET)
- [high] Gate failure mode 1 (measured): newly-listed strikes after an overnight gap show OI=0 with heavy volume - QQQ gapped -0.76% and its most-ATM 0DTE strike (712) had OI=0 with volume 2,813 (call) / 6,255 (put) at 09:35. OCC open interest updates once daily pre-open and never reflects same-day strike additions or intraday 0DTE flow; volume >~ (Live snapshot; https://support.spotgamma.com/hc/en-us/articles/15350782964755 ; https://w~)
- [high] Gate failure mode 2 (measured): ITM contracts (delta 0.6-0.93 - the region the delta gate targets) routinely fail both gates even on SPY: 0DTE 745C OI 697 / vol 32; 748C OI 457 / vol 123; 1DTE 745C vol 13 at 09:35 (OI 26,330); 5DTE 740C OI 74 / vol 0. Retail OI and volume concentrate in ATM/OTM strikes. (Live Robinhood snapshot 2026-07-16 09:35 ET)
- [high] Single-name OI: NVDA weekly ATM/near strikes carried OI 25,370-93,515 and day volume 292-4,856 by 09:35 - OI>=300 is trivially passed by liquid large-cap weeklies; it would bind only on mid-cap catalyst names and far strikes (not directly measured here). (Live Robinhood snapshot 2026-07-16 09:35 ET)
- [high] Regulatory gap: SEC Rule 605 execution-quality reporting covers NMS stocks only; listed options have NO mandated execution-quality disclosure (the CCMR 2025 staff report states options markets lack disclosures enabling price-improvement-by-order-type analysis and urges extending Rule 605 to options). All broker options-PI claims are volu~ (https://capmktsreg.org/wp-content/uploads/2025/06/CCMR-Staff-Report-Retail-Options-Market~)
- [medium] Huang-Jorion-Schwarz (2025) experiment: ~7,000 identical small market orders across 6 retail brokers (mid-Mar to Jun 2024, 18 tickers ~45% of options market volume): average round-trip execution cost ranged 0% to 7% of premium across brokers for identical trades; higher PFOF associated with lower price improvement; Robinhood worst (secon~ (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=4951825 ; https://www.tradersmagazine~)
- [high] Mechanism: options must execute on exchanges (no off-exchange internalization) and NBBO trade-throughs are prohibited, so a marketable retail 1-lot fills at or inside NBBO essentially always; wholesaler SLAN price-improvement auctions grant improvement on most retail marketable orders but the amount is typically about one tick (auction e~ (Bryzgalova-Pavlova-Sikorskaya JF 2023 https://onlinelibrary.wiley.com/doi/full/10.1111/jo~)
- [high] SEC DERA staff paper (2025) on SPXW 0DTE: customer non-marketable limit orders sit at the best bid >50% of the time on slightly-OTM strikes (>80% at the offer well-OTM); customer-to-customer executions rise from 10% (early session) to 18% (late) with average spread paid of only $0.10 in SPX points (~$0.01 SPY-equivalent); non-marketable ~ (https://www.sec.gov/files/dera-hope-reasonable-prc-2503.pdf (via https://www.sec.gov/abou~)
- [high] Magnitude anchors: Muravyev-Pearson (RFS 2020) - average options effective spread is ~25% smaller than quoted; execution-timing traders achieve effective spreads <40% of conventional measures (2.2% effective vs 17.2% quoted). Beckmeyer-Branger-Gayda - retail 0DTE traders benefit from the price-improvement mechanism (lower effective sprea~ (https://academic.oup.com/rfs/article-abstract/33/11/4973/5732665 ; https://papers.ssrn.co~)
- [high] Lambda measured live (lambda = delta*S/premium at 09:35 ET): SPY 0DTE ATM 271 (0.41-delta), 0.69-delta 173, 0.82-delta 136, 0.93-delta 96; SPY 1DTE ATM 164, 0.82-delta 78; SPY 5DTE ATM 107, 0.86-delta 49; QQQ 0DTE 0.54-delta 118, 0.77-delta 82; IWM 0DTE ATM 180; NVDA 1DTE (~49% IV) ATM 46.5, 0.76-delta 30.6. (Computed from live Robinhood deltas and mid prices, 2026-07-16 09:35 ET)
- [high] Black-Scholes grid (S=750, r=4%; IV 12/16/20%; DTE 0.25/1/2/3/5; delta 0.40-0.80): lambda spans 30-372. lambda<=90 excludes EVERY contract at DTE<=1 for IV<=20% (lambda 67-372) and most of DTE 2 at IV<=16%. The [15,90] band intersected with the 0.40-0.80 delta gate on a 12-20% IV index leaves essentially only DTE 3-5 (plus deep-ITM DTE 2~ (Deterministic computation (py -3), cross-validated against live quotes)
- [high] Decay and IV context (measured): SPY 0DTE ATM theta -1.15 $/day on $1.145 premium = ~-100% of premium per calendar day; SPY 1DTE ATM theta -0.99 on $2.07 (~-48%/day). Observed ATM IVs: SPY 0DTE 18.6-19%, IWM 0DTE 27%, QQQ 0DTE 36-38% (event morning), SPY 5DTE only 10% - a fixed '12-20% IV' planning band understates short-dated event-day ~ (Live Robinhood snapshot 2026-07-16 09:35 ET)
VERDICT: Gate-by-gate: (1) Spreads - SPY/QQQ/IWM near-ATM 0-5 DTE quote at the $0.01 tick floor (0.3-1.5% of premium) within minutes of the open; ITM strikes and single names run $0.03-0.26 (1-3.4%); the literature says option spreads are widest at the open, decay fast, and stay flat (no equity-style close-widening), so a two-regime max(absolute, relative) cap with a ~09:45 boundary is the right shape. (2) OI>=1000 / >=300 and vol>=100 at 09:35 are trivially satisfied by ATM/OTM index and large-cap strikes but systematically veto (a) newly-listed ATM strikes on gap days (OI=0 with thousands traded - measured live on QQQ this morning) and (b) the ITM 0.6-0.9-delta contracts the delta gate targets; OI is stale-by-construction (once-daily OCC update) for daily expirations where volume greatly exceeds OI - convert to an OR-gate with live volume/depth. (3) The WORST ledger is realistic, not overly pe~
IMPLICATIONS: Spread gate - make it two-regime and max(absolute, relative): index 0-5 DTE near-ATM, quoted spread <= max($0.02, 1.5% of mid) is comfortably satisfiable from ~09:45 (live ATM spreads sit at the $0.01 tick floor by 09:35); allow max($0.05, 2.5%) for 09:30-09:~ | Add a premium floor (~$0.50) as the real liquidity gate: below ~$0.30 premium the 1-tick minimum spread alone is 3-6% of premium (measured: SPY 745P at $0.175 mid = 5.7%), a round-trip drag no p_thesis edge in the current funnel plausibly clears. This address~ | OI gate is wrong-shaped, not wrong-sized: OI>=1000 (index) / >=300 (single-name) passes ATM/OTM by 4-90x but hard-vetoes (a) newly-listed ATM strikes on gap days - measured live: QQQ's most-ATM 0DTE strike had OI=0 with 6,255 contracts traded by 09:35 after a~ | Day-volume>=100 at 09:35 is reasonable only for 0-1 DTE ATM index contracts (measured 385-16,038). It fails routinely for DTE>=2 (SPY 5DTE ATM volume 60-61 at 09:35) and ITM strikes (0-123). For DTE>=2 or delta>=0.6, test quote depth instead (live bid/ask siz~ | Keep the WORST (buy-at-ask/sell-at-bid) ledger as the grading bar - it is a true bound (options cannot trade through NBBO) and for Robinhood-routed 1-lots (the venue ATLAS's grading tokens come from, and the worst price-improvement venue among six majors in t~ | Register a venue-cost constant in the exit-engine EV math as DERIVED (per the provenance law): ~1% of premium round-trip for index ATM 0-5 DTE, ~3% for single-name weeklies, bounded above by the 0-7% cross-broker round-trip range from the 7,000-order experime~ | The [15,90] lambda band contradicts the 0.40-0.80 delta gate for index 0-2 DTE at real IVs - measured ATM lambdas were 107-271 (all three ETFs, DTE 0-5) and the BS grid gives 67-372 at DTE<=1 for 12-20% IV. As configured, index 0-1 DTE near-ATM entries are de~ | The lambda>=15 floor quietly vetoes the best deep-ITM high-IV single-name contracts (0.80-delta, 5DTE, 55% IV computes to ~11.5): lower the floor to ~10 or condition it on delta<0.7, otherwise the stock-replacement end of the earnings-name book is unreachable. | Decay model and grading bar must be intraday at short DTE: measured 0DTE ATM theta is ~-100% of premium per calendar day (-1.15 on $1.145), so accrue decay against remaining session minutes, not calendar days, and mark WORST exits against the live bid at each~ | Read IV live per contract at decision time (the quote feed returns it): observed 0DTE ATM IVs this morning were SPY 19% / IWM 27% / QQQ 36-38% while SPY 5DTE was 10% - any gate constant hard-coded from a '12-20% IV' assumption (lambda band, premium expectatio~ | Calibration caveat to record in the sweep ledger: the spread/OI/volume numbers here are one Thursday-morning 09:35 snapshot (mild gap-down; no index-event day; no earnings-day single-name). Before hard-coding gate constants, replay the same 4-symbol/~26-contr~

## RESEARCH Best-practice sequential statistics for validating a trading strategy from a small live sample: (1) SPRT/mSPRT/alpha-sp~
- [high] Wald's SPRT (1945) is provably optimal in expected sample size for iid observations at fixed error rates (Wald-Wolfowitz 1948), typically needing 30-50% fewer observations on average than a fixed-N test; but it assumes a simple H1 and iid data, and naive daily peeking at a conventional p-value without such a design badly inflates Type I ~ (Wald 1945 Ann. Math. Statist.; https://metricgate.com/docs/sequential-probability-ratio/;~)
- [high] mSPRT / always-valid p-values (mixture SPRT, Robbins 1970; Johari, Koomen, Pekelis, Walsh - KDD 2017 'Peeking at A/B Tests' and Operations Research 70(3) 2022) give p-values and confidence intervals that remain valid under continuous monitoring with optional stopping at any time; deployed in production at Optimizely since 2015. (https://arxiv.org/abs/1512.04922; https://dl.acm.org/doi/10.1287/opre.2021.2135; http://l~)
- [medium] Measured cost of anytime-validity (simulation study): to reach 80% power at alpha=5%, SPRT needed ~22% more maximum sample than a fixed-N test, always-valid mSPRT needed ~85% more, while a group-sequential design (AGILE) needed roughly the fixed-N amount; at equal max N, mSPRT's Type II error was ~1.9x the group-sequential design's. (https://blog.analytics-toolkit.com/2022/comparison-of-the-statistical-power-of-sequential~)
- [high] Alpha-spending (Lan & DeMets 1983) permits interim looks without pre-fixing their number/timing: O'Brien-Fleming spending f(t)=2-2*Phi(Phi^-1(1-alpha/2)/sqrt(t)), Pocock spending alpha*ln(1+(e-1)t). Textbook boundaries for K=5 equal looks, alpha=0.05 two-sided: OBF z = 4.56, 3.23, 2.63, 2.28, 2.04; Pocock constant z ~ 2.41 (Jennison & Tu~ (Lan & DeMets 1983 Biometrika 70:659-663; https://keaven.github.io/gsDesign/reference/sfLD~)
- [high] For skewed/heavy-tailed P&L where normality fails, nonparametric anytime-valid tools exist: time-uniform confidence sequences (Howard, Ramdas, McAuliffe, Sekhon, Ann. Statist. 2021), betting/empirical-Bernstein confidence sequences for BOUNDED variables (Waudby-Smith & Ramdas, JRSS-B 2024), and Catoni-style sequences for heavy tails (Wan~ (https://arxiv.org/abs/2010.09686 (Howard et al.); https://rss.org.uk/RSS/media/File-libra~)
- [high] Fixed-sample power analysis (computed, N = ((z_a+z_b)*sigma/mu)^2, one-sided alpha=5%, power 80%): edge 20% of premium with sigma 50% -> N=39 trades; 20%/100% -> 155; 10%/100% -> 618; 10%/150% -> 1,391; 5%/100% -> 2,473; 5%/150% -> 5,564. For 90% power multiply by ~1.38. The hypothesized ATLAS edge band (5-20% of premium, sigma 50-150%) ~ (Standard normal-approximation power formula (Cohen 1988); computed this session via py -3~)
- [high] At N=25 with per-trade sigma = 100% of premium, the standard error of mean P&L is 20% of premium: the smallest detectable edge is ~39% of premium at t=1.96 and ~60% of premium at the Harvey-Liu t>=3 multiple-testing hurdle - i.e., 2-6x larger than the entire hypothesized 5-20% edge band. N=25 cannot statistically confirm the edges ATLAS ~ (Computed (SE = sigma/sqrt(N)); hurdle from Harvey, Liu & Zhu, Rev. Financial Studies 29(1~)
- [high] SPRT expected sample sizes at alpha=5%, beta=20% (Wald approximations, computed): for edge 10%/sigma 100%, E[N|edge real]=381 vs fixed-N 618 (~38% saving), E[N|no edge]=268; for 20%/100%, E[N|H1]=95. Sequential testing saves ~38% on average but does NOT change the order of magnitude - hundreds of trades are still required for the plausib~ (Wald 1947 approximations E[N] = ((1-b)A+bB)/KL; computed this session; savings range corr~)
- [high] Harvey, Liu & Zhu (RFS 2016) document 316+ published return factors and argue a newly discovered strategy must clear t > 3.0 (not 2.0) after multiple-testing adjustment; 'most claimed research findings in financial economics are likely false.' Harvey & Liu 'Backtesting' (J. Portfolio Mgmt 42(1) 2015) operationalize this as haircut Sharpe~ (https://academic.oup.com/rfs/article/29/1/5/1843824; https://people.duke.edu/~charvey/Res~)
- [high] When grading the best-of-many variants on the same data, the correct tools are White's Reality Check bootstrap (Econometrica 2000), Hansen's SPA test (2005, better power via re-centering), and Romano-Wolf stepwise testing (Econometrica 2005, identifies ALL outperformers while controlling family-wise error); Benjamini-Hochberg(-Yekutieli)~ (https://www.researchgate.net/publication/4896389_A_Reality_Check_for_Data_Snooping; http:~)
- [high] Bailey & Lopez de Prado's Deflated Sharpe Ratio (J. Portfolio Mgmt 40(5) 2014): under N unskilled trials the EXPECTED maximum Sharpe grows ~ sqrt(2 ln N) x SD(SR) (exact form uses (1-gamma)*Phi^-1(1-1/N)+gamma*Phi^-1(1-1/(N*e)), gamma=0.5772); the companion 'Pseudo-Mathematics and Financial Charlatanism' (Notices of the AMS 61(5) 2014) s~ (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2460551; https://www.ams.org/notices/~)
- [high] Probabilistic Sharpe Ratio / Minimum Track Record Length (Bailey & Lopez de Prado 2012): MinTRL = (1 - skew*SR + (kurt-1)/4*SR^2) * (z_(1-alpha)/(SR - benchmark))^2 observations - positive skew (typical of long premium) shortens, fat tails lengthen the required record. Worked example: an observed annualized SR ~1.0 vs benchmark 0.75 requ~ (https://portfoliooptimizer.io/blog/the-probabilistic-sharpe-ratio-bias-adjustment-confide~)
- [high] Profit factor at N=25 (Monte Carlo, 200k paths, zero-edge long-premium P&L: 40% winners ~ Exp(mean +75% of premium), 60% losers ~ Uniform(-20%,-80%), mean 0, sigma 78%): median PF=0.97 but the null 95th percentile is PF=2.20; P(PF>1.5)=19.6% and P(PF>2.0)=7.4% with ZERO edge. A harsher 0DTE profile (30% win / 70% lose -50%) gives 95th pe~ (Monte Carlo computed this session (py -3, seed 20260716, assumptions stated); practitione~)
- [medium] Practitioner PF folklore (PF 1.5-2.0 = 'good') carries no sample-size conditioning and is not a statistical claim; multiple trading-education sources independently state PF over fewer than 50 trades is noise and 100-200+ trades are needed before PF stabilizes - consistent with the Monte Carlo above. (https://www.tradezella.com/blog/profit-factor; https://www.quantvps.com/blog/how-to-calcu~)
- [high] Harvey (AFA Presidential Address, J. Finance 72(4) 2017) recommends Bayesianized p-values: combine the minimum Bayes factor with prior odds reflecting economic plausibility; because true trading edges are rare, even t>3 produces many false positives under low prior odds. (https://onlinelibrary.wiley.com/doi/abs/10.1111/jofi.12530; https://papers.ssrn.com/sol3/~)
- [high] Conjugate-normal skeptical-prior posteriors (computed): with prior edge ~ N(0, (10% premium)^2) and sigma=100%: observing mean +10% over N=25 trades gives P(edge>0) = 58.8% (with a tighter tau=5% skeptic: 54.8%); N=100 -> 76.0%; N=400 -> 96.3%. Observing +20% at N=25 -> 67.3%; even +40% at N=25 only reaches 81.4%. To hit P(edge>0)>=95% r~ (Standard normal-normal conjugate update, computed this session; prior-scale logic per Har~)
VERDICT: Small-N live validation of a 5-20%-of-premium edge is statistically infeasible at N=25: that sample can only certify edges of 39-60%+ of premium, while the hypothesized band needs ~155-2,500 trades (central case ~618; SPRT/anytime-valid designs shave ~38% at best and cost 1.5-2x if daily peeking must stay legal). The right architecture for ATLAS is therefore: anytime-valid confidence sequences (betting/empirical-Bernstein on winsorized premium-normalized P&L) to make the daily EOD peek exact; BHY-FDR plus Romano-Wolf/SPA and Deflated-Sharpe deflation across the 5 lanes and replay variants; a sample-size-dependent profit-factor bar (PF >= ~2.2 at N=25, ~1.5 at N=100 - flat PF folklore is noise below 50 trades); and a skeptical-prior posterior P(edge>0) as the headline scorecard number (+10% mean at N=25 = only ~59% conviction). This vindicates the existing GO-LIVE-FIRST structural-proof ~
IMPLICATIONS: The N>=25/lane verdict bar is a SCREEN, not a validation: at sigma ~100% of premium it can only certify edges >=39% of premium (t=1.96) or >=60% (Harvey-Liu t>=3), versus the hypothesized 5-20% band which needs N~155-2,473 (central case 10%/100%: 618 trades).~ | At the current funnel rate (0.14 fires/day ~ 35 trades/yr), 618 trades is ~17 years: per-lane P&L significance is unreachable on any useful horizon. The existing GO-LIVE-FIRST policy (promote only provable/structural or proven-system fundamentals) is the stat~ | The daily /eodreport look at cumulative P&L is textbook peeking. Legalize it with an anytime-valid monitor instead of ad-hoc thresholds: run a betting/empirical-Bernstein confidence sequence (Waudby-Smith & Ramdas) on premium-normalized per-trade P&L, winsori~ | If a fixed-horizon test is ever wanted (e.g., 'grade this tweak over 100 trades'), use O'Brien-Fleming spending via Lan-DeMets: early stops then require z~4.5, which correctly prevents day-3 euphoria/panic; final-look threshold ~2.04 preserves most of the fix~ | Multiplicity is ATLAS's biggest silent alpha-inflater: 5 lanes x dozens of replay variants ~ 100+ hypotheses per grading cycle. Apply BHY (dependence-robust FDR, q=10%) to lane-level p-values in /eodreport, and Romano-Wolf or Hansen SPA when crowning a best r~ | Profit-factor gate, concrete: at N=25 require PF >= 2.2-2.3 (the zero-edge 95th percentile under a realistic long-premium P&L shape) for even nominal significance; PF 1.5 at N=25 is luck 20% of the time. At N=100 the nominal bar drops to ~1.5, at N=200 to ~1.~ | Add a skeptical-prior Bayesian line to the per-lane scorecard: posterior P(edge>0) under prior N(0, (10% premium)^2) - this is the honest small-N statistic. Calibration: +10% mean at N=25 -> ~59%; +20% at N=25 -> ~67%; 95% conviction needs N~123 even at a sus~ | Exit-engine v2 at 1/25 graded: one trade has SE ~100% of premium - zero statistical content; its grade is mechanism-only (did rule (b) fire correctly), never evidence about EV. Guard against anyone (including future Claude sessions) citing its P&L directional~ | Positive skew of long-premium P&L is a small tailwind: in the PSR/MinTRL formula the -skew*SR term SHORTENS the required record when skew>0, so premium-normalized grading (bounded loss, unbounded win) needs somewhat fewer trades than the normal-approximation ~ | Sequential-testing budget reality: even the optimal SPRT only saves ~38% average sample under H1 (e.g., 381 vs 618 trades for a 10%/100% edge) and needs ~268 trades on average to ACCEPT the null. No sequential design turns 25 trades into evidence - the honest~

## DESIGN Repair-in-Place: minimal-diff coherency program (keep lanes -> selector -> ladder -> WORST)
THESIS: The architecture is sound but three confirmed joints make it incoherent and evidence-incapable: a cadence-dependent noise-clock exit ladder that corrupts every graded sample (the only twice-upheld critical cluster: TRAJECTORY-1 / LANES-1 / EXIT-ENGINE-1), a funnel starved by mis-specified config rather than by markets (3 expirations, an empty lambda-x-delta band measured live 07-16, wrong-clock OI/volume floors, a stale hunt list), and a grader whose PASS bar false-passes a zero-edge lane ~33-69% of the time with no pooled row. Fix exactly those with the smallest registered diffs - one funnel config cohort, one exit-engine v3 cohort, one grader rewrite of ~100 lines - after first repairing the replay lab so every change can be validated before arming; delete or park the ~5,000 lines of st~
- (days) Wave 0a - Replay-lab capture repair (atlas/options/replay.py + the mark loop in scripts/run_options_shadow.py): keep polling NBBO for EXITED positions until the thesis horizon (EOD for 0DTE, next-day close for DTE>=1), rows flagged post_exit:true at~ - The lab is the promotion ladder's mandatory stage-4 toll gate and currently has zero discriminating power on 100% of history (grid n_replayed=0; all 5 AB variants byte-identical). Capture stops at the live exit, so no h~
- (days) Wave 0b - Grader integrity + pooled evidence (scripts/grade_options_shadow.py): dedup exits and entries by position_id with loud quarantine (GRADER-7); count merged multi-lane exits ONCE in a new pooled ALL row (GRADER-8); extend _malformed to day/l~ - The current bar is a fixed-threshold filter: ~33% false-PASS per lane at N=25, ~69% under the nightly all-history re-grade (unbounded peeking), ~87% familywise across 5 lanes, and PF>=1.2 is analytically t=0.36. One $2-~
- (hours) Wave 0c - Measure-disagreement instrumentation (selector.py entry record + exit_engine.py state dict + shadow.py entry row): log p_mkt (risk-neutral touch under the trade's own solved IV - one extra om.p_touch call) beside the already-computed p_tou~ - The live IWM trade carried five unreconciled probabilities (0.042 / 0.086 / 0.5 / ~1.0 / 0.0) for one event with none of the disagreement visible. This is hours of work, cohort-neutral, and produces the calibration data~
- (days) Wave 1a - ONE registered funnel-config change (single new config_hash cohort): (i) max_chain_expirations 3 -> 6 (run_options_shadow.py:79); (ii) per-DTE lambda caps {0: 200, 1: 120, >=2: 90} replacing the flat lambda_band upper 90 (selector.py:63 an~ - Measured live 2026-07-16: ATM index lambdas run 107-271 across DTE 0-5, so the flat <=90 cap x 0.40-0.80 delta is jointly EMPTY for index - the registered 0DTE carve-out has gated zero rows because nothing survives to r~
- (hours) Wave 1b - Selector coherence gate (selector.py EV stage, ~:225-233): new hard gate touch_feasibility requiring p_touch_target >= 0.25 - the quantity is already computed and logged at :225 and gated on nothing. - 100% of the live trade's claimed +53.31% ev_pct was manufactured by the hardcoded p_thesis=0.5 prior (market-implied weight -0.004; mu_thesis attains the target by construction, so ev_pct is a target-ambition meter). Th~
- (days) Wave 1c - Hunt-list freshness + lane-2 universe repair (run_options_shadow.py hunt-list read; scripts/build_day_briefing.py; lane-2 admission): reject hunt_list.json whose session date != today and journal it (RUNNER-4 - observed live 07-16 hunting ~ - Lane 2 has never produced a pick partly because its universe is stale or dis-armed, and partly because the crew feeds it 13.9-108%-spread books that can NEVER pass a WORST ledger (round-trip cost 5-25x any plausible edg~
- (hours) Wave 1d - Feed retry hardening (atlas/collect/tradier_data.py): extend the retry loop beyond HTTP 429 to timeouts/connect errors/5xx with capped exponential backoff; bound Retry-After and handle its HTTP-date form; carry quote/trade timestamps throu~ - Chain fetch is first-failure-fatal today - one transient blip kills the whole signal (TRADIER-FEED-1), so part of the 25% no_quote first-fail share is availability, not liquidity. Hours of work that directly raises sign~
- (hours) Wave 1e - Lane latch moved from emission to entry (lanes.py:249-252 IndexTrend, :423-426 InPlayORB): burn the one-per-side daily latch only on selector acceptance/entry, with a 30-minute re-arm while the lane predicate still holds. - The observed death mode is selector rejection AFTER which the lane is disarmed for the day (36 signal_expired plus rejection burns in the journal); retrying the same thesis against fresh books multiplies Wave 1a's effec~
- (week-plus) Wave 2 - Exit-engine v3, ONE registered version (exit_engine.py, trajectory.py, run_options_shadow.py:871-887), validated on the MC harness + 529-session cached-path replay BEFORE arming, then confirmed by the repaired paired-replay lab: (a) rule (b~ - This is the platform's only twice-upheld critical cluster. MC shows 77% pure-noise forced sales within 30 min at 300s cadence (83% at 60s); replay shows 41% of lane-1 entries cut at the first mark; the one live trade wa~
- (hours) Wave 2b - RTH-gate LiveBarBuilder (atlas/hunter/feed.py:101-160): drop pre-09:30 commits (or reset builder state at the open) so svwap, HOD, and the mu window contain only RTH bars, matching the already-RTH-only backfill. - Premarket prevclose-echo bars permanently contaminate session VWAP and the drift window that BOTH entry admission and exit invalidation consume; restart-vs-continuous runs currently disagree on svwap. Structural, cohort~
- (week-plus) Wave 3 - Per-lane p_thesis calibration offline (new script over the dormant ~500-session runtime/intraday_cache; constructor params already exist at run_options_shadow.py:440-449): replay the pure lane predicates, estimate per-lane P(touch target wi~ - Replaces the fanned global 0.5 - the root of the manufactured-EV defect - with an empirical number, using infrastructure that already exists (the LANES-1 refuters prototyped the harness). This is the durable fix behind ~
- (hours) Wave 3b - watch_hub options tab (scripts/watch_hub.py): render the already-computed snap.options as the default tab. - The platform's only human RTH surface shows archived equity panels while the live options shadow is invisible; pure observability for every change above, no registration needed.
EXPECTED: Funnel: lane fires reaching a chain rise from ~0.5/day to ~2-3/day and entries from 0.33/day (2 in 6 sessions) to 1.0-2.0/day pooled, dominated by index DTE 1-5 tenors (Wave 1a is the binding unlock: the funnel-throughput judge's replay showed morning index signals gaining DTE 3-5 tenors with >=1.6x third-of-life slack and in-band lambda). Evidence timescale: pooled N=25 lands in ~3-5 trading weeks of Wave 1 arming (vs never today - no pooled row exists), pooled N=100 inside one quarter; the nightly MDE line will honestly report that per-lane 5-20%-of-premium edges need hundreds of trades, and the pooled e-process wealth becomes the single go/no-go number. Cost per trade: median WORST round~
DELETE: DELETE selector.py Stage 3 iv_rank penalty (:240-241) and the hv20 IV-richness penalty (:238-239) - provably order-invariant no-ops inside ~ | DELETE the C5 price-shock and news-shock mark-accel blocks in scripts/run_options_shadow.py (0 fires lifetime; under the current ladder an ~ | DELETE watch_hub's seven equity-era tabs and their feeders (parse_rh_mcp, _guardian_view, _slippage_view, discovery scans) in scripts/watch~ | DELETE day_briefing.json's dead outputs - the up-to-25 Finnhub earnings calls/day, scorecard digest, opex flags, permanently-null vix - shr~ | DELETE dead code: atlas/options/math.py prob_itm; tradier_data.py get_daily_history; the 4,289-row tenor-30 legacy seed in the IV archive; ~ | PARK Last30Lane (lanes.py:282-328; roster slot at run_options_shadow.py:443) - twice-upheld LANES-4: its 15%-of-move target cannot arithmet~ | PARK the stage-2 covariate stack - news_flag_tap (C6 classifier), the premarket catalyst_context build, lab job C's catmem append - re-arm ~ | PARK mention_tap (drop the launcher block; keep code and the 847 banked baseline rows) - re-arm tied to lane-2's first sustained picks; res~ | KEEP explicitly (anti-scope-creep guard, per the 07-12 over-cut lesson): the WORST ledger as the go-live bar, the full lanes -> selector ->~
RISKS: Sequencing risk: exit-engine v3 arms on MC + cached-path-harness evidence because the repaired replay lab only starts accumulating post-exit quotes now; the paired-replay confirma~ | Opposing forces on throughput: the p_touch_target>=0.25 coherence gate throttles exactly the lottery entries that Wave 1a's extra tenors admit; if lane targets stay ambitious the ~ | Debounced exits hold losers longer by construction - mean loss per losing trade will rise (bounded by premium since positions are long-only); judge only via the pooled e-process, ~ | Several Wave 1 inputs are VERIFY-DIED findings whose primary evidence is the live 2026-07-16 microstructure measurement; per-DTE lambda caps scale ~1/(sigma*sqrt(T)) and need re-d~ | The 0DTE decay model stays wrong for now (sigmoid leaves ~66% of morning TV alive at the close, understating afternoon decay 3-6x): more short-DTE entries will flow through it aft~ | Cohort discipline: Waves 1 and 2 each split the entry cohort once; e-process wealth restarts per cohort, so verdicts are delayed and pre-fix history can never be pooled into post-~ | Post-exit marking adds quote load; if the 60/min Tradier self-cap binds during multi-position afternoons, post-exit cadence degrades to 600s before live marking ever does. | Latch-to-entry (Wave 1e) had split refuter verdicts; if re-arms produce correlated duplicate entries on the same thesis, the 30-min re-arm window tightens or reverts - telemetry s~ | Parking last30, the covariate stack, and mention_tap reads as demotion of prior authorized work; every PARK carries a written re-arm condition in the sweep ledger (ladder rule 3) ~

## DESIGN ONE-MEASURE: calibrated touch-probability core on an e-process evidence spine
THESIS: The system currently manufactures its edge at entry (a p=0.5 prior times a drift that attains the target by construction, so ev_pct is a target-ambition meter, not a mispricing claim), destroys its own samples at exit (a zero-hysteresis noise clock that cuts in minutes what it priced to horizon), and grades the wreckage with a test that false-PASSes a zero-edge lane ~33-87% of the time while needing years to reach N. The rebuild replaces the probabilistic core with ONE calibrated object carried through the whole position life - p_t = P(touch target within remaining horizon | evidence), calibrated offline per lane from the ~500-session intraday cache - with edge DEFINED as p_0 minus the market's risk-neutral touch probability (already computed and logged at selector.py:225, currently gated~
- (days) Build the offline prior-calibration harness: new scripts/calibrate_lane_priors.py replays the pure lane predicates (lanes.py IndexTrendLane, InPlayORBLane, MacroReactionLane) over the dormant runtime/intraday_cache (~500 sessions of Alpaca 1-min bar~ - PROBABILITY-MEASURE-1/6 and LANES-7: the entire claimed edge is currently the hardcoded 0.5 (5x the level the EV gate actually needs, per PROBABILITY-MEASURE-4), while an RVOL-8 catalyst break and a 2bp macro drift rece~
- (days) Thread the calibrated prior and kill the constant: delete p_thesis: 0.5 from DEFAULTS (run_options_shadow.py:72) and the fan-out at :440-449; lanes stamp p_0 from state/lane_priors.json at signal build, plus a feasibility cap |ln(1+target)| <= 2*iv*~ - PROBABILITY-MEASURE-6 (calibration sources exist and are dead-ended) and LANES-6 (late-session fires request day-scale moves in minutes and pass as priced lotteries). The feasibility cap kills the ev_pct-linear-in-ambit~
- (days) Rewrite the selector EV stage as a one-measure edge test (selector.py:216-233): compute p_mkt = risk-neutral touch probability under the contract's own solved IV (om.p_touch with mu=0 - already at :225); gate entry on p_0 >= p_mkt + margin (margin p~ - PROBABILITY-MEASURE-1/2 and COST-AND-EDGE-3: the market-implied mixture weight on the live trade was -0.004 - 100% of the +53.31% ev_pct was manufactured. Defining edge as p_0 - p_mkt makes the selector's claim falsifia~
- (days) Exit engine on the same measure: replace mu_prior = pv.mu_thesis at exit_engine.py:242 with a Bayesian posterior of the SAME p_t the entry priced - posterior odds = prior odds(p_0) x likelihood ratio of the observed drift under thesis-vs-null, updat~ - PROBABILITY-MEASURE-3 and TRAJECTORY-6: the system buys at 50% belief and holds at 100% - entry and exit disagree about the same event at the same instant with identical inputs. One posterior object makes hold/sell cohe~
- (days) Variance-aware drift estimator (trajectory.py:78-122): replace the endpoint-only mu_hat with a robust regression slope over the window's committed closes (median-of-3 endpoints minimum), SE from realized bar variance rather than the contract's solve~ - TRAJECTORY-1/2/3 and TIMESCALE-STACK-3: a 20-min two-point drift carries ~14-annualized-units of noise against a ~0.016 decision threshold - the current exit ladder is a noise clock, and every ledger row it produces poi~
- (days) Evidence-grid exits: rules b/h/i* evaluate only on committed 5-min boundary closes (pass ctx.close + committed minute into _thesis_valid at run_options_shadow.py:871-887, never tick S + wall minute) with a registered 10-min persistence requirement (~ - TIMESCALE-STACK-1/2, EXIT-ENGINE-1, RUNNER-2: entry demands a committed 5-min close beyond the band, exit kills on a raw 10-second tick against a wider band - this asymmetry decided the platform's only live trade (cut a~
- (days) Fix replay-lab capture so exit changes have a validation path: keep polling NBBO for exited positions until the thesis horizon (EOD for 0DTE, next-day close for DTE>=1), rows flagged post_exit:true (run_options_shadow.py mark loop + replay.py); stor~ - REPLAY-LAB-1/2 and DEAD-LAYERS-1: the mandatory stage-4 toll gate has zero discriminating power on 100% of history - no hold-longer variant can ever be credited. Changes 4-6 are unvalidatable without this, so it ships f~
- (days) Replace the grader with an anytime-valid e-process (grade_options_shadow.py:135-136): score each exit as R_i = net_pnl_usd/(100*worst_entry_fill) (bounded below by -1 by construction), maintain betting wealth W_n = mean over lambda in {0.05,0.1,0.2,~ - EVIDENCE-MACHINE-1/2/3 and GRADER-1/2: PF>=1.2 is analytically t=0.36 (no test), the nightly all-history re-grade is unbounded peeking, and all-USD stats let one $2-3k contract carry 94% of a lane's variance. The e-proc~
- (days) Throughput pack so power is reachable within a quarter (ship after replay validation of each, as one registered tweak set): (a) max_chain_expirations 3->6 (run_options_shadow.py:79); (b) replace flat lambda_band (15,90) at selector.py:63 with per-DT~ - FUNNEL-THROUGHPUT root mechanism and EVIDENCE-MACHINE-2: at 0.14 fires/day the one-year MDE (~17% of premium) exceeds the selector's own 10% design target - no statistical redesign can rescue a starved funnel. Measured ~
- (days) Retire the incoherent theta/decay conventions in one registered change: standardize theta to trading-time everywhere (selector.py:250 still applies the 0DTE multiplier the exit engine removed, and vendor calendar-day theta is multiplied by trading-d~ - The exit engine currently believes a held 0DTE retains most of its morning time value at the bell - it will systematically hold losers into the 15:30 collapse - while the selector and ledger book theta on two different ~
- (hours) Instrumentation for the single measure: every entry row logs {p_0, p_mkt, margin, prior_cell_n}; every mark logs {p_t, p_mkt}; every exit logs realized touch-by-horizon (resolvable thanks to the post-exit capture in change 7) so the prior tables ret~ - PROBABILITY-MEASURE rec 6 and EVIDENCE-MACHINE rec 6: the 07-13 entry carried nine null covariates and no measure-disagreement record; a calibrated system must record exactly the quantities that would prove it wrong. Th~
- (hours) Ship changes 3-6 and 10 as ONE registered exit-engine/selector version (single new config_hash, fresh e-process wealth 1.0, one sweep_ledger registration) after the replay-lab validation battery passes; deploy only outside RTH per the live-session c~ - The seam judges are explicit that these fixes interact (the inflated exit prior currently brakes the noise clock; the growing band feeds both entry admission and the kill) - shipping piecemeal both fragments the config_~
EXPECTED: Measurable predictions, each falsifiable from the journal/ledgers: (1) Throughput: pooled entries rise from 0.14 picks/day to 1.5-2.5/day within two weeks of the throughput pack (mechanism: DTE 3-6 tenors clear third-of-life with >=1.6x slack, per-DTE lambda unbans 0-1 DTE index ATM, latch-at-entry retries the ~33% staleness losses); pooled N=25 lands in 2-3 weeks instead of 6 months-to-years. (2) Cost: WORST round-trip cost per trade on the restricted universe = 0.9-2.1% of premium on index, <=5% on admitted single names (median lane-2 cost falls from 13.9-108% to <=5% by construction of the admission gate). (3) Exit behavior: median hold rises from ~5 minutes to >=60 minutes or thesis hor~
DELETE: last30 lane (lanes.py Last30Lane, :317-318 refit stub) - emits arithmetically un-passable signals (6/6 EV-stage deaths, LANES-4); re-derive~ | pre_earnings lane stub - cannot reach N within a quarter; park the concept, delete the stub from the roster | The ev_hold_thesis p=0.5 mixture as the selector EV stage (selector.py:218-233) and the ev_vs_spread double-netting - replaced by the one-m~ | The life-extrapolated p_target_thesis exit measure and mu_prior = full mu_thesis at exit_engine.py:242 - replaced by the posterior p_t | iv_rank score penalty and hv20 IV-richness penalty (selector.py:238-241) - provably order-invariant no-ops (DEAD-LAYERS-3, IV-ARCHIVE-2); k~ | C5 price-shock and news-shock mark-accel blocks in run_options_shadow.py (reval_shock_* DEFAULTS :86-87) - 0 fires lifetime; under the curr~ | zero_dte_decay_rate sigmoid and the selector-side 0DTE theta multiplier (selector.py:250) - replaced by the variance-clock hazard and one t~ | exit_engine_legacy.py (323 lines) - superseded engine kept live in the tree | The PF>=1.2 / mean>0 / top-day-share<0.5 PASS bar and the unbounded all-history re-grade (grade_options_shadow.py:73,135-136) - replaced by~ | flat lambda_band (15,90) as configured (selector.py:63) - replaced by per-DTE caps + premium floor | watch_hub's 7 equity-era tabs and their feeders (parse_rh_mcp, _guardian_view, _slippage_view, discovery scans) - ship the options tab that~ | day_briefing.json dead outputs: 25 Finnhub earnings calls/day, scorecard digest, opex flags, permanently-null vix - shrink build_day_briefi~ | PARK with written re-arm conditions (re-arm: trailing-20-session lane-2 entries >= 10): news_flag_tap C6 classifier, premarket catalyst_con~ | PARK macro_reaction post-FOMC-14:00 continuation entries - the published record shows no post-print continuation edge and a robust presser-~ | Replay-lab LLM/questions stages and lexicographic best_variant naming (REPLAY-LAB-4) - the lab's only retained role is paired counterfactua~
RISKS: Calibration may deliver the honest bad news: if no lane's p_0 clears p_mkt + 0.10 on any cell, the funnel goes to ~zero fires - this is discovery, not failure, but it will read as~ | The prior tables are calibrated on underlying touch rates from the intraday cache under the historical physical measure; option EV additionally depends on IV path and skew over th~ | Widening to DTE 3-6 and admitting OI=0 fresh strikes increases overnight/vega exposure and relies on the still-open overnight-policy question (OWNER_RULES retracted); the overnight ~ | The e-process on bounded-below returns is anytime-valid but its power depends on the lambda grid and the winsorization of the heavy right tail of long premium; a badly chosen grid~ | 10-minute persistence on exits will occasionally eat a real reversal that the current hair-trigger would have dodged - expected cost is bounded by the frozen-band width times admi~ | Fires/day predictions rest on replaying stored chains from 6 sessions (one Thursday-morning microstructure snapshot for the gate constants); regime shift (IV compression below ~12~ | One quarter at the target rate yields ~90-120 pooled trades: only edges >=~20% of premium are certifiable; the 5-10% hypothesized band stays statistically invisible, so any go-liv~ | Shipping discipline risk: the changes are explicitly coupled (exit prior vs noise clock vs band growth); any hotfix that lands one piece alone mid-cohort re-creates the incoherenc~

## DESIGN PIT BOSS - bounded-loss convexity harvest (play only priced tables, bet calibrated odds, farm the right tail)
THESIS: A casino does not forecast - it prices. It (1) only operates tables where the rake is a known small fraction of the wager, (2) only books bets where its own odds table beats the posted price, (3) never cuts a live bet mid-hand - the table limit IS the stop - and (4) systematically harvests winners. Long options give ATLAS the house structure natively: max loss = premium, unbounded convex upside. The current system inverts every casino principle: it hunts on 13.9–108%-spread tables (lane-2 catalyst names) while starving the 0.9–2.1% index tables (one marginal expiration to shop, empty lambda×delta band); it bets a hardcoded p=0.5 that manufactures 100% of its own claimed edge (market-implied weight on the one live trade: −0.004) instead of comparing calibrated odds to the market's price; a~
- (hours) Register the objective function as the system's single source of truth: new docs/OBJECTIVE.md + a %-of-premium R convention constant consumed by grader and ledgers. R_i = net_pnl_usd/(100*worst.entry_fill), bounded below by -1; SHADOW optimizes pool~ - Every Part-1 incoherence traces to the missing objective: selector maximizes target ambition, exit engine minimizes per-mark regret, grader scores USD means one QQQ contract can dominate (EVIDENCE-MACHINE-3). A casino r~
- (days) Selector v2 in atlas/options/selector.py + DEFAULTS in scripts/run_options_shadow.py - price edge against the house, open the priced tables: (a) coherence gate: require p_touch_target >= 0.25 (already computed at selector.py:225, currently gated on ~ - This is casino rule #2 (only book bets your odds table says are mispriced) and rule #1 (only on low-rake tables). PROBABILITY-MEASURE-1/2 proved ev_pct is manufactured by the prior; SELECTOR-1/2/6 + the live microstruct~
- (days) Calibrate p per lane offline and thread it through: new scripts/calibrate_lane_p.py replaying the pure lane predicates over the ~529-session runtime/intraday_cache to measure empirical P(touch target within horizon | signal), bucketed by lane x targ~ - PROBABILITY-MEASURE-6 showed calibration sources exist and are dead-ended; the external research says p=0.5 is defensible ONLY at 0.3x-range EOD targets and indefensible at 1x. A casino that guesses its own odds is a ma~
- (week-plus) Exit engine v3 = harvest engine, one registered cohort in atlas/options/exit_engine.py + the mark loop in scripts/run_options_shadow.py (:926-1133): (a) KEEP hard clocks (0DTE afternoon clock, planned horizon, overnight gates); (b) rule (b) invalida~ - Casino rules #3 and #4: the table limit (premium) is the only stop, and winners get harvested systematically. TRAJECTORY-1, EXIT-ENGINE-1/3, TIMESCALE-STACK-1/2 proved the current ladder is a cadence-dependent noise clo~
- (days) Replay-lab capture fix FIRST (prerequisite, atlas/options/replay.py + the runner's quote capture): keep polling NBBO for exited positions to the thesis horizon (EOD for 0DTE, next-day close for DTE>=1), rows flagged post_exit:true; recompute thesis_~ - REPLAY-LAB-1/2/3: capture stops at the live exit, so no hold-longer variant can EVER be credited - the lab has replayed 0% of real trades and is byte-identical across all variants. Without this fix the harvest engine (c~
- (week-plus) Lane re-founding in atlas/options/lanes.py on the three published cost-surviving edges: (a) index_trend: default horizon = EOD hold (not intraday fractions), target expressed in noise-band-edge multiples, band recalibrated via the pre-registered swe~ - These are the only three intraday mechanisms with published, cost-adjusted evidence (Sharpe 1.33 index noise-area; +0.08R above the RVOL flip; 54.4% peer-reviewed last-30 hit rate), and each maps onto a venue whose meas~
- (days) Evidence machine v2 in scripts/grade_options_shadow.py: score R_i in %-of-premium; pooled ALL row (deduped by position_id, merged-lane trades counted once) runs a betting e-process (lambda grid {0.05,0.1,0.2}) as the PRIMARY existence-of-edge verdic~ - GRADER-1/2 + EVIDENCE-MACHINE-1/2: the current PASS bar false-passes a zero-edge lane ~33-87% of the time while being unable to confirm the design-target edge inside a year - a filter wearing a lab coat. The e-process m~
- (days) Runner hygiene bundle in scripts/run_options_shadow.py + atlas/collect/tradier_data.py + feed: RTH-gate LiveBarBuilder (drop premarket commits into svwap/HOD/mu-window state); hunt-list freshness contract (reject session_date != today - live bug: 07~ - RUNNER-1/4, TIMESCALE-STACK-4/6/7, PREMARKET-CREW-10: these are unglamorous but each one either corrupts the evidence the whole design runs on (premarket-contaminated VWAP feeds both entry and kill decisions) or silentl~
- (hours) Macro_reaction lane: PARK as a continuation lane (delete from the active roster in atlas/options/lanes.py, keep code), with a written re-arm condition: re-register only as a post-print REVERSAL/fade lane (2:30pm presser reversal pattern) or on new e~ - The external research is unambiguous: pre-FOMC drift died post-2015, post-print hours are flat on average, and the robust modern pattern is presser-window REVERSAL - the lane as designed bets a continuation edge the lit~
- (days) Watch_hub options surface (scripts/watch_hub.py): render the already-computed options snapshot as the default tab - positions with live R (%-of-premium), e-process wealth trajectory, per-lane fire/rejection funnel counts, harvest-rule attribution (w~ - DEAD-LAYERS-4: the platform's only human RTH surface currently renders archived equity systems while the options shadow is invisible. The pit boss needs to SEE the tables: per-rule exit attribution is also the fastest h~
EXPECTED: Measurable predictions, checkable from the journal/ledgers/scorecard: (1) THROUGHPUT: pooled entries rise from 0.14/day to 1.0-2.0/day within 2 weeks of selector v2 + lane re-founding (index lanes 0.7-1.2, last30 0.3-0.5, orb 0.2-0.4); first-fail rejection mass shifts from OI/no_quote/third-of-life (structural) to EV/coherence gates (economic) - the funnel starts dying for the RIGHT reason. (2) COST: median WORST round-trip on entered positions <= 3% of premium (index 0.9-2.1%, capped single-names <= 5%), vs the 13.9-108% books lane 2 currently shops. (3) HOLDS: median hold rises from ~5 minutes to >= 2 hours; first-mark exits fall from ~41% of entries to < 5%; >= 25% of winners exceed +50%~
DELETE: Exit rules d2 (cost-basis breakeven backstop) and i* (p_regain) in atlas/options/exit_engine.py - the right-tail amputator and its near-ine~ | atlas/options/exit_engine_legacy.py - 23KB dead code, superseded twice. | The zero_dte_decay_rate sigmoid in atlas/options/math.py - replaced by the variance-clock hazard; it leaves 66% of time value alive at the ~ | Selector Stage-3 iv_rank penalty and the hv20 IV-richness term (atlas/options/selector.py:238-241) - provably order-invariant no-ops inside~ | The unreachable zero_dte_third_of_life_exempt carve-out as currently registered (selector.py:74) - superseded by per-DTE lambda caps and th~ | C5 price-shock and news-shock mark-accel blocks in scripts/run_options_shadow.py - 0 fires lifetime; under any residual noise in the exit l~ | MacroReactionLane from the active roster (PARK, written re-arm condition: reversal-lane re-registration only) and the pre_earnings stub lan~ | Stage-2 covariate stack - PARK with re-arm condition 'trailing-20-session entries >= 10': news_flag_tap C6 classifier, premarket catalyst_c~ | day_briefing.json fat in scripts/build_day_briefing.py - the 25 Finnhub earnings calls, scorecard digest, opex flags, and permanently-null ~ | The seven equity-era watch_hub tabs and their feeders (parse_rh_mcp, _guardian_view, _slippage_view, scans) in scripts/watch_hub.py. | The AI crew as SOLE lane-2 universe source (PREMARKET-CREW-8): demote to candidate generator behind the deterministic <=5%-spread/RVOL scre~
RISKS: Loosening exits before the harvest rules are validated could convert the shadow from insta-cut to hold-to-zero, especially 0DTE afternoons: the variance-clock decay hazard and har~ | p-calibration from the 529-session intraday cache is in-sample history: touch rates from 2024-26 regimes may not persist, and the pure-predicate replay ignores fill feasibility; a~ | One mega-cohort (selector v2 + exit v3 + new p tables = new config_hash) discards continuity with all prior history - acceptable at 2 lifetime entries, but it means every Part-1 e~ | The harvest ladder (target-touch take, +100%-armed ratchet trail) reintroduces price-point-shaped exits that the retracted OWNER_RULES banned; the retraction makes this decidable on~ | Throughput predictions could miss low: if post-fix pooled fires land below ~0.5/day, time-to-verdict stretches past a quarter and the MDE stays above the entry floor - the nightly~ | Forced exits graded at stale/zero-bid NBBOs (SHADOW-LEDGER-3, RUNNER-10) inject phantom values into e-process wealth; the fill-provenance and dedup fixes must ship in the SAME cha~ | Graduated pilot at wealth>=5 tolerates roughly a 20% false-arm probability under the null by construction - that is deliberate bounded-tuition casino posture, but it must be commu~ | The live pilot's venue (Robinhood-routed 1-lots) is the worst measured price-improvement venue; WORST-ledger grading remains a true bound, but realized pilot economics may sit clo~ | Multiplicity discipline is load-bearing: 3 re-founded lanes x harvest-parameter sweeps recreate the selection-then-confirmation loop unless every promoted variant starts a fresh e~ | EVENTS-CALENDAR-1 (2027 rollover silently disarms macro protection) predates this design and survives it - fix before year-end or the event-day decay overlay and blackout gates go~
