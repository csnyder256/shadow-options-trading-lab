## math-core
### MATH-CORE-1 [high] {VERIFY-PENDING} p_regain 0DTE double-decays the clock: zero_dte_effective_T is fed the horizon-END model time, violating its own derivation
CLAIM: math.py:299 passes max(T_now - dt_h, MIN_T) (the model time AT the sell clock) into zero_dte_effective_T, whose derivation (math.py:264-269) defines T_eff(end) = T_now * exp(-2I) from the CURRENT time value - so the interval's decay is charged twice (once via the model clock subtraction, once via the empirical exp(-2I)). The docstring's own promised behavior ('T_eff may exceed the model's own T(end) in the MORNING') is mathematically unreachable as called, since the input is already T(end) and exp(-2I)<1 always.
REPRO: 0DTE SPY 630, K=632C, iv=0.15, 13:00, sell clock 15:00: T_now=180min, model T(end)=60min, I=0.1793 => coded t_minus=41.9min vs docstring-intent 125.8min. Resulting regain barrier move 0.00329 vs 0.00117; p_regain = 0.530 coded vs 0.824 intent (0.595 with the plain model clock). At 10:00: coded 35.4min vs model 60min - 'exceeds T(end) in the morning' never happens.
FIX: One line: pass T_now (the current remaining life), not T_now - dt_h, at math.py:299 - i.e. t_minus = zero_dte_effective_T(T_now, minute, sell_clock_min). Then re-examine p_regain_min=0.25 on replay, because every historical p_regain was computed under the double decay.
### MATH-CORE-2 [high] {VERIFY-PENDING} p_regain systematically understates true regain probability 2-3x and its 'conservative-toward-holding' label is backwards
CLAIM: The end-solved barrier makes p_regain a strict LOWER bound of P(contract value >= c_ref at some point in the horizon): paths that regain c_ref early at spots BELOW the end-barrier are not counted. The docstring (math.py:286-288) calls this 'the conservative-toward-holding bound', but rule i* SELLS when p < p_regain_min=0.25, so a lower bound is conservative toward SELLING - the bias direction is exactly the owner's hair-trigger concern for the profit-take rule.
REPRO: Brownian-bridge MC of the TRUE event (value >= c_ref at any mark, model clock, mu=0), 0DTE 13:00-14:30 grid, S=630 iv=0.15: shipped p_regain 0.313-0.619 vs TRUE 0.818-0.912 (e.g. 13:00 K=632: shipped 0.530, true 0.913; 14:00 K=635: shipped 0.330, true 0.850). With mild fade mu_eff=-2.0, K=633 13:30: shipped 0.328 vs true 0.853 - one more notch of OTM/fade crosses the 0.25 sell floor while the true probability stays ~
FIX: Fix MATH-CORE-1 first, correct the docstring's direction claim, and either (a) price the time-varying barrier (solve regain_move at each mark step - cheap, ~90 bisection calls) or (b) keep the bound but recalibrate p_regain_min on replayed TRUE-regain frequencies, never on the biased shipped values.
## trajectory
### TRAJECTORY-1 [critical] {UPHELD/blocks-goal+UPHELD/blocks-goal} Exit rules h/i* are a noise clock: 20-min annualized drift noise (SE ~ 14) vs a ~0.016 decision threshold guarantees a forced sale within ~30-50 minutes of any entry under pure noise
CLAIM: mu_hat's sampling SE is iv/sqrt(T_K) = 70.1*iv (= 14.02 annualized at iv=0.20), while rule h flips to SELL when mu_eff < ~+0.016 for a representative ATM index call. With the lane prior mu_thesis=1.0 held valid, h fires whenever the trailing 20-min t-stat < -0.41, i.e. any -0.118% 20-min drift on SPY - probability 0.34 PER MARK under zero true drift. The engine cannot hold any position through routine noise, making the 'maximize profit taking' goal (letting winners run) mathematically impossible; the closed form t_flip ~ -(mu_thesis/SE)^(1/3) ~
REPRO: Repro (repo's own ev_hold, .venv python): SE = 0.20/sqrt(20/98280) = 14.02. ATM SPY call S=K=600, iv=0.20, 5 DTE, 6c spread: BS mid 6.981, ev_sell = -0.030, bisected mu_flip = +0.0156. Solving (14.02*t^3 + 1.0)/(1+t^2) = 0.0156 gives t_flip = -0.412 = -0.118% 20-min move; P(t < t_flip | H0) = 0.340. Monte Carlo, 20,000 zero-drift days, marks every 5 min: median time-to-h-fire = 30 min, quartiles 20/50 min, P(survive~
FIX: Scale the blend by the estimator's known variance (w *= tau^2/(tau^2+SE^2) with tau ~ the lane-thesis scale - SE is closed-form from iv and span), or equivalently cap |w*mu_hat| at a few multiples of mu_thesis; and require a persistent breach (2-cycle debounce, as rule g already does) before h/i* may fire.
VOTE UPHELD(blocks-goal): Independent re-derivation with the repo's own code confirms every number: mu_flip = +0.01566 (repo ev_hold bisection; analytic r − hs/(S·Δ·dt) = +0.01569), SE = iv/√T_K = 14.02, t_flip = −0.412 → per-mark fire probability 0.340 under zero drift, and the 0.20–0.37 band across mu_thesis 0.5–8.2. Mont~
VOTE UPHELD(blocks-goal): Confirmed, not refuted - reproduced end-to-end with the repo's own code. Bisecting the repo's ev_hold gives mu_flip=0.01565 (claim 0.0156); t_flip=-0.412; per-mark h-fire probability under zero drift = 0.338 driving decide_exit itself (claim 0.340). Monte Carlo through decide_exit (thesis_valid pin~
### TRAJECTORY-2 [high] {REFUTED/degrades-edge} w = t^2/(1+t^2) is a scale-free gate that overweights a +/-1400%-annualized-noise estimate ~20-40x relative to any calibrated Bayes weight
CLAIM: The shrinkage weight depends only on significance, never on the ratio of observation noise (SE=14) to plausible drift scale (O(1)): under pure noise E[w]=0.344 and w>0.5 on 31.7% of marks, injecting std(w*mu_hat)=9.6 annualized of noise into mu_eff, where a conjugate posterior with even a generous prior width tau=2 would give w=tau^2/(tau^2+SE^2)=0.020. The blend implicitly asserts that 1400%-annualized drifts are as plausible a priori as the lane's own 100% thesis. The registered alternative family w=t^2/(c+t^2), c in {0.5,2} (opts-calib-mu-b~
REPRO: 400,000-draw simulation, t~N(0,1): E[w]=0.344, P(w>0.5)=0.317, std(w*mu_hat)=9.57 annualized at iv=0.20. Calibrated comparison: tau=2 -> w*=4/(4+196.6)=0.020; tau=1 -> 0.0051. Live w at t=1 is 0.500 = 25-98x the calibrated weight. Measured blend outputs with prior +1.0: 20-min move -0.25% -> mu_eff=-4.76; -0.35% -> -9.90; +0.50% -> +18.7.
FIX: Multiply the evidence gate by the scale factor tau^2/(tau^2+SE^2) (one line: SE=iv/sqrt(T_K) is already computed as s/T_K), register tau against the lane mu_thesis distribution, and add the scaled weight as the new live formula with the old one as the replay column.
VOTE REFUTED(degrades-edge): Arithmetic sub-results reproduce (E[w]=0.344, P(w>0.5)=0.317, std(w*mu_hat)=9.57, blend outputs match), but the claim's load-bearing premises are false. (1) The 'plausible drift scale O(1)' / tau=1-2 baseline contradicts the system's own lane construction: mu_thesis = ln(1+target)/horizon_T with in~
### TRAJECTORY-3 [high] {VERIFY-PENDING} Endpoint-only estimator is maximally microstructure-sensitive: one 10bp stale print at a window edge moves t by 0.35 sigma - alone comparable to the entire rule-h threshold
CLAIM: mu_hat uses exactly two closes (w_closes[0], w_closes[-1]) built from ~6 Tradier last-trade polls/min, so a single stale/bounced print at either endpoint shifts t by e/(iv*sqrt(T_K)): 10bp -> 0.35 sigma at iv=0.20, vs the h-fire threshold of 0.41 sigma - one print can very nearly fire h on its own, and on InPlayORB names (price >= $5, where 1-2 ticks = 20-40bp) a single bid-ask bounce can exceed the threshold outright. The docs/brief description ('OLS over trailing closes') does not match the code; the 21 intermediate closes and the n>=10 bar ~
REPRO: delta_t = 0.0010/(0.20*0.014264) = 0.350 sigma per 10bp endpoint error (vs |t_flip| = 0.412). On a $5 name at iv=0.40: one 1c tick = 20bp -> delta_t = 0.35 sigma; two opposite-endpoint bounce ticks -> 0.70 sigma, > any realistic threshold.
FIX: Use robust endpoints (median of the first/last 3 window closes) or the full-window regression estimate, and/or subtract an endpoint-noise term from t's denominator (sqrt(iv^2*T_K + 2*sigma_tick^2) with sigma_tick from the symbol's spread) so thin names don't fire on bounce.
### TRAJECTORY-4 [high] {VERIFY-PENDING} Optional-stopping cadence dependence: the first-match ladder re-samples the noisy t every mark, so the probability of a noise exit compounds with polling frequency - which the runner raises exactly a~
CLAIM: decide_exit is memoryless per mark (except theta breaches): each mark draws a fresh, heavily-overlapping t (5-min shift shares 15/20 of the window; 60s marks share 19/20) and sells on first crossing. The exit distribution therefore depends on the MARK CADENCE, not just the market path - a pure decision rule should be cadence-invariant. The runner escalates cadence to 120s (0DTE afternoon) and 60s (last 30 min) exactly when Last30/0DTE positions exist and windows overlap most, multiplying first-passage draws; there is no debounce on h or i* (un~
REPRO: Monte Carlo at 5-min cadence: median 30 min to h-fire under zero drift. Each extra draw is a fresh chance at P~0.34 (correlation 0.75 between adjacent draws); at 60s cadence the same 20-min window is sampled 20x per refresh, so the running max of |t| over a window's life crosses any sub-1-sigma threshold with near-certainty - the last-30-min cadence increase converts the final half hour into a near-deterministic noi~
FIX: Add the same 2-consecutive-cycle debounce h/i* that g already has (or require the breach to persist across one full non-overlapping window, i.e. 20 min), making the decision approximately cadence-invariant.
## lanes
### LANES-1 [critical] {UPHELD/degrades-edge+REFUTED/none} Zero-hysteresis, single-tick, growing-barrier invalidation makes rule (b) a near-guaranteed minutes-scale exit
CLAIM: IndexTrendLane.invalidated (and InPlayORB's analogue) declares the thesis dead on ONE polled quote either recrossing VWAP or re-entering the noise band, while the entry -- by construction the FIRST boundary close just beyond that same band -- sits ~one 5-min-close overshoot (~9 bps for IWM) from the barrier, and the band itself grows ~sqrt(t) toward the position; most entries are therefore cut within minutes at round-trip-spread losses, which is exactly how the only live trade (2026-07-13 IWM 293P, -$10..16) died.
REPRO: IWM sigma_day ~1.0% -> sigma_5min = 1.0% x sqrt(5/390) = 11.3 bps; conditional overshoot of the first crossing close ~0.8 x sigma_5min ~= 9 bps = entry-to-barrier distance. sigma_1min = 5.1 bps -> P(touch barrier within 5 min) = 2*Phi(-9/(5.1*sqrt(5))) = 2*Phi(-0.79) ~= 0.43; within 30 min = 2*Phi(-0.32) ~= 0.75 -- before OR-ing in the VWAP recross and the band growth (+~5 bps over the 30 min after a 10:30 entry: 0.~
FIX: Add hysteresis and persistence: invalidate only when the mark is inside 0.8 x noise (or beyond VWAP by a noise-scaled buffer) on 2 consecutive revals or on a completed-bar close; freeze the invalidation band at its entry-time width instead of letting the widening average absorb a stalling position.
VOTE UPHELD(degrades-edge): Real-data replay of the actual production code confirms the numeric repro almost exactly. Running build_noise_profile + IndexTrendLane.update/.invalidated over 529 real cached IWM sessions (plus SPY/QQQ) with the live reval cadence (300s, latest-tick mark): 41% of lane-1 entries are rule-(b) cut at~
VOTE REFUTED(none): Mechanics confirmed but the defect claim is refuted on materiality, with the finding's own remedy disproven empirically. Replaying the REAL lane code over the real 1-min cache reproduces the finding's numbers (IWM 41% cut at first 300s reval, 76% within 30 min, median overshoot 8.6bps) - yet a pair~
### LANES-2 [high] {REFUTED/cosmetic+REFUTED/degrades-edge} One-per-side daily latch burns at EMISSION, so selector rejections (the observed death mode) permanently disarm the lane for the day
CLAIM: IndexTrendLane and InPlayORBLane add to _fired BEFORE returning the signal, and the runner processes each signal exactly once with no retry inside the 10-min TTL; since candidates die overwhelmingly at contract selection (no_quote/spread/OI), a noise-scale fire at the open that gets rejected consumes the day's only latch and the genuine mid-day break can never fire -- a direct mechanical cause of the 0.14 fires/day starvation.
REPRO: P(band exceedance per boundary under noise) = P(|Z|>sqrt(2/pi)) = 0.425 (see LANES-5), so the first latch-burning fire clusters at the open when option spreads are widest -- precisely when selector rejection probability peaks; one rejection then suppresses ALL later fires that side for the remaining ~380 minutes.
FIX: Burn the latch on FILL (shadow entry recorded), not on emission: have the runner call back lane.release(symbol, direction) on no_pick, or keep the signal alive and re-run selection each poll until expires_minute; combined with LANES-5's weak band this converts wasted open-noise fires into retryable opportunities.
VOTE REFUTED(cosmetic): Code-reading cites are accurate (lanes.py:249-252/423-426 latch before return, no release, runner drops no_pick at run_options_shadow.py:745), but the claim's impact story fails on every testable point. (1) Causality inverted: fires/day counts emissions; the latch only suppresses a SECOND same-side~
VOTE REFUTED(degrades-edge): Code mechanics confirmed (latch burns pre-selection at lanes.py:252/426; runner never retries), but the claim fails on reachability/materiality against the shadow's full recorded life. (1) The headline scenario never happened: the only open-clustered index fire (07-13 09:35 IWM call no_pick) was fo~
### LANES-3 [high] {REFUTED/cosmetic+REFUTED/cosmetic} range_percentile_14d gate is scale-invariant: it deletes a fixed ~43% of trading days and cannot detect a vol regime by construction
CLAIM: The 'dead-vol regime' stand-down ranks YESTERDAY'S range within its own trailing 14 days, so under any stationary regime -- high vol or low -- it stands lane 1 down on a fixed ~6/14 of days; it is a coin weighted 57/43 on 'was yesterday relatively quiet within the fortnight', not a regime detector, and it is the single largest structural starvation multiplier on the only lane that has ever traded.
REPRO: Ranks are uniform on {1..14}/14 under exchangeability: pass requires count>=7 -> ranks 7..14 -> 8/14 = 57.1% pass, 42.9% stand-down -- independent of the actual volatility level. At the n_days=5 minimum: pct in {20,40,60,80,100} -> 40% stand-down. Multiply by 3 sessions observed: expected ~1.3 of 3 SPY/QQQ/IWM lane-days deleted before any market condition is consulted.
FIX: Gate on a vol LEVEL, not a rank: e.g. 5-day mean realized range vs a long-run (90d+) baseline, or ATR percentile vs a 6-month window; if day-freshness of vol matters, use the median of the last 3 days rather than the single latest day (a rank of one observation has maximal estimator variance).
VOTE REFUTED(cosmetic): The arithmetic reproduces exactly (6/14=42.9% stand-down under exchangeability; 40% at n=5; scale invariance verified through build_noise_profile itself), and the gate is live at range_percentile_min=50 with empirical stand-down 45-46% over ~500 real cache sessions per symbol. But the finding's cor~
VOTE REFUTED(cosmetic): The arithmetic (fixed ~43% marginal stand-down under exchangeability; empirically 44.6-46.4% over ~500 real sessions/symbol) is correct, but the thesis fails twice. (1) 'Cannot detect a vol regime by construction' is empirically false on the exact cache the runner reads: rolling replay of the gate ~
### LANES-4 [high] {UPHELD/degrades-edge+UPHELD/degrades-edge} Last30's 15%-of-move target cannot arithmetically clear the selector's EV floors at p_thesis=0.5 except on ~2-sigma days
CLAIM: The lane triggers on ~40% of days (|close-to-15:30 ret| > 0.5 x avg range is a weak condition) but sizes target_move at 0.15 x |ret| (floor 10 bps) over 25 minutes, which under the 0.5 thesis mixture yields ev_pct far below the selector's 10% floor and EV$ below 2x spread on typical qualifying days -- the lane is a designed-in no_pick generator, i.e. pure starvation plus funnel noise.
REPRO: SPY: trigger passes at |ret|=0.5% (avg range 0.8%). Target = max(0.15x0.5%, 10bps) = 10 bps = $0.63 on S=630. 1DTE ATM call: delta 0.6, mid $2.10, ask $2.15, spread $0.10. Thesis branch ~= 0.6x0.63 - theta(25/390 day on ~1.06d life: 2.10x0.5x0.064/1.06 ~= $0.06) = +$0.31; mu0 branch ~= -$0.05; mixture = +$0.13; minus exit half-spread $0.05 -> ev_share $0.08 -> ev_pct = 0.08/2.15 = 3.7% < 10% floor; EV$ = $8 < 2 x sp~
FIX: Back-solve the trigger from the floor: only fire when 0.15 x |ret| >= the target that clears ev_pct>=10 for a 1DTE ATM contract (~25 bps -> |ret| >= ~1.7%), or refit the 15% continuation coefficient from replay data conditioned on day-move size; alternatively lower the floor for this lane's 25-min horizon where theta ~
VOTE UPHELD(degrades-edge): Independent re-derivation with the real code CONFIRMS the finding. (1) Live path verified: runner builds Last30Lane with p_thesis=0.5 (DEFAULTS, no hunter.yaml override) and feeds sig.target_move/horizon_T straight into select_contract with 1-DTE-preferred expiries. (2) Real-code numerics: at the 1~
VOTE UPHELD(degrades-edge): Finding survives adversarial refutation. Reachability: live path confirmed - Last30Lane is in the runner roster and runtime/options_shadow_journal.jsonl shows both real firings (QQQ puts 2026-07-13 |ret|=0.98% and 2026-07-15 |ret|=1.08%) ended in no_pick with the funnel survivors dying exactly at e~
### LANES-5 [high] {UPHELD/degrades-edge+REFUTED/degrades-edge} IndexTrend noise band is the MEAN |move|, which pure noise exceeds 42.5% of the time at any boundary
CLAIM: Using avg |close(m)/open - 1| as the breakout band means a driftless random walk closes outside it at any given 5-min boundary with probability ~0.425 (both sides), so the band supplies almost no evidence of a trend day; the first-crossing therefore lands early on noise (band ~7 bps at 09:35 for SPY), burning the daily latch (LANES-2) and creating entries with the ~zero barrier distance that LANES-1 then executes.
REPRO: E|X| = sigma sqrt(2/pi) = 0.798 sigma for X~N(0,sigma^2); P(|X| > E|X|) = 2(1-Phi(0.798)) = 0.425. At 09:35 (5 min in), band = 0.798 x 0.756% x sqrt(5/390) ~= 6.8 bps for SPY -- one 7 bp drift from the open on the matching VWAP side (heavily correlated with the move's sign) latches the side for the day.
FIX: Raise the band to an empirical percentile of |move| per minute (p80-p90, still computable from the same 14-session arrays), or require k x noise with k~1.5-2 (P(|Z|>1.6) ~= 11%); pair with LANES-2's latch fix so the tighter band does not simply starve further.
VOTE UPHELD(degrades-edge): Empirical walk-forward simulation on the real intraday cache (~500 sessions per symbol, using the actual build_noise_profile and lane fire condition) reproduces the claim: 09:35-boundary full-condition fire rates are SPY 38% / QQQ 42% / IWM 44% vs the predicted ~42.5%; the VWAP-side condition remov~
VOTE REFUTED(degrades-edge): The claim's numbers are wrong against the real system and its damage chain is broken on the live path. (1) The band is not ~7 bps: build_noise_profile's per-minute empirical mean over 14 real sessions embeds U-shaped open vol and trend-day drift, giving SPY 13.5 / IWM 18.3 / QQQ 28.6 bps at the 09:~
### LANES-6 [high] {UPHELD/degrades-edge+UPHELD/degrades-edge} No target-vs-remaining-time feasibility check: late-session fires request full-day-scale moves in minutes and pass the selector as priced lotteries
CLAIM: IndexTrend's 2xnoise target floor GROWS to ~2x avg|close-open| (~1.2%) by late day while the horizon shrinks to minutes, and ORB/macro have no fire-time cutoff at all; mu_thesis = ln(1+target)/horizon then explodes (hundreds annualized), the thesis branch of the EV mixture becomes 'the target is hit with certainty', p_profit ~= 0.5 x 1 clears the 0.40 floor, and the frozen mu_thesis feeds the exit engine's mu_prior -- a slightly-casino posture is fine, but this is an unpriced-feasibility hole, not an informed edge.
REPRO: 15:50 index fire: noise(950) ~= 0.798 x 0.756% x sqrt(380/390) ~= 0.60%, target = 2x = 1.19%, horizon = 10 min -> mu = ln(1.0119) x (390x252)/10 ~= 116/yr, i.e. a drift 154x the annualized sigma of ~0.12 -- 12.9 sigma over the horizon (feasible ~never), yet the mixture EV treats it as a 50% certainty and p_profit ~= 0.5 >= 0.40 passes.
FIX: Cap mu_thesis relative to what the market prices as possible: require |ln(1+target)| <= k x iv x sqrt(horizon_T) (k~2-3) or shrink the target to min(target, k x noise-scaled remaining move); add a latest-fire minute per lane (ORB before ~11:00; index_trend before ~15:00).
VOTE UPHELD(degrades-edge): CONFIRMED by independent re-derivation on the real code + real cached SPY data. Real 14-day profile (build_noise_profile on runtime/intraday_cache/SPY_1min.parquet): at a 15:50 IndexTrend fire, target = 2xnoise = 0.929% (the 0.35x remaining_range term correctly shrinks to 0.116% and is dominated by~
VOTE UPHELD(degrades-edge): Reproduced with the real modules: a 15:50 index fire yields mu_thesis=117/yr (9.9-sigma target, p_touch(mu=0)=6.5e-23) yet select_contract passes picks at ev_pct 50-60%, p_profit 0.73, with its own p_touch_target column logging 0.0 - feasibility is priced nowhere and the mu=0 branch alone fails ev_~
## selector
### SELECTOR-1 [high] {VERIFY-PENDING} Lambda band (<=90) x delta gate (0.40-0.80) is jointly EMPTY for index options at current IV - structural starvation; the registered 0DTE carve-out is unreachable
CLAIM: Because lambda = delta*S/mid scales as ~delta/(iv*sqrt(T)), the joint feasible set of the delta window and lambda<=90 is empty for SPY-class index chains at calendar IV 12-14%: ALL 0DTE strikes fail (min lambda in the delta window 110.8-137.9), and DTE-2 is empty at 12-13% on $1 strikes. Index lanes can therefore essentially never buy anything until IV rises, and the 0DTE skew the platform explicitly built a carve-out for (opts-tweak-0dte-carveout-v1) has never been reachable.
REPRO: BS(630,K,r=.04,calIV=.13,T_cal=(dte+.27)/365) chain -> solve trading-clock IV -> greeks: 0DTE min lambda over delta-window = 122.9 (K=626, delta .79, mid 4.05); DTE-2: K=626 delta .761 lam 92.9, K=625 delta .811 (out of band) - every strike fails one of the two gates. At calIV .20: 0DTE K=624 delta .783 lam 81.3 passes (1 strike).
FIX: Make the lambda ceiling volatility-scaled (lambda_max ~ c/(iv*sqrt(T))) or drop the ceiling entirely and let the EV stage price leverage; at minimum, journal a per-no_pick flag when the delta-window x lambda-band intersection is empty so the starvation is visible.
### SELECTOR-2 [high] {VERIFY-PENDING} hold<=1/3-of-life gate bans ALL DTE-1 contracts before 12:45 ET for rest-of-day signals; combined with SELECTOR-1 the morning index funnel is fully closed
CLAIM: Every index lane emits horizon_T = rest-of-day; at 09:35 that is 0.9872 trading days, but a DTE-1 contract's T/3 is 0.6624 - so the gate rejects the ENTIRE DTE-1 expiry until 12:45 ET. With 0DTE lambda-infeasible (SELECTOR-1) and DTE-2 lambda-empty at 12-13% IV, a morning SPY/QQQ signal at current vol has NO expiry that can pass the hard gates at all.
REPRO: horizon(09:35) = (960-575)/390/252 = 0.9872 td; DTE-1 T = 1.9872 td, T/3 = 0.6624 < 0.9872 -> REJECT; solving 3h <= h+1 gives h <= 0.5 day = 195 min -> first DTE-1 pass at minute 765 = 12:45 ET. DTE-2 always passes (3h <= h+2 for h<=1).
FIX: The gate conflates planned hold with maximum hold: either cap the EFFECTIVE horizon used for gating at min(horizon_T, T/3) and let the exit ladder govern (it already does), or exempt DTE-1 the way DTE-0 is exempted - the 1/3 rule was designed for multi-day theta bleed, not same-day exits on tomorrow's expiry.
### SELECTOR-3 [high] {VERIFY-PENDING} EV stage measures target ambition, not edge: flat p_thesis=0.5 x a drift that attains the target by construction makes ev_pct ~linear in target size
CLAIM: ev_hold_thesis's thesis branch uses mu_t = ln(1+target)/horizon, so the mixture EV is guaranteed ~half the option payoff of the full target move regardless of how improbable the target is; p_thesis is a hardcoded 0.5 for every lane. Consequence A (false fire): the live entry logged ev_pct=53.31 and p_profit=0.686 on a 1.71-sigma/140-min move that the selector's OWN p_touch scored at 0.086, with the mu=0 branch at -1.18% - the +53% is pure assumed-drift credit. Consequence B (starvation): modest-target lanes are structurally dead - last30's tar~
REPRO: Repro F reproduces the live IWM record exactly (solved_iv .1803, ev% 53.31, p_profit .686); its mu=0 branch alone: ev% = -1.18, p_profit .432. Repro D, same SPY DTE-2 contract: target .15% -> ev% 4.50; .28% -> 10.96 (floor threshold); .48% -> 21.41; 1.16% -> 59.94 - while p_touch(mu=0) for those targets falls 0.53 -> 0.039. last30 sweep: target .15% best ev% 4.30-5.67 < 10 at any IV -> lane can never pick.
FIX: Replace the flat 0.5 with per-lane p_thesis calibrated from realized target-touch rates (the shadow/journal data is accumulating exactly this); or floor the decision on the mu=0 branch plus a registered empirical hit-rate. Immediately: journal ev_mu0 and p_touch beside ev_pct and alarm when ev_pct/p_touch implies an i~
### SELECTOR-7 [high] {VERIFY-PENDING} Non-index spread gates (<=10% AND <=$0.30) are unpassable for lane-2 catalyst names - the lane has never produced a pick
CLAIM: Catalyst-day single-name chains (post-gap, first 15 minutes) essentially never quote inside 10%-of-mid AND $0.30 absolute at the delta 0.40-0.80 strikes; all three live ORB signals died with 100% of same-direction rows rejected at no_quote/spread_pct/spread_abs before any other gate could even run. The lane burns premarket hunt-list infrastructure to feed a selector that cannot accept its merchandise.
REPRO: CTAS 2026-07-15: 106 rows fetched, 53 same-direction, 53 rejected at quote/spread gates, 0 reached OI/delta/EV. Cumulative lane-2 live: 3 signals, 0 picks, 0 rows past the spread stage.
FIX: Decide which is wrong: if the spread floor is the law (defensible - 10% round-trip vs a 10% EV floor is self-canceling), retire or re-scope lane 2 to penny-pilot mega-caps; if the lane is the point, let the EV stage price the actual spread (it already subtracts it) and drop the absolute cap, keeping only a wider pct c~
### SELECTOR-6 [high] {VERIFY-PENDING} OI/volume floors use mismatched clocks: prior-day OI + full-day volume bar judged 5-15 minutes into the session
CLAIM: open_interest (>=1000 index / >=300) is a stale prior-day snapshot and volume>=100 is day-cumulative volume evaluated at 09:35-09:45 for morning signals - a full-day liquidity standard applied to minutes of tape. On IWM/QQQ the index OI floor rejects most non-round strikes outright; live it is the single largest hard-gate killer and its position in gate order masks the volume gate almost completely. On 0DTE the staleness cuts both ways: heavily-traded new strikes fail (yesterday's OI thin) while dead strikes with big stale OI pass into the IV ~
REPRO: IWM 2026-07-13 09:44 call fire: 474 rows -> 237 calls -> OI 101 + no_quote 66 + hold 55 + spread 10 + lotto 2 + lambda 1 + volume 2 = 237 (100% rejected; 0 reached the EV stage).
FIX: Scale the volume floor by elapsed session fraction (e.g. >=100 * minutes_elapsed/390, floor ~10-20 in the first half hour) and for 0DTE gate on (OI >= floor OR day-volume >= floor) since prior-day OI is definitionally unrepresentative; log OI and volume per rejected row so the floor can be recalibrated from data.
## exit-engine
### EXIT-ENGINE-1 [critical] {VERIFY-PENDING} Rule (b) thesis_invalid is a structurally guaranteed insta-cut for marginal lane-1 entries: no debounce, tick-sampled, against a rising barrier the entry sits epsilon outside of
CLAIM: The (b) rung sells the instant the lane predicate flips, with no confirmation, evaluated on a single 10s-poll tick against a stale VWAP and an intraday-GROWING noise band; because lane-1 entries fire on the first 5-min close marginally beyond that same band, positions begin ~0.1 sigma outside their own kill barrier and are cut on pure noise - this decided the platform's only live trade and will keep scratching nearly every lane-1 entry, starving all grading (both failure modes at once: fires on noise AND starves the system).
REPRO: Entry margin 0.000107/0.001286 = 0.083 sigma of one 5-min mark (iv=0.1803, sigma_5min = 0.18*sqrt(5/98280) = 0.001286 = $0.379 on open 295.00). P(sampled back inside band at mark 1 | martingale) = Phi(-0.083) = 0.47; P(touch within 300s) ~ 2*Phi(-0.083) = 0.93. Growing barrier: band(entry+10min) ~ 0.005819*sqrt(260/250) = 0.005934 > entry move 0.005926 - with S FROZEN the position is invalid within ~10 minutes; only~
FIX: Make invalidation evidence-symmetric with entry: evaluate on confirmed bar closes (5-min boundary like the entry), require 2 consecutive invalid marks or a hysteresis buffer (~1 sigma_5min inside the band / beyond VWAP, which drops the noise-cut probability from ~0.93 to ~0.28), and freeze the noise band at its entry-~
### EXIT-ENGINE-2 [high] {VERIFY-PENDING} mu_eff noise amplification: a routine 1-sigma 20-minute move becomes a ±631%/yr drift that swings EV_hold by ~100x the (h) threshold and drives (f)/(g)/(h)/(i*)
CLAIM: decide_exit extrapolates a 20-minute drift estimate to a 1-day decision horizon with weight w = t^2/(1+t^2) that keeps 50% of a 1-sigma noise draw, so every EV and touch probability in the ladder is effectively a sign-detector of the last 20 minutes; (h) false-fires on any ~1-sigma adverse blip (~16% hazard per fresh window) even with the thesis intact, and the logged EVs are economically fictional.
REPRO: 1-sigma 20-min move at iv=0.18: r_K = 0.18*sqrt(20/98280) = 0.257% -> mu_hat = 12.62/yr; t=1 -> w=0.50 -> mu_eff = 6.31. On the live IWM put state (S=293.5, K=293, T=0.00931, iv=0.179, mid=1.73): ev_hold(mu=0) = +0.021, ev_hold(+6.31) = -1.599, ev_hold(-6.31) = +5.173, vs ev_sell = -0.010 - a single ordinary 20-min fluctuation moves the (h) decision quantity ~160x its threshold in either direction.
FIX: Shrink the blend for horizon extrapolation: weight by window/horizon ratio (e.g. w' = w * T_K/(T_K + dt)) or cap |mu_eff|*dt at a k-sigma daily move; alternatively compute the t-stat against the DECISION horizon's sigma (iv*sqrt(dt)) instead of the window's. Sweep as a registered replay column (the mu-blend family swe~
### EXIT-ENGINE-3 [high] {VERIFY-PENDING} d2 cost-basis backstop is a breakeven stop armed by spread noise (P~0.99/day on a pure martingale) that overrides positive EV_hold and amputates the right tail the grader needs
CLAIM: Arming needs the bid to rise by exactly one spread (0.17 sigma of a single 5-min mark on the live contract), so nearly every position arms on noise; once armed, a 1-tick giveback fires a mandatory scratch at bid regardless of EV_hold, because d2 sits above (g)/(h)/(i*) in the ladder - with (c) inert this makes the realized distribution 'losers -spread or clock, winners scratched at ~$0', the opposite skew of both the PF>=1.2 promotion gate and the owner's profit-taking casino posture; retracted-rule provenance (consideration 23) no longer defe~
REPRO: IWM 293P: spread $0.03, option sigma per 5-min mark = delta*S*iv*sqrt(5/98280) = 0.468*293.25*0.0012840 = $0.176; arm needs +$0.03 = 0.17 sigma -> P(arm within one mark) ~ 2*Phi(-0.17) = 0.87, within a day ~ 2*Phi(-0.03/1.547) = 0.985. After a marginal arm (+1 tick), P(1-tick giveback within one mark) ~ 2*Phi(-0.01/0.176) = 0.955. Joint arm+scratch on a zero-move martingale: dominant path.
FIX: Co-require the EV rule (fire only when ev_hold < ev_sell too - winner protection then only accelerates already-negative-edge exits), or arm at peak_bid > entry_ask + k*spread with k in a registered sweep {1,2,3}, or demote d2 to a paired-replay column and let (h) own the decision. Also require a two-sided bounded-spre~
## shadow-ledger
### SHADOW-LEDGER-2 [high] {UPHELD/degrades-edge+UPHELD/degrades-edge} Rebuild fails open on an unreadable exits file (mass position resurrection) and append has no torn-line guard; the grader never dedupes exits by position_id
CLAIM: read_jsonl returns [] on ANY OSError, so if the exits file is transiently unreadable at the day-roll rebuild (Windows AV/backup lock is the realistic trigger), _open_positions sees an empty exited-set and resurrects every unexpired historical position; the runner then re-marks and RE-EXITS them, and grade() pools every well-formed exit row per lane with no position_id dedupe - duplicate exits inflate N toward the N>=25 PASS bar and double-count P&L, invisibly to all three falsification checks. Independently, a crash between write() and fsync l~
REPRO: Deterministic desk-check: with exits read returning [], the 07-13 entry has no matching exit, so _open_positions returns it; only its expiry (2026-07-15 < today) diverts it to expired_unexited. Any unexpired overnight grantee instead re-enters self.positions and produces a second exit row; the grader would then report index_trend n=2, net sum -32.00 from one real -16.00 trade (both rows pass _malformed and the ident~
FIX: (1) Make the rebuild fail closed: in _open_positions, distinguish 'exits file unreadable' (raise / abort the roll and journal loudly) from 'exits file absent/empty'. (2) In append_jsonl, if the file exists and its last byte is not \n, write a newline first and journal a torn_tail_repaired event. (3) In read_jsonl, cou~
VOTE UPHELD(degrades-edge): Reproduced every leg executably against the real modules and real ledger records: (1) an exists-but-unreadable exits file (simulated both as dir-in-place and as a genuine FILE_SHARE_NONE Windows lock, which raised PermissionError exactly as claimed) makes read_jsonl return [] silently, and open_pos~
VOTE UPHELD(degrades-edge): Core claim survives adversarial verification with a deterministic repro against the real code: read_jsonl swallows OSError to [] (proven), open_positions_all() then resurrects an exited unexpired position (proven: pos-1 returned), the runner's only divert is expiry<today (run_options_shadow.py:462)~
### SHADOW-LEDGER-3 [high] {UPHELD/degrades-edge+REFUTED/cosmetic} Forced exits are graded at stale fallback NBBOs with no fill-provenance field in the exit record - 0DTE/collapsed-book losers book phantom exit value on the WORST grading ledger
CLAIM: _fresh_option_quote requires bid>0, so a deep-OTM 0DTE dying to a 0.00-bid book (or a mid-hold halted name) can never exit through the engine - rule (a)'s 15:00 clock is unreachable without a usable quote - and rides to the after-hours late_close_flat forced exit, which fills at the LAST STORED MARK's bid (possibly hours stale) or even the ENTRY NBBO. build_exit_record has no field recording this degradation (decision_state gets only {forced: True}); the degradation lives solely in the journal, which the grader never reads, so fictional fills ~
REPRO: Scenario arithmetic: SPY 0DTE put marked bid 0.15/ask 0.17 at 14:50, book goes 0.00x0.01 by 15:00 (routine for dying OTM 0DTE). Every reval 15:00->16:10 is mark_no_quote (bid=0), so rule (a) never executes; at 16:10 late_close_flat books sell at the 14:50 mark's bid 0.15. True terminal value 0.00. WORST-ledger net is overstated by +$15.00/contract, understating the exact loss the N>=25 lane verdict is supposed to se~
FIX: (1) Persist fill provenance in the exit record itself: add nbbo_source: fresh|last_mark|entry_nbbo and nbbo_age_s to build_exit_record (threaded from _force_exit), and have the grader quarantine or separately report exits with stale sources - same class as the halt quarantine. (2) For a 0DTE whose book is one-sided (b~
VOTE UPHELD(degrades-edge): Every cited mechanism verified against the code and by execution. _fresh_option_quote (run_options_shadow.py:860,865) rejects bid==0 on both paths, and decide_exit - the only place rule (a)'s 15:00 0DTE clock lives (exit_engine.py:353-355) - is unreachable when the quote is None (:988-1003 journals~
VOTE REFUTED(cosmetic): The code mechanism is accurately cited (bid>0 gate starves the engine; forced-exit fallback to last-mark/entry NBBO; degradation journaled only), but the finding fails the reachability/materiality test. (1) Never exercised: the system's entire lifetime ledger is 1 entry + 1 exit (clean b_thesis_inv~
## runner
### RUNNER-1 [high] {VERIFY-PENDING} Premarket ticks contaminate LiveBarBuilder session state: svwap and the mu_hat window are built from 08:30-09:29 bars (incl. prevclose-echo bars)
CLAIM: The process starts 08:30 ET daily (log-proven) and tick() polls whenever minute < close_min with no OPEN gate (run_options_shadow.py:1159-1169). LiveBarBuilder.on_tick has no RTH filter (feed.py:160-193), so premarket bars are committed and _commit permanently folds them into cum_pv/cum_v (session VWAP), HOD, and the session arrays (feed.py:115-138). The RTH filter exists only in front of lane dispatch (run_options_shadow.py:579-583). Consequences: (a) every RTH bar's svwap handed to lanes and to thesis invalidation includes premarket typical*~
REPRO: Hunt-list name, prevclose 20.00, gaps +4% (opens 20.80, no premarket prints): premarket bars sit at 20.00; ORB entry ~09:36, first mark 09:41 -> window [09:21,09:41] has c0=20.00 (premarket echo), c1=20.83. mu_hat = ln(20.83/20.00)/(20/98280) = 0.0407*4914 = 200.0/yr; with iv=0.60, t_stat = 0.0407/(0.60*sqrt(20/98280)) = 4.75; w = t^2/(1+t^2) = 0.958 -> mu_eff ~192/yr of pure gap artifact drives p_touch/ev_hold/p_re~
FIX: Gate builder commits to OPEN_MIN <= minute < close (either drop premarket ticks in tick(), or filter minute < OPEN_MIN inside seed/commit), or reset the builder at 09:30. Alternatively have session_arrays/bar_input expose RTH-only views. Register as a structural fix (cohort-neutral: not in DEFAULTS).
### RUNNER-2 [high] {VERIFY-PENDING} b_thesis_invalid hair-trigger is structural: invalidation reads a 10-second tick against the same zero-hysteresis band that admitted the entry with epsilon margin
CLAIM: Lane entry requires a committed 5-min-boundary close beyond the noise band; the runner's _thesis_valid then feeds pos.close = the instantaneous latest tick (S from tick.last, run_options_shadow.py:1007-1009 -> 871-887) into IndexTrendLane.invalidated, which cuts on ANY re-entry inside the band with zero hysteresis (lanes.py:274-277). Because the first boundary close past a slowly-growing band overshoots by construction only marginally, the position is a near coin flip to be cut at the first 300-second mark, paying the round-trip spread each ti~
REPRO: IWM 2026-07-13: entry 13:40, move_from_open=-0.5926%, noise=0.5819% -> margin beyond band = 1.07 bp (3.15 cents on a $293 underlying). Exit ts = entry+307 s (the FIRST 300-s mark): S 293.25->293.50 (+8.5 bp) -> |move| = 0.5078% < 0.5819% -> b_thesis_invalid, worst-ledger -$16. Engine state at exit shows ev_hold=+0.021 > ev_sell=-0.01 and p_regain=0.775 - every EV rule said hold; only the 1-bp-margin band re-entry fi~
FIX: Evaluate invalidation on the last COMMITTED 1-min close (ctx.close), not the raw tick, and add registered hysteresis: invalidate only when the close re-enters below a fraction (e.g. 0.5x) of the band or on 2 consecutive marks inside. Sweep the hysteresis on the paired replay before going live.
### RUNNER-3 [high] {VERIFY-PENDING} Overnight positions are thesis-judged by TODAY's fresh lane instances in TODAY's reference frame, structurally defeating the overnight grant
CLAIM: _lane_by_name is rebuilt every day-roll (run_options_shadow.py:439-453) and _thesis_valid builds PositionCtx from TODAY's ctx (session_open, svwap, current minute). An index_trend position granted an overnight ride (held FOR tomorrow's named catalyst) is evaluated next morning against |move from TODAY's open| < noise -> near-certainly true in the first minutes (move-from-open starts at ~0), so rule b cuts it minutes after the open, hours before the 14:00 FOMC-class catalyst the grant existed for. An inplay_orb overnight position gets judged ag~
REPRO: SPY-class example: overnight grantee marked 09:35; |close/today_open - 1| at 09:31-09:35 is typically 0-10 bp while noise_at(571..575) is the 14-day MEAN |move| at those minutes (~5-10 bp) -> P(inside band) >> 50%; combined with the VWAP-recross arm (svwap ~ open in the first minutes) the first-morning-mark cut probability approaches certainty for any non-trending open.
FIX: For positions with entry_day != today, either skip lane invalidation until the position's own frame is re-establishable (e.g. after 30 RTH minutes), or persist the ENTRY-day thesis parameters (band edge, OR range, entry svwap side) on the position and evaluate against those; the 09:35 re-decision should be an explicit~
### RUNNER-4 [high] {VERIFY-PENDING} Lane 2 armed with a STALE hunt list: no session_date check and a single 08:30 read - live today (07-16) the shadow is hunting 07-15's candidates
CLAIM: load_hunt_list (run_options_shadow.py:263-289) ignores the file's session_date/generated_ts fields entirely, and _lane2_candidates is invoked exactly once per day at the 08:30 day-roll (438, 490-535). runtime/hunt_list.json currently carries session_date 2026-07-15 (written 07-15 05:17) - today, 07-16, InPlayORB is armed with yesterday's earnings names (BLK, BNY, ...) and is blind to today's actual in-play names. This is simultaneously a starvation root cause for the funnel's highest-frequency lane (the day's real gap/catalyst names are never ~
REPRO: File mtime Jul 15 05:17 vs run date Jul 16: staleness = 1 full session. Lane-2 candidate overlap with today's true in-play set: 0 by construction (07-15 earnings reporters are not 07-16 reporters).
FIX: Reject candidates when session_date != today (fall back to the lane2_scan_symbols gap scan and journal hunt_list_stale); re-read the file every N minutes until ~09:25 so a crew that finishes after 08:30 still arms the day.
## events-calendar
### EVENTS-CALENDAR-1 [high] {VERIFY-PENDING} 2027 rollover silently disarms the entire macro-protection layer and the holiday calendar
CLAIM: Every date source is 2026-only with no forward path: FOMC days are a hardcoded 2026 tuple with NO fetch mechanism at all (even a working FRED key would not supply 2027 FOMC dates), the CPI/NFP fallback branch ignores its `year` argument and always emits the 2026 tables, and session_calendar._fallback_days(2027) returns {}. On 2027-01-01 - with no code change and no warning - in_blackout always returns None, MacroReactionLane never arms, print_window_flat and the overnight named-catalyst gate go inert, and (if Tradier is ever unreachable) every~
REPRO: py -3 repro (run this session): upcoming_events(2027-01-04 09:00 ET, horizon_days=30) == [] using the default tables; _fallback_days(2027) == {}; is_trading_day(date(2027,1,1), days={}) == True - New Year's Day (a Friday, NYSE closed) classified as a trading session.
FIX: Add a build_events year-coverage assertion at load (warn/journal when max(event date) < today+30d); make the fallback branch fail LOUD (journal event) when yr != 2026; add the published 2027 FOMC calendar when the Fed releases it and a FRED-driven CPI/NFP fetch (finding 2) so only FOMC needs an annual manual mirror.
## iv-archive
### IV-ARCHIVE-1 [high] {REFUTED/cosmetic+REFUTED/cosmetic} iv_rank is None for every possible entry until at least 2026-07-24, and the documented VIX/VXN warm-up fallback was never built - the EV-max selector runs with zero vol-regime input
CLAIM: The selector's only vol-regime input (iv_rank at tenor_dte=2) is dead: the archive holds 4 in-band sessions (2026-07-10/13/14/15) against a >=10-session gate, so iv_rank returns None on every call through at least the 2026-07-23 EOD snapshot (first non-None during the 2026-07-24 session); the module-docstring's promised FRED VIX/VXN percentile fallback (iv_archive.py:4-5, 203) does not exist anywhere in atlas/, and vix is hardcoded None in the entry record. The system therefore prices entries with no awareness of whether it is buying cheap or ~
REPRO: tol = max(2, 2//4) = 2 -> band [0,4]. In-band distinct SPY days as of 07-16 open = 4 (07-10,13,14,15) < 10 -> None. Adding sessions 07-16,17,20,21,22,23 reaches 10 after the 07-23 15:45 ET snapshot -> first non-None value available during the 2026-07-24 session.
FIX: Pick one and register it: (a) build the VIX percentile warm-up input the docstring promises (FRED VIXCLS daily series gives 252 sessions of history on day one) and wire it as the regime signal until the archive matures; or (b) delete the fallback claim from iv_archive.py:4-5,203 and accept a stated no-vol-regime postu~
VOTE REFUTED(cosmetic): The warm-up arithmetic is correct (4 in-band sessions < 10, iv_rank=None until ~07-24; FRED fallback unbuilt) but the finding's core impact claim is false: iv_rank is decision-inert by construction. It is a single per-underlying scalar applied as an identical constant score offset to every contract~
VOTE REFUTED(cosmetic): The facts check out (4 in-band sessions, iv_rank=None live, no FRED VIX feed - all reproduced read-only), but the [high] "real edge leak" claim is immaterial on the live path: iv_rank is decision-inert by construction. It enters select_contract as one scalar per call and only ever subtracts a unifo~
### IV-ARCHIVE-2 [high] {UPHELD/degrades-edge+UPHELD/cosmetic} Even after warm-up, iv_rank is structurally a no-op: a per-underlying constant subtracted from scores that are only sorted within one selector call
CLAIM: select_contract applies score -= 0.2*max(0, iv_rank-50) using the SAME iv_rank value for every candidate row (it is a scalar function argument, not per-contract), and score is consumed solely by scored.sort(...)[:top_n] inside that one call - there is no absolute score threshold anywhere (all gates are Stage-1/2, pre-score) and the runner never compares scores across underlyings or signals (grep shows pick.score is only logged). Subtracting a constant from every candidate preserves ordering exactly, so iv_rank can NEVER change which contract i~
REPRO: Two candidates with base scores 18.4 and 15.1, iv_rank=80 -> penalty 0.2*(80-50)=6.0 applied to both -> 12.4 and 9.1: identical ordering, identical top_n, identical entry decision. This holds for every possible iv_rank value by construction (order-invariance of x -> x - c).
FIX: Decide what vol regime should do and wire it where it can act: (a) as an entry-level input - scale the required EV edge or the p_thesis haircut by regime; (b) as a per-contract term - interact iv_rank with the contract's vega*dte so high-rank penalizes long-vega picks relative to short-dated ones (this CAN reorder); o~
VOTE UPHELD(degrades-edge): Claim survives independent re-derivation and empirical execution. The iv_rank penalty (selector.py:240-241) subtracts the same scalar constant from every candidate in a select_contract call; score is consumed solely by sort+[:top_n] (selector.py:264-266) with no absolute threshold (all 17 gates fir~
VOTE UPHELD(cosmetic): Confirmed by direct execution against the real code. iv_rank is a per-call scalar (runner computes it once per signal at run_options_shadow.py:715-718 and passes it at :737; selector.py:123-128), the penalty at selector.py:240-241 subtracts the same constant from every surviving candidate, and scor~
## grader
### GRADER-1 [high] {VERIFY-PENDING} PASS bar is a fixed-threshold filter, not a significance test: ~36% false-PASS per lane at N=25 under a zero-net-edge null, ~63% with nightly re-grade peeking, ~99% family-wise across 5 lanes; 35-64%~
CLAIM: The go-live evidence gate (n>=25 AND mean net>0 AND gross>0 AND PF>=1.2 AND top_day_share<0.5, grade_options_shadow.py:135-136) has almost no discriminating power at its own sample floor, and its power barely improves with N because PF>=1.2 and mean>0 are fixed effect-size thresholds rather than N-scaled significance tests. Worse, the verdict is recomputed nightly on all history (line 73: load_exits(day=None)), which is uncorrected sequential testing: a truly zero-edge lane will flash PASS on at least one night with ~63% probability between N=~
REPRO: Analytic: population PF=1.2 under Normal(mu,sigma) <=> mu/sigma=0.0727, i.e. t-stat 0.36 at N=25 (one-sided p~0.36), 0.51 at N=50, 0.73 at N=100 - never near significance. Monte Carlo (100k sims mirroring lines 133-136, 2 trades/day): zero-NET-edge null at N=25 -> P(PASS)=0.360-0.362 across sigma=10-30% of premium; 5 lanes -> 0.893. Sequential peeking N=25..100 (40k sims): P(ever PASS)=0.629 single lane, 0.993 acros~
FIX: Replace the mean>0 + PF>=1.2 pair with an N-scaled test: PASS requires the one-sided 95% bootstrap (or t) lower confidence bound of WORST mean net > 0, applied at pre-registered looks only (N=25, 50, 100) or via an anytime-valid sequential test (e-values / SPRT) since the grader inherently peeks nightly; apply a famil~
### GRADER-2 [high] {VERIFY-PENDING} Evidence machine cannot conclude on any actionable timescale: N>=25 per lane at ~0.14 fires/day means 6 months to years per verdict, and no pooled cross-lane row exists to accumulate evidence faster
CLAIM: The grader's only unit of verdict is the individual lane, each needing N>=25 exits (line 135-136) with go-live talk at 50. At the live funnel rate (~0.14 lane fires/day, 1 entry in 3 sessions), even if EVERY fire landed in one lane the first HELP/HURT verdict is ~180 trading days away; spread across 4 active lanes it is multi-year. The grader compounds failure mode B (starvation): it is the component that decides whether anything ever goes live, and its evidence design guarantees the answer stays ACCUMULATING for quarters while providing no po~
REPRO: 25 / 0.14 fires/day = 179 trading days ≈ 8.5 calendar months to the FIRST N=25 verdict if one lane monopolizes every fire; at the observed 1 completed trade per 3 sessions it is 75 sessions ≈ 3.5 months per lane only if all fires stay in index_trend, and >1 year for any second lane.
FIX: Add a pooled ALL-lanes pseudo-row (deduped by position_id so merged trades count once) graded with the same falsification gates - it reaches any given N 4-5x faster and answers the owner's actual question ('does the whole system have edge') first; adopt a sequential e-value test so every single trade updates evidence;~
## replay-lab
### REPLAY-LAB-1 [high] {VERIFY-PENDING} Quote capture stops at the live exit, so both replay stages are one-sided: no variant can ever be credited for holding longer than the live engine
CLAIM: The runner deletes the position and stops writing quote rows at the live SELL (run_options_shadow.py:1126-1131), so the counterfactual 'variant would hold' outcome is never graded: stage 1 forces uncrossed variants to exit at the ACTUAL exit bid (run_overnight_lab.py:254, 301), and stage 1b marks them 'unexited' with n counted but no P&L (replay.py:160-163, 196-198), making net_worst_sum a comparison over different effective samples. The lab is structurally incapable of vindicating patience - while the owner's flagged concern (hair-trigger rul~
REPRO: The one real trade: live exit at bid 1.72, net −$16.00 (worst). Post-exit rows available to grade the hold counterfactual: 0. The truth-validation that the cut was correct had to be done manually outside the lab - the lab itself could not produce it, and never will under current capture.
FIX: Keep polling an exited contract's NBBO at the same cadence until EOD (0DTE) or a registered horizon (e.g. next-day close for DTE>=1), writing post-exit quote rows flagged post_exit:true. Then grade 'unexited' variants at a defined horizon fill instead of censoring, and report per-variant paired mean difference over th~
### REPLAY-LAB-2 [high] {VERIFY-PENDING} thesis_valid is replayed, not recomputed, and rule (b) precedes every swept parameter in both engines - the A/B has zero discriminating power on the exact rule under owner suspicion
CLAIM: replay.py:118 passes ext's stored thesis_valid to every variant; rule (b) is parameter-free and fires before the swept rules in both ladders (exit_engine.py:357 before :360 stop_frac and :396 p_regain_min; exit_engine_legacy.py:270-271 before :273). Any path where the lane flips thesis before a price threshold crosses produces byte-identical outcomes across ALL pre-registered variants, so the AB can never measure whether b_thesis_invalid is hair-trigger - the owner's #1 flagged question - nor let the p_regain/stop sweep points ever fire on suc~
REPRO: 2026-07-15 report: all five variants (legacy_v1, legacy_v1_stop50, v2_default, v2_pregain_15, v2_pregain_35) return identically {n:1, net_worst_sum:-16.0, rule_mix:{b_thesis_invalid:1}, unexited:0} at exit_minute 825 - 100% of real-data history yields zero divergence. The exit state itself shows every other rule would have HELD (ev_hold 0.021 > ev_sell −0.01, p_regain 0.775): rule b was the sole decider, and it is o~
FIX: Store the raw per-lane thesis inputs (or per-lane verdicts) in ext so variant thesis policies can be recomputed (e.g. 'b requires 2 consecutive invalid marks', 'b with a noise-band re-entry check'), and register at least one AB variant that varies the thesis mechanism. Combined with REPLAY-LAB-1's post-exit capture, t~
### REPLAY-LAB-3 [high] {REFUTED/cosmetic} Grid cohort gate (>=2 quote rows) excludes every first-mark exit - the grid has replayed 0% of all real trades and systematically excludes the fast-cut population
CLAIM: run_overnight_lab.py:288 skips positions with fewer than 2 stored rows. The runner sets last_mark_ts at entry (run_options_shadow.py:829) and marks at 300s cadence (841-851), so any position exited on its first mark - precisely the hair-trigger cuts the platform needs to study - produces exactly 1 quote row and is excluded from the grid forever. The stage's lifetime coverage of real trades is zero.
REPRO: IWM 293P: entry 13:40 ET (minute 820), first and only mark at 13:45 (825) = entry + 300s, which was the SELL mark. rows=1 < 2 → skipped. Report confirms: the grid stage aggregated an empty cohort while exit_engine_ab replayed the same position fine with 1 row.
FIX: Lower the gate to >=1 row (a 1-row path still supports the paired convention: variant crossing on that row vs fallback to actual), or better, fix the root cause: mark immediately on entry (write a t=0 quote row from the entry NBBO) so every position has an entry-anchored path, and pair that with REPLAY-LAB-1's post-ex~
VOTE REFUTED(cosmetic): The cited mechanics are real (first-mark exits store exactly 1 quote row, and run_overnight_lab.py:288 excludes them), but the defect claim fails independent re-derivation: the single stored row is byte-identical to the exit NBBO (runner writes quote row and exit record from the same bid/ask in the~
## premarket-crew
### PREMARKET-CREW-1 [high] {VERIFY-PENDING} No freshness check on runtime/hunt_list.json - lane 2 arms on a stale, possibly days-old AI watchlist
CLAIM: load_hunt_list (run_options_shadow.py:263-289) reads only the symbols/candidates array and ignores the session_date and generated_ts fields the crew explicitly writes (research_crew.py:370-371). _lane2_candidates consumes it once per day in _roll_day with no mtime/date validation, so if ATLAS-Premarket (05:15 CT) fails to fire, is killed by its 300s cap (see -2), or crashes before the atomic write, the 07:30 CT shadow launch silently hunts YESTERDAY'S catalyst names all day. The failure is also invisible post-hoc: the journal records only lane~
REPRO: date(session_date in artifact) = 2026-07-15; date(today) = 2026-07-16; delta = 1 trading day. Weekend case observed: 07-11 arm used a list generated 07-10 (~33h stale) and journaled stand-downs for its names.
FIX: In load_hunt_list, parse session_date (fallback: file mtime) and compare to today's ET date; on mismatch return [] and journal {event: hunt_list_stale, session_date, today} so the day arms with an empty lane-2 universe LOUDLY instead of a dead-catalyst one. Also stamp the consumed session_date into the day 'armed' jou~
### PREMARKET-CREW-2 [high] {VERIFY-PENDING} Crew worst-case runtime (gather + 5 sequential 90s provider calls) exceeds the 300s _run_crew kill window - timeout leaves the hunt list silently stale
CLAIM: build_day_briefing._run_crew caps the whole crew subprocess at CREW_TIMEOUT_S=300s (build_day_briefing.py:59,266) and subprocess.run KILLS the child on expiry. research_crew fans out to up to 5 providers SEQUENTIALLY at 90s timeout each (research_crew.py:319,347-355) after a gather phase whose movers scan is self-documented at ~2 minutes (research_crew.py:228). The crew writes hunt_list.json only at the END (atomic, research_crew.py:388-390), so a timeout kill discards everything - including votes from providers that DID answer - and leaves ye~
REPRO: 4 hung providers x 90s = 360s > 300s with zero gather time. Realistic: measured gather+context path ~135-150s (task start 05:15:15 -> hunt write 05:17:46) leaves 165s < 2 x 90s - two hung providers suffice. Absolute worst case ~135 + 5x90 = 585s = 1.95x the budget.
FIX: Budget-aware fan-out: pass each provider timeout = max(15, (deadline - now)/providers_remaining); or raise CREW_TIMEOUT_S to >= 600s; and/or write hunt_list.json incrementally (after gather with candidates=[], then rewrite after merge) so a kill can never strand yesterday's artifact. Pair with the -1 freshness gate so~
### PREMARKET-CREW-3 [high] {VERIFY-PENDING} Consensus ranking collapses to ALPHABETICAL ordering on single-model days, and the runner's 10-name cap consumes exactly that order
CLAIM: merge_consensus sorts by (-models_agree, -confidence, symbol) (consensus.py:256). Free models emit uncalibrated confidence (groq returned 1.0 for 9/10 names), so on a 1-model day every candidate ties at (agree=1, conf=1.0) and the rank becomes symbol-ascending - alphabetical. Downstream, _lane2_candidates sorts by abs(gap_pct) which is hardcoded 0.0 for all crew names (finding -7), so Python's stable sort preserves that alphabetical order into the lane2_max_candidates=10 truncation (run_options_shadow.py:534-535) while the crew may write up to~
REPRO: 07-15 artifact: 9/10 rows conf=1.0, models_agree=1, sorted BLK<BNY<CTAS<ELV<FHN<JNJ<MS<MTB<PNC (pure lexicographic). Had groq returned 12 valid names, rows 11-12 by alphabet (e.g. any T-Z ticker) would be cut by the 10-cap regardless of catalyst quality.
FIX: Break ties with deterministic packet features rather than symbol: e.g. mover _score (already computed in gather_movers), event magnitude, or |gap|. Journal a degraded-consensus flag when answered<2 so the day's lane-2 universe is known to be single-model. Ignore or shrink self-reported confidence (it is 1.0 spam); mod~
### PREMARKET-CREW-5 [high] {VERIFY-PENDING} Earnings gather is today-only - the packet structurally omits yesterday's after-close reporters, the richest morning-gap cohort
CLAIM: gather_earnings queries Finnhub with from=to=session (research_crew.py:104), so the packet contains only TODAY'S scheduled reporters. Names that reported YESTERDAY after the close - the classic cohort that gaps at today's open with first-5 RVOL >= 5, i.e. exactly what InPlayORB is built to trade - never appear in the earnings section. The movers section cannot recover them: it screens the PRIOR session's last daily bar (open/prev_close gap and volume of the session BEFORE the print, research_crew.py:248-258), and the catalyst-events section on~
REPRO: For a stock reporting 07-14 amc: earnings section (07-15 calendar) = absent; movers section = 07-14 session bar, gap computed 07-14 open vs 07-13 close = pre-announcement noise; so P(in packet) ~= P(catalyst artifact caught the PR) only. The cohort with the highest expected first5_rvol has ~0 packet coverage on any day the 8-K feed is quiet.
FIX: Fetch two calendar days: from=prev_trading_day, to=session; keep prev-day rows only where hour=='amc' (and today's rows where hour in bmo/dmh), tagging each with reported_last_night=True so models can prioritize them. Cheap (same endpoint, one extra day) and directly widens the lane-2 funnel with its intended prey.
### PREMARKET-CREW-8 [high] {VERIFY-PENDING} The deterministic gap-scan is dead code (lane2_scan_symbols=[]) - five flaky free LLMs are the SOLE lane-2 universe source, with no deterministic fallback
CLAIM: The scan branch in _lane2_candidates (run_options_shadow.py:500-513) only runs when cfg['lane2_scan_symbols'] is non-empty; the default is [] (run_options_shadow.py:78) and config/hunter.yaml has no options_shadow block (sweep_ledger opts-runner-defaults: 'hunter.yaml block absent -> DEFAULTS rule'), so it is unreachable in production and lane2_gap_min_pct=4.0 is inert config. Consequently the entire lane-2 universe flows through the research crew, which yesterday was down to 1/5 providers (groq). One groq outage (or a 300s kill, finding -2) m~
REPRO: P(lane-2 universe exists) = P(>=1 of 5 free providers answers AND names survive the allowlist). Observed 07-15: exactly 1/5 answered. If groq had also 429'd: candidates=[], lane2_cands=0, InPlayORB dead for the day while DAL-class earnings gappers trade without the shadow watching.
FIX: Union the AI list with a deterministic floor: write gather_movers' top rows into the artifact as candidates with a source:'movers_screen' tag (they already pass symbol/liquidity checks), or set lane2_scan_symbols from the movers output. Keeps the crew as ranking/context enrichment rather than a single point of failure~
## tradier-feed
### TRADIER-FEED-1 [high] {VERIFY-PENDING} Retry loop covers only HTTP 429 - timeouts/5xx/connect errors are first-failure-fatal, and the chain-fetch caller kills the whole signal on one blip
CLAIM: _request's 'retries=2' applies exclusively to 429 responses: any httpx transport exception (ReadTimeout at the 5s scalar timeout, ConnectError) or 5xx raises immediately on the first attempt, and the runner's _enter treats any _chain_rows exception as terminal for the signal (journal chain_fetch_error, return, never re-attempted) - so a single transient network blip during the largest payloads the system fetches (greeks=true chains) permanently deletes a lane fire in a funnel already starved at ~0.14 fires/day.
REPRO: A signal costs 1 expirations + up to 3 chain requests = 4 first-failure-fatal calls. With a modest 2% per-request transient failure rate, P(signal killed) = 1-0.98^4 = 7.8%; at 0.14 fires/day each killed signal is ~1/0.14 = 7.1 days of expected funnel output. One retry drops the loss to 1-(1-0.02^2)^4 = 0.16%.
FIX: Inside _request, retry transport exceptions and 5xx once or twice with a short capped backoff (e.g. 0.5s, 1s) before raising; independently, let _enter retry _chain_rows once ~2s later before abandoning a signal. Both are cheap against a 100/min budget used at ~6-16/min.