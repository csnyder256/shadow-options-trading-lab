# PART 1 DIGEST

## PROBES
### probe: READ-ONLY preflight of C:/path/to/shadow-options-trading-lab for the ATLAS options-shadow session firing 2026-07-16 07:30 CT (probe ran 01:27-01:30 CT). Nothing was modified, no process touched, no secret printed. Overall verdict: GREEN - ATLAS-OptionsDay is Ready (NextRun 7/16 07:30:30, LastResult 0), is_trading_day(2026-07-16)=True, the Tradier token authenticates live righ~
- OK: 1. config/tradier_shadow.local.yaml: present (314 B), no BOM, parses as YAML dict under utf-8-sig; keys {env, token}; token = non-empty str, length 28 (value not read out); env: production.
- OK: 1. config/tradier.local.yaml: present (367 B), HAS a UTF-8 BOM but parses cleanly under utf-8-sig - which is exactly what the tolerant client builder uses (run_options_shadow.py:193-194 'a BOM must never kill the feed'); token non-empty str length 28; env: pr~
- OK: 1+. LIVE token check (mirrors the launcher's own preflight, prints only OK/EMPTY): TradierData builds from tradier_shadow.local.yaml and get_quotes(['SPY']) returned non-empty at 01:28 CT - the token authenticates today.
- OK: 2. config/hunter.yaml: parses; single top-level key 'hunter'; NO options_shadow block (exactly as the code comment prescribes - defaults rule). Effective config == DEFAULTS byte-for-byte and config_hash == 6c1dc2a4e1a7, the expected corrected-exit-engine coho~
- OK: 3. runtime/options_shadow.lock: exists (pid 12248, ts 2026-07-15 07:30:01 CT) but PID 12248 is NOT running (Get-Process: no such process). acquire_lock (scripts/run_options_shadow.py:149-160) only refuses when _pid_alive(old_pid) is True, and _pid_alive (line~
- OK: 4. runtime/STOP_DAY.flag: does NOT exist. Launcher claim verified in source: scripts/launch_options_day.ps1 lines 203-204 delete the flag just before the monitor loop ('running until $UntilTime (STOP_DAY.flag honored)'), so even a stray flag could not block l~
- OK: 5. runtime/hunt_list.json: mtime 2026-07-15 05:17:46 CT, session_date field '2026-07-15', 10 candidate rows (BLK/BNY/CTAS/ELV/FHN/JNJ/MS/MTB/PNC earnings + LEDS legal 8-K). It is yesterday's file, but ATLAS-Premarket (build_day_briefing.py) is Ready, LastResu~
- OK: 6. runtime/intraday_cache: 549 parquet files. The lane1/1b watch set SPY/QQQ/IWM was refreshed 2026-07-15 15:35 CT (SPY 3.87 MB, QQQ 3.75 MB, IWM 3.60 MB) - ATLAS-CacheRefresh ran on schedule (LastRun 7/15 15:35:35, LastResult 0, NextRun 7/16 15:35:35). Lane1~
- OK: 7. runtime/symbol_state.json: parses; generated_epoch 2026-07-15 15:19:40 CT; 6 halts (SLAI, NUVL, CPRX, LCDL, AMZE, OPFI.W), 0 suspensions/SSR, unresolved_count 0 - the halt gate has clean data from yesterday and the poller is launcher-scoped (restarts at 07~
- OK: 7. runtime/catalyst_context.json: parses; 10 symbol keys matching the 07-15 hunt list; written 05:17:47 by the same premarket run that will refresh it today.
- OK: 7. runtime/news_sources_heartbeat.json: parses; schema 2; benzinga healthy (2 items last poll, 0 consecutive failures) and finnhub healthy (HTTP 200, 100 items, cursor 07-15 16:19 ET) as of 07-15 16:20 ET shutdown.
- OK: 7. runtime/options_iv.db: present (2.44 MB), valid SQLite header, opens read-only; tables iv_daily (4,419 rows) + iv_surface (14,576 rows); last written 07-15 14:45 CT by ATLAS-IVSnapshot (Ready, LastResult 0, NextRun 7/16 14:45).
- OK: 8. config/alerts.json: NO BOM; parses as plain utf-8 AND utf-8-sig; ntfy block present with base_url ntfy.sh and topic 'atlas-...' (28 chars, masked) - the launcher's Page() function (launch_options_day.ps1:38-49) can page.
- OK: 9. Last-session logs are CLEAN: options_shadow.err.log is 0 bytes; NO restart logs exist (zero crashes on 07-15); options_shadow.out.log shows 'tradier client UP', 'options-shadow UP ... cfg_hash=6c1dc2a4e1a7 tradier=yes', 'day 2026-07-15 armed' with 5 lanes ~
- OK: 10. runtime/options_shadow_heartbeat.json: schema 2, ts_epoch 1784146803.954 = 2026-07-15 15:20:03 CT (10 s before the 15:20:13 teardown - died by teardown, not crash), client_present true, pid 12248 (matches lock), mode shadow, 5 lanes armed, open_positions ~
- OK: 11. Disk: C: has 320.77 GB free - no space risk.
- OK: 12. runtime/backtest_out/sweep_ledger.jsonl: present, 96 rows, 0 malformed JSON rows.
- OK: 13. Venv import smoke test passed from the repo cwd: 'IMPORT OK 10.0' (scripts.run_options_shadow imports; DEFAULTS['poll_seconds'] = 10.0).
- OK: 14. Clock/timezone sane: system TZ = Central Time (US & Canada) with DST; local 2026-07-16 01:27:44 vs UTC 06:27:44 = UTC-5 (correct CDT for July); derived ET 02:27:44 and CT 01:27:44 agree with the OS clock; all file mtimes/epochs are mutually consistent. Th~
- OK: 14+. Scheduler: ATLAS-OptionsDay Ready, action = launch_options_day.ps1 -UntilTime 15:20, weekly trigger enabled, LastRun 7/15 07:30:30 result 0, NextRun 7/16 07:30:30 - today IS in its schedule; is_trading_day(date(2026,7,16)) returns True so the launcher's ~
- RUN-ENDER [medium / Medium - the failure already happened yesterday; today's 05~]: Premarket crew is down to 1/5 providers - hunt list rides on groq alone | EVIDENCE: runtime/hunt_list.json (2026-07-15 05:17) carries note '1/5 providers answered (failed: cerebras, gemini, openrouter, zai)'; every candidate has models_agree=1 via groq only. If groq also fails at today's 05:15:15 ATLAS-Premarket run, candidates=[] and lane2/inplay_orb start blind (loader is fail-o~ | MITIGATION: After 05:15 CT and before 07:30, eyeball runtime/hunt_list.json: candidates count > 0 and the providers.answered list. Longer term: check API keys/quotas for cerebras, gemini, openrouter, zai - four simultaneous failures smells like expired keys or a shared q~
- RUN-ENDER [medium / Low - PID 12248 is free right now, but Windows recycles PID~]: Stale lock PID-reuse edge: if Windows recycles PID 12248 to any live process before 07:30, the start aborts | EVIDENCE: runtime/options_shadow.lock holds pid 12248 (dead). _pid_alive returns True for ANY live process bearing that pid (run_options_shadow.py:126-141 checks STILL_ACTIVE, not identity), acquire_lock then refuses, the shadow exits within launch's 3-second check window, and launch_options_day.ps1:116-121 ~ | MITIGATION: Cheapest insurance: delete runtime/options_shadow.lock before 07:30 (provably safe - the holder is dead; this probe was read-only so I did NOT do it). If skipped, the odds are low and a failure pages loudly via ntfy, so it fails observable, not silent.
- RUN-ENDER [low / Certain (already the state) but impact is gradual degradati~]: Intraday cache is a week stale for all non-watch symbols - inplay/lane-2 noise profiles degrade | EVIDENCE: 546 of 549 runtime/intraday_cache/*.parquet last written 2026-07-09 15:20-15:50 (a tail from 07-07). ATLAS-CacheRefresh only rewrites SPY/QQQ/IWM at 15:35. Hunt-list symbols (e.g. CTAS yesterday) get noise profiles built from bars ending 07-09 (_read_cached_profile, run_options_shadow.py:292-303) - ~ | MITIGATION: None needed for today's start (fail-open: missing/unreadable profile returns None and the lane stands down per design). If single-name lanes matter this week, extend ATLAS-CacheRefresh to cover hunt-list/lane-2 symbols, not just the watch trio.
- RUN-ENDER [low / High that it continues today - the IP block has persisted s~]: GDELT feed persistently 429-blocked (5 consecutive failures) | EVIDENCE: runtime/news_sources_heartbeat.json: gdelt http_status 429, consecutive_failures 5, last_ok 07-15 14:50 ET, items_last_poll 0. Known condition (memory: 'GDELT persistently 429-from-IP, fail-open'); benzinga + finnhub are healthy and finnhub general carries macro. | MITIGATION: No action for today - fail-open by design and two of three sources are healthy. Fix is an ops task (rotate IP / add GDELT backoff), not a session blocker.
- RUN-ENDER [low / High - 0.14 fires/day trend, dies at contract selection]: Funnel produced 0 entries on 07-15 (and 07-14) - today may again yield no evidence despite a healthy stack | EVIDENCE: Heartbeat entries_today=0, last_mark_epoch=0.0; out.log shows only two funnel attempts all day, both NO PICK on microstructure (no_quote 10, spread_pct 33/12, open_interest 16, lotto_delta_ban 4). This is the known selection-starvation condition already in memory - the machine is fine; the funnel i~ | MITIGATION: Nothing to do before 07:30 - this is a strategy-layer question (selection gates), not a machine-state defect. Flagged so a third zero-entry day isn't misread as an infra failure.

### probe: READ-ONLY probe of the 11 ATLAS scheduled tasks on <HOSTNAME> (TZ=Central Standard Time, probed 2026-07-16 01:29 CT), cross-checked against scripts/pivot_task_cutover.ps1 (intended registrations), scripts/launch_options_day.ps1 (guards/lock/teardown), scripts/run_overnight_lab.py (RTH guard + write surface), scripts/run_options_shadow.py (pid-lock idiom), live runtime lock/flag files, and th~
- OK: ATLAS-OptionsDay: Ready, next run 7/16/2026 07:30 local = 07:30 CT today (machine TZ verified Central Standard Time via tzutil). Action is powershell.exe -NoProfile -ExecutionPolicy Bypass -WindowStyle Hidden -File C:\path\to\tra~
- OK: ATLAS-LiveDay is Disabled (Status: Disabled; XML <Enabled>false</Enabled>; tombstone kept per cutover script) and ATLAS-AutoEOD is Disabled (matches its 2026-07-15 decommission). Neither can fire today.
- OK: ATLAS-Premarket: Ready, next run 7/16 05:15 CT (weekdays), last run 7/15 05:15 result 0 = success, so the hunt list / day_briefing.json was fresh yesterday and the task is armed for today. WakeToRun + StartWhenAvailable + 1h limit.
- OK: ATLAS-CacheRefresh last result 0x0 (7/15 15:35, next 7/16 15:35) and ATLAS-IVSnapshot last result 0x0 (7/15 14:45, next 7/16 14:45); both weekday-scoped, WakeToRun, StartWhenAvailable, run-as owner. IVSnapshot's in-session 14:45 CT fire is by design (15:45 ET A~
- OK: ATLAS-OvernightLab: Ready, weekdays 15:25 CT, last run 7/15 result 0, PT8H limit. Its OWN guard run_overnight_lab.py rth_blocked() (lines 107-112, enforced at main() line 766, exit 3, 'deliberately no override') blocks 09:15-16:15 ET weekdays; 15:25 CT = 16:2~
- OK: ATLAS-Rehearsal: one-shot trigger Sunday 2026-07-12 18:00 already fired (last run 7/12 18:00, result 0) so Next Run Time = N/A - it cannot fire on any schedule today, and StartWhenAvailable has no pending catch-up for a completed one-time trigger. It runs lau~
- OK: Session calendar confirms today: is_trading_day(date(2026,7,16)) = True, session_close_minute = 960 (normal 16:00 ET close, no early close), so OptionsDay's -UntilTime 15:20 CT (=16:20 ET, covering the 16:15 ET index-ETF quote window and 16:10 ET hard flat) i~
- OK: Runtime state is clean for the 07:30 boot: runtime\STOP_DAY.flag ABSENT (a leftover flag would end the day at the first 60s poll); runtime\options_shadow.lock exists but its holder pid 12248 is DEAD, and run_options_shadow.py's dead-pid reclaim admits today's~
- OK: launch_options_day.ps1 guards verified in source: post-close catch-up exits cleanly (UntilTime-passed check, no overnight run), session guard is fail-open (guard ERROR launches anyway), Tradier preflight WARNs-and-launches with an ntfy page (degrades to obser~
- OK: Support tasks armed: ATLAS-ModelServer Ready (at-logon + weekdays 15:22 CT, start-only/idempotent - never stops the server, feeds the lab's 16:25 ET LLM slot; last 0), ATLAS-AuthKeepalive Ready (Sundays 18:00, last success 7/12, next 7/19 - and the options la~
- OK: The interactive-logon precondition currently holds: this probe ran inside the owner's logged-on session at 01:29 CT on 7/16, before both the 05:15 and 07:30 fires.
- RUN-ENDER [high / low - the owner's session is logged in as of 01:29 CT and no rebo~]: Every ATLAS task is 'Interactive only' (LogonType InteractiveToken) - a logoff/reboot before 07:30 silently kills the whole day | EVIDENCE: schtasks /xml for all 11 tasks shows <LogonType>InteractiveToken</LogonType>; Logon Mode 'Interactive only' in every /v listing. WakeToRun=true only covers SLEEP - it does not log the user back in after a reboot. With no session, Premarket 05:15, OptionsDay 07:30 and everything downstream simply ne~ | MITIGATION: Keep the owner session logged on (locked is fine) through 07:30 CT; if the machine reboots overnight, log in before 05:15 CT - StartWhenAvailable will NOT rescue an InteractiveToken task with no session. A morning glance at runtime\launch.log for today's '=== l~
- RUN-ENDER [medium / low - its one-shot trigger is spent (Next Run N/A), so it t~]: ATLAS-Rehearsal is a still-Enabled tombstone whose action bypasses the session guard (-SkipSessionCheck -UntilTime 18:45) | EVIDENCE: Task Ready with action launch_options_day.ps1 -SkipSessionCheck -UntilTime 18:45. If manually fired DURING the real session: its shadow instance exits on the live options_shadow.lock (no ledger corruption - run_options_shadow.py lines 149-159), but the rehearsal launcher then burns its 3 restarts a~ | MITIGATION: Do not run it manually. When convenient (post-close, elevated shell), disable or Unregister-ScheduledTask ATLAS-Rehearsal - it served its one-shot Sunday purpose; a disabled tombstone like ATLAS-LiveDay is the established pattern. No action taken in this prob~
- RUN-ENDER [medium / low - the task is Ready, next-run armed, and yesterday's ru~]: Today's 05:15 Premarket success is unproven in advance - a failed fire means OptionsDay opens on a stale hunt list | EVIDENCE: Last Result 0 is for 7/15's run; the probe ran at 01:29 CT, before today's fire. build_day_briefing.py fans out to cloud crews, so a transient provider failure at 05:15 leaves runtime\day_briefing.json at yesterday's content with no downstream abort - the 07:30 launcher does not check briefing fres~ | MITIGATION: After 05:20 CT (or before 07:30), check schtasks /query /tn ATLAS-Premarket last result = 0 and that runtime\day_briefing.json mtime is today; if stale, rerun the briefing manually before the open.
- RUN-ENDER [low / low - no signal of token trouble; last shadow day (7/15) ra~]: Tradier token/quote failure at 07:30 degrades today to a heartbeat-only evidence day (not silent, but a lost day of fills) | EVIDENCE: launch_options_day.ps1 preflight (lines 76-100) WARNs-and-launches on any Tradier failure: the stack boots, the shadow records no fills, and an ntfy page ('ATLAS options: Tradier token WARN', priority high) fires at 07:30. By design an observable degradation rather than an abort, but the day's fill~ | MITIGATION: Nothing to do pre-open beyond being reachable on ntfy at 07:30 CT; if the WARN page arrives, fix config\tradier_shadow.local.yaml and let the shadow's respawn/lock-reclaim pick it up, or restart via the launcher.

### probe: PART A PASS (2 HTTP requests, token never printed): TradierData loaded from config/tradier_shadow.local.yaml (production base https://api.tradier.com) via the launcher's own interpreter .venv\Scripts\python.exe. get_quotes(["SPY","QQQ","IWM"]) -> HTTP 200, all three symbols present with non-null bid/ask/last (SPY 754.62/754.69/754.81, QQQ 717.33/717.43/717.74, IWM 295.80/295.88/295.77; prevcloses~
- OK: Tradier token VALID + live: quotes 200 for SPY/QQQ/IWM with non-null bid/ask/last, SPY expirations=34 (nearest 2026-07-16); shadow config file (tradier_shadow.local.yaml) is the one picked up, matching the shadow's own fallback order (scripts/run_options_shad~
- OK: run_options_shadow.py imports NO broker/account module (imports list :43-59: clock, tradier_data, symbol_state, config_loader, fsutil, hunter.feed, options.*) - grep for atlas.execution/rh_mcp/robinhood/alpaca = zero hits; 'positions' everywhere means SHADOW ~
- OK: Entry gating is money-free: staleness (:630), RTH window (:632), event blackout (:635), lane merge (:640), max_concurrent COUNT of open shadow positions (:651), client-present (:655). Zero hits for size/qty/dollar/notional/capital/risk_of_ruin in run_options_~
- OK: atlas/options/** has zero account/balance/portfolio/robinhood reads (only English-prose hits like 'Account for upcoming volatility events' in exit_engine docstrings)
- OK: atlas/hunter/feed.py is Tradier market-data only; 'the account's 120/min' (:18) refers to the API rate cap shared across processes, not account value; every method FAILS OPEN to {}/[] (:19-20)
- OK: atlas/collect/tradier_data.py touches ONLY /v1/markets/* endpoints (quotes, timesales, expirations, chains, calendar, history) - no /v1/user or /v1/accounts anywhere
- OK: The halt gate is SYMBOL-level exchange-halt data (Nasdaq trade-halts RSS via runtime/symbol_state.json), fail-open, asserts 'halted' only on data fresh <=180s (atlas/collect/symbol_state.py:11,82-103; run_options_shadow.py:721-732) - unrelated to account stat~
- OK: Launcher needs NO Robinhood/Alpaca/llama-swap (launch_options_day.ps1:6-9 and preflight :76-100 which is quote-only WARN-and-launch, never abort on token failure); equity risk_limits.yaml (equity_min 2000 etc.) is only loaded by atlas.config_loader.load_confi~
- OK: alert_watch --options-only runs ONLY options-shadow heartbeat, data-plane-zombie, and news-flag watches (alert_watch.py:921-924); the equity analyst/halt/broker-reject/app-silent watches (the only ones that even mention cash/broker) are all gated on analyst_w~
- OK: watch_hub is stdlib-only, read-only, no alert channel (grep ntfy/notify/email/smtp = zero real hits); its subprocess use is only 'schtasks /query' for the services panel (:493); account panel data can only come from stale equity-era runtime/rh_mcp.log, and no~
- OK: the weekly auth-keepalive probe (not part of the public copy) is print-only (masked acct, prints totals, exit 0) and runs only via ATLAS-AuthKeepalive weekly Sunday 18:00 (verified live via schtasks: next run 7/19/2026 6:00 PM) - outside trading hours, independent process, output consumed by nobody; a $0 po~
- OK: No inbound command surface can stop the shadow except runtime/STOP_DAY.flag (written by stop_all.ps1) and process kill - watch_hub accepts no commands (:13), so no 'announcement' of an empty account can propagate into the stack; the launcher also deletes any ~
- OK: Scheduled-task day path is options-clean: ATLAS-OptionsDay 07:30 Ready, ATLAS-LiveDay Disabled, ATLAS-AutoEOD Disabled (matches the manual-EOD posture); Premarket/CatalystArchive/IVSnapshot/CacheRefresh/OvernightLab are separate best-effort processes whose fa~
- RUN-ENDER [high / low]: Shadow process fails to start within 3s of launch -> whole day ABORTED (exit 3) | EVIDENCE: launch_options_day.ps1:116-122 - the ONLY abort in the launcher: if the shadow process has exited 3s after spawn, it pages 'OPTIONS DAY ABORTED' and exits; nothing runs. Not account-related (would come from an import error, venv damage, or syntax error). Today's realistic import path was just prove~ | MITIGATION: Already paged via ntfy on occurrence. If it fires: read runtime/options_shadow.err.log and rerun scripts/launch_options_day.ps1. Optional hardening: a pre-07:30 smoke `.venv\Scripts\python.exe -c "import scripts.run_options_shadow"` equivalent (the launcher p~
- RUN-ENDER [medium / low]: Shadow crashes 4x during the day -> launcher gives up (evidence stops) | EVIDENCE: launch_options_day.ps1:213-225 - up to 3 respawns, then 'giving up for the day' + page. Crash cause would be a code/data bug, not account state (per-tick exceptions are contained at run_options_shadow.py:1216). | MITIGATION: Paged on occurrence; manual restart of scripts/run_options_shadow.py re-attaches to open positions via the ledger rebuild (run_options_shadow.py:454-486).
- RUN-ENDER [medium / low]: Mid-day Tradier token/feed death -> DEGRADED heartbeat-only (positions unmanaged), not a stop | EVIDENCE: build_tradier_client returns None -> entries skip via signal_no_client_skip (run_options_shadow.py:655-657) and marks stop; alert_watch data-plane watch pages 'DATA-PLANE DEAD (process alive)' (alert_watch.py:703-716). Token was live at probe time. This is the only real DEGRADE mode of the day and ~ | MITIGATION: Already covered by the schema-2 data-plane pager in --options-only mode; on page, fix config/tradier_shadow.local.yaml and restart the shadow.
- RUN-ENDER [medium / low]: Zombie shadow process holding runtime/options_shadow.lock blocks start AND all 3 respawns | EVIDENCE: run_options_shadow.py:149-160 - a LIVE pid in the lock file makes new instances exit immediately ('another options-shadow holds ... - exiting'); dead pids are reclaimed, but a hung live process from a rehearsal/manual run would burn the launcher's abort (:116) or its 3 restarts. | MITIGATION: Before 07:30, confirm no stray python running run_options_shadow (e.g. tasklist / the hub services tab); stop_all.ps1 / killing the stale pid clears it.
- RUN-ENDER [low / low]: Human misreads watch_hub's stale equity account panel ($0/'--' pills) and stops the day manually - the ONLY path by which 'no money in the portfolio' can end t~ | EVIDENCE: watch_hub.py:361-367 fills the header 'value/cash/bp' pills ONLY from stale equity-era runtime/rh_mcp.log events; in the options stack nothing writes that log, so the panel shows empty/old numbers. No code reads it back - the risk is purely a human 'no money!?' reaction to a cosmetic panel. Similar~ | MITIGATION: Know that the pills are equity-era leftovers, not shadow health (open item in memory: options tile for the hub). The shadow needs no funds - do not stop the day over any portfolio display.
- RUN-ENDER [low / low]: Post-close catch-up / late task fire -> clean no-op exit (by design, looks like 'nothing launched') | EVIDENCE: launch_options_day.ps1:59-63 - a fire after UntilTime (15:20 CT) exits 0 without launching; :64-72 - non-trading-day exits 0 (session probe is fail-open on error). 2026-07-16 is a normal Wednesday session, so neither applies to an on-time 07:30 fire. | MITIGATION: None needed; if the 07:30 fire is missed entirely (machine asleep), StartWhenAvailable catch-up launches late - check runtime/launch.log.

## SUBSYSTEM AUDITS
### math-core - 9 findings
- [high] MATH-CORE-1 p_regain 0DTE double-decays the clock: zero_dte_effective_T is fed the horizon-END model time, violating its own derivation [VERIFY-PENDING (refuters died on limit)]
    CLAIM: math.py:299 passes max(T_now - dt_h, MIN_T) (the model time AT the sell clock) into zero_dte_effective_T, whose derivation (math.py:264-269) defines T_eff(end) = T_now * exp(-2I) from the CURRENT time value - so the interval's decay is charged twice (once via the model clock subtraction, once via the empirical exp(-2I)). The docstring's own promised behavior ('T_eff may exceed the model's own T(end) in the MORNING')~
    EVIDENCE: atlas/options/math.py:299 (call site) vs atlas/options/math.py:264-269 (derivation); exit_engine.py:237 asserts the ramp 'REPLACES the model clock' - as called it stacks on it. Consumer: rule i* at exit_engine.py:277-279, 396-398.
    REPRO: 0DTE SPY 630, K=632C, iv=0.15, 13:00, sell clock 15:00: T_now=180min, model T(end)=60min, I=0.1793 => coded t_minus=41.9min vs docstring-intent 125.8min. Resulting regain barrier move 0.00329 vs 0.00117; p_regain = 0.530 coded vs 0.824 intent (0.595 with the plain model clock). At 10:00: coded 35.4min vs model 60min - 'exceeds T(end) in ~
    FIX: One line: pass T_now (the current remaining life), not T_now - dt_h, at math.py:299 - i.e. t_minus = zero_dte_effective_T(T_now, minute, sell_clock_min). Then re-examine p_regain_min=0.25 on replay, because every historical p_regain was computed under the double decay.
- [high] MATH-CORE-2 p_regain systematically understates true regain probability 2-3x and its 'conservative-toward-holding' label is backwards [VERIFY-PENDING (refuters died on limit)]
    CLAIM: The end-solved barrier makes p_regain a strict LOWER bound of P(contract value >= c_ref at some point in the horizon): paths that regain c_ref early at spots BELOW the end-barrier are not counted. The docstring (math.py:286-288) calls this 'the conservative-toward-holding bound', but rule i* SELLS when p < p_regain_min=0.25, so a lower bound is conservative toward SELLING - the bias direction is exactly the owner's ~
    EVIDENCE: atlas/options/math.py:285-288 (direction claim), 306-311 (end-barrier + touch construction); consumer exit_engine.py:396-398 (sell if p < 0.25).
    REPRO: Brownian-bridge MC of the TRUE event (value >= c_ref at any mark, model clock, mu=0), 0DTE 13:00-14:30 grid, S=630 iv=0.15: shipped p_regain 0.313-0.619 vs TRUE 0.818-0.912 (e.g. 13:00 K=632: shipped 0.530, true 0.913; 14:00 K=635: shipped 0.330, true 0.850). With mild fade mu_eff=-2.0, K=633 13:30: shipped 0.328 vs true 0.853 - one more~
    FIX: Fix MATH-CORE-1 first, correct the docstring's direction claim, and either (a) price the time-varying barrier (solve regain_move at each mark step - cheap, ~90 bisection calls) or (b) keep the bound but recalibrate p_regain_min on replayed TRUE-regain frequencies, never on the biased shipped values.
- [medium] MATH-CORE-3 zero_dte_decay_rate ramp is not a valid time-value decay model: it leaves 66% of TV alive at expiry close and is SLOWER than BSM after 14:30
    CLAIM: The hardcoded sigmoid (2%/hr -> 16%/hr ceiling) integrated over the full session gives I=0.416, i.e. exp(-I)=66% of opening time value surviving to 16:00 on expiry day (truth: 0%), and after ~14:30 its rate is BELOW BSM's own instantaneous ATM decay rate - contradicting the documented purpose that 0DTE decay 'collapses after 14:00' faster than the model. Any use of the ramp near the close is extrapolating a curve th~
    EVIDENCE: atlas/options/math.py:334-339 (ramp), 261-276 (effective clock built on it); docstring math.py:267-269 states the intended afternoon-collapse behavior.
    REPRO: I(9:30->16:00) = sum(rate(m))/60 = 0.4157 => exp(-0.4157) = 0.660 TV remaining at close. Rate comparison (fraction of remaining TV per hour): 15:00 empirical 0.131 vs BSM-own 1/(2*T_hours)=0.500; 15:30 empirical 0.143 vs BSM 1.000 - the 'faster than BSM' regime never occurs after ~14:30.
    FIX: Treat the ramp as valid only for mid-day windows ending <= 15:00 (the sell clock currently enforces this by accident - make it explicit), and prioritize the planned refit from stored 0DTE quote paths (plan section O5) before any calibration that consumes p_regain.
- [medium] MATH-CORE-4 theta_share books theta as pure loss and omits the gamma earn-back - overstates 'decay dominance' ~50x in EV terms for 0DTE near-ATM
    CLAIM: theta_share's decay side is |theta*dt| with no gamma credit, but for short-dated near-ATM options theta is almost exactly the price of gamma: the true expected cost of holding is theta + 0.5*gamma*S^2*sigma^2*dt, near zero. The metric therefore labels maximally-convex positions 'decay dominated' and hands rule g a pro-exit bias that fights ev_hold (rule h), which prices the same tradeoff correctly one line later in ~
    EVIDENCE: atlas/options/math.py:316-323 (delta-only offset); consumers exit_engine.py:250-252 (breach counter) and :381 (rule g), :303 (vehicle_mismatch flag).
    REPRO: 0DTE 13:00, S=630 iv=0.15, K=632C (mid 0.8235): theta side over 1h = -0.2446, gamma earn-back E[0.5*G*dS^2] = +0.2397, true EV of holding 1h (ev_hold, mu=0) = -0.0049 - 50x smaller than the 'decay' theta_share scores; theta_share reads 0.420. K=636 (delta 0.072): share 0.549 > 0.50 trigger on the most convex position on the board.
    FIX: Add the gamma term to the decay side (decay = max(0, |theta*dt| - 0.5*gamma*S^2*(iv/16)^2*dt_days), same units) or demote theta_share to a logged diagnostic and let rule h's EV comparison own the decision - it already does this correctly.
- [medium] MATH-CORE-5 Selector still applies the 0DTE theta multiplier the exit engine removed as double-counting, and omits the 365/252 rescale - entry and exit ledgers l~
    CLAIM: selector.py:250 computes theta_day = g.theta * zero_dte_theta_multiplier(minute) for 0DTE picks and never applies the *365/252 trading-day rescale, while exit_engine.py:239 applies *365/252 and explicitly removed the multiplier as double-counting (its rate ratio is also dimensionally incoherent: it scales model theta by empirical-rate/mean-empirical-rate although model theta's own rate already exceeds the empirical ~
    EVIDENCE: atlas/options/selector.py:250-254 (multiplier + no rescale, theta_paid decomposition) vs atlas/options/exit_engine.py:233-239 (365/252, multiplier REMOVED as double-counting); atlas/options/math.py:342-347 (multiplier definition).
    REPRO: 15:00 0DTE: multiplier=2.12 while the ramp's own rate (0.131/hr) is 3.8x BELOW BSM's own-rate (0.500/hr) - the scaled theta asserts ~8x the decay the ramp itself claims. Convention gap vs exit engine: 0DTE 15:00 entry logs theta 2.12x raw vs exit's 1.449x (46% overstatement); 10:00 entry logs 0.34x vs 1.449x (4.3x understatement); non-0D~
    FIX: In selector.py:250 drop the multiplier and use the same g.theta * (365/252) as exit_engine.py:239, so pick decompositions and exit states share one convention.
- [medium] MATH-CORE-6 vendor_iv fallback plugs calendar-annualized IV into trading-time math: +/-20-31% effective vol error on that path
    CLAIM: math.py's convention is T in trading years with IV solved under the same clock (math.py:5-6; selector.py:201 does this). When the solver returns None the selector falls back to cq.vendor_iv (selector.py:202-203), which vendors annualize on calendar time - plugged into trading-T formulas it is scale-wrong: sigma_trading = sigma_cal * sqrt(T_cal/T_trading). All greeks, EV, p_touch and p_profit computed for that pick i~
    EVIDENCE: atlas/options/selector.py:201-203 (fallback); atlas/options/math.py:5-10 (trading-time convention).
    REPRO: Vendor 20% calendar IV on a weekday 2-day option: correct trading-convention sigma = 0.20*sqrt((2/365)/(2/252)) = 16.6% -> plugging 20.0% is a +20% vol overstatement (pro-entry: fatter p_touch/p_profit). Friday->Monday expiry: correct = 0.20*sqrt((3/365)/(1/252)) = 28.8% -> plugging 20.0% is a -31% understatement.
    FIX: Rescale at the seam: iv = vendor_iv * sqrt(T_calendar/T_trading) using the contract's calendar DTE, or reject the candidate when the solver fails instead of falling back (the quote is suspect anyway - solver-None usually means crossed/stale).
- [low] MATH-CORE-7 trading_T counts a phantom partial session when called on a weekend timestamp
    CLAIM: trading_T computes frac_today from close_minute minus wall-clock minutes with no weekday check on now_et, so a Saturday/Sunday call books up to a full phantom trading day. Live weekday operation is unaffected; weekend replays, ad-hoc audits, or grading tooling get pro-hold time and understated theta.
    EVIDENCE: atlas/options/math.py:46-47 (no weekday guard on now_et); weekday paths verified correct (premarket capped at 1.0 day, after-hours 0, Saturday expiry contributes 0 days).
    REPRO: trading_T(Sat 2026-07-18 12:00, expiry Mon 2026-07-20) = 1.615 trading days vs correct 1.0 (+61%). Also seconds are truncated: 15:59:59 same-day returns 1.00 min (max ~1-min overstatement, floor-adjacent).
    FIX: Set frac_today = 0 when now_et.weekday() >= 5 (and optionally on holidays via the same calendar the launcher uses).
- [low] MATH-CORE-8 prob_itm is dead code with a measure mismatch waiting to be wired in
    CLAIM: prob_itm (risk-neutral N(d2)) has no caller anywhere in atlas/options; the platform's live P(profit) is ev_hold's physical-measure p_profit. If a future consumer uses it as 'P(profit)' per its docstring, it silently swaps drift mu for r-q, inconsistent with every other probability in the system.
    EVIDENCE: atlas/options/vendor/blackscholes.py:198-215; repo grep shows only the definition (no call sites in atlas/options).
    REPRO: For SPY 630/632C iv 0.12 T=2/252: risk-neutral N(d2) vs physical mu=0 P(S_T>K) differ by the (r-q)T drift ~ 630*0.04*0.0079 = $0.20 of forward drift - small here, material at longer T or in rate regimes.
    FIX: Delete from the vendored file or add a header comment forbidding its use for physical probabilities.
- [low] MATH-CORE-9 implied_vol faithfully solves penny-quote far-OTM contracts to extreme IVs - a red-herring feeder, not a solver bug
    CLAIM: The solver is correct (round-trips exact, None on bound violations, bisection fallback machine-precise), but on quote-granularity inputs it returns the mathematically correct yet economically meaningless root: minimum-tick premiums on near-expiry far-OTM contracts solve to 65-97% IV, which then inflates greeks, the selector's p_touch sigma (max(iv, hv20)) and EV decomposition for those candidates.
    EVIDENCE: atlas/options/vendor/blackscholes.py:133-195 (solver, verified); atlas/options/selector.py:201, 225 (consumption).
    REPRO: $0.01 quote, S=630 K=660, 30 min to expiry: solved iv = 0.970. $0.05 quote K=645 same expiry: iv = 0.649. ATM control: price 1.88619 round-trips to 0.1200000000.
    FIX: Keep the existing premium/OI/spread gates AHEAD of the solve (they are today), and add a solved-IV sanity cap relative to hv20/iv-surface (e.g. flag iv > 3x hv20) so penny artifacts can't win scoring ties.

### trajectory - 10 findings
- [critical] TRAJECTORY-1 Exit rules h/i* are a noise clock: 20-min annualized drift noise (SE ~ 14) vs a ~0.016 decision threshold guarantees a forced sale within ~30-50 minu~ [votes: UPHELD(blocks-goal), UPHELD(blocks-goal)]
    CLAIM: mu_hat's sampling SE is iv/sqrt(T_K) = 70.1*iv (= 14.02 annualized at iv=0.20), while rule h flips to SELL when mu_eff < ~+0.016 for a representative ATM index call. With the lane prior mu_thesis=1.0 held valid, h fires whenever the trailing 20-min t-stat < -0.41, i.e. any -0.118% 20-min drift on SPY - probability 0.34 PER MARK under zero true drift. The engine cannot hold any position through routine noise, making ~
    EVIDENCE: atlas/options/trajectory.py:112-116 (mu_hat = r/T_K with T_K = span/98280, no variance accounting); exit_engine.py:242-243 (mu_eff), 385-386 (rule h: ev_h < ev_sell), 263-271 (ev_hold on mu_eff over dt_T=1/252); math.py:153-201 (ev_hold: ev ~ S*delta*(mu-r)*dt, so flip at mu ~ r - half_spread/(S*delta*dt)); marks every 300s (runner) re-r~
    REPRO: Repro (repo's own ev_hold, .venv python): SE = 0.20/sqrt(20/98280) = 14.02. ATM SPY call S=K=600, iv=0.20, 5 DTE, 6c spread: BS mid 6.981, ev_sell = -0.030, bisected mu_flip = +0.0156. Solving (14.02*t^3 + 1.0)/(1+t^2) = 0.0156 gives t_flip = -0.412 = -0.118% 20-min move; P(t < t_flip | H0) = 0.340. Monte Carlo, 20,000 zero-drift days, m~
    FIX: Scale the blend by the estimator's known variance (w *= tau^2/(tau^2+SE^2) with tau ~ the lane-thesis scale - SE is closed-form from iv and span), or equivalently cap |w*mu_hat| at a few multiples of mu_thesis; and require a persistent breach (2-cycle debounce, as rule g already does) before h/i* m~
    VOTE UPHELD (blocks-goal): Independent re-derivation with the repo's own code confirms every number: mu_flip = +0.01566 (repo ev_hold bisection; analytic r − hs/(S·Δ·dt) = +0.01569), SE = iv/√T_K = 14.02, t_flip = −0.412 → per-mark fire probability 0.340 under zero drift, and the 0.20–0.37 band across mu_thesis 0.5–8.2. Mont~
    VOTE UPHELD (blocks-goal): Confirmed, not refuted - reproduced end-to-end with the repo's own code. Bisecting the repo's ev_hold gives mu_flip=0.01565 (claim 0.0156); t_flip=-0.412; per-mark h-fire probability under zero drift = 0.338 driving decide_exit itself (claim 0.340). Monte Carlo through decide_exit (thesis_valid pin~
- [high] TRAJECTORY-2 w = t^2/(1+t^2) is a scale-free gate that overweights a +/-1400%-annualized-noise estimate ~20-40x relative to any calibrated Bayes weight [votes: REFUTED(degrades-edge)]
    CLAIM: The shrinkage weight depends only on significance, never on the ratio of observation noise (SE=14) to plausible drift scale (O(1)): under pure noise E[w]=0.344 and w>0.5 on 31.7% of marks, injecting std(w*mu_hat)=9.6 annualized of noise into mu_eff, where a conjugate posterior with even a generous prior width tau=2 would give w=tau^2/(tau^2+SE^2)=0.020. The blend implicitly asserts that 1400%-annualized drifts are a~
    EVIDENCE: atlas/options/trajectory.py:22-39 and 74 (formula + the label-correction note conceding it is a calibration heuristic); docs/OPTIONS_SYSTEM_FLOW.md:571-582 (registered family). The module's own docs ('1 sigma of observed drift -> 50/50') confirm the design intent that half weight goes to a point estimate whose 1-sigma sampling error is 1~
    REPRO: 400,000-draw simulation, t~N(0,1): E[w]=0.344, P(w>0.5)=0.317, std(w*mu_hat)=9.57 annualized at iv=0.20. Calibrated comparison: tau=2 -> w*=4/(4+196.6)=0.020; tau=1 -> 0.0051. Live w at t=1 is 0.500 = 25-98x the calibrated weight. Measured blend outputs with prior +1.0: 20-min move -0.25% -> mu_eff=-4.76; -0.35% -> -9.90; +0.50% -> +18.7.
    FIX: Multiply the evidence gate by the scale factor tau^2/(tau^2+SE^2) (one line: SE=iv/sqrt(T_K) is already computed as s/T_K), register tau against the lane mu_thesis distribution, and add the scaled weight as the new live formula with the old one as the replay column.
    VOTE REFUTED (degrades-edge): Arithmetic sub-results reproduce (E[w]=0.344, P(w>0.5)=0.317, std(w*mu_hat)=9.57, blend outputs match), but the claim's load-bearing premises are false. (1) The 'plausible drift scale O(1)' / tau=1-2 baseline contradicts the system's own lane construction: mu_thesis = ln(1+target)/horizon_T with in~
- [high] TRAJECTORY-3 Endpoint-only estimator is maximally microstructure-sensitive: one 10bp stale print at a window edge moves t by 0.35 sigma - alone comparable to the ~ [VERIFY-PENDING (refuters died on limit)]
    CLAIM: mu_hat uses exactly two closes (w_closes[0], w_closes[-1]) built from ~6 Tradier last-trade polls/min, so a single stale/bounced print at either endpoint shifts t by e/(iv*sqrt(T_K)): 10bp -> 0.35 sigma at iv=0.20, vs the h-fire threshold of 0.41 sigma - one print can very nearly fire h on its own, and on InPlayORB names (price >= $5, where 1-2 ticks = 20-40bp) a single bid-ask bounce can exceed the threshold outrig~
    EVIDENCE: atlas/options/trajectory.py:106-116 (c0,c1 endpoints only; r = ln(c1/c0)); atlas/hunter/feed.py:160-193 (bar close = last poll-tick of the minute, ~10s cadence); docs/OPTIONS_SYSTEM_FLOW.md:557-562 documents the same endpoint formula (the FOCUS brief's 'OLS' is wrong - OLS exists only for the IV trend, run_options_shadow.py:1051).
    REPRO: delta_t = 0.0010/(0.20*0.014264) = 0.350 sigma per 10bp endpoint error (vs |t_flip| = 0.412). On a $5 name at iv=0.40: one 1c tick = 20bp -> delta_t = 0.35 sigma; two opposite-endpoint bounce ticks -> 0.70 sigma, > any realistic threshold.
    FIX: Use robust endpoints (median of the first/last 3 window closes) or the full-window regression estimate, and/or subtract an endpoint-noise term from t's denominator (sqrt(iv^2*T_K + 2*sigma_tick^2) with sigma_tick from the symbol's spread) so thin names don't fire on bounce.
- [high] TRAJECTORY-4 Optional-stopping cadence dependence: the first-match ladder re-samples the noisy t every mark, so the probability of a noise exit compounds with pol~ [VERIFY-PENDING (refuters died on limit)]
    CLAIM: decide_exit is memoryless per mark (except theta breaches): each mark draws a fresh, heavily-overlapping t (5-min shift shares 15/20 of the window; 60s marks share 19/20) and sells on first crossing. The exit distribution therefore depends on the MARK CADENCE, not just the market path - a pure decision rule should be cadence-invariant. The runner escalates cadence to 120s (0DTE afternoon) and 60s (last 30 min) exact~
    EVIDENCE: exit_engine.py:385-398 (h and i* fire on a single mark's mu_eff/p_regain with no persistence requirement) vs exit_engine.py:381 (g requires breaches >= theta_share_cycles=2 - the debounce pattern exists in the same function); mark cadence 300/120/60s per the runner's reval scheduling; trajectory window recomputed per mark from run_option~
    REPRO: Monte Carlo at 5-min cadence: median 30 min to h-fire under zero drift. Each extra draw is a fresh chance at P~0.34 (correlation 0.75 between adjacent draws); at 60s cadence the same 20-min window is sampled 20x per refresh, so the running max of |t| over a window's life crosses any sub-1-sigma threshold with near-certainty - the last-30~
    FIX: Add the same 2-consecutive-cycle debounce h/i* that g already has (or require the breach to persist across one full non-overlapping window, i.e. 20 min), making the decision approximately cadence-invariant.
- [medium] TRAJECTORY-5 t_stat's sigma is the contract's own solved IV (skew + VRP + 45-min stale carry), not the underlying's vol - evidence is deflated precisely during ad~
    CLAIM: s_K = iv*sqrt(T_K) uses the position's solved IV as the underlying's physical diffusion sigma. OTM-put skew and event IV blowouts inflate iv 1.5-2x over realized, halving t and collapsing w exactly when live adverse evidence should be weighted most - the engine then holds the loser on the full mu_thesis prior (h needs mu_eff < ~0.016 to fire, and (1-w)*mu_thesis alone keeps it above that). The stale-IV carry (failed~
    EVIDENCE: atlas/options/trajectory.py:115-116 (s = iv*sqrt(T_K)); run_options_shadow.py:1059-1061 (iv passed is the option's per-mark solved/carried IV); math.py:11-17 documents the IV-as-physical-sigma model risk for the EVs but the same choice inside the EVIDENCE WEIGHT is undocumented and cuts the opposite way (suppression, not pro-hold bias).
    REPRO: OTM put solving at iv=0.35 on an underlying realizing 0.18: a true 2-sigma-realized adverse move (r_K = 2*0.18*0.014264 = 0.51%) reads t = 0.0051/(0.35*0.014264) = 1.03 -> w = 0.51, instead of t = 2.0 -> w = 0.80; the live drift estimate's weight is cut by 36% by the option's own skew at the exact moment the tape moves against the thesis.
    FIX: Compute s_K from the underlying's vol (ATM IV from the existing iv_surface, or realized vol from the intraday cache the noise profiles already read), keeping the contract's solved IV only for pricing/EV.
- [medium] TRAJECTORY-6 Prior incoherence entry vs exit: selector prices the thesis as a 50% mixture, exit engine's prior is the FULL mu_thesis while valid - the exit holds ~
    CLAIM: Entry EV uses ev_hold_thesis with p_thesis=0.5 (a 50/50 mixture of target-drift and mu=0), but decide_exit sets mu_prior = pv.mu_thesis (weight 1.0) while the thesis holds. At t~0 the exit engine therefore evaluates EV_hold under a drift 2x the belief that passed the entry gate. This inflated prior is currently the only brake on the TRAJECTORY-1 noise clock (it moves t_flip from -0.33 to -0.41), i.e., one error is p~
    EVIDENCE: atlas/options/selector.py:218-220 (ev_hold_thesis(..., p_thesis=p_thesis)) with p_thesis=0.5 defaults in every lane (atlas/options/lanes.py:221, 293, 370, 479); exit_engine.py:242 (mu_prior = pv.mu_thesis if pv.thesis_valid else 0.0); math.py:204-215 (the mixture the entry side uses).
    REPRO: With the entry-consistent prior 0.5*mu_thesis = 0.5: t_flip solves (14.02t^3+0.5)/(1+t^2) = 0.0156 -> t = -0.326, P(fire/mark) = 0.372 (vs 0.340 at prior 1.0) - the exit ladder's noise survival time is materially set by an unregistered doubling of the thesis belief.
    FIX: Set mu_prior = p_thesis*mu_thesis (entry-consistent measure) as part of the same change that rescales the blend weight (TRAJECTORY-2) - shipping either alone shifts the noise-exit rate in an unintended direction.
- [medium] TRAJECTORY-7 mu_eff saturates p_regain/p_touch on routine moves, creating a buy-high-sell-low oscillator and structurally shadowing rule i*'s designed region
    CLAIM: A +/-0.35% 20-min move swings mu_eff to +/-10, driving p_regain across nearly its full range (0.24 to 0.98) and p_touch(+0.5%/1d) from 0.28 to 0.97: favorable wiggles block ALL EV exits (h, i*, g's p_tgt_eff) until the wiggle mean-reverts, whereupon the symmetric negative fire dumps the position - systematic sell-into-pullback behavior. Additionally, because h (fires at mu_eff < ~+0.016) always precedes i* (p_regain~
    EVIDENCE: exit_engine.py:385-398 (h before i*, both on mu_eff); math.py:279-312 (p_regain: p_touch under mu at dt_h; dt_h -> 0 near sell_clock forces p -> 0 for m > 0); math.py:73-89 (p_touch drift term nu*T with mu_eff ~ +/-10 vs daily sigma 0.0126 = +/-3+ sigma/day of assumed drift).
    REPRO: Repo p_regain, 0DTE ATM call at 13:00 (sell clock 15:00, regain move +0.28%): mu_eff -10 -> p=0.236 (< 0.25, fires i*), -5 -> 0.435, 0 -> 0.685, +5 -> 0.884, +10 -> 0.975. mu_eff = -9.9 is produced by a mere -0.35% 20-min move with prior +1.0 (P(|t|>=1.22) = 0.22 per window under noise). p_touch(+0.5% in 1 day): 0.276/0.690/0.965 at mu =~
    FIX: Fixing TRAJECTORY-1/2 (scaled blend) removes the saturation; separately, re-sweep p_regain_min AFTER the rescale, and consider evaluating i* before h or on a mu-conditioned branch so the skew rule has a live region that h does not pre-empt.
- [medium] TRAJECTORY-8 Frozen/halted or unpolled tape reads as 'no evidence' -> full-thesis HOLD: h cannot fire and g is blocked, with only a journal entry as defense
    CLAIM: A halted underlying keeps returning the same last price, so committed bars go flat, r_K -> 0, t -> 0, w -> 0, and mu_eff = mu_thesis - the engine treats a halt as 'thesis fully intact': h cannot fire (mu_eff = 1.0+ >> 0.016 flip) and p_tgt_eff under full thesis drift stays above g's 0.30 floor. If polling stops entirely, the window anchors at mins[-1] (not wall clock) and serves the LAST drift as current with no sta~
    EVIDENCE: atlas/options/trajectory.py:93-101 (window anchored at last bar's minute; no 'now' input, so no staleness tie); run_options_shadow.py:1015-1025 (possible_halt journal-only); exit_engine.py:381, 385 (g and h both blocked by a full-thesis mu_eff); the symbol_state HALT GATE exists on the ENTRY side (memory: poll_symbol_state) but nothing f~
    REPRO: Flat window: r=0 -> t=0 -> w=0 -> mu_eff = mu_thesis = 1.0 -> ev_hold ~ S*delta*(1.0-0.04)/252 = +2.29/contract-share for the ATM SPY example, vastly above ev_sell=-0.03: unconditional HOLD for as long as the tape stays frozen.
    FIX: Pass the current minute into underlying_state and return no_view when mins[-1] is >~3 min stale; feed the possible_halt/symbol_state flag into thesis_valid (or force mu_prior=0) so a halt degrades to the conservative measure instead of the most bullish one.
- [low] TRAJECTORY-9 er/n_bars/window_min computed but never logged (docstring claims otherwise); defense placeholders threaded as live; the registered mu-window sweep is~
    CLAIM: UnderlyingState's docstring says 'all fields logged per mark', but the runner's ext snapshot carries only mu_hat/mu_t_stat (+the permanently-default defense fields) - er, n_bars, window_min are dropped, so the one cheap trend-quality covariate dies at the boundary and window health can never be audited from ledgers. opposing_defense/defense_zone_score are hardcoded False/0.0 (structure.py never landed) yet flow runn~
    EVIDENCE: atlas/options/trajectory.py:56 ('all fields logged per mark') and 62-66 (placeholders); run_options_shadow.py:1099-1115 (ext: no er/n_bars/window_min); runtime/backtest_out/sweep_ledger.jsonl line 59 (mu-window registration text); atlas/options/replay.py:135-141 (replay consumes stored mu_hat - correct, but window-variant replay is impos~
    REPRO: n/a (absence of data); verifiable by inspecting any runtime/options_shadow_quotes/*.jsonl ext keys against UnderlyingState's field list.
    FIX: Add er, n_bars, window_min to the ext dict (3 keys, schema-compatible since readers are tolerant), and either persist per-mark trailing closes or amend the registration to name the intraday cache join as the sweep's data source.
- [low] TRAJECTORY-10 Convention slip: mu_hat estimates the LOG drift (mu - sigma^2/2) but every consumer treats it as the PRICE drift mu, double-subtracting sigma^2/2
    CLAIM: r_K/T_K is an estimator of E[ln(S_T/S)]/T = mu_price - sigma^2/2, but p_touch and ev_hold document and use their mu argument as the PRICE drift and internally subtract sigma^2/2 again (nu = mu - sigma^2/2; drift = (mu - 0.5*sigma^2)*dt). The unbiased price-drift estimator is r_K/T_K + sigma^2/2; the omission is a systematic -sigma^2/2 bias in mu_eff's live component.
    EVIDENCE: atlas/options/trajectory.py:112-114 (mu_hat = r/t_years, no +sigma^2/2); math.py:73-77 ('mu is the PRICE drift (ln S drifts at mu - sigma^2/2)') and math.py:176 (drift = (mu - 0.5*sigma*sigma)*dt); exit_engine.py:243 passes mu_hat-blended mu_eff straight into both.
    REPRO: Bias = -0.5*iv^2 = -0.020 annualized at iv=0.20 (-0.045 at 0.30) - about the same size as the entire rule-h flip threshold (+0.016) but three orders of magnitude below the estimator's SE (14.02), so it is decision-irrelevant until the variance problems (TRAJECTORY-1/2) are fixed, after which it becomes a visible systematic pro-sell tilt ~
    FIX: Return mu_hat = r/T_K + 0.5*iv^2 (or explicitly document mu_hat as log-drift and add sigma^2/2 at the consumers) in the same change-set as the blend rescale.

### lanes - 14 findings
- [critical] LANES-1 Zero-hysteresis, single-tick, growing-barrier invalidation makes rule (b) a near-guaranteed minutes-scale exit [votes: UPHELD(degrades-edge), REFUTED(none)]
    CLAIM: IndexTrendLane.invalidated (and InPlayORB's analogue) declares the thesis dead on ONE polled quote either recrossing VWAP or re-entering the noise band, while the entry -- by construction the FIRST boundary close just beyond that same band -- sits ~one 5-min-close overshoot (~9 bps for IWM) from the barrier, and the band itself grows ~sqrt(t) toward the position; most entries are therefore cut within minutes at roun~
    EVIDENCE: lanes.py:264-278 (VWAP recross OR band re-entry, no buffer, no persistence); lanes.py:242-252 (entry fires at the first crossing of the SAME band, so entry-to-barrier distance ~= the conditional overshoot); lanes.py:126 (noise_by_minute = avg |close(m)/open-1|, monotonically widening ~sqrt(t)); run_options_shadow.py:871-887 (PositionCtx ~
    REPRO: IWM sigma_day ~1.0% -> sigma_5min = 1.0% x sqrt(5/390) = 11.3 bps; conditional overshoot of the first crossing close ~0.8 x sigma_5min ~= 9 bps = entry-to-barrier distance. sigma_1min = 5.1 bps -> P(touch barrier within 5 min) = 2*Phi(-9/(5.1*sqrt(5))) = 2*Phi(-0.79) ~= 0.43; within 30 min = 2*Phi(-0.32) ~= 0.75 -- before OR-ing in the V~
    FIX: Add hysteresis and persistence: invalidate only when the mark is inside 0.8 x noise (or beyond VWAP by a noise-scaled buffer) on 2 consecutive revals or on a completed-bar close; freeze the invalidation band at its entry-time width instead of letting the widening average absorb a stalling position.
    VOTE UPHELD (degrades-edge): Real-data replay of the actual production code confirms the numeric repro almost exactly. Running build_noise_profile + IndexTrendLane.update/.invalidated over 529 real cached IWM sessions (plus SPY/QQQ) with the live reval cadence (300s, latest-tick mark): 41% of lane-1 entries are rule-(b) cut at~
    VOTE REFUTED (none): Mechanics confirmed but the defect claim is refuted on materiality, with the finding's own remedy disproven empirically. Replaying the REAL lane code over the real 1-min cache reproduces the finding's numbers (IWM 41% cut at first 300s reval, 76% within 30 min, median overshoot 8.6bps) - yet a pair~
- [high] LANES-2 One-per-side daily latch burns at EMISSION, so selector rejections (the observed death mode) permanently disarm the lane for the day [votes: REFUTED(cosmetic), REFUTED(degrades-edge)]
    CLAIM: IndexTrendLane and InPlayORBLane add to _fired BEFORE returning the signal, and the runner processes each signal exactly once with no retry inside the 10-min TTL; since candidates die overwhelmingly at contract selection (no_quote/spread/OI), a noise-scale fire at the open that gets rejected consumes the day's only latch and the genuine mid-day break can never fire -- a direct mechanical cause of the 0.14 fires/day ~
    EVIDENCE: lanes.py:249-252 and lanes.py:423-426 (self._fired.add(key) precedes the LaneSignal return; nothing ever removes entries); run_options_shadow.py:625-747 (each signal handled once; no_pick at line 745 journals and drops it; expires_minute checked only at line 628 to kill stale backfill signals -- there is no retry loop); doc header run_op~
    REPRO: P(band exceedance per boundary under noise) = P(|Z|>sqrt(2/pi)) = 0.425 (see LANES-5), so the first latch-burning fire clusters at the open when option spreads are widest -- precisely when selector rejection probability peaks; one rejection then suppresses ALL later fires that side for the remaining ~380 minutes.
    FIX: Burn the latch on FILL (shadow entry recorded), not on emission: have the runner call back lane.release(symbol, direction) on no_pick, or keep the signal alive and re-run selection each poll until expires_minute; combined with LANES-5's weak band this converts wasted open-noise fires into retryable~
    VOTE REFUTED (cosmetic): Code-reading cites are accurate (lanes.py:249-252/423-426 latch before return, no release, runner drops no_pick at run_options_shadow.py:745), but the claim's impact story fails on every testable point. (1) Causality inverted: fires/day counts emissions; the latch only suppresses a SECOND same-side~
    VOTE REFUTED (degrades-edge): Code mechanics confirmed (latch burns pre-selection at lanes.py:252/426; runner never retries), but the claim fails on reachability/materiality against the shadow's full recorded life. (1) The headline scenario never happened: the only open-clustered index fire (07-13 09:35 IWM call no_pick) was fo~
- [high] LANES-3 range_percentile_14d gate is scale-invariant: it deletes a fixed ~43% of trading days and cannot detect a vol regime by construction [votes: REFUTED(cosmetic), REFUTED(cosmetic)]
    CLAIM: The 'dead-vol regime' stand-down ranks YESTERDAY'S range within its own trailing 14 days, so under any stationary regime -- high vol or low -- it stands lane 1 down on a fixed ~6/14 of days; it is a coin weighted 57/43 on 'was yesterday relatively quiet within the fortnight', not a regime detector, and it is the single largest structural starvation multiplier on the only lane that has ever traded.
    EVIDENCE: lanes.py:202-203 (pct = 100 x count(r <= latest)/n, latest self-included) with lanes.py:235-236 (< 50 stands down); the interpretation note at lanes.py:29-31 claims it distinguishes a 'dead-vol regime', which a within-window rank mathematically cannot do (it is invariant to multiplying all 14 ranges by any constant).
    REPRO: Ranks are uniform on {1..14}/14 under exchangeability: pass requires count>=7 -> ranks 7..14 -> 8/14 = 57.1% pass, 42.9% stand-down -- independent of the actual volatility level. At the n_days=5 minimum: pct in {20,40,60,80,100} -> 40% stand-down. Multiply by 3 sessions observed: expected ~1.3 of 3 SPY/QQQ/IWM lane-days deleted before an~
    FIX: Gate on a vol LEVEL, not a rank: e.g. 5-day mean realized range vs a long-run (90d+) baseline, or ATR percentile vs a 6-month window; if day-freshness of vol matters, use the median of the last 3 days rather than the single latest day (a rank of one observation has maximal estimator variance).
    VOTE REFUTED (cosmetic): The arithmetic reproduces exactly (6/14=42.9% stand-down under exchangeability; 40% at n=5; scale invariance verified through build_noise_profile itself), and the gate is live at range_percentile_min=50 with empirical stand-down 45-46% over ~500 real cache sessions per symbol. But the finding's cor~
    VOTE REFUTED (cosmetic): The arithmetic (fixed ~43% marginal stand-down under exchangeability; empirically 44.6-46.4% over ~500 real sessions/symbol) is correct, but the thesis fails twice. (1) 'Cannot detect a vol regime by construction' is empirically false on the exact cache the runner reads: rolling replay of the gate ~
- [high] LANES-4 Last30's 15%-of-move target cannot arithmetically clear the selector's EV floors at p_thesis=0.5 except on ~2-sigma days [votes: UPHELD(degrades-edge), UPHELD(degrades-edge)]
    CLAIM: The lane triggers on ~40% of days (|close-to-15:30 ret| > 0.5 x avg range is a weak condition) but sizes target_move at 0.15 x |ret| (floor 10 bps) over 25 minutes, which under the 0.5 thesis mixture yields ev_pct far below the selector's 10% floor and EV$ below 2x spread on typical qualifying days -- the lane is a designed-in no_pick generator, i.e. pure starvation plus funnel noise.
    EVIDENCE: lanes.py:313 (trigger 0.5 x avg_daily_range on a close-to-close ret vs a high-low range baseline), lanes.py:318 (target = max(0.15|ret|, 0.001)), lanes.py:316 (25-min horizon); selector.py:75-77 (ev_pct_min=10, ev_vs_spread_mult=2.0) and selector.py:218-231 (mixture EV then floors); math.py:204-215 (p x drift-branch + (1-p) x mu0-branch).
    REPRO: SPY: trigger passes at |ret|=0.5% (avg range 0.8%). Target = max(0.15x0.5%, 10bps) = 10 bps = $0.63 on S=630. 1DTE ATM call: delta 0.6, mid $2.10, ask $2.15, spread $0.10. Thesis branch ~= 0.6x0.63 - theta(25/390 day on ~1.06d life: 2.10x0.5x0.064/1.06 ~= $0.06) = +$0.31; mu0 branch ~= -$0.05; mixture = +$0.13; minus exit half-spread $0.~
    FIX: Back-solve the trigger from the floor: only fire when 0.15 x |ret| >= the target that clears ev_pct>=10 for a 1DTE ATM contract (~25 bps -> |ret| >= ~1.7%), or refit the 15% continuation coefficient from replay data conditioned on day-move size; alternatively lower the floor for this lane's 25-min ~
    VOTE UPHELD (degrades-edge): Independent re-derivation with the real code CONFIRMS the finding. (1) Live path verified: runner builds Last30Lane with p_thesis=0.5 (DEFAULTS, no hunter.yaml override) and feeds sig.target_move/horizon_T straight into select_contract with 1-DTE-preferred expiries. (2) Real-code numerics: at the 1~
    VOTE UPHELD (degrades-edge): Finding survives adversarial refutation. Reachability: live path confirmed - Last30Lane is in the runner roster and runtime/options_shadow_journal.jsonl shows both real firings (QQQ puts 2026-07-13 |ret|=0.98% and 2026-07-15 |ret|=1.08%) ended in no_pick with the funnel survivors dying exactly at e~
- [high] LANES-5 IndexTrend noise band is the MEAN |move|, which pure noise exceeds 42.5% of the time at any boundary [votes: UPHELD(degrades-edge), REFUTED(degrades-edge)]
    CLAIM: Using avg |close(m)/open - 1| as the breakout band means a driftless random walk closes outside it at any given 5-min boundary with probability ~0.425 (both sides), so the band supplies almost no evidence of a trend day; the first-crossing therefore lands early on noise (band ~7 bps at 09:35 for SPY), burning the daily latch (LANES-2) and creating entries with the ~zero barrier distance that LANES-1 then executes.
    EVIDENCE: lanes.py:200 (noise = per-minute MEAN of |close/day_open - 1|), lanes.py:242-248 (fire when move > noise on the VWAP side); the docstring at lanes.py:214-216 frames it as a trend-day confirmation but a mean-abs band is a ~58th-percentile threshold, not a breakout level.
    REPRO: E|X| = sigma sqrt(2/pi) = 0.798 sigma for X~N(0,sigma^2); P(|X| > E|X|) = 2(1-Phi(0.798)) = 0.425. At 09:35 (5 min in), band = 0.798 x 0.756% x sqrt(5/390) ~= 6.8 bps for SPY -- one 7 bp drift from the open on the matching VWAP side (heavily correlated with the move's sign) latches the side for the day.
    FIX: Raise the band to an empirical percentile of |move| per minute (p80-p90, still computable from the same 14-session arrays), or require k x noise with k~1.5-2 (P(|Z|>1.6) ~= 11%); pair with LANES-2's latch fix so the tighter band does not simply starve further.
    VOTE UPHELD (degrades-edge): Empirical walk-forward simulation on the real intraday cache (~500 sessions per symbol, using the actual build_noise_profile and lane fire condition) reproduces the claim: 09:35-boundary full-condition fire rates are SPY 38% / QQQ 42% / IWM 44% vs the predicted ~42.5%; the VWAP-side condition remov~
    VOTE REFUTED (degrades-edge): The claim's numbers are wrong against the real system and its damage chain is broken on the live path. (1) The band is not ~7 bps: build_noise_profile's per-minute empirical mean over 14 real sessions embeds U-shaped open vol and trend-day drift, giving SPY 13.5 / IWM 18.3 / QQQ 28.6 bps at the 09:~
- [high] LANES-6 No target-vs-remaining-time feasibility check: late-session fires request full-day-scale moves in minutes and pass the selector as priced lotteries [votes: UPHELD(degrades-edge), UPHELD(degrades-edge)]
    CLAIM: IndexTrend's 2xnoise target floor GROWS to ~2x avg|close-open| (~1.2%) by late day while the horizon shrinks to minutes, and ORB/macro have no fire-time cutoff at all; mu_thesis = ln(1+target)/horizon then explodes (hundreds annualized), the thesis branch of the EV mixture becomes 'the target is hit with certainty', p_profit ~= 0.5 x 1 clears the 0.40 floor, and the frozen mu_thesis feeds the exit engine's mu_prior ~
    EVIDENCE: lanes.py:254 (target = max(0.35 x remaining_range, 2.0 x noise(m)) -- the noise term does not shrink with the clock), lanes.py:428 (ORB target 2xOR any time before close_min, no window bound), lanes.py:522-523 (macro horizon floor 1 minute), lanes.py:112-116 (mu = ln(1+target)/horizon); math.py:209-215 (mu_t x dt = ln(1+target) exactly -~
    REPRO: 15:50 index fire: noise(950) ~= 0.798 x 0.756% x sqrt(380/390) ~= 0.60%, target = 2x = 1.19%, horizon = 10 min -> mu = ln(1.0119) x (390x252)/10 ~= 116/yr, i.e. a drift 154x the annualized sigma of ~0.12 -- 12.9 sigma over the horizon (feasible ~never), yet the mixture EV treats it as a 50% certainty and p_profit ~= 0.5 >= 0.40 passes.
    FIX: Cap mu_thesis relative to what the market prices as possible: require |ln(1+target)| <= k x iv x sqrt(horizon_T) (k~2-3) or shrink the target to min(target, k x noise-scaled remaining move); add a latest-fire minute per lane (ORB before ~11:00; index_trend before ~15:00).
    VOTE UPHELD (degrades-edge): CONFIRMED by independent re-derivation on the real code + real cached SPY data. Real 14-day profile (build_noise_profile on runtime/intraday_cache/SPY_1min.parquet): at a 15:50 IndexTrend fire, target = 2xnoise = 0.929% (the 0.35x remaining_range term correctly shrinks to 0.116% and is dominated by~
    VOTE UPHELD (degrades-edge): Reproduced with the real modules: a 15:50 index fire yields mu_thesis=117/yr (9.9-sigma target, p_touch(mu=0)=6.5e-23) yet select_contract passes picks at ev_pct 50-60%, p_profit 0.73, with its own p_touch_target column logging 0.0 - feasibility is priced nowhere and the mu=0 branch alone fails ev_~
- [medium] LANES-7 p_thesis=0.5 constant: uninformative at entry, thrown away at exit -- the system buys at 50% belief and holds at 100%
    CLAIM: Every lane emits the same coin-flip prior, so the selector halves each thesis's delta capture (interacting with the 10% floor to starve moderate signals) and no lane evidence (RVOL 8 catalyst vs 2 bps macro drift) differentiates EV; worse, the exit engine never reads p_thesis -- mu_prior = full mu_thesis while thesis_valid -- so the entry and exit legs of the same position price the identical thesis at 0.5 and ~1.0 ~
    EVIDENCE: lanes.py:221, 293, 370, 479 (p_thesis=0.5 defaults, never modulated by any measured quantity); run_options_shadow.py:826 (p_thesis stored on the position) but zero occurrences of p_thesis in exit_engine.py; exit_engine.py:242 (mu_prior = pv.mu_thesis if pv.thesis_valid else 0.0 -- unscaled).
    REPRO: Selector decomposition (selector.py:253): delta_capture = delta x S x target x p_thesis -- at p=0.5 the required target to clear the 10% floor doubles versus p=1; in the exit blend with no marks yet (t_stat~0), w = t^2/(1+t^2) ~= 0 -> mu_eff = mu_thesis exactly = 2x the drift the entry EV averaged.
    FIX: Short term: propagate the prior (mu_prior = p_thesis x mu_thesis) for lifecycle coherence. Medium term: per-lane empirical p_thesis from the WORST-ledger replay/grader (target-touch base rate at N>=25), and let lane evidence (RVOL, catalyst bool, measured-move z-score) shift it.
- [medium] LANES-8 MacroReactionLane has no noise floor on the measured move: `move != 0.0` plus a 20 bps target floor fires coin-flip-direction signals on sub-noise dr~
    CLAIM: A 1-3 bps 09:30->09:45 SPY drift (0.07-0.2 sigma of the 15-min distribution) arms the lane with direction = the sign of noise and a target floored at 20 bps -- up to 20x the measurement -- inverting the stated thesis ('the measured reaction repeats') into 'an unmeasured move will happen'; additionally _pending never expires, so a post-blackout emission can trade a direction measured 50+ minutes earlier.
    EVIDENCE: lanes.py:496-498 and 506-507 (`if move != 0.0` is the ONLY magnitude gate -- float inequality on real prices); lanes.py:523 (target = max(|move|, 0.002) -- the floor EXCEEDS sub-noise measurements); lanes.py:487, 517-519 (_pending held indefinitely until a blackout-free bar, no staleness bound).
    REPRO: SPY 15-min sigma = 0.756% x sqrt(15/390) ~= 14.8 bps. A measured move of +2 bps (z = 0.14, direction ~Bernoulli(0.5) under noise) fires with target 20 bps = 10x the measurement; P(sign correct) ~= 0.5 exactly where the lane claims directional information.
    FIX: Arm only when |move| >= ~1 x 15-min noise (reuse the injected NoiseProfile: noise_at(measure_end) - noise_at(measure_start) scale, or a fixed ~15 bps for SPY); drop the 20 bps floor (emit target = |move| and let the selector's EV floor reject small ones); expire _pending after ~15 minutes.
- [medium] LANES-9 Overnight-held index_trend positions are auto-invalidated on the first poll of the next morning, nullifying the 15:45 overnight-evidence grant
    CLAIM: invalidated() anchors to TODAY's session_open and TODAY's noise band; the morning after an overnight grant, move-from-open ~= 0 < noise (and price ~= VWAP), so the first thesis poll of the new day declares the thesis invalid with high probability regardless of whether the overnight gap vindicated the position -- the exit engine's carefully-gated overnight hold is followed by a coin-flip-at-best forced sale at the op~
    EVIDENCE: lanes.py:269-277 (band re-entry test on abs(close/session_open - 1) with session_open = the NEW day's open); run_options_shadow.py:406-408 (day roll clears _last_ctx; once the first new bar arrives session_open re-primes to today's open); run_options_shadow.py:871-887 (thesis poll uses that new-day ctx); lane instances are rebuilt fresh ~
    REPRO: At 09:31 the new-day band = avg|close(09:31)/open - 1| ~= 3 bps while |move from open| of the first mark ~= 1-2 bps -> band re-entry test is true with P ~= P(|Z|<0.8) ~= 0.58, OR'd with a ~50% VWAP recross (price ~= VWAP at the open) -> combined kill probability ~= 0.75-0.8 on poll 1, uncorrelated with overnight P/L direction.
    FIX: Make invalidation position-anchored, not day-anchored: persist the entry-day band edge and entry VWAP on the position (they are in sig.notes already: noise_width, svwap) and evaluate re-entry against those, or exempt the first N minutes of a new session and require the gap direction to oppose the p~
- [medium] LANES-10 Merge semantics invert invalidation: one always-False lane merged onto an index_trend position disables rule (b) entirely
    CLAIM: Last30 and MacroReaction return invalidated()==False unconditionally, and the runner declares a merged position invalid only when EVERY constituent lane invalidates; merging a last30 or macro tag onto a live index_trend position (same underlying+direction, a normal 15:30 occurrence) therefore flips that position from the hair-trigger regime (LANES-1) to a NEVER-invalidate regime in one step -- two theses, each indiv~
    EVIDENCE: lanes.py:327-328 and 533-534 (unconditional False); run_options_shadow.py:641-647 (merge appends the lane tag to pos.lanes) and 871-887 ('invalid only when EVERY constituent lane says invalidated' -- all(verdicts)); exit_engine.py:357-358 (rule b keyed solely on that boolean).
    REPRO: Deterministic repro: index_trend call open at 15:31 + Last30 fires call on the same index -> pos.lanes = [index_trend, last30]; verdicts = [True, False] on any subsequent VWAP recross -> all(verdicts) = False -> thesis_valid stays True forever; rule (b) unreachable for the position's remaining life.
    FIX: Distinguish 'clock-bound: no opinion' from 'thesis intact': have clock-bound lanes return None/abstain and exclude abstentions from the all() vote (invalid when all NON-abstaining lanes invalidate), or evaluate rule (b) per lane tag against the lane's own planned exit.
- [medium] LANES-11 Uniform FIRST5_UCURVE_FRAC=0.04 makes the effective RVOL threshold vary ~2x either way by name (the per-name U-curve shape is available and unused)
    CLAIM: The RVOL gate is correctly scaled to the first 5 minutes (NOT a raw full-day baseline -- verified) and IEX volume verifiably never gates, but the single 4% constant misprices names whose true first-5 share is 2% (effective threshold rvol_true>=10, false suppression on the exact hunt-list names the lane exists for) or 6% (effective >=3.3, noise passes); the IEX cache could supply each name's U-curve SHAPE as a ratio,~
    EVIDENCE: lanes.py:53 (FIRST5_UCURVE_FRAC = 0.04, 'fixed, documented approximation'), lanes.py:387-389 (baseline = average_volume x 0.04; avg_first5_volume explicitly informational-only -- clean per the brief's leak check, confirmed by grep: no gating consumer anywhere), lanes.py:130/208 (the profile already computes per-name avg_first5_volume; ad~
    REPRO: Name with 10M ADV: baseline = 400k shares in 5 min; RVOL>=5 demands 2M shares = 20% of an average full day in 5 minutes. If the name's true first-5 share is 2% (typical flat-open mid-cap), true first-5 average = 200k -> the gate actually requires 10x that name's own first-5 norm, double the registered intent of 5x.
    FIX: Extend NoiseProfile with avg_daily_volume (IEX) and use first5_share_name = avg_first5_volume/avg_daily_volume (a venue-ratio, approximately venue-invariant) in place of 0.04, falling back to 0.04 when the cache is absent; register the change and validate the ratio's stability on SPY/QQQ/IWM where ~
- [medium] LANES-12 build_noise_profile has no recency check: a stale parquet silently drives today's stand-down gate and bands
    CLAIM: The profile uses the last 14 sessions present in the file with no test that the latest session is recent, and the runner logs only 'cache absent/thin' -- if ATLAS-CacheRefresh (15:35 CT) fails or lags, range_percentile_14d ('the LATEST complete session') is silently computed on a session days or weeks old, and the day's lane-1 stand-down decision plus all noise bands become fossils with no journal evidence.
    EVIDENCE: lanes.py:157-169 (groupby, keep sessions >= 30 bars, slice [-lookback:], no date check anywhere); run_options_shadow.py:292-303 (read parquet, no freshness check) and 435-437 (logs only the absent/thin case).
    REPRO: With the cache last refreshed k trading days ago, the gate's 'yesterday' is day T-k: given daily-range rank autocorrelation well under 1, P(gate decision differs from the true-yesterday decision) approaches the unconditional 2 x 0.571 x 0.429 ~= 49% as k grows -- the gate degenerates toward an arbitrary coin without any log line changing.
    FIX: Return the latest session date in NoiseProfile (or None when latest < previous trading day) and have the runner journal profile_stale with the date and stand lane 1 down EXPLICITLY -- a visible stand-down instead of a silently wrong gate; alternatively assert freshness at day-start and page via the~
- [low] LANES-13 Minor estimator quirks: self-inclusive percentile floor at 7.1%, and floor-biased noise lookup between sparse minute keys
    CLAIM: pct counts the latest day against itself (minimum rank 7.1% at n=14, and the >=50 boundary passes exactly at rank 7/14 = 50.0), and NoiseProfile._lookup returns the nearest key <= minute, i.e. the EARLIER (smaller, since noise grows in t) band value between missing keys -- both are small, direction-known biases rather than errors.
    EVIDENCE: lanes.py:203 (r <= latest inclusive), lanes.py:235 (< 50 comparison passes rank 7/14 exactly), lanes.py:133-148 (floor lookup; between keys the band is understated, marginally increasing fire probability).
    REPRO: n=14: pct in {7.1, 14.3, ..., 100}; the inclusive count shifts every rank up by one notch (7.1 points) relative to an exclusive rank -- rank-6 days (42.9 exclusive would be 46.2) sit just below the gate either way, but rank-7 days pass at exactly 50.0 only because of the inclusive count.
    FIX: Use (count of r < latest)/(n-1) for an exclusive rank if the 50 threshold is meant as a true median split; interpolate linearly between minute keys in _lookup.
- [low] LANES-14 InPlayORB looseness cluster: sub-$5 pass via or_high, no fire-time window, and sparse-OR false breaks
    CLAIM: Three small laxities in lane 2: (1) price_ok accepts a name whose 09:35 price has faded below $5 if the OR high touched $5; (2) the OR break can fire any time before the close, so a 14:00+ cross is treated as opening momentum (the mu-explosion aspect is LANES-6; the thesis-drift aspect is separate); (3) a sparse candidate whose 09:31-09:34 bars never form gets a 1-bar opening range, making the 09:35 'break' near-tau~
    EVIDENCE: lanes.py:410-411 (or-condition on st.or_high), lanes.py:417-419 with no upper minute bound (only close_min via horizon), lanes.py:399-404 + 412-415 (OR built from whatever bars arrived; bars_seen >= 1 suffices).
    REPRO: bars_seen = 1 with a single 09:30 bar of range 0.1%: any 1-min close drifting 0.1% from the open 'breaks' the OR -- for a gapping small cap with 1-min sigma ~0.3% that is P ~= 0.74 on the very first eligible bar, i.e. an automatic fire whose target 2x(0.1%) = 0.2% is then far too small to clear the EV floor: latch burnt, nothing gained.
    FIX: Require ctx.close >= price_min alone; bound the trigger window (e.g. fire only before 11:00); require bars_seen >= 3 (or or_h - or_l >= a noise-scaled minimum) before declaring the OR tradable.

### selector - atlas/options/selector.py (17 hard gates -> EV stage -> scoring), the funnel choke point - 14 findings
- [high] SELECTOR-1 Lambda band (<=90) x delta gate (0.40-0.80) is jointly EMPTY for index options at current IV - structural starvation; the registered 0DTE carve-out i~ [VERIFY-PENDING (refuters died on limit)]
    CLAIM: Because lambda = delta*S/mid scales as ~delta/(iv*sqrt(T)), the joint feasible set of the delta window and lambda<=90 is empty for SPY-class index chains at calendar IV 12-14%: ALL 0DTE strikes fail (min lambda in the delta window 110.8-137.9), and DTE-2 is empty at 12-13% on $1 strikes. Index lanes can therefore essentially never buy anything until IV rises, and the 0DTE skew the platform explicitly built a carve-o~
    EVIDENCE: selector.py:63 (lambda_band=(15,90)), :211-213 (gate); repro on repo code: SPY 630, $1 strikes - DTE=0 EMPTY for calIV 0.12-0.18, first feasible calIV 0.20 with exactly 1 strike; DTE=2 EMPTY at 0.12-0.13, 2 strikes at 0.14. Live corroboration: 3 sessions, 1 entry - and it was IWM with solved_iv 0.1803 trading-clock (~22% calendar), lam 7~
    REPRO: BS(630,K,r=.04,calIV=.13,T_cal=(dte+.27)/365) chain -> solve trading-clock IV -> greeks: 0DTE min lambda over delta-window = 122.9 (K=626, delta .79, mid 4.05); DTE-2: K=626 delta .761 lam 92.9, K=625 delta .811 (out of band) - every strike fails one of the two gates. At calIV .20: 0DTE K=624 delta .783 lam 81.3 passes (1 strike).
    FIX: Make the lambda ceiling volatility-scaled (lambda_max ~ c/(iv*sqrt(T))) or drop the ceiling entirely and let the EV stage price leverage; at minimum, journal a per-no_pick flag when the delta-window x lambda-band intersection is empty so the starvation is visible.
- [high] SELECTOR-2 hold<=1/3-of-life gate bans ALL DTE-1 contracts before 12:45 ET for rest-of-day signals; combined with SELECTOR-1 the morning index funnel is fully c~ [VERIFY-PENDING (refuters died on limit)]
    CLAIM: Every index lane emits horizon_T = rest-of-day; at 09:35 that is 0.9872 trading days, but a DTE-1 contract's T/3 is 0.6624 - so the gate rejects the ENTIRE DTE-1 expiry until 12:45 ET. With 0DTE lambda-infeasible (SELECTOR-1) and DTE-2 lambda-empty at 12-13% IV, a morning SPY/QQQ signal at current vol has NO expiry that can pass the hard gates at all.
    EVIDENCE: selector.py:68 (hold_max_frac_of_life=1/3), :175-180 (gate, 0DTE exempt); lanes.py:105-109 (_rest_of_day_T horizons). Live: 165 hold_exceeds_third_of_life rejections across the 9 no_picks (e.g. IWM 09:44 call 55/237 call rows; SPY CPI 09:45 110 rows).
    REPRO: horizon(09:35) = (960-575)/390/252 = 0.9872 td; DTE-1 T = 1.9872 td, T/3 = 0.6624 < 0.9872 -> REJECT; solving 3h <= h+1 gives h <= 0.5 day = 195 min -> first DTE-1 pass at minute 765 = 12:45 ET. DTE-2 always passes (3h <= h+2 for h<=1).
    FIX: The gate conflates planned hold with maximum hold: either cap the EFFECTIVE horizon used for gating at min(horizon_T, T/3) and let the exit ladder govern (it already does), or exempt DTE-1 the way DTE-0 is exempted - the 1/3 rule was designed for multi-day theta bleed, not same-day exits on tomorro~
- [high] SELECTOR-3 EV stage measures target ambition, not edge: flat p_thesis=0.5 x a drift that attains the target by construction makes ev_pct ~linear in target size [VERIFY-PENDING (refuters died on limit)]
    CLAIM: ev_hold_thesis's thesis branch uses mu_t = ln(1+target)/horizon, so the mixture EV is guaranteed ~half the option payoff of the full target move regardless of how improbable the target is; p_thesis is a hardcoded 0.5 for every lane. Consequence A (false fire): the live entry logged ev_pct=53.31 and p_profit=0.686 on a 1.71-sigma/140-min move that the selector's OWN p_touch scored at 0.086, with the mu=0 branch at -1~
    EVIDENCE: selector.py:75-77 (ev_pct_min=10, p_profit_min=.40), :218-232; math.py:204-215 (ev_hold_thesis); runtime/options_shadow_entries.jsonl (ev_pct 53.31, p_touch_target 0.086 side by side); runtime/options_shadow_journal.jsonl 2026-07-13/15 QQQ last30 (ev_pct_floor x5, x1).
    REPRO: Repro F reproduces the live IWM record exactly (solved_iv .1803, ev% 53.31, p_profit .686); its mu=0 branch alone: ev% = -1.18, p_profit .432. Repro D, same SPY DTE-2 contract: target .15% -> ev% 4.50; .28% -> 10.96 (floor threshold); .48% -> 21.41; 1.16% -> 59.94 - while p_touch(mu=0) for those targets falls 0.53 -> 0.039. last30 sweep:~
    FIX: Replace the flat 0.5 with per-lane p_thesis calibrated from realized target-touch rates (the shadow/journal data is accumulating exactly this); or floor the decision on the mu=0 branch plus a registered empirical hit-rate. Immediately: journal ev_mu0 and p_touch beside ev_pct and alarm when ev_pct/~
- [high] SELECTOR-7 Non-index spread gates (<=10% AND <=$0.30) are unpassable for lane-2 catalyst names - the lane has never produced a pick [VERIFY-PENDING (refuters died on limit)]
    CLAIM: Catalyst-day single-name chains (post-gap, first 15 minutes) essentially never quote inside 10%-of-mid AND $0.30 absolute at the delta 0.40-0.80 strikes; all three live ORB signals died with 100% of same-direction rows rejected at no_quote/spread_pct/spread_abs before any other gate could even run. The lane burns premarket hunt-list infrastructure to feed a selector that cannot accept its merchandise.
    EVIDENCE: selector.py:53-54 (spread_max_pct=.10, spread_abs_max_nonindex=.30), :181-186; live journal: UNTY put (12 rows: no_quote 6, spread_pct 6 = all), FAST call/put (14 rows each: spread_abs+spread_pct+no_quote = all), CTAS call (53 rows: spread_abs 10, spread_pct 33, no_quote 10 = all).
    REPRO: CTAS 2026-07-15: 106 rows fetched, 53 same-direction, 53 rejected at quote/spread gates, 0 reached OI/delta/EV. Cumulative lane-2 live: 3 signals, 0 picks, 0 rows past the spread stage.
    FIX: Decide which is wrong: if the spread floor is the law (defensible - 10% round-trip vs a 10% EV floor is self-canceling), retire or re-scope lane 2 to penny-pilot mega-caps; if the lane is the point, let the EV stage price the actual spread (it already subtracts it) and drop the absolute cap, keepin~
- [high] SELECTOR-6 OI/volume floors use mismatched clocks: prior-day OI + full-day volume bar judged 5-15 minutes into the session [VERIFY-PENDING (refuters died on limit)]
    CLAIM: open_interest (>=1000 index / >=300) is a stale prior-day snapshot and volume>=100 is day-cumulative volume evaluated at 09:35-09:45 for morning signals - a full-day liquidity standard applied to minutes of tape. On IWM/QQQ the index OI floor rejects most non-round strikes outright; live it is the single largest hard-gate killer and its position in gate order masks the volume gate almost completely. On 0DTE the stal~
    EVIDENCE: selector.py:56-57 (floors), :188-192 (gates, OI before volume); live journal sums across 9 no_picks: open_interest ~473 rejections (IWM 09:44: 101 of ~237 call rows; QQQ last30: 109 of 178), volume only 4 (ordering artifact).
    REPRO: IWM 2026-07-13 09:44 call fire: 474 rows -> 237 calls -> OI 101 + no_quote 66 + hold 55 + spread 10 + lotto 2 + lambda 1 + volume 2 = 237 (100% rejected; 0 reached the EV stage).
    FIX: Scale the volume floor by elapsed session fraction (e.g. >=100 * minutes_elapsed/390, floor ~10-20 in the first half hour) and for 0DTE gate on (OI >= floor OR day-volume >= floor) since prior-day OI is definitionally unrepresentative; log OI and volume per rejected row so the floor can be recalibr~
- [medium] SELECTOR-5 Deep-ITM IV solve is ill-conditioned and its failure path mixes clocks (vendor calendar IV applied to trading-time T)
    CLAIM: implied_vol runs on trading-clock T, making the no-arb floor S - K*exp(-r*T_trad) sit ~2x the market's calendar carry above intrinsic, so legitimately-priced deep-ITM mids fail the solve and silently fall back to vendor_iv - a CALENDAR-clock IV - applied to TRADING-clock T, overstating sigma*sqrt(T) by ~37% (2 DTE). This pulls deltas toward 0.5 (can admit true-delta>0.80 rows into the band) and roughly doubles EV-st~
    EVIDENCE: selector.py:201-203 (solve then vendor fallback), :206 (greeks on the mixed pair); vendor/blackscholes.py:147-151 (floor on the passed T). Repro A table: K=612-619 all [VENDOR-IV FALLBACK]; K=620-622 solved IVs 0.0656/0.0780/0.0826 vs the 0.092 trading-clock equivalent of the true 13% (up to 29% low from $0.01 rounding alone).
    REPRO: K=622, DTE-2, market mid = intrinsic + K*r*T_cal + $0.10 tv = 8.255; code floor = 630 - 622*exp(-.04*.011854) = 8.295 > mid -> implied_vol returns None -> vendor 13% on T_trad gives delta 0.827 vs true 0.899 (delta shifted -0.072 toward the band).
    FIX: Convert vendor_iv to the trading clock before any use (iv_trad = vendor_iv*sqrt(T_cal/T_trad)); alternatively reject rows whose mid-solve fails (treat like no_iv) rather than switching conventions mid-row. Add a flag when solved and vendor IV disagree >20% so selection-time greek disputes are visib~
- [medium] SELECTOR-4 Score's ev_pct cheapness gradient dominates the +5 delta-preference bonus - picks pin at the delta-gate FLOOR, opposite the declared 0.55-0.75 skew
    CLAIM: ev_pct falls monotonically with premium (similar EV$ divided by a larger ask), giving the score a systematic gradient toward the cheapest, lowest-delta gate survivor that outweighs the 5-point preferred-band bonus. The selector therefore reliably buys delta 0.40-0.47 - the brief asked whether the bonus dominates; it is the reverse, and the stated skew is decorative.
    EVIDENCE: selector.py:236-244 (score = ev_pct - penalties + 5 if in 0.55-0.75); the only live entry: winner IWM 293P delta -0.468, ev% 53.31, score 52.509 vs runner-up 295P delta -0.629 (in-band, +5 applied), ev% 43.73, score 48.211 (runtime/options_shadow_entries.jsonl runner_up_snapshot).
    REPRO: ev_pct gap between adjacent viable strikes 53.31-43.73 = 9.58 > 5.0 bonus; third pick 296P (delta -0.708) gapped another 8.4. Bonus can only decide ties within ~5 ev% - which adjacent-strike ev_pct spacing (~8-10) never produces.
    FIX: If the 0.55-0.75 skew is still wanted post-retraction, express it in the objective (score on ev_usd, or weight ev_pct by delta) or narrow the hard gate; if not wanted, delete the bonus - as-is it just adds noise to ranking without changing outcomes.
- [medium] SELECTOR-9 theta_day unit error: vendor per-CALENDAR-day theta on a 252-trading-day pipeline (31% understatement) - exit engine corrects itself, selector/ledger~
    CLAIM: vendor greeks() returns theta = annual/365, where 'annual' here is per trading-year (T is trading-clock). One trading day of decay is annual/252, so the selector's theta_day understates decay per trading day by factor 252/365 = 0.690. exit_engine.py multiplies by 365/252 to fix its own copy, but the selector's ScoredPick.theta_day, the entry decomposition.theta_paid (theta_day * trading-days), and shadow.py's exit-r~
    EVIDENCE: selector.py:250-254 (theta_day = g.theta * 0DTE-mult; theta_paid = theta_day*dt_days with dt_days in TRADING days); vendor/blackscholes.py:115 (theta_annual/365); exit_engine.py:239 (the * (365.0/252.0) correction the selector lacks); shadow.py:182 and run_overnight_lab.py:462-463 (consumers of the uncorrected value).
    REPRO: 252/365 = 0.6904: every theta_paid figure in entries/exits/overnight-lab is 31% smaller than the model's own truth. Live entry: theta_paid -10.14 recorded; consistent value would be -10.14*365/252 = -14.69.
    FIX: Multiply by 365/252 at selector.py:250 (matching exit_engine.py:239) so one convention flows everywhere; regenerate any lab study that consumed theta_paid_usd.
- [medium] SELECTOR-8 EV-stage gates are mutually inconsistent about transaction costs: ev_vs_spread double-nets (effective 3x spread requirement) while p_profit ignores e~
    CLAIM: ev_usd is already net of the FULL round trip (buy at ask + half-spread exit vs the model mid), yet the ev_vs_spread gate then requires that net figure to exceed 2x round-trip spread again - the true requirement is E[V]-mid >= 3x spread, tighter than the registered '2x' description. Meanwhile p_profit's threshold is the bare ask: it counts scenarios profitable at the model MID, ignoring the exit half-spread the WORST~
    EVIDENCE: selector.py:217 (exit_cost = spread/2), :218-223 (ev = E[V]-ask-exit_cost), :229-230 (ev_usd >= 2*spread*100), :232 (p_profit vs mark_mid=cq.ask inside ev_hold_thesis, no exit cost); docstring :76 claims 'EV$ >= 2x round-trip spread'.
    REPRO: ask = mid + s/2 => gate ev_usd >= 2s <=> E[V] - mid - s >= 2s <=> E[V] - mid >= 3s. For the live IWM entry (s=$0.03): required mid-edge $9/contract vs documented $6. p_profit counted V>1.88 (ask) not V>1.88+0.015 (ask+exit half-spread): threshold understated by 0.8% of premium.
    FIX: Pick one cost model: gate GROSS EV >= 2x round-trip (change exit_cost handling or the multiplier) and pass mark_mid = ask + exit_cost into the p_profit threshold so all three gates price the same trade the WORST ledger grades.
- [medium] SELECTOR-10 Inert selector inputs believed active: hv20 (p_touch floor + IV-richness penalty), event_blackout, may_run_overnight
    CLAIM: The runner hardcodes hv20=None and event_blackout=None on every call, and every lane horizon is <= rest-of-day so may_run_overnight is never True. Therefore: the p_touch vol floor max(iv, hv20) never floors, the score's IV-richness penalty (-0.3*(iv/hv20-1.2)) has never executed, the selector-level blackout gate is dead (blackout only enforced at signal level - a chain fetched after a signal but inside a later-start~
    EVIDENCE: run_options_shadow.py:737 (hv20=None, event_blackout=None), :713-714 (may_overnight vs lane horizons from lanes.py:105-109 which cap at rest-of-day); selector.py:225 (max(iv, hv20 or 0)), :238-239 (penalty inside `if hv20 and hv20 > 0`), :197, :172-174.
    REPRO: grep: hv20 literal None at both call sites (selector + entry record hv20:null in the live ledger); penalty branch coverage in live operation = 0 of 1 entries + 9 no_picks.
    FIX: Wire hv20 from the intraday cache (dormant-but-reusable per the buy-low postmortem) or delete the parameters and the dead branches so the config surface matches reality; if event blackouts matter at selection time, pass self._blackout_at(dt) instead of None.
- [medium] SELECTOR-14 First-fail rejection telemetry + fixed gate order misattributes the funnel choke (the owner's 'dies at no_quote/spread/OI' is partly an ordering arti~
    CLAIM: Each row logs only its FIRST failing gate, and liquidity gates run before delta/lambda/EV; so the reason histogram the owner tunes against structurally over-counts no_quote/spread/OI and under-counts the binding downstream walls (lambda joint-infeasibility, EV floor) - fixing OI or spreads alone would simply move the same rejections one gate later. The halt gate additionally runs before the wrong-half filter, so a h~
    EVIDENCE: selector.py:116-120 (_gate appends one reason then continue), :140-149 (halt gate before opt_type filter), gate order :151-232; live journal: lambda_band totals only 24 and ev_pct_floor 6 across 9 no_picks while repro shows lambda/EV would reject the entire surviving delta-window at current IV (SELECTOR-1/-3).
    REPRO: SPY 14:30 put fire: 501 put rows -> spread_pct 105 + no_quote 142 + OI 125 + zero_dte 95 + lotto 16 + delta 4 + lambda 13 + volume 1 = 501; the 13 lambda rejections are the only survivors of five earlier gates - the histogram reads 'liquidity problem' where the math wall is invisible.
    FIX: In no_pick journaling, additionally record the furthest-surviving row per stage and what killed it, or run a diagnostic pass that evaluates ALL gates per row (bitmask instead of first-fail) - pure function, zero risk; move the halt check before the row loop.
- [medium] SELECTOR-11 Premium-cap removal + ev% ranking vs fixed-1-contract dollar grading makes lane verdicts premium-mix-dependent
    CLAIM: The selector optimizes ev_pct (return per premium dollar) with no premium ceiling, while the nightly grader passes/fails lanes on mean NET DOLLARS and PF at 1 contract: a $550 deep-ITM SPY pick (which the lambda gate actively forces at low IV, see SELECTOR-1) and a $150 IWM pick get equal per-trade weight but ~3-4x different dollar variance. At N=25 a handful of high-premium trades will dominate mean-$ and PF, so th~
    EVIDENCE: selector.py:58-62 (premium_max_usd=None, registered opts-tweak-remove-premium-cap-v1), :236 (score anchored on ev_pct); grading contract per brief/system docs: WORST ledger, always 1 contract, lane PASS at N>=25 on mean net $>0, PF>=1.2, top-day share<0.5.
    REPRO: Feasible index picks at 13-14% calIV are $5.2-6.1 mids (repro A: K=625/626) = $520-610 premium vs the live IWM pick at $186.5: same 10% ev floor implies per-trade $ stakes differing 3.3x inside one lane's N.
    FIX: Grade in return-on-premium units (net/premium) alongside $, or size the shadow to constant premium notionals (e.g. contracts = round(k/premium) in ledger units); alternatively re-introduce the premium cap as a grading-homogeneity bound rather than an affordability rule.
- [low] SELECTOR-12 decomposition.delta_capture has the wrong sign for puts
    CLAIM: delta_capture = delta*S*target_move*p_thesis uses the UNSIGNED target magnitude, so every put pick logs a negative expected capture even though its thesis expects profit from the down-move - the field is unusable for the counterfactual/attribution studies the entry ledger feeds.
    EVIDENCE: selector.py:253; live entry decomposition delta_capture = -79.95 on the winning IWM put (runtime/options_shadow_entries.jsonl).
    REPRO: -0.468 * 293.25 * 0.011638 * 0.5 * 100 = -79.87 (~ledger -79.95, gamma rounding); correct signed form |delta|*S*target*p*100 = +79.95.
    FIX: Use signed target (as line 219 already does for the EV call): delta * S * (target if call else -target) * p_thesis * 100.
- [low] SELECTOR-13 best_dte_outside_skew uses calendar DTE, mislabeling Monday expiries picked on Friday
    CLAIM: The renegotiation-evidence flag fires on calendar dte >= 4, but over a weekend a Monday expiry is calendar-3-to-4 while only 1-2 trading days out - Friday selections can be flagged (or not) inconsistently with the 'DTE 4-5 outside the owner's 0-3 skew' intent, polluting the evidence stream that was built to renegotiate the cap.
    EVIDENCE: selector.py:166 (dte = calendar day diff), :265 (outside = scored[0].dte >= 4); docstring :8-9 states the flag's purpose.
    REPRO: Friday 2026-07-17 pick of Tuesday 2026-07-21 expiry: calendar dte 4 -> flagged, but trading dte 2 - inside the skew the flag claims it is outside.
    FIX: Compute the flag on trading days (reuse trading_T's weekday count) or record both; one-line change, keeps the evidence stream honest.

### exit-engine (atlas/options/exit_engine.py, with its computational core atlas/options/math.py p_regain/ev_hold/theta_share, trajectory.mu_blend as consumed, and the runner mark-loop feed in scripts/run_options_shadow.py:926-1133) - 10 findings
- [critical] EXIT-ENGINE-1 Rule (b) thesis_invalid is a structurally guaranteed insta-cut for marginal lane-1 entries: no debounce, tick-sampled, against a rising barrier the e~ [VERIFY-PENDING (refuters died on limit)]
    CLAIM: The (b) rung sells the instant the lane predicate flips, with no confirmation, evaluated on a single 10s-poll tick against a stale VWAP and an intraday-GROWING noise band; because lane-1 entries fire on the first 5-min close marginally beyond that same band, positions begin ~0.1 sigma outside their own kill barrier and are cut on pure noise - this decided the platform's only live trade and will keep scratching nearl~
    EVIDENCE: exit_engine.py:356-358 (bare 'if not pv.thesis_valid: sell', first-match, no debounce); run_options_shadow.py:1007-1009 (S = last tick), :871-887 (_thesis_valid uses stale ctx.svwap with live tick close, exceptions default to valid); lanes.py:264-278 (invalidated = one tick VWAP recross OR |move-from-open| < noise_at(minute), where noise~
    REPRO: Entry margin 0.000107/0.001286 = 0.083 sigma of one 5-min mark (iv=0.1803, sigma_5min = 0.18*sqrt(5/98280) = 0.001286 = $0.379 on open 295.00). P(sampled back inside band at mark 1 | martingale) = Phi(-0.083) = 0.47; P(touch within 300s) ~ 2*Phi(-0.083) = 0.93. Growing barrier: band(entry+10min) ~ 0.005819*sqrt(260/250) = 0.005934 > entr~
    FIX: Make invalidation evidence-symmetric with entry: evaluate on confirmed bar closes (5-min boundary like the entry), require 2 consecutive invalid marks or a hysteresis buffer (~1 sigma_5min inside the band / beyond VWAP, which drops the noise-cut probability from ~0.93 to ~0.28), and freeze the nois~
- [high] EXIT-ENGINE-2 mu_eff noise amplification: a routine 1-sigma 20-minute move becomes a ±631%/yr drift that swings EV_hold by ~100x the (h) threshold and drives (f)/(~ [VERIFY-PENDING (refuters died on limit)]
    CLAIM: decide_exit extrapolates a 20-minute drift estimate to a 1-day decision horizon with weight w = t^2/(1+t^2) that keeps 50% of a 1-sigma noise draw, so every EV and touch probability in the ladder is effectively a sign-detector of the last 20 minutes; (h) false-fires on any ~1-sigma adverse blip (~16% hazard per fresh window) even with the thesis intact, and the logged EVs are economically fictional.
    EVIDENCE: exit_engine.py:241-243 (mu_eff = mu_blend into every downstream state), :263-268 (ev_hold under mu_eff), :277-278 (p_regain under mu_eff), :381 (p_tgt_eff gate for (g)); trajectory.py:68-75 (w = t^2/(1+t^2), t measured against the WINDOW sigma, not the decision horizon); incident fixture output: mu_eff swung -28.4 -> +5.6 within 60 minut~
    REPRO: 1-sigma 20-min move at iv=0.18: r_K = 0.18*sqrt(20/98280) = 0.257% -> mu_hat = 12.62/yr; t=1 -> w=0.50 -> mu_eff = 6.31. On the live IWM put state (S=293.5, K=293, T=0.00931, iv=0.179, mid=1.73): ev_hold(mu=0) = +0.021, ev_hold(+6.31) = -1.599, ev_hold(-6.31) = +5.173, vs ev_sell = -0.010 - a single ordinary 20-min fluctuation moves the ~
    FIX: Shrink the blend for horizon extrapolation: weight by window/horizon ratio (e.g. w' = w * T_K/(T_K + dt)) or cap |mu_eff|*dt at a k-sigma daily move; alternatively compute the t-stat against the DECISION horizon's sigma (iv*sqrt(dt)) instead of the window's. Sweep as a registered replay column (the~
- [high] EXIT-ENGINE-3 d2 cost-basis backstop is a breakeven stop armed by spread noise (P~0.99/day on a pure martingale) that overrides positive EV_hold and amputates the ~ [VERIFY-PENDING (refuters died on limit)]
    CLAIM: Arming needs the bid to rise by exactly one spread (0.17 sigma of a single 5-min mark on the live contract), so nearly every position arms on noise; once armed, a 1-tick giveback fires a mandatory scratch at bid regardless of EV_hold, because d2 sits above (g)/(h)/(i*) in the ladder - with (c) inert this makes the realized distribution 'losers -spread or clock, winners scratched at ~$0', the opposite skew of both th~
    EVIDENCE: exit_engine.py:283 (arm: peak_bid > entry_ask), :367-368 (fire: bid <= entry_ask, positioned BEFORE (g)/(h)/(i*) at :380-398), :329-330 (same in the AH ladder against junk AH bids); the module's own no-anchoring claims at :73-77 concede peak_bid is a peak anchor; run_options_shadow.py:1028 (peak_bid high-water on every mark); sweep_ledge~
    REPRO: IWM 293P: spread $0.03, option sigma per 5-min mark = delta*S*iv*sqrt(5/98280) = 0.468*293.25*0.0012840 = $0.176; arm needs +$0.03 = 0.17 sigma -> P(arm within one mark) ~ 2*Phi(-0.17) = 0.87, within a day ~ 2*Phi(-0.03/1.547) = 0.985. After a marginal arm (+1 tick), P(1-tick giveback within one mark) ~ 2*Phi(-0.01/0.176) = 0.955. Joint ~
    FIX: Co-require the EV rule (fire only when ev_hold < ev_sell too - winner protection then only accelerates already-negative-edge exits), or arm at peak_bid > entry_ask + k*spread with k in a registered sweep {1,2,3}, or demote d2 to a paired-replay column and let (h) own the decision. Also require a tw~
- [medium] EXIT-ENGINE-4 (h)'s 'no-view' prior mu=0 plus r-discounting makes EV_hold(mu=0) exactly -r*S*delta*dt: a systematic call-seller / put-never-seller, and the -half-s~
    CLAIM: Answering the brief's question exactly: theta does NOT make (h) a time stop - with implied vol as physical vol, decay is fully compensated by expected realized variance and EV_hold(mu=0) collapses to the carry term -r*S*delta*dt (verified against the live ledger to 4 decimals). Consequences: (1) at mu_eff~0, ATM calls on expensive underlyings bleed r*S*delta*dt > half-spread and are auto-sold (SPY-scale: -5.2c/day v~
    EVIDENCE: exit_engine.py:271 (ev_sell = bid - mid), :385 (ev_h < ev_sell); math.py:153-201 (ev_hold discounts at r, physical mu from caller); live exit state runtime/options_shadow_exits.jsonl: ev_hold_mu0=0.021 == +r*S*|delta|*dt = 0.04*293.5*0.449/252 = 0.0209 (put, positive); math.py:11-17 documents the IV-as-physical-vol pro-hold bias but not ~
    REPRO: Same-strike call at the live state: ev_hold(mu=0) = -0.0256 = -0.04*293.5*0.640/252 = -0.0257 (fires (h) with half-spread 0.015); the put: +0.0212 (can never fire). SPY 630 ATM 5DTE call: ev_hold(mu=0) = -0.0518/day. Renewal subsidy: bleed 0.9*0.015/day held 20 days = -$0.27/share cumulative vs the one-time $0.015 the threshold was prote~
    FIX: Set the no-view prior to the risk-neutral forward drift (mu_prior = r - q when thesis dead), which makes EV_hold(no-view) = 0 and (h) symmetric across calls/puts; compare EV_hold against 0 rather than -half-spread (the spread is paid once whenever the position exits, so timing does not save it), or~
- [medium] EXIT-ENGINE-5 p_regain's 0DTE horizon double-decays the clock: zero_dte_effective_T is applied to the already-decremented T_now - dt_h, contradicting its own regis~
    CLAIM: The registered derivation defines T_eff(end) = T_now * exp(-2I) as a REPLACEMENT for the model clock over [now, sell_clock], but p_regain passes max(T_now - dt_h, MIN_T) into the function, applying both the linear model decrement AND the empirical exponential for the same interval - t_minus comes out ~3x too small, the regain barrier ~2.6x too far, and p_regain is understated, so (i*) overfires on 0DTE afternoon win~
    EVIDENCE: math.py:297-300 (t_minus = zero_dte_effective_T(max(T_now - dt_h, _MIN_T), minute, sell_clock)); math.py:261-269 docstring ('the model time that reproduces the empirical decay is T_eff(end) = T_now * exp(-2I)' - parameter named T_now, integral already spans now->end); exit_engine.py:236-238 ('it survives only inside zero_dte_effective_T ~
    REPRO: 0DTE ATM S=K=500, iv=0.20 at 13:00, clock 15:00, close 16:00: T_now = 180min = 0.001832, dt_h = 120min, I = 0.1793, exp(-2I) = 0.699. Coded t_minus = (T_now - dt_h)*0.699 = 0.000427 (42 model-min); derived = T_now*0.699 = 0.001280 (126 model-min). regain_move: 0.283% coded vs 0.107% derived; p_regain 0.685 vs 0.878 - a 0.19 understatemen~
    FIX: Pass T_now (not T_now - dt_h) to zero_dte_effective_T in math.py p_regain, since exp(-2I) already encodes the interval's passage; re-run the p_regain_min sweep on the corrected horizon before trusting the 0.25 floor for 0DTE.
- [medium] EXIT-ENGINE-6 (i*) is near-inert for DTE>=1 - first-passage to the epsilon-near regain barrier tends to 1 under ANY drift, and any drift extreme enough to breach 0~
    CLAIM: For a profitable position the regain barrier is only the interval's theta compensation (0.1-0.5% moves), and the reflection term makes P(touch) -> 1 as the barrier nears S regardless of drift, so p_regain < 0.25 is essentially unreachable for DTE>=1; the ladder's advertised skew-capture profit-taking (retracted rules 15/16, 'sell while the profit is still realizable') does not operationally exist - winners exit only~
    EVIDENCE: exit_engine.py:387-398 (i* after (h) at :385 - (h) is strictly more sensitive to adverse mu_eff); math.py:279-312 (barrier = regain_move of TODAY'S mid at t_minus = theta-compensation only); repro on the live contract: p_regain = 1.000 at mu = -10, -20, -40/yr (reflection dominance), 0.775 at mu=0; adverse mu_eff = +6.31 gives p_regain 0~
    REPRO: Live IWM put (S=293.5, mid=1.73, T=0.00931): regain_move = -0.32%; p_regain(mu=0) = 0.775, p_regain(mu=-40/yr!) = 1.000. To reach p<0.25 needs adverse mu_eff ~ +6.3 where ev_hold = -1.60 vs (h) threshold -0.01 - (h) fires ~1 mark earlier in every path.
    FIX: Either accept and document (i*) as a 0DTE-late-day rule (after fixing EXIT-ENGINE-5) and delete the DTE>=1 branch, or restore the retracted rule-2 semantics with a reference value that creates a real barrier (e.g. c_ref = peak realizable value or entry-thesis target value) - which requires re-litig~
- [medium] EXIT-ENGINE-7 Objective mismatch: the engine maximizes per-contract mean EV while its promotion gate is PF/skew-based and its entry model prices hold-to-horizon - ~
    CLAIM: Three subsystems price three different games: the selector admits on hold-to-horizon EV (entry: ev_usd $100.23, p_profit 0.686), the exit ladder's noise-dominated overrides ((b), d2) cut median holds to minutes, and the grader requires PF>=1.2 with N>=25 - unreachable at 0.14 fires/day (~180 trading days) with a distribution whose right tail the ladder itself amputates; no variance/sizing consideration exists anywhe~
    EVIDENCE: runtime/options_shadow_entries.jsonl (ev_usd 100.23, p_profit 0.686, horizon_T = rest-of-day) vs runtime/options_shadow_exits.jsonl (hold_trading_days 0.0128 = 5 minutes, rule b); exit_engine.py:353-400 (ladder order placing predicate/anchor rules above EV); task brief's grader spec (N>=25, PF>=1.2, top-day<0.5); no sizing/variance term ~
    REPRO: N required: 25 fires / 0.14 fires-day = ~179 trading days per lane. Realized per-trade distribution under findings 1+3: P(scratch at -spread within ~2 marks) ~ 0.5-0.9 for marginal entries; entry-priced EV (+$100) is never given its horizon (5-min realized hold vs 140-min priced horizon on the live trade).
    FIX: Grade the exit ladder by paired replay on the stored schema-2 quote paths (replay.py already exists): run {live ladder} vs {debounced-b} vs {no-d2} vs {EV-only} columns so exit-rule EV cost is measured without waiting 180 days for lane N; state explicitly that the shadow estimates per-trade mean ed~
- [low] EXIT-ENGINE-8 Overnight grant gates: calendar-day DTE inflates runway across weekends and 'named catalyst tomorrow' degrades to 'any macro event day' - the grant i~
    CLAIM: dte = (expiry - today).days is calendar days, so Thu->Mon-expiry (2 trading days of life) passes dte=4 >= 3 while Wed->Fri-expiry (also 2 trading days) is blocked at dte=2 - identical runway, opposite verdicts; and the runner computes named_catalyst_tomorrow as is_event_day(next weekday) (macro calendar only), so a single-name catalyst (earnings) can never earn a ride while EVERY symbol qualifies on a macro-day eve ~
    EVIDENCE: exit_engine.py:225 (dte calendar), :318-319 and :346-347 (dte >= overnight_min_dte gates); run_options_shadow.py:944-951 (named_tomorrow = oevents.is_event_day(next weekday) - macro events only, no symbol dimension), :966 (pre-filter repeats the calendar test without the delta check, deferring correctly to the engine).
    REPRO: Thu 2026-07-16 -> Mon 2026-07-20 expiry: calendar dte=4 (GRANTABLE) but 2 trading days; Wed -> Fri expiry: calendar dte=2 (blocked) but the same 2 trading days. Boundary flips solely on weekend interposition.
    FIX: Compute grant runway in trading days (om.trading_T or a weekday count already exists in trading_T's loop); make the catalyst gate per-underlying (macro events grant index underlyings; an earnings/FDA feed grants single names) - both now open for redesign since the fork-answer provenance is retracte~
- [low] EXIT-ENGINE-9 Rule (f) post-print 'decision window' never closes: minutes_since_print is unbounded and never resets, so macro positions run an every-mark EV coin f~
    CLAIM: The intended one-time forced decision ~15 minutes after a print is implemented as 'minutes_since_print >= 15' with no upper bound and a print_minute that never resets, making (f) a permanently-armed duplicate of (h) (with <= vs <) positioned earlier in the ladder - it inherits the mu_eff noise of EXIT-ENGINE-2 and corrupts per-rule grading mixes (fires that are really (h)-class get logged as f_post_print_no_edge).
    EVIDENCE: exit_engine.py:372-375 (one-sided >= condition; ev_h <= ev_sell); :142 (post_print_decision_min documented as 'rule (f) window'); run_options_shadow.py:1077-1078 (minutes_since_print = minute - pos.print_minute, monotone increasing, only macro lane sets print_minute).
    REPRO: A macro position entered 09:45 (print_minute 570): (f) evaluates at 100% of its marks from 09:45 onward (mins_since_print 15, 20, 25, ... all >= 15), not at one decision point - every (h)-class exit for the position's entire life is attributed to (f).
    FIX: Bound the check to one cadence-width window (15 <= mins_since_print < 15 + mark_interval/60) or latch a decided_post_print flag on the position after the first evaluation; per-rule grading then reflects cause.
- [low] EXIT-ENGINE-10 IV sentinel/freshness gaps: iv_fresh guards only (i*), stale carried IV counts as fresh for 45 minutes, and the 1e-4 sentinel can grant the 0DTE deep~
    CLAIM: The degraded-IV protections are asymmetric: (i*) is guarded by iv_fresh but rule (a)'s delta>=0.80 extension, (g)'s theta_share, and (h)'s EVs still run on sentinel or stale IV - at sigma=1e-4 delta is a 0/1 step, so a barely-ITM 0DTE with a dead solve stream gets a fictional delta~1.0 and 30 extra minutes of exposure; a 44-minute-old carried IV passes as 'fresh' everywhere.
    EVIDENCE: exit_engine.py:229-231 (iv = max(solved_iv, 1e-4); iv_fresh = solved_iv > 1e-3 - consumed only at :396), :275-276 (extension uses adelta unguarded); run_options_shadow.py:1039-1051 (failed solves carry series[-1] up to the 45-min purge, then 0.0 -> sentinel; carried values are indistinguishable from fresh solves downstream).
    REPRO: At iv=1e-4, T=0.0005 (0DTE afternoon), S/K = 1.001 (barely ITM call): d1 = ln(1.001)/(1e-4*sqrt(0.0005)) ~ 447 -> delta = 1.0 -> extension granted at :275 on pure sentinel data; the true delta at iv=0.20 would be ~0.55, below the 0.80 gate.
    FIX: Gate the deep-ITM extension (and optionally (g)) on iv_fresh; carry an iv_age_s field in PositionView and journal when decisions run on carried IV older than ~10 minutes so replay can quantify the exposure; the fail-toward-HOLD design otherwise stands.

### shadow-ledger - 8 findings
- [medium] SHADOW-LEDGER-1 theta_paid decomposition mixes per-calendar-day theta with trading-day hold counts, has an inverted sign vs spread_paid, and keeps a 0DTE multiplier ~
    CLAIM: build_exit_record's theta_paid understates true decay by ~31% (factor 252/365), records costs as negative while spread_paid is positive, and for 0DTE entries bakes in the zero_dte_theta_multiplier snapshot that exit_engine's own audit fix (opts-fix-math-audit-20260710) removed as double-counting - so the scorecard's theta_paid_total is wrong in units, sign, and (for 0DTE) shape. Falsification check #1 (cost visibili~
    EVIDENCE: atlas/options/shadow.py:182 (theta_paid = pos.entry_theta_day * max(0.0, hold_trading_days) * mult) and :226 (entry_theta_day from pick.theta_day); atlas/options/selector.py:250 (pick theta_day = g.theta with NO 365/252 conversion, WITH zero_dte_theta_multiplier for dte==0); atlas/options/vendor/blackscholes.py:9,115 (theta returned per ~
    REPRO: Real 2026-07-13 exit row: theta_paid_usd = -0.36 = -0.2825 (pick theta, calendar-day) x 0.012821 (trading days) x 100. Trading-day-consistent value: -0.2825 x 365/252 x 0.012821 x 100 = -0.52 - a 31.0% understatement (252/365 = 0.6904). The engine's own exit-time state.theta_day = -0.4062 vs pick's -0.2825 gives ratio 1.4379 ~ 365/252 = ~
    FIX: In build_exit_record, convert to one convention: theta_paid_usd = abs(pos.entry_theta_day) * (365.0/252.0) * hold_trading_days * mult (positive-cost), store the entry theta WITHOUT the 0DTE multiplier snapshot (or recompute from the exit-state theta_day which is already trading-day), and derive hol~
- [high] SHADOW-LEDGER-2 Rebuild fails open on an unreadable exits file (mass position resurrection) and append has no torn-line guard; the grader never dedupes exits by posi~ [votes: UPHELD(degrades-edge), UPHELD(degrades-edge)]
    CLAIM: read_jsonl returns [] on ANY OSError, so if the exits file is transiently unreadable at the day-roll rebuild (Windows AV/backup lock is the realistic trigger), _open_positions sees an empty exited-set and resurrects every unexpired historical position; the runner then re-marks and RE-EXITS them, and grade() pools every well-formed exit row per lane with no position_id dedupe - duplicate exits inflate N toward the N>~
    EVIDENCE: atlas/options/shadow.py:253-256 (read_text OSError -> return []), :262-264 (JSONDecodeError -> continue, no accounting), :333 (exited set built from load_exits - [] means nothing ever exited), :242-245 (append_jsonl writes rec + newline with no check that the file currently ends in a newline); scripts/run_options_shadow.py:461 (day-roll ~
    REPRO: Deterministic desk-check: with exits read returning [], the 07-13 entry has no matching exit, so _open_positions returns it; only its expiry (2026-07-15 < today) diverts it to expired_unexited. Any unexpired overnight grantee instead re-enters self.positions and produces a second exit row; the grader would then report index_trend n=2, ne~
    FIX: (1) Make the rebuild fail closed: in _open_positions, distinguish 'exits file unreadable' (raise / abort the roll and journal loudly) from 'exits file absent/empty'. (2) In append_jsonl, if the file exists and its last byte is not \n, write a newline first and journal a torn_tail_repaired event. (3~
    VOTE UPHELD (degrades-edge): Reproduced every leg executably against the real modules and real ledger records: (1) an exists-but-unreadable exits file (simulated both as dir-in-place and as a genuine FILE_SHARE_NONE Windows lock, which raised PermissionError exactly as claimed) makes read_jsonl return [] silently, and open_pos~
    VOTE UPHELD (degrades-edge): Core claim survives adversarial verification with a deterministic repro against the real code: read_jsonl swallows OSError to [] (proven), open_positions_all() then resurrects an exited unexpired position (proven: pos-1 returned), the runner's only divert is expiry<today (run_options_shadow.py:462)~
- [high] SHADOW-LEDGER-3 Forced exits are graded at stale fallback NBBOs with no fill-provenance field in the exit record - 0DTE/collapsed-book losers book phantom exit value~ [votes: UPHELD(degrades-edge), REFUTED(cosmetic)]
    CLAIM: _fresh_option_quote requires bid>0, so a deep-OTM 0DTE dying to a 0.00-bid book (or a mid-hold halted name) can never exit through the engine - rule (a)'s 15:00 clock is unreachable without a usable quote - and rides to the after-hours late_close_flat forced exit, which fills at the LAST STORED MARK's bid (possibly hours stale) or even the ENTRY NBBO. build_exit_record has no field recording this degradation (decisi~
    EVIDENCE: scripts/run_options_shadow.py:853-869 (_fresh_option_quote returns None unless bid>0, both quote and chain fallback), :986-1003 (quote None before late-close -> mark_no_quote journal + skip; the SELL ladder is never consulted), :988-993 + :889-924 (_force_exit fallback chain: fresh quote -> last mark NBBO -> entry NBBO; 'degraded' record~
    REPRO: Scenario arithmetic: SPY 0DTE put marked bid 0.15/ask 0.17 at 14:50, book goes 0.00x0.01 by 15:00 (routine for dying OTM 0DTE). Every reval 15:00->16:10 is mark_no_quote (bid=0), so rule (a) never executes; at 16:10 late_close_flat books sell at the 14:50 mark's bid 0.15. True terminal value 0.00. WORST-ledger net is overstated by +$15.0~
    FIX: (1) Persist fill provenance in the exit record itself: add nbbo_source: fresh|last_mark|entry_nbbo and nbbo_age_s to build_exit_record (threaded from _force_exit), and have the grader quarantine or separately report exits with stale sources - same class as the halt quarantine. (2) For a 0DTE whose ~
    VOTE UPHELD (degrades-edge): Every cited mechanism verified against the code and by execution. _fresh_option_quote (run_options_shadow.py:860,865) rejects bid==0 on both paths, and decide_exit - the only place rule (a)'s 15:00 0DTE clock lives (exit_engine.py:353-355) - is unreachable when the quote is None (:988-1003 journals~
    VOTE REFUTED (cosmetic): The code mechanism is accurately cited (bid>0 gate starves the engine; forced-exit fallback to last-mark/entry NBBO; degradation journaled only), but the finding fails the reachability/materiality test. (1) Never exercised: the system's entire lifetime ledger is 1 entry + 1 exit (clean b_thesis_inv~
- [medium] SHADOW-LEDGER-4 config_hash is stamped on every entry but read by nothing downstream - lane PASS verdicts pool across entry cohorts
    CLAIM: The entry schema carries config_hash precisely to separate decision cohorts (the platform's own registration discipline held it at 6c1dc2a4e1a7 through every edit to avoid splitting the cohort), but grade() never joins or filters on it: if any registered tweak legitimately changes the hash, the lane scorecard will silently pool old-cohort and new-cohort trades into one N, mean, PF, and PASS/FAIL verdict - statistica~
    EVIDENCE: atlas/options/shadow.py:116 (config_hash written per entry; real row carries "config_hash":"6c1dc2a4e1a7"); scripts/grade_options_shadow.py:1-244 contains zero references to config_hash - grade() loads all exits ever (:73) and pools per lane (:83-99); exit rows do not carry the hash at all (shadow.py:183-196), so the grader could not fil~
    REPRO: Structural, not numeric: grep config_hash scripts/grade_options_shadow.py -> 0 hits; grep config_hash runtime/options_shadow_entries.jsonl -> 1 hit per entry. Pooling demonstration: 13 old-cohort trades at mean -$5 plus 12 new-cohort trades at mean +$8 grades as one lane at n=25, mean +$1.24 - a PASS-shaped mean neither cohort earned.
    FIX: Copy config_hash into build_exit_record (it is available via the position's entry record; simplest: pass it through ShadowPosition or join by position_id as the grader already does for risk_flags), then report per-lane stats per config_hash and make the PASS bar apply within the newest cohort, with~
- [low] SHADOW-LEDGER-5 BASE_FILL_FRAC = 0.35 is an unregistered constant - only the WORST ledger's fill definition is pre-registered
    CLAIM: The BASE ledger's 0.35 price-improvement fraction appears nowhere in the sweep ledger: the four lane registrations pin only fills_ledger: worst=buy@ask/sell@bid, and the docs merely restate the code. Under the platform's own every-constant-is-owner-verbatim|derived|registered law (and post-retraction, on merits), 0.35 is an uncited model of marketable-limit price improvement; net_base_mean is published in the scorecar~
    EVIDENCE: atlas/options/shadow.py:31 (BASE_FILL_FRAC = 0.35, comment only restates the formula); runtime/backtest_out/sweep_ledger.jsonl lines 32-35 (lane registrations: 'fills_ledger': 'worst=buy@ask/sell@bid' - no base/optimistic definition, and a grep of all 96 lines finds no 0.35 fill registration; the only 0.35 hits are an equity-era stop_buf~
    REPRO: grep -c 'BASE_FILL\|base.*fill.*frac' runtime/backtest_out/sweep_ledger.jsonl -> 0. Sensitivity on the real trade: base net was -14.37; at frac 0.25 it would be -14.13, at 0.50 -14.75 - a +/-4% swing in the base column from an unregistered knob.
    FIX: Register it: add an opts-calib-base-fill-frac-v1 row to sweep_ledger.jsonl declaring 0.35 as CALIBRATION with a planned validation (once real fills exist in any go-live pilot, fit realized improvement vs half-spread; until then sweep 0.25/0.35/0.50 as paired replay columns). Grading is unaffected (~
- [low] SHADOW-LEDGER-6 peak_bid initializes to 0.0 on a fresh live position but to entry bid on rebuild/replay - replay.py's 'exactly like live' claim is false
    CLAIM: The runner's _enter constructs ShadowPosition without peak_bid (dataclass default 0.0) while position_from_entry (restart) and the paired replay initialize it to the entry NBBO bid; replay.py explicitly claims peaks start 'exactly like a fresh live position'. The divergence is decision-inert today because d2* arming requires peak_bid > entry_ask (entry bid can never exceed entry ask), but it is a documented-exactnes~
    EVIDENCE: scripts/run_options_shadow.py:822-831 (ShadowPosition(... peak_mid=entry_mid, last_mark_ts=now ...) - no peak_bid argument -> 0.0 via shadow.py:84); atlas/options/shadow.py:228 (rebuild: peak_bid=float(nbbo.get('bid', 0.0))); atlas/options/replay.py:13-14 ('peak_mid / peak_bid start at entry mid / entry bid, exactly like a fresh live pos~
    REPRO: 07-13 trade: live in-memory peak_bid after entry = 0.0; a restarted process would rebuild it as 1.85; replay uses 1.85. d2_armed requires peak_bid > 1.88 (entry ask), so 0.0 vs 1.85 both evaluate False - inert now, divergent state nonetheless.
    FIX: Pass peak_bid=cq.bid in the runner's _enter construction (one line), making live match rebuild/replay and the replay docstring true. Cohort-neutral: no decision input changes under the current rule set.
- [low] SHADOW-LEDGER-7 Rebuild re-reads entire ledger files inside per-entry loops - O(entries x (entries + marks)) day-roll cost that grows quadratically
    CLAIM: _open_positions calls load_merges(day=None) (a full entries-file read+parse) inside the loop over entries, and load_marks(pid) (a full marks-file read+parse) once per open entry; _roll_day then re-reads the marks file again per position for the IV re-seed, plus the whole journal for the expired-unexited guard, and _force_exit does another full marks read on the late-close hot path. Immaterial at today's N=1 (the mar~
    EVIDENCE: atlas/options/shadow.py:341 (for m in self.load_merges(day=None) - inside the per-entry loop at :335), :347 (self.load_marks(pos.position_id) - full-file re-read per open entry), :309-311 (load_marks re-parses the whole file every call, no cache); scripts/run_options_shadow.py:458-460 (whole-journal read each day-roll), :477-484 (marks r~
    REPRO: Projection: 3 concurrent positions x 78 marks/day x 60 trading days = 14,040 mark rows; 10 unexited entries at day-roll => 10 full parses of 14k rows + 10 full parses of the entries file for merges = ~150k redundant json.loads per roll vs ~14k in one pass - a 10x parse blowup that scales linearly with open entries.
    FIX: Read each file once per rebuild: build exited-set, merges-by-pid, and marks-by-pid dicts in a single pass over each file, then assemble positions from the dicts; give _force_exit the last-mark cache the runner already effectively has (it wrote the marks). Pure refactor of shadow.py:_open_positions ~
- [low] SHADOW-LEDGER-8 Schema incoherence cluster: one-sided-quote mid conventions disagree, per-ledger gross is a duplicated global, and contracts is hardcoded to 1 agains~
    CLAIM: Three latent contract inconsistencies in the record schemas: (a) exit_fills/entry_fills compute mid=(bid+ask)/2 even when bid==0 while build_mark_record and the exit engine use mid=max(bid,0) for one-sided books - currently unreachable (all quote paths gate bid>0) but a landmine for any new exit path; (b) gross_pnl_usd is stored identically inside each of the three per-ledger dicts despite being mid-to-mid and ledge~
    EVIDENCE: (a) atlas/options/shadow.py:40,51 (mid=(bid+ask)/2 unconditionally) vs :139 (mark mid = max(bid,0) when not both>0) and atlas/options/exit_engine.py:226 (same max(bid,0) convention); (b) shadow.py:174-180 (gross computed once, copied into all three ledgers' gross_pnl_usd) with grade_options_shadow.py:53-55 acknowledging 'worst carries it~
    REPRO: (a) bid=0, ask=0.10: fills mid=0.05 vs mark/engine mid=0.00 - a 5-cent phantom optimistic fill if the gate is ever bypassed. (c) A 2-lot scaleout entry would persist contracts:1; after any restart the rebuilt position marks and exits 1 contract - 50% of the P&L silently vanishes from the grading ledger.
    FIX: (a) Unify on the max(bid,0) one-sided convention (or assert bid>0) in entry_fills/exit_fills; (b) move gross_pnl_usd up one level to the record root (grader change is one line; keep the old location one release for compat); (c) write contracts from pos.contracts in build_entry_record and read rec.g~

### runner - 14 findings
- [high] RUNNER-1 Premarket ticks contaminate LiveBarBuilder session state: svwap and the mu_hat window are built from 08:30-09:29 bars (incl. prevclose-echo bars) [VERIFY-PENDING (refuters died on limit)]
    CLAIM: The process starts 08:30 ET daily (log-proven) and tick() polls whenever minute < close_min with no OPEN gate (run_options_shadow.py:1159-1169). LiveBarBuilder.on_tick has no RTH filter (feed.py:160-193), so premarket bars are committed and _commit permanently folds them into cum_pv/cum_v (session VWAP), HOD, and the session arrays (feed.py:115-138). The RTH filter exists only in front of lane dispatch (run_options_~
    EVIDENCE: run_options_shadow.py:1159-1169 (poll gate is close-only), feed.py:115-138 and 160-193 (no session filter in _commit/on_tick), run_options_shadow.py:579-583 (filter only before lanes), run_options_shadow.py:1058-1061 (mu window fed from session_arrays), atlas/collect/tradier_data.py:130-132 (last->prevclose fallback), runtime/options_sha~
    REPRO: Hunt-list name, prevclose 20.00, gaps +4% (opens 20.80, no premarket prints): premarket bars sit at 20.00; ORB entry ~09:36, first mark 09:41 -> window [09:21,09:41] has c0=20.00 (premarket echo), c1=20.83. mu_hat = ln(20.83/20.00)/(20/98280) = 0.0407*4914 = 200.0/yr; with iv=0.60, t_stat = 0.0407/(0.60*sqrt(20/98280)) = 4.75; w = t^2/(1~
    FIX: Gate builder commits to OPEN_MIN <= minute < close (either drop premarket ticks in tick(), or filter minute < OPEN_MIN inside seed/commit), or reset the builder at 09:30. Alternatively have session_arrays/bar_input expose RTH-only views. Register as a structural fix (cohort-neutral: not in DEFAULTS~
- [high] RUNNER-2 b_thesis_invalid hair-trigger is structural: invalidation reads a 10-second tick against the same zero-hysteresis band that admitted the entry with e~ [VERIFY-PENDING (refuters died on limit)]
    CLAIM: Lane entry requires a committed 5-min-boundary close beyond the noise band; the runner's _thesis_valid then feeds pos.close = the instantaneous latest tick (S from tick.last, run_options_shadow.py:1007-1009 -> 871-887) into IndexTrendLane.invalidated, which cuts on ANY re-entry inside the band with zero hysteresis (lanes.py:274-277). Because the first boundary close past a slowly-growing band overshoots by construct~
    EVIDENCE: run_options_shadow.py:871-887 (_thesis_valid builds PositionCtx with tick S and wall minute), 1007-1009 (S = tick.last), lanes.py:264-278 (invalidated: VWAP recross OR |move|<noise, no hysteresis), lanes.py:237-254 (entry: 5-min boundary close beyond band). Live case: runtime/options_shadow_entries.jsonl + options_shadow_exits.jsonl for ~
    REPRO: IWM 2026-07-13: entry 13:40, move_from_open=-0.5926%, noise=0.5819% -> margin beyond band = 1.07 bp (3.15 cents on a $293 underlying). Exit ts = entry+307 s (the FIRST 300-s mark): S 293.25->293.50 (+8.5 bp) -> |move| = 0.5078% < 0.5819% -> b_thesis_invalid, worst-ledger -$16. Engine state at exit shows ev_hold=+0.021 > ev_sell=-0.01 and~
    FIX: Evaluate invalidation on the last COMMITTED 1-min close (ctx.close), not the raw tick, and add registered hysteresis: invalidate only when the close re-enters below a fraction (e.g. 0.5x) of the band or on 2 consecutive marks inside. Sweep the hysteresis on the paired replay before going live.
- [high] RUNNER-3 Overnight positions are thesis-judged by TODAY's fresh lane instances in TODAY's reference frame, structurally defeating the overnight grant [VERIFY-PENDING (refuters died on limit)]
    CLAIM: _lane_by_name is rebuilt every day-roll (run_options_shadow.py:439-453) and _thesis_valid builds PositionCtx from TODAY's ctx (session_open, svwap, current minute). An index_trend position granted an overnight ride (held FOR tomorrow's named catalyst) is evaluated next morning against |move from TODAY's open| < noise -> near-certainly true in the first minutes (move-from-open starts at ~0), so rule b cuts it minutes~
    EVIDENCE: run_options_shadow.py:439-453 (lanes rebuilt per day), 453 (_lane_by_name), 871-887 (today-frame PositionCtx for any entry_day), 660-680 (_chain_rows nearest-3 expirations), lanes.py:264-278 (index invalidation vs today's open/noise), lanes.py:446-458 (ORB invalidation vs today's or_range/svwap), exit_engine.py:313-321 and 345-350 (grant~
    REPRO: SPY-class example: overnight grantee marked 09:35; |close/today_open - 1| at 09:31-09:35 is typically 0-10 bp while noise_at(571..575) is the 14-day MEAN |move| at those minutes (~5-10 bp) -> P(inside band) >> 50%; combined with the VWAP-recross arm (svwap ~ open in the first minutes) the first-morning-mark cut probability approaches cer~
    FIX: For positions with entry_day != today, either skip lane invalidation until the position's own frame is re-establishable (e.g. after 30 RTH minutes), or persist the ENTRY-day thesis parameters (band edge, OR range, entry svwap side) on the position and evaluate against those; the 09:35 re-decision s~
- [high] RUNNER-4 Lane 2 armed with a STALE hunt list: no session_date check and a single 08:30 read - live today (07-16) the shadow is hunting 07-15's candidates [VERIFY-PENDING (refuters died on limit)]
    CLAIM: load_hunt_list (run_options_shadow.py:263-289) ignores the file's session_date/generated_ts fields entirely, and _lane2_candidates is invoked exactly once per day at the 08:30 day-roll (438, 490-535). runtime/hunt_list.json currently carries session_date 2026-07-15 (written 07-15 05:17) - today, 07-16, InPlayORB is armed with yesterday's earnings names (BLK, BNY, ...) and is blind to today's actual in-play names. Th~
    EVIDENCE: run_options_shadow.py:263-289 (no freshness/date validation), 438 and 490-535 (one-shot load at day-roll), runtime/hunt_list.json ('generated_ts': '2026-07-15T05:17:46-05:00', 'session_date': '2026-07-15'; today = 2026-07-16); memory notes premarket crew task scheduling was deploy-pending, so stale files are the expected steady state.
    REPRO: File mtime Jul 15 05:17 vs run date Jul 16: staleness = 1 full session. Lane-2 candidate overlap with today's true in-play set: 0 by construction (07-15 earnings reporters are not 07-16 reporters).
    FIX: Reject candidates when session_date != today (fall back to the lane2_scan_symbols gap scan and journal hunt_list_stale); re-read the file every N minutes until ~09:25 so a crew that finishes after 08:30 still arms the day.
- [medium] RUNNER-5 may_overnight flips on any >=1-minute bar-completion delay, silently rejecting all DTE<2 contracts for an intraday signal
    CLAIM: may_overnight = sig.horizon_T > rest_of_day*1.001 (run_options_shadow.py:713-714) compares a horizon frozen at the BAR minute (lanes._rest_of_day_T uses close-(m+1)) against rest-of-day at PROCESSING time. Normal path is exactly equal (bar m completes in minute m+1) -> False. But if bar completion slips one minute (six consecutive failed polls, or the loop stalled by serial chain/quote timeouts during entries/revals~
    EVIDENCE: run_options_shadow.py:713-714; lanes.py:105-109 (_rest_of_day_T anchored at bar close); feed.py:160-193 (bar completes only on a later-minute tick); selector.py:172-174 (overnight_needs_dte2); main loop sleep-after-work at run_options_shadow.py:1210-1223 (loop period stretches under slow ticks). Healthy-path check: IWM 07-13 entry has ho~
    REPRO: Bar m=819 (horizon 140 min) processed at minute 822 instead of 820: rest_of_day = 138/98280 = 0.0014042 < 0.0014245*1.001 -> may_overnight=True; every dte-0 and dte-1 row then dies with reason overnight_needs_dte2 despite an intraday thesis.
    FIX: Compute may_overnight from the signal's own timeline: may_overnight = sig.expiry-agnostic horizon extends past today's close as of the BAR minute, i.e. compare horizon_T against (close - (bar.minute+1)) rather than wall-clock now; or recompute horizon_T from now before comparing.
- [medium] RUNNER-6 Backfill bounded-retry is burned pre-open every day (3 attempts vs a structurally-empty 09:30+ query) and gives a mid-session restart only ~30 s of t~
    CLAIM: feed.backfill fetches timesales start='{day} 09:30' (feed.py:287-289); at the daily 08:30 start that window is in the future, returns [], and the runner counts it as a failed attempt (run_options_shadow.py:547-565), so ALL watch symbols hit backfill_gave_up by ~08:31 EVERY day - the retry mechanism (opts-fix-backfill-retry-v1, comment lines 95-100) can never perform its stated job on the normal path (benign only bec~
    EVIDENCE: run_options_shadow.py:95-100, 547-565, 568-569; feed.py:245-253, 280-304; runtime/options_shadow_journal.jsonl: 39 backfill_gave_up rows (~13 symbols x 3 sessions, i.e. every symbol every day), rows lack timestamps.
    REPRO: 39 gave-up rows / 3 sessions = 13/day = the full watch set; retry horizon = 3 attempts x 10 s = 30 s vs the 'transient morning feed failure' the fix was registered to survive.
    FIX: Skip (and do not count) backfill attempts before 09:31 ET; make the give-up time-based (retry until e.g. 5 minutes elapsed since first attempt); do not count _budget_ok rejections as attempts; add ts_epoch/day to the journal row.
- [medium] RUNNER-7 Backfill-era signals within TTL proceed to ENTRY with a stale S (the _on_signal backfill flag is dead), against live chain NBBO
    CLAIM: _on_signal accepts backfill= but never reads it (run_options_shadow.py:622-658); only minute_now > expires_minute kills a signal (628), so on a mid-session restart any lane signal generated from backfilled bars inside the ~11-minute TTL enters. Because _backfill runs BEFORE the first poll in tick() (1156 vs 1163), _last_ticks is empty and S falls back to the stale backfill bar close (699-701): entry_S, target_underl~
    EVIDENCE: run_options_shadow.py:14, 622-658, 628, 699-701, 1152-1170; feed.py:152-158, 173-176, 287-304.
    REPRO: Restart 10:08 after a signal bar at 10:00 (ttl 10 -> expires 10:11): SPY 630.00 at the bar vs 631.50 live (+0.24%). target_move 0.35% -> target_under = 630*1.0035 = 632.21, i.e. only +0.11% from the true spot - p_thesis/EV computed for a materially different trade; implied_vol(live mid, S=630) is solved with a 24 bp spot error.
    FIX: Thread the backfill flag: on backfill-origin signals either require a fresh poll first (defer entry one tick and use tick.last) or journal-and-drop; never let S fall back to a backfill bar close for entry pricing.
- [medium] RUNNER-8 Lane merges drop the merged signal's protective context (print_minute / planned_exit_minute) while making rule b unreachable via never-invalidating l~
    CLAIM: A same-underlying same-direction fire only appends the lane tag (run_options_shadow.py:640-650); sig.notes is discarded. A macro_reaction signal merged into an existing SPY index_trend position therefore never sets pos.print_minute -> exit rule f (post-print forced decision) is unreachable for that exposure, and a merged last30's planned_exit_minute is lost. Simultaneously _thesis_valid requires ALL constituent lane~
    EVIDENCE: run_options_shadow.py:640-650 (merge), 871-887 (all-lanes-invalid rule), 830 and 1077-1083 (print_minute/planned_exit read from the ORIGINAL entry only); lanes.py:327-328, 533-534; shadow.py:128-132 (merge record carries the signal but nothing re-reads it into the position).
    REPRO: CPI day: index_trend SPY call entered 09:40, macro_reaction call fires 09:45 and merges. minutes_since_print stays None (rule f dead) and thesis_valid can never go False (macro verdict False forever) -> the position's loss exits reduce to h/g/clocks only; with c inert this removes 2 of the 4 documented loss-exit paths.
    FIX: On merge, fold protective notes into the position (min of planned_exit_minutes, set print_minute if absent) and count only lanes that CAN invalidate in the all-invalid vote (skip constant-False lanes), or track per-lane notes.
- [medium] RUNNER-9 _roll_day latches the new day before rebuilding state: one transient exception mid-roll kills the whole day's lanes (stale latches, yesterday's clock~
    CLAIM: self._day = day is set at the TOP of _roll_day (run_options_shadow.py:405) before the session-calendar journal write (423-425, append_jsonl can raise OSError on a locked/full disk), lane construction (439-453), and the position rebuild (456-473). If anything throws mid-roll, tick()'s containment eats it, and the day==self._day guard (403-404) prevents the roll from EVER re-running that day: lanes stay yesterday's in~
    EVIDENCE: run_options_shadow.py:401-405 (early latch), 423-425 (unguarded ledger write), 456-473 (positions cleared then rebuilt), 1210-1218 (tick exception containment loops forever).
    REPRO: Failure window = everything between line 405 and 488 executing exactly once per day; a single OSError at line 423 at 08:30 costs 100% of the day's lane fires (funnel already at 0.14 fires/day).
    FIX: Build the new day's state into locals and commit (including self._day) only after success; or wrap the roll so a failure resets self._day to the previous value for retry next tick.
- [medium] RUNNER-10 Zero-bid books make losers unmarkable: the exit engine is never consulted and the eventual forced exit grades at a stale mark or even entry NBBO
    CLAIM: _fresh_option_quote requires bid > 0 on both the quote and chain paths (run_options_shadow.py:853-869). A deep-OTM long that collapses to bid=0/ask=0.05 - the normal end state of a loser - returns None forever: the position loops mark_no_quote (988-1003), decide_exit is never called, no exit rule can fire, and the position rides until the post-close force-flat, which prices at the LAST GOOD mark or entry NBBO (889-9~
    EVIDENCE: run_options_shadow.py:853-869 (bid>0 required), 988-1003 (no-quote loop marks nothing), 899-909 (_force_exit fallback chain last-mark -> entry NBBO).
    REPRO: Loser at bid 0/ask 0.05 from 13:00: true realizable value 0.00; forced flat at 16:0x books bid from the last two-sided mark (e.g. 0.15 at 12:55) -> worst-ledger loss understated by $15/contract and the exit timestamp is fiction.
    FIX: Accept a one-sided book as a valid mark (bid=0 is a price: realizable value 0; mid = ask/2 for optimistic), pass it to the engine, and let the ladder exit; keep no_quote only for a truly absent/crossed book.
- [low] RUNNER-11 C5 shock deque survives the day roll: the overnight gap fires a spurious price_shock and pollutes the trailing baseline
    CLAIM: _bar_moves is not cleared in _roll_day (run_options_shadow.py:401-425 clears seven other state dicts; 355/593-610 persist). The first RTH bar of a new day computes move vs YESTERDAY's last close = the overnight gap, which beats max(0.002, 3x 1-min mean) on most days -> a spurious reval_trigger/price_shock journal row whenever positions carried overnight, and the gap value enters the trailing-21 mean, inflating the t~
    EVIDENCE: run_options_shadow.py:355, 401-425 (no clear), 592-610 (move computed vs hist[-1] across the day boundary; hist.append includes the gap).
    REPRO: SPY gap +0.60%: move=0.006 > floor 0.002 (3x base ~0.0009) -> fires; baseline mean over 21 then rises from ~0.0003 to ~0.00057 -> 3x base = 0.0017, still below the 0.002 floor for SPY, but for a higher-vol single name (base 0.0012) the polluted threshold rises from 0.0036 to ~0.0050, suppressing real 40 bp shocks.
    FIX: Clear _bar_moves (and _s_freeze) in _roll_day, or skip the move computation when the previous bar's day differs.
- [low] RUNNER-12 _trading_days_held hardcodes the 960 close: entry-day fraction overstated up to ~4x on half days; after-hours minutes counted as trading time
    CLAIM: frac_entry = (960 - entry_minute)/390 (run_options_shadow.py:258) ignores the session calendar the rest of the file threads through (self._close_min): on a 13:00 half day an entry at 12:00 books frac_entry = 240/390 = 0.615 trading days vs the true 60/390 = 0.154. Same-day after-hours exits also count post-close minutes (251). Downstream this only feeds hold_trading_days -> theta_paid_usd in the exit decomposition (~
    EVIDENCE: run_options_shadow.py:247-260 (960/390 hardcoded, 916 and 1125 call sites pass no close), shadow.py:181-182 (theta_paid consumer). Cross-check: IWM 07-13 exit hold_trading_days 0.012821 = 5/390 exactly (correct on a normal day).
    REPRO: Half-day entry 12:00, exit next day 10:00: reported = (960-720)/390 + (600-570)/390 = 0.692; true = (780-720)/390 + 30/390 = 0.231 -> theta_paid overstated 3.0x.
    FIX: Pass self._close_min into _trading_days_held and use it for both frac_entry and the same-day cap; clamp now_minute at the close.
- [low] RUNNER-13 load_shadow_config's promised per-key type tolerance does not exist; typo'd keys are silently ignored
    CLAIM: The docstring says 'wrong-typed values fall back per key' but the code accepts any non-None value (run_options_shadow.py:182). A future hunter.yaml options_shadow block with watch: 'SPY' yields ['S','P','Y'] as the watch set (silent full starvation of lanes 1/1b); a non-numeric poll_seconds crashes main() at float() before the heartbeat exists; a misspelled key (reval_shock_mult vs reval_shock_mults) is silently dro~
    EVIDENCE: run_options_shadow.py:173-186 (only `k in cfg and v is not None`), 1196 (float(cfg['poll_seconds']) at startup), 539 (watch iterated as a string would be).
    REPRO: watch: 'SPY' -> [str(s).upper() for s in 'SPY'] = ['S','P','Y']; Tradier returns no quotes for 'S'? (S IS a real ticker - Sprint successor SentinelOne 'S') -> lanes armed on the wrong instrument with no error anywhere.
    FIX: Validate per key against type(DEFAULTS[k]) with a journaled fallback, and journal unknown keys in the block.
- [low] RUNNER-14 Sparse-tick bar fidelity: OR height biased low from <=6 last-trade samples/min, and thin names emit echo bars at a stale last price stamped with rece~
    CLAIM: Bars are built from ~6 last-trade samples per minute, so bar high/low understate true extremes: InPlayORB's OR height is biased low, which both understates the profit target (target = 2x OR, lanes.py:428) and makes the 'close beyond OR' break easier to hit on sampling noise (more marginal fires into an understated target - a double EV penalty). Separately, HunterFeed stamps every tick with receive time (feed.py:272)~
    EVIDENCE: feed.py:160-193 (OHLC from last-trade samples), 270-275 (ts_epoch=now, no trade timestamp), lanes.py:399-434 (OR built from those bars); brief's own 6-ticks/min bound.
    REPRO: For a name with true 1-min range R and ~6 uniform samples, expected sampled range ~= R*(1 - 2/(6+1)) ~= 0.71R -> OR (5-min max-min of sampled extremes) understated ~10-20%; target = 2x OR inherits the same bias while the break threshold (sampled close beyond sampled OR edge) loosens.
    FIX: Carry Tradier's trade timestamp into Tick and skip bars whose last is stale; optionally require a minimum tick count per OR bar, or widen the OR by a registered sampling-correction factor swept on the paired replay.

### events-calendar (atlas/options/events.py + atlas/options/session_calendar.py) - 9 findings
- [high] EVENTS-CALENDAR-1 2027 rollover silently disarms the entire macro-protection layer and the holiday calendar [VERIFY-PENDING (refuters died on limit)]
    CLAIM: Every date source is 2026-only with no forward path: FOMC days are a hardcoded 2026 tuple with NO fetch mechanism at all (even a working FRED key would not supply 2027 FOMC dates), the CPI/NFP fallback branch ignores its `year` argument and always emits the 2026 tables, and session_calendar._fallback_days(2027) returns {}. On 2027-01-01 - with no code change and no warning - in_blackout always returns None, MacroRea~
    EVIDENCE: atlas/options/events.py:47-50 (FOMC_DECISION_DAYS_2026 hardcoded), events.py:250-251 (fallback branch emits CPI_2026_FALLBACK/NFP_2026_FALLBACK regardless of `yr`), events.py:265-266 (load_events always builds FOMC from the 2026 tuple); atlas/options/session_calendar.py:132-140 (_fallback_days filters the 2026 tables by startswith(year) ~
    REPRO: py -3 repro (run this session): upcoming_events(2027-01-04 09:00 ET, horizon_days=30) == [] using the default tables; _fallback_days(2027) == {}; is_trading_day(date(2027,1,1), days={}) == True - New Year's Day (a Friday, NYSE closed) classified as a trading session.
    FIX: Add a build_events year-coverage assertion at load (warn/journal when max(event date) < today+30d); make the fallback branch fail LOUD (journal event) when yr != 2026; add the published 2027 FOMC calendar when the Fed releases it and a FRED-driven CPI/NFP fetch (finding 2) so only FOMC needs an ann~
- [medium] EVENTS-CALENDAR-2 FRED upgrade path is dead (no key) so a static mirrored table is the sole live source, with zero drift detection - and 2026 already proved reschedule~
    CLAIM: The designed primary source (FRED release/dates, ids 10/50) never runs because config/ contains no 'fred' key (grep verified); runtime/econ_calendar.json has been source=fallback_bls_2026 since 2026-07-09 and the weekly 'refresh' just rewrites the same constants. BLS reschedules are not hypothetical: the Jan-2026 CPI moved Feb 11 -> Feb 13 and the Jan-2026 Employment Situation landed on WEDNESDAY Feb 11 (post-shutdo~
    EVIDENCE: atlas/options/events.py:239-249 (key present -> fetch; absent -> silent fallback), events.py:171-180 (_read_fred_key returns None - no fred stanza in config, grep of config/ found 0 matches); runtime/econ_calendar.json: "source": "fallback_bls_2026", "fetched_at": "2026-07-09T19:32:21-04:00". Reschedule precedent: dol.gov/newsroom/econom~
    REPRO: Cache age at today's 08:30 ET open = 6.54 days (< 7.0) -> today's session served the 7-day-old fallback verbatim; the FRED branch has executed 0 times since deployment.
    FIX: Register a free FRED API key in config/credentials.local.yaml (fred: api_key), journal {source, fetched_at} into the day-roll session_calendar record, and page (existing data-plane pager) when source=fallback while any table date is within 2 days.
- [medium] EVENTS-CALENDAR-3 FOMC presser modeled as a point event: 'all clear' at 14:46 lands mid-press-conference, and lane 3 enters on a 31-minute-stale pre-presser direction
    CLAIM: The presser is an EconEvent at 14:30 with the same [-5m,+15m] hard window as an 08:30 print, so blackout clears at 14:46 while the press conference Q&A (~14:30-15:15) is still live and still moving markets. MacroReactionLane's held FOMC signal (direction measured 14:00->14:15, i.e., the pre-presser knee-jerk) then enters at >=14:46 mid-event - the classic FOMC whipsaw (presser reverses the decision move) is entered ~
    EVIDENCE: atlas/options/events.py:52 (FOMC_PRESSER_TIME = 14:30), events.py:81-84 (HARD_AFTER_MIN=15 applied uniformly), events.py:141-148; atlas/options/lanes.py:463-501 (measure 14:00->14:15, hold, emit on first blackout-free bar); runtime/backtest_out/sweep_ledger.jsonl line 39 ('FOMC emission lands ~14:46'); atlas/options/exit_engine.py:136,33~
    REPRO: Probe run on 2026-07-29: in_blackout = pre_print at 13:00 through fomc at 14:45, None at 14:46 - a gap-free 105-minute corridor ending 29+ minutes before a typical presser ends (14:30 + ~45 min ≈ 15:15). Direction staleness at entry: measured over [14:00,14:15], entered >=14:46 -> 31+ minutes stale, spanning the single most reversal-pron~
    FIX: Model the presser as an interval event (hard window [14:25, ~15:15]) or gate lane 3's emission on direction re-validation: emit only if sign(S_now - S_14:30ref) agrees with the measured move; both changes also give print_window_flat a truthful clock through the Q&A.
- [medium] EVENTS-CALENDAR-4 CPI/NFP blackouts are 100% premarket - print days have zero RTH event protection, enabling an opposite-direction double-fire at the open
    CLAIM: For 08:30 releases the entire protective structure ([07:30,08:30) pre_print + [08:25,08:45] hard) expires 45 minutes before the 09:30 open, and the entry gate independently rejects anything before 09:30 anyway - so the blackout machinery does literally nothing on CPI/NFP days. The volatile post-print open (the actual risk window) is treated as a normal session: IndexTrendLane can fire on a 09:3x noise-band break of ~
    EVIDENCE: Numeric probe on real CPI day 2026-07-14: in_blackout = pre_print 07:30, cpi 08:25-08:45, None at 08:46/09:30/09:31/09:45 - every RTH minute clean. Entry gate run_options_shadow.py:632 (minute >= 570 required) makes the premarket windows unreachable for entries; lanes see only RTH bars (run_options_shadow.py:578-583). Lane arming and mea~
    REPRO: On 2026-07-14 the blackout expired at 08:45:00 ET, 45 min before the first bar a lane can act on; P(entry blocked by CPI blackout) = 0 for every 08:30 release in the 2026 tables (24 of 24 CPI+NFP events).
    FIX: Feed events_today into the non-macro lanes: on print days, either raise IndexTrendLane's range_percentile/RVOL requirement for the first 15 RTH minutes or extend a soft pre_print-like veto over [09:30, 09:45] for lanes that are not the designated reaction lane. Cheap, targeted, and preserves lane 3~
- [low] EVENTS-CALENDAR-5 Overnight-grant 'next trading day' is computed with a weekday-only skip, ignoring the session calendar one import away
    CLAIM: run_options_shadow computes the day whose events justify an overnight hold by skipping weekends only; after a Friday it resolves to a holiday Monday instead of the true next session Tuesday. Since releases never land on full market holidays, the error can only FALSELY SUPPRESS a grant (named_tomorrow=False when the real next session has a catalyst), never falsely grant - and no 2026 table date falls on a Tuesday aft~
    EVIDENCE: scripts/run_options_shadow.py:946-948 (`while nd.weekday() >= 5: nd += timedelta(days=1)`) with scal already imported at line 52 and is_trading_day used 500 lines earlier (line 425); consumed by exit_engine overnight exception via named_catalyst_tomorrow (run_options_shadow.py:1111, exit_engine.py:346-347).
    REPRO: Repro run: after Friday 2026-09-04 the loop yields 2026-09-07 (Labor Day, is_trading_day=False); the true next session is 2026-09-08. Any event on 09-08 would be invisible to the grant gate (none exists in 2026 - CPI is 09-11).
    FIX: Replace the weekday loop with `while not scal.is_trading_day(nd, days=days): nd += timedelta(days=1)` using the day-roll `days` map.
- [low] EVENTS-CALENDAR-6 An 'open' Tradier row missing close_min silently overrides the known half-day table with a 16:00 close
    CLAIM: session_close_minute gives a fetched open row absolute precedence: if the row's close_min is absent/unparseable it returns CLOSE_MIN_DEFAULT (960) without consulting HALF_DAYS_2026_FALLBACK - so a malformed Tradier calendar row on 2026-11-27 or 2026-12-24 would set a 16:00 close on a 13:00 session. Every derived clock then shifts ~3 hours late: the 15:45 overnight evidence rule, the 0DTE sell clock, planned-exit cap~
    EVIDENCE: atlas/options/session_calendar.py:87-96 - the `row is not None and status=='open'` branch returns cm or CLOSE_MIN_DEFAULT and never reaches line 96's HALF_DAYS fallback; runtime/market_calendar.json currently holds {'status':'open','close_min':780} for 2026-11-27/12-24 (verified). Downstream blast radius: run_options_shadow.py:419-422 (E~
    REPRO: session_close_minute(date(2026,12,24), days={'2026-12-24':{'status':'open'}}) == 960; truth is 780 (returned correctly when the row is absent). Delta = 180 minutes of clock skew on a half day.
    FIX: In the open-row branch, when close_min is missing/invalid fall back to HALF_DAYS_2026_FALLBACK.get(iso, CLOSE_MIN_DEFAULT) instead of a bare CLOSE_MIN_DEFAULT.
- [low] EVENTS-CALENDAR-7 options_close_minute is dead code whose docstring claims it powers the late-close marking mode
    CLAIM: options_close_minute (+15 for index ETFs) has ZERO production consumers - only the module docstring and tests reference it. The runner independently derives its late-close boundary as close+10 (_late_close_flat_min) and checks INDEX_UNDERLYINGS itself, so the module's advertised role ('what makes the post-close option-NBBO marking mode a daily feature') is false. Maintenance trap: editing LATE_CLOSE_EXTRA_MIN change~
    EVIDENCE: Grep across all *.py: options_close_minute appears only in session_calendar.py:8,99 and tests/test_session_calendar.py:18,19,27. Runner's parallel implementation: run_options_shadow.py:336,420 (close+10) and 958 (INDEX_UNDERLYINGS membership check), used at 990-993 for late_close_flat.
    REPRO: Consumer count outside tests = 0; the runner flats at 970 (16:10) while the module says options quote to 975 (16:15) - a deliberate 5-min buffer that lives only as an unexplained constant in the runner.
    FIX: Either have the runner derive _late_close_flat_min from options_close_minute(d, sym) - 5 (single source, self-documenting buffer) or delete the function and correct the module docstring.
- [low] EVENTS-CALENDAR-8 is_trading_day is journal-only inside the shadow: holiday sessions run ungated, and a test comment enshrines the wrong reason it is safe
    CLAIM: The shadow computes is_trading_day once per day roll and only writes it into the journal; nothing gates ticking, lane arming, or entries on it. On weekday full holidays (2026: Sep 7, Nov 26, Dec 25... and Apr 3, Jul 3 already past) the 07:30 CT task launches and the core runs, protected only by the absence of fresh ticks/quotes (staleness gates). That held on 2026-07-03 (journal shows no session damage) but it is an~
    EVIDENCE: scripts/run_options_shadow.py:425 (sole consumption - journal field); no other is_trading_day call in the runner (grep). Journal confirms weekend sessions run and roll days: {'event':'session_calendar','day':'2026-07-11','trading_day':false} / '2026-07-12' false. Wrong comment: tests/test_session_calendar.py:40-41.
    REPRO: grep count of gating uses of trading_day in run_options_shadow.py = 0 (1 journal write only); 3 weekday full-holiday launches remain in 2026 (Sep 7, Nov 26, Dec 25).
    FIX: At day roll, when trading_day is False put the core into an idle mode (skip lane construction and entry processing, keep the heartbeat) and fix the test comment to state the true reason (market closed -> no bars).
- [low] EVENTS-CALENDAR-9 A fresh fallback-source cache pins out an available upgrade source for up to 7 days in both calendars
    CLAIM: Both refresh_calendar implementations check cache freshness BEFORE checking whether a client/key is available, so a cache written on a degraded run (source=fallback) short-circuits fetch attempts for up to CACHE_MAX_AGE_DAYS even when the better source has recovered. events.py documents this as intended ('the first week with a key present upgrades it automatically'); session_calendar inherits it silently: one Tradie~
    EVIDENCE: atlas/options/events.py:231-237 (fresh cache returned before key check at 239); atlas/options/session_calendar.py:152-158 (fresh cache returned before client check at 159); daily client injection that gets ignored: run_options_shadow.py:416.
    REPRO: Worst-case staleness after a single failed weekly fetch = 7.0 days x 86400 s (events.py:79 / session_calendar.py:45) during which client-present refresh_calendar calls return the degraded data 5 trading days in a row.
    FIX: Treat source as part of freshness: a cached record with source=fallback_* is considered stale whenever a client/key is currently available (one-line condition alongside the age check).

### iv-archive - 9 findings
- [high] IV-ARCHIVE-1 iv_rank is None for every possible entry until at least 2026-07-24, and the documented VIX/VXN warm-up fallback was never built - the EV-max selector~ [votes: REFUTED(cosmetic), REFUTED(cosmetic)]
    CLAIM: The selector's only vol-regime input (iv_rank at tenor_dte=2) is dead: the archive holds 4 in-band sessions (2026-07-10/13/14/15) against a >=10-session gate, so iv_rank returns None on every call through at least the 2026-07-23 EOD snapshot (first non-None during the 2026-07-24 session); the module-docstring's promised FRED VIX/VXN percentile fallback (iv_archive.py:4-5, 203) does not exist anywhere in atlas/, and ~
    EVIDENCE: atlas/options/vendor/volatility.py:137 (len(history)<10 -> None); atlas/options/iv_archive.py:199-207 (wrapper), 4-5 and 203 (fallback claim); scripts/run_options_shadow.py:718 (tenor_dte=2), 816 (vix=None); grep of atlas/ shows FRED only in events.py (CPI/NFP dates), no VIX feed - confirmed by docs/OPTIONS_SYSTEM_NOW.md:104-106; live DB~
    REPRO: tol = max(2, 2//4) = 2 -> band [0,4]. In-band distinct SPY days as of 07-16 open = 4 (07-10,13,14,15) < 10 -> None. Adding sessions 07-16,17,20,21,22,23 reaches 10 after the 07-23 15:45 ET snapshot -> first non-None value available during the 2026-07-24 session.
    FIX: Pick one and register it: (a) build the VIX percentile warm-up input the docstring promises (FRED VIXCLS daily series gives 252 sessions of history on day one) and wire it as the regime signal until the archive matures; or (b) delete the fallback claim from iv_archive.py:4-5,203 and accept a stated~
    VOTE REFUTED (cosmetic): The warm-up arithmetic is correct (4 in-band sessions < 10, iv_rank=None until ~07-24; FRED fallback unbuilt) but the finding's core impact claim is false: iv_rank is decision-inert by construction. It is a single per-underlying scalar applied as an identical constant score offset to every contract~
    VOTE REFUTED (cosmetic): The facts check out (4 in-band sessions, iv_rank=None live, no FRED VIX feed - all reproduced read-only), but the [high] "real edge leak" claim is immaterial on the live path: iv_rank is decision-inert by construction. It enters select_contract as one scalar per call and only ever subtracts a unifo~
- [high] IV-ARCHIVE-2 Even after warm-up, iv_rank is structurally a no-op: a per-underlying constant subtracted from scores that are only sorted within one selector call [votes: UPHELD(degrades-edge), UPHELD(cosmetic)]
    CLAIM: select_contract applies score -= 0.2*max(0, iv_rank-50) using the SAME iv_rank value for every candidate row (it is a scalar function argument, not per-contract), and score is consumed solely by scored.sort(...)[:top_n] inside that one call - there is no absolute score threshold anywhere (all gates are Stage-1/2, pre-score) and the runner never compares scores across underlyings or signals (grep shows pick.score is ~
    EVIDENCE: atlas/options/selector.py:240-241 (constant penalty), 264-266 (score used only for sort + top_n slice; no threshold); selector.py:123-128 (iv_rank is a call-level argument); scripts/run_options_shadow.py:737 (single iv_rank per select_contract call), 756/809/838 (score only logged, never compared cross-call); docs/OPTIONS_SYSTEM_FLOW.md:~
    REPRO: Two candidates with base scores 18.4 and 15.1, iv_rank=80 -> penalty 0.2*(80-50)=6.0 applied to both -> 12.4 and 9.1: identical ordering, identical top_n, identical entry decision. This holds for every possible iv_rank value by construction (order-invariance of x -> x - c).
    FIX: Decide what vol regime should do and wire it where it can act: (a) as an entry-level input - scale the required EV edge or the p_thesis haircut by regime; (b) as a per-contract term - interact iv_rank with the contract's vega*dte so high-rank penalizes long-vega picks relative to short-dated ones (~
    VOTE UPHELD (degrades-edge): Claim survives independent re-derivation and empirical execution. The iv_rank penalty (selector.py:240-241) subtracts the same scalar constant from every candidate in a select_contract call; score is consumed solely by sort+[:top_n] (selector.py:264-266) with no absolute threshold (all 17 gates fir~
    VOTE UPHELD (cosmetic): Confirmed by direct execution against the real code. iv_rank is a per-call scalar (runner computes it once per signal at run_options_shadow.py:715-718 and passes it at :737; selector.py:123-128), the penalty at selector.py:240-241 subtracts the same constant from every surviving candidate, and scor~
- [medium] IV-ARCHIVE-3 Tenor band [0,4] mixes weekend-calendar-deflated Friday IVs into a min-max rank, distorting future ranks by tens of points
    CLAIM: iv_history at tenor_dte=2 accepts tenors 0-4; for daily-expiry ETFs the archived tenor set is weekday-dependent (Mon-Wed nearest = 2-DTE, Friday nearest = 3-DTE spanning a weekend). Calendar-time IV quoting deflates weekend-spanning short-dated IV by roughly sqrt(trading/calendar days), so Fridays systematically contribute artifact lows. Because iv_rank is a min-max statistic, one Friday artifact pins the range floo~
    EVIDENCE: atlas/options/iv_archive.py:76-79 (band = max(2, 2//4) = 2), 167-185 (nearest-in-band per day); live DB: SPY 2026-07-10 (Friday) rows tenors {0,3,4} with 3-DTE atm_iv=0.0612 vs 0-DTE 0.1179 same day, while Mon-Wed 2-DTE values are 0.1501/0.1192/0.1126; scripts/snapshot_iv.py:77 (nearest-3 expirations produce the weekday-dependent tenor s~
    REPRO: Weekend deadtime check: 0.1179 * sqrt(1/3) = 0.0681 ~ recorded 0.0612 - the Friday 3-DTE low is a quoting-convention artifact, not regime. Rank distortion: history [0.0612, 0.1501, 0.1192, 0.1126], current 0.113 -> rank (0.113-0.0612)/(0.1501-0.0612)*100 = 58.3; on weekday-consistent 2-DTE-only history [0.1501, 0.1192, 0.1126] -> rank 1.~
    FIX: Normalize archived short-dated IV to trading-time before ranking (iv_norm = iv * sqrt(calendar_dte / trading_dte)) or build a constant-maturity 2-day forward-variance interpolation across the two nearest expirations; exclude 0-DTE rows from rank history outright (keep them in iv_daily for other use~
- [medium] IV-ARCHIVE-4 iv_rank ranks YESTERDAY'S snapshot, not live IV - it will miss the same-day vol spikes that matter most at entry time
    CLAIM: IVArchive.iv_rank with current_iv=None (how the runner calls it) defaults current to history[-1], the most recent EOD archive row - i.e., yesterday's (or Friday's) 15:45 IV. Intraday entry decisions would therefore rank stale vol: on a day when IV gaps up 50-70% (exactly when buying premium becomes expensive), the rank still reflects yesterday's calm. The one thing a vol-regime input must catch is the thing this wir~
    EVIDENCE: atlas/options/iv_archive.py:199-207 (current_iv default = history[-1]); scripts/run_options_shadow.py:718 (called with no current_iv); the runner already self-computes per-contract IV from live mids (tradier_data.py:70-72 note: 'decision greeks are SELF-COMPUTED from live mid') so a live current_iv is available at the call site.
    REPRO: Live archive: IWM in-band IV 2026-07-10 -> 2026-07-13 went 0.1375 (4-DTE, nearest available) -> 0.2387 (2-DTE), a +74% day-over-day vol spike on the exact day of the system's only entry; a rank computed at entry on 07-13 would have used 07-10's 0.1375 as 'current'.
    FIX: Pass the live self-computed ATM IV (already available in the selector's per-contract loop, or the nearest-strike live IV at signal time) as current_iv, ranking it against the archived history; keep the archive as the history source only.
- [medium] IV-ARCHIVE-5 10-session warm-up gate contradicts the module's own 60-session claim and activates a pure-noise min-max statistic with a -10-point score penalty
    CLAIM: The vendored gate opens at len(history)>=10 while iv_archive.py's own docstring says rank 'become[s] meaningful at ~60 sessions'. A min-max rank over 10 exchangeable observations is close to uniform noise - the newest point is the running max with probability 1/10 (rank=100) and the running min with probability 1/10 (rank=0). From the first eligible day (~2026-07-24) the selector would apply up to -0.2*(100-50) = -1~
    EVIDENCE: atlas/options/vendor/volatility.py:137 (gate = 10); atlas/options/iv_archive.py:4-5 ('meaningful at ~60 sessions'); atlas/options/selector.py:240-241 (penalty magnitude vs typical ev_pct scores in the 10-30 range).
    REPRO: For n=10 iid draws, P(newest = max of all 10) = 1/10 -> rank exactly 100 -> penalty 10.0 points, ~half the magnitude of a typical base score, from pure chance. At n=60 that probability is 1/60 and the min-max range is far more stable.
    FIX: Raise the None gate to ~60 sessions (matching the stated design), or shrink the penalty by a maturity factor, e.g. multiply by min(1, (n-10)/50), so early ranks carry proportionally less weight; register the change before the first eligible session (2026-07-24) to avoid an unregistered behavior fli~
- [medium] IV-ARCHIVE-6 Hardcoded tenor_dte=2 means iv_rank can only ever mature for SPY/QQQ/IWM - InPlayORB hunt names get None forever
    CLAIM: The runner requests tenor 2 (band [0,4]) for every underlying. Monthly-expiry names archive tenors 9-128 (nearest-3 expirations), matching zero rows in the band - history=[] and iv_rank=None permanently, regardless of warm-up. Weekly-expiry names only get snapshotted on days they happen to be on the hunt list, so accruing 10 distinct in-band days is practically unreachable. Net: the one lane that trades single names~
    EVIDENCE: scripts/run_options_shadow.py:718 (tenor_dte=2 for sig.underlying, all lanes); atlas/options/iv_archive.py:76-79 (band [0,4]); scripts/snapshot_iv.py:77,99-126 (nearest-3 expirations; hunt-list names snapshotted only when listed); live DB tenor distribution for non-ETF names: 9,10,11,16,17,18,37,38,39,65,66,67,94,95,128 - all outside [0,~
    REPRO: A monthly-expiry hunt name with snapshot tenors {37,38,39}: band query BETWEEN 0 AND 4 returns 0 rows -> history length 0 -> None on every call, forever. Even a weekly name needs 10 distinct hunt-list appearances; at the observed churn (~25-name cap, mostly one-day appearances) that is months away.
    FIX: Request the rank at the symbol's own nearest archived tenor (e.g. iv_rank(sym, tenor_dte=latest(sym).tenor_dte)) or at a per-symbol-class tenor; alternatively normalize all short tenors to a constant-maturity 2-day figure at snapshot time so one bucket serves everyone. For coverage, snapshot any na~
- [medium] IV-ARCHIVE-7 Snapshotter dies silently: missing non-shadow token or failed quote batch exits 0 with no heartbeat or page - unrecoverable history gaps extend the w~
    CLAIM: snapshot_iv.py runs on config/tradier.local.yaml (the NON-shadow token, distinct from tradier_shadow.local.yaml the runner uses). If that file is absent/empty it prints 'nothing to do' and exits 0; if the single get_quotes batch fails it prints and exits 0. No heartbeat file is written, and the data-plane pager (heartbeat schema 2) covers only the runner - so a revoked/expired second token or a transient outage at 1~
    EVIDENCE: scripts/snapshot_iv.py:169-172 (token absent -> exit 0), 180-182 (quote batch failure -> exit 0), no heartbeat write anywhere in the file; docs/OPTIONS_SYSTEM_NOW.md:87-88 (token note); config/ listing shows both tradier.local.yaml and tradier_shadow.local.yaml exist today; scripts/run_options_shadow.py:716-720 (bare except swallows any ~
    REPRO: One missed session pushes first-non-None from 2026-07-24 to 2026-07-27 (next trading day); k missed sessions push it k trading days - with zero observable symptom until someone queries the DB. Currently 07-11/07-12 are correctly absent (weekend guard) but 07-16's run tonight has no success check.
    FIX: Have snapshot_iv write runtime/iv_snapshot_heartbeat.json (day, rows written, per-symbol failures) on every run including skip paths, and teach alert_watch to page next morning if the last trading session's stamp is missing or total==0. Cheap, off the decision path, closes the only silent-death vec~
- [low] IV-ARCHIVE-8 Solved-from-mid fallback floors 0-DTE time at half a day, understating a 15:45 ET 0-DTE IV by ~7x when the ORATS block is missing
    CLAIM: _atm_iv_for_expiration uses T = max(dte/365, MIN_T_YEARS) with MIN_T_YEARS = 0.5/365. At the 15:45 ET snapshot a 0-DTE option has ~15 minutes of life (2.85e-5 years), but the solver is given 1.37e-3 years - 48x too much time - so the implied vol that reproduces the observed ATM mid comes out at ~14% of the true figure. It only fires when the vendor ORATS IV is absent/zero (the preferred path is fine), but a fired in~
    EVIDENCE: scripts/snapshot_iv.py:40 (MIN_T_YEARS = 0.5/365), 58 (T = max(dte/365.0, MIN_T_YEARS)), 59-64 (fallback solve), 43-65 (ORATS-first ordering, verified by tests/test_iv_archive.py:148-163); dte computed from calendar dates (line 79) so dte=0 all day on expiration day.
    REPRO: sqrt(T_true/T_floor) = sqrt(2.85e-5/1.37e-3) = 0.144 via the ATM approximation C ~ 0.4*S*sigma*sqrt(T): a true 40% IV solves to ~5.8%. Such a row entering a 10-point min-max history as the min would inflate every later rank exactly as in IV-ARCHIVE-3's repro.
    FIX: Compute remaining time to 16:00 ET intraday for dte==0 (minutes/(60*24*365)) instead of a half-day floor, or simply skip the solved fallback for dte==0 rows (drop the row; the archive already tolerates missing tenors).
- [low] IV-ARCHIVE-9 The 4,289-row legacy seed is dead weight: 2 days of history in a tenor-30 bucket that no production query can ever read
    CLAIM: seed_from_options_db imported ~2,150 symbols x 2 days (2026-06-29/30, SPY through 07-02) under tenor_dte=30. Every production read uses tenor_dte=2 -> band [0,4], which excludes tenor 30 categorically; and even a hypothetical tenor-30 query finds 2-4 days, far under the 10-session gate. So 97% of iv_daily rows are unreachable by the only consumer, the 'archive is the moat' premise is unmet, and day_count(symbol) (wh~
    EVIDENCE: atlas/options/iv_archive.py:39 (SEED_TENOR_DTE=30), 15-25 (seed rationale), 217-226 (day_count counts all tenors when tenor_dte=None); live DB probe: 4,293 rows at tenor 30 vs 126 tradier rows at tenors 0-4; per-day counts: 06-29 -> 1,934 rows, 06-30 -> 2,355, then nothing until 07-10; sweep_ledger line 81 already concedes 'options_iv.db~
    REPRO: SPY: day_count('SPY') = 8 (06-29,06-30,07-01,07-02,07-10,07-13,07-14,07-15) but in-band-for-tenor-2 distinct days = 4 - a 2x overstatement of warm-up if anyone gates on day_count.
    FIX: No action needed on the data (harmless rows, correct provenance tags). If any warm-up telemetry is added (per IV-ARCHIVE-7), compute it as in-band distinct days for the production tenor, never day_count or raw row counts; optionally document in the module docstring that the seed is inert for tenor-~

### grader - 10 findings
- [high] GRADER-1 PASS bar is a fixed-threshold filter, not a significance test: ~36% false-PASS per lane at N=25 under a zero-net-edge null, ~63% with nightly re-grad~ [VERIFY-PENDING (refuters died on limit)]
    CLAIM: The go-live evidence gate (n>=25 AND mean net>0 AND gross>0 AND PF>=1.2 AND top_day_share<0.5, grade_options_shadow.py:135-136) has almost no discriminating power at its own sample floor, and its power barely improves with N because PF>=1.2 and mean>0 are fixed effect-size thresholds rather than N-scaled significance tests. Worse, the verdict is recomputed nightly on all history (line 73: load_exits(day=None)), whic~
    EVIDENCE: C:/path/to/shadow-options-trading-lab/scripts/grade_options_shadow.py:135-136 (passes = ...), :73 (all-history nightly re-grade), :36-38 (PASS_N_MIN=25, GO_LIVE_N=50); selector gates at atlas/options/selector.py:52-53 (spread_max_pct=0.10), :75-77 (ev_pct_min=10, ev_vs_spread_mult=2, p_profit_min=0.40); registered ba~
    REPRO: Analytic: population PF=1.2 under Normal(mu,sigma) <=> mu/sigma=0.0727, i.e. t-stat 0.36 at N=25 (one-sided p~0.36), 0.51 at N=50, 0.73 at N=100 - never near significance. Monte Carlo (100k sims mirroring lines 133-136, 2 trades/day): zero-NET-edge null at N=25 -> P(PASS)=0.360-0.362 across sigma=10-30% of premium; 5 lanes -> 0.893. Sequ~
    FIX: Replace the mean>0 + PF>=1.2 pair with an N-scaled test: PASS requires the one-sided 95% bootstrap (or t) lower confidence bound of WORST mean net > 0, applied at pre-registered looks only (N=25, 50, 100) or via an anytime-valid sequential test (e-values / SPRT) since the grader inherently peeks ni~
- [high] GRADER-2 Evidence machine cannot conclude on any actionable timescale: N>=25 per lane at ~0.14 fires/day means 6 months to years per verdict, and no pooled cr~ [VERIFY-PENDING (refuters died on limit)]
    CLAIM: The grader's only unit of verdict is the individual lane, each needing N>=25 exits (line 135-136) with go-live talk at 50. At the live funnel rate (~0.14 lane fires/day, 1 entry in 3 sessions), even if EVERY fire landed in one lane the first HELP/HURT verdict is ~180 trading days away; spread across 4 active lanes it is multi-year. The grader compounds failure mode B (starvation): it is the component that decides wh~
    EVIDENCE: C:/path/to/shadow-options-trading-lab/scripts/grade_options_shadow.py:36-38 (PASS_N_MIN/GO_LIVE_N), :135-138 (per-lane-only verdict, GO_LIVE_N only in an f-string), :190-201 (no journal/fire-count fields in the returned card); live rate: runtime/options_shadow_exits.jsonl has exactly 1 row after 3 sessions; runtime/o~
    REPRO: 25 / 0.14 fires/day = 179 trading days ≈ 8.5 calendar months to the FIRST N=25 verdict if one lane monopolizes every fire; at the observed 1 completed trade per 3 sessions it is 75 sessions ≈ 3.5 months per lane only if all fires stay in index_trend, and >1 year for any second lane.
    FIX: Add a pooled ALL-lanes pseudo-row (deduped by position_id so merged trades count once) graded with the same falsification gates - it reaches any given N 4-5x faster and answers the owner's actual question ('does the whole system have edge') first; adopt a sequential e-value test so every single tra~
- [medium] GRADER-3 Concentration gate drifted from its pre-registered definition, and its abs-day-sum construction both misses registered concentration and false-FAILs ~
    CLAIM: The registered pass bar says "not concentrated in <20% of days" (sweep_ledger lane registrations), but the code implements max|day_net| / sum(|day_net|) < 0.5 over single days (lines 133-136). These disagree in both directions. (1) Looser than registration: two days each carrying 45% of total |net| = 90% concentrated in 20% of a 10-day sample, yet top_day_share=0.45 PASSes. (2) Stricter than intended for clustered l~
    EVIDENCE: C:/path/to/shadow-options-trading-lab/scripts/grade_options_shadow.py:133-136 vs runtime/backtest_out/sweep_ledger.jsonl line 32 (opts-lane1_index_trend pass_bar: "not concentrated in <20% of days"; same text on lines 33-35 for the other lanes).
    REPRO: Repro 1 (looser): day |net| sums [45,45,2,2,2,1,1,0.5,0.5,1] -> top_day_share = 45/100 = 0.45 < 0.5 PASS while top 20% of days hold 90%. Repro 2 (stricter): Monte Carlo with real edge (net +8%/trade, sigma 20%): 25 trades over 5 days -> topday-gate survival 0.891; over 4 days -> 0.576 (i.e. 42% false-FAIL from this gate alone). Repro 3 (~
    FIX: Implement the registered metric: sort day nets, require that the top ceil(0.2*D) days contribute < X% of total positive net (or use per-trade, not per-day, concentration: max single-trade |net| share); compute concentration on PROFITS (positive day sums) rather than |sums| so loss days cannot fake ~
- [medium] GRADER-4 All-USD lane stats after the premium-cap removal let one expensive contract dominate mean, PF and top-day share - no %-of-premium metric exists
    CLAIM: Every graded quantity (net mean, PF, win contributions, day sums) is in dollars for exactly 1 contract (lines 104-136), and since opts-tweak-remove-premium-cap-v1 removed the $350 premium cap the ledger can mix $0.50 contracts with $30+ contracts. Dollar P&L variance scales with premium squared, so a single expensive trade can carry ~94% of a lane's variance and effectively decide the verdict while 24 cheap trades a~
    EVIDENCE: C:/path/to/shadow-options-trading-lab/scripts/grade_options_shadow.py:104-136 (all stats from net_pnl_usd, 1 contract); atlas/options/shadow.py:174-180 (mult=100*contracts, contracts always 1); runtime/backtest_out/sweep_ledger.jsonl line 52 (opts-tweak-remove-premium-cap-v1: premium_max_usd 350 -> None, "shadow must~
    REPRO: 24 trades on a $1.50-premium contract (per-trade $ sd ≈ 0.20*150 = $30) plus 1 trade on a $30.00 contract ($ sd ≈ 0.20*3000 = $600): variance share of the single expensive trade = 600^2 / (600^2 + 24*30^2) = 360000/381600 = 0.943 - one trade is 94% of the lane's statistical weight.
    FIX: Grade the PASS bar on percent-of-premium returns (net_pnl_usd / (entry ask*100), available from ledgers[name].entry_fill) - mean %, PF on %, day sums in % - and keep the $ columns as descriptive detail; alternatively winsorize/size-normalize per trade. This is also what makes lanes with different u~
- [medium] GRADER-5 Schema falsification gate only protects ledgers+rule; day, lanes, and decomposition fields fail soft (0.0 / epoch-string / '?'), so the 2026-07-09 si~
    CLAIM: _malformed (lines 58-69) checks only ledgers.{worst,base,optimistic}.{net,gross} and rule. Every other consumed field degrades silently: decomposition.spread_paid_usd / theta_paid_usd default to 0.0 (lines 121-123) - a renamed key would zero the spread tax and cost_share_of_losses would quietly report ~0.0 or None with no alarm, exactly the "grader reads fields the builders never wrote" failure the module docstring ~
    EVIDENCE: C:/path/to/shadow-options-trading-lab/scripts/grade_options_shadow.py:58-69 (_malformed scope), :115 (day fallback), :98 (lanes or ['?']), :121-123 (decomposition .get defaults 0.0), :131-132 (no assertion on cost share); docstring lines 59-61 states the anti-drift purpose.
    REPRO: str(1783964709.21)[:10] = "1783964709" (the real IWM exit's ts_epoch) - not a date; two day-less exits 1s apart get distinct "days", so 25 day-less rows yield top_day_share ≈ max|net_i|/sum|net_i| which passes <0.5 for any non-degenerate sample, while the same rows with real days might fail. A decomposition key rename makes spread_paid_t~
    FIX: Extend _malformed to require day (ISO-parseable), non-empty lanes, and a decomposition dict with both *_usd keys - quarantine loudly on absence exactly like ledgers/rule; add a falsification bound to #1 (e.g. flag if spread_paid_total == 0 while n>0, or if cost_share is None/0 on a lane with losses~
- [medium] GRADER-6 Expired-unexited process-gap positions are invisible on expiry night (expy >= today_d skips them), which is precisely the 0DTE common case; terminal ~
    CLAIM: Line 175 treats expiry == grading day as "still a live position", so a 0DTE position orphaned by a mid-day crash (the exact process-gap scenario this reporter exists for) does not appear in expired_unexited on the nightly run of that day - it surfaces only if the grader runs again with a later --day, and the EOD report that night sees a clean scorecard. For a system whose IndexTrend/Last30/Macro lanes trade DTE 0-1,~
    EVIDENCE: C:/path/to/shadow-options-trading-lab/scripts/grade_options_shadow.py:175-176 (if expy >= today_d: continue), :178-185 (rows[-1] S, no exercise threshold), :158-160 (OCC auto-exercise comment); test pins only the later-day case (tests/test_grade_options_shadow.py:131 grades day 2026-07-20 for a 2026-07-17 expiry).
    REPRO: 0DTE SPY position entered 2026-07-16 09:45, runner crashes 10:00, option expires 16:00 same day; nightly grade --day 2026-07-16: date.fromisoformat('2026-07-16') >= expiry 2026-07-16 -> continue -> expired_unexited == [] on the only night anyone looks. Same position appears 2026-07-17 only if a run happens then.
    FIX: Treat expiry == today as expired when grading after the session close (compare against session close time, or simply use expy > today_d only when an explicit --day in the future is requested and expy >= today_d -> expired for the nightly run); use the max-ts quote row at/after 15:55 ET when availab~
- [low] GRADER-7 No dedup of exit rows by position_id - a duplicated line double-counts into n, mean, PF; duplicate entry position_ids silently last-win for the risk-~
    CLAIM: The grader pools every non-quarantined exit row (lines 77-99) with no uniqueness check on position_id, so any duplicate append (second runner instance despite the session guard, manual replay/backfill concatenation, ledger repair) counts the same trade twice in lane stats and N-progress. flags_by_pid (line 75) is a dict comprehension over entries, so duplicated entry pids keep only the last row's flags - a halted_un~
    EVIDENCE: C:/path/to/shadow-options-trading-lab/scripts/grade_options_shadow.py:75 (dict comprehension last-wins), :77-99 (no seen-pid set); scripts/run_options_shadow.py:917-921, :1126-1130 (single-writer discipline).
    REPRO: Duplicate the one real exit row: index_trend n becomes 2, net_worst_sum -32.0, N-progress 2/25 - one decision graded as two.
    FIX: Track seen position_ids while iterating exits; quarantine repeats into a duplicate_exits list parallel to malformed/identity (loud, excluded); assert uniqueness of entry pids the same way.
- [low] GRADER-8 Merged multi-lane exits are fully double-counted in every lane: lane verdicts are correlated (aggravating multiple comparisons) and cross-lane sums e~
    CLAIM: A merged position (lanes=[a,b]) contributes its full P&L and one count to BOTH lanes (lines 98-99; deliberately pinned by tests/test_grade_options_shadow.py:139-149). Consequences: (1) the same trade can carry two lanes toward N=25 simultaneously, so 5 lane PASS tests are positively correlated and the family-wise false-PASS from GRADER-1 cannot be corrected by treating lanes as independent; (2) summing lanes' net_wo~
    EVIDENCE: C:/path/to/shadow-options-trading-lab/scripts/grade_options_shadow.py:98-99; tests/test_grade_options_shadow.py:139-149 (equal net_worst_sum in both lanes); atlas/options/shadow.py:128-132 (merge keeps both tags, one entry).
    REPRO: One merged trade with WORST net -16: index_trend and last30 each report n=1, net -16; 'sum over lanes' = -32 vs true -16 (2x overstatement per merged trade).
    FIX: Add per-lane n_shared (rows with len(lanes)>1) and a pooled ALL row deduped by position_id (also serves GRADER-2); if merges become common, grade fractional attribution (1/len(lanes) weight) as a sensitivity column.
- [low] GRADER-9 theta_paid sign convention inconsistent with spread_paid: 'paid' totals are negative, and theta is excluded from the cost-visibility falsification en~
    CLAIM: build_exit_record computes theta_paid_usd = entry_theta_day * hold_days * 100 with theta_day negative, so the ledger stores a NEGATIVE number under a name meaning cost paid, while spread_paid_usd is positive; the grader sums both as-is (lines 122-123) and the scorecard shows theta_paid_total: -0.36 next to spread_paid_total: 2.5 - consumers adding 'total costs' get spread minus theta. Falsification #1 (cost_share_of~
    EVIDENCE: C:/path/to/shadow-options-trading-lab/atlas/options/shadow.py:182 (theta_paid = entry_theta_day * ... with negative theta_day); scripts/grade_options_shadow.py:122-123, :131-132 (spread-only ratio), :151; live confirmation: runtime/options_shadow_scorecard.json theta_paid_total -0.36.
    REPRO: Real IWM row: theta_day=-0.2825, hold=0.012821 trading days -> -0.2825*0.012821*100 = -0.362 -> 'theta_paid_usd': -0.36 (a negative payment).
    FIX: Negate at the builder (or take abs in the grader with a comment), and extend falsification #1 to (spread_paid + theta_cost)/losses so both taxes are visibility-checked.
- [low] GRADER-10 Cosmetic/operational: verdict trichotomy conflates failure causes; n_today zeroes on a past-midnight run; GO_LIVE_N appears only in a display string
    CLAIM: FAILING is emitted for any n>=25 lane missing ANY gate (line 155) - a profitable-but-day-concentrated lane and a money-losing lane read identically, hiding which gate failed; today = date.today() when --day is omitted (line 101), so the common 'run the nightly after midnight' case reports n_today=0/net_worst_today=0.0 for the session just ended; and GO_LIVE_N=50 exists only inside the n_progress f-string (line 138) ~
    EVIDENCE: C:/path/to/shadow-options-trading-lab/scripts/grade_options_shadow.py:155 (single FAILING branch), :101 (date.today fallback), :36-38 and :138 (GO_LIVE_N usage); registered bar text in sweep_ledger.jsonl line 32.
    REPRO: Lane with mean net +$5, PF 1.5, but top_day_share 0.55 at n=25 -> verdict FAILING, indistinguishable from mean -$5/PF 0.6.
    FIX: Emit failed_gates: [list] alongside the verdict; default --day to the last session date (from the exits' max day) instead of the wall clock; add an explicit GO_LIVE_READY state (or n>=GO_LIVE_N flag) once the GRADER-1 statistical fix defines what evidence suffices.

### replay-lab (atlas/options/replay.py + scripts/run_overnight_lab.py - the paired-replay counterfactual lab, its grid/AB/efficiency/questions/LLM stages, and the ATLAS-OvernightLab schedule/guard plumbing) - 12 findings
- [high] REPLAY-LAB-1 Quote capture stops at the live exit, so both replay stages are one-sided: no variant can ever be credited for holding longer than the live engine [VERIFY-PENDING (refuters died on limit)]
    CLAIM: The runner deletes the position and stops writing quote rows at the live SELL (run_options_shadow.py:1126-1131), so the counterfactual 'variant would hold' outcome is never graded: stage 1 forces uncrossed variants to exit at the ACTUAL exit bid (run_overnight_lab.py:254, 301), and stage 1b marks them 'unexited' with n counted but no P&L (replay.py:160-163, 196-198), making net_worst_sum a comparison over different ~
    EVIDENCE: runtime/options_shadow_quotes/2026-07-13.jsonl contains exactly 1 row, ending at the 13:45 ET exit, though the IWM 07-15 put lived two more days; atlas/options/replay.py:160-163 ('unexited' contributes no dollars); scripts/run_overnight_lab.py:254 (fallback_bid = actual exit bid); grade_options_shadow.py:124 merely counts variant_would_h~
    REPRO: The one real trade: live exit at bid 1.72, net −$16.00 (worst). Post-exit rows available to grade the hold counterfactual: 0. The truth-validation that the cut was correct had to be done manually outside the lab - the lab itself could not produce it, and never will under current capture.
    FIX: Keep polling an exited contract's NBBO at the same cadence until EOD (0DTE) or a registered horizon (e.g. next-day close for DTE>=1), writing post-exit quote rows flagged post_exit:true. Then grade 'unexited' variants at a defined horizon fill instead of censoring, and report per-variant paired mea~
- [high] REPLAY-LAB-2 thesis_valid is replayed, not recomputed, and rule (b) precedes every swept parameter in both engines - the A/B has zero discriminating power on the ~ [VERIFY-PENDING (refuters died on limit)]
    CLAIM: replay.py:118 passes ext's stored thesis_valid to every variant; rule (b) is parameter-free and fires before the swept rules in both ladders (exit_engine.py:357 before :360 stop_frac and :396 p_regain_min; exit_engine_legacy.py:270-271 before :273). Any path where the lane flips thesis before a price threshold crosses produces byte-identical outcomes across ALL pre-registered variants, so the AB can never measure wh~
    EVIDENCE: atlas/options/replay.py:118 (thesis_valid=bool(ext.get('thesis_valid', True))); atlas/options/exit_engine.py:356-361, 395-398; atlas/options/exit_engine_legacy.py:269-274; runtime/overnight_lab_report.json exit_engine_ab stage.
    REPRO: 2026-07-15 report: all five variants (legacy_v1, legacy_v1_stop50, v2_default, v2_pregain_15, v2_pregain_35) return identically {n:1, net_worst_sum:-16.0, rule_mix:{b_thesis_invalid:1}, unexited:0} at exit_minute 825 - 100% of real-data history yields zero divergence. The exit state itself shows every other rule would have HELD (ev_hold ~
    FIX: Store the raw per-lane thesis inputs (or per-lane verdicts) in ext so variant thesis policies can be recomputed (e.g. 'b requires 2 consecutive invalid marks', 'b with a noise-band re-entry check'), and register at least one AB variant that varies the thesis mechanism. Combined with REPLAY-LAB-1's ~
- [high] REPLAY-LAB-3 Grid cohort gate (>=2 quote rows) excludes every first-mark exit - the grid has replayed 0% of all real trades and systematically excludes the fast-c~ [votes: REFUTED(cosmetic)]
    CLAIM: run_overnight_lab.py:288 skips positions with fewer than 2 stored rows. The runner sets last_mark_ts at entry (run_options_shadow.py:829) and marks at 300s cadence (841-851), so any position exited on its first mark - precisely the hair-trigger cuts the platform needs to study - produces exactly 1 quote row and is excluded from the grid forever. The stage's lifetime coverage of real trades is zero.
    EVIDENCE: scripts/run_overnight_lab.py:288 (if e is None or len(rows) < 2 or entry_mid <= 0); runtime/overnight_lab_report.json: exit_grid n_exited=1, n_replayed=0, n_skipped_no_path_or_malformed=1; runtime/options_shadow_quotes/2026-07-13.jsonl = 1 row (513 bytes).
    REPRO: IWM 293P: entry 13:40 ET (minute 820), first and only mark at 13:45 (825) = entry + 300s, which was the SELL mark. rows=1 < 2 → skipped. Report confirms: the grid stage aggregated an empty cohort while exit_engine_ab replayed the same position fine with 1 row.
    FIX: Lower the gate to >=1 row (a 1-row path still supports the paired convention: variant crossing on that row vs fallback to actual), or better, fix the root cause: mark immediately on entry (write a t=0 quote row from the entry NBBO) so every position has an entry-anchored path, and pair that with RE~
    VOTE REFUTED (cosmetic): The cited mechanics are real (first-mark exits store exactly 1 quote row, and run_overnight_lab.py:288 excludes them), but the defect claim fails independent re-derivation: the single stored row is byte-identical to the exit NBBO (runner writes quote row and exit record from the same bid/ask in the~
- [medium] REPLAY-LAB-5 exit_engine_ab silently drops engine_errors - a variant that crashes on every row is indistinguishable from a patient one
    CLAIM: replay_decide_exit counts engine exceptions (including TypeError from PositionView(**kw) construction, which sits inside the try at replay.py:145-149) and returns engine_errors, but exit_engine_ab discards the field: per-variant aggregates track only n/net_worst_sum/rule_mix/unexited (replay.py:180) and per_position rows keep only rule/exit_minute/net_worst (replay.py:201-202). If v1/v2 PositionView drifts a field, ~
    EVIDENCE: atlas/options/replay.py:145-149 (construction + decide inside try, continue on exception), :180 (agg dict lacks engine_errors), :201-202 (row_out drops engine_errors/marks_replayed/skipped_no_ext).
    REPRO: Introspection today keys on 'mu_hat' and 'trailing' (replay.py:87-89). Add any new required field to v2's PositionView without updating replay kw → every v2 row raises TypeError → report shows v2_default {n:1, unexited:1, net_worst_sum:0.0} - identical in shape to a genuine hold-to-end, engine_errors=1 invisible.
    FIX: Sum engine_errors (and marks_replayed/skipped_no_ext) into each variant's aggregate and include them in per_position rows; print a loud line when any variant's engine_errors > 0 (it should be exactly 0 for the live engine by definition).
- [medium] REPLAY-LAB-6 AB variants replay half-day paths with normal-day clocks - future clock-rule divergence will be misattributed to engine differences
    CLAIM: The live runner rebuilds params per day via ExitParams.for_close(close_min) (run_options_shadow.py:421), shifting every close-anchored clock on half days (13:00 close → eod_flat 12:45). AB_VARIANTS pin plain ExitParams() (run_overnight_lab.py:97-103) = 16:00 clocks. A half-day path where live sold at 12:45 (overnight_evidence_rule) replays under 15:45 clocks: every variant holds past the stored path end and reports ~
    EVIDENCE: scripts/run_options_shadow.py:419-421 (per-day for_close); scripts/run_overnight_lab.py:97-103 (AB_VARIANTS with default ExitParams()); exit_engine.py:160-169 (for_close derivation: eod_flat_min 945 → 765 for a 780 close); ext schema (run_options_shadow.py:1099-1115) carries after_hours but not close_min.
    REPRO: for_close(780): eod_flat_min = 15*60+45 + (780−960) = 765 (12:45) vs default 945 (15:45) - a 180-minute clock error on every replayed half-day mark.
    FIX: Write close_min (or the session-calendar day type) into ext at mark time and have replay_decide_exit construct params via engine.ExitParams-equivalent for_close per position day; AB_VARIANTS become param FACTORIES (close_min -> params) rather than frozen instances.
- [medium] REPLAY-LAB-7 Runner writes the quote row only after a successful decide_exit - engine-error marks update live peaks invisibly, breaking replay peak fidelity in ex~
    CLAIM: In _reval_positions, peak_mid/peak_bid update at run_options_shadow.py:1026-1028, but an engine exception continues at :1094 BEFORE write_quote at :1097 - so live high-waters can include marks that never enter the stored path. The replay rebuilds peaks only from stored rows (replay.py:109-111), so after any live engine error the replayed peak_bid understates live, and the d2* cost-basis backstop (d2_armed = peak_bid~
    EVIDENCE: scripts/run_options_shadow.py:1026-1028 (peaks) vs :1088-1094 (except → continue) vs :1097 (write_quote); atlas/options/replay.py:107-111 (peaks from stored rows only) and :12-16 (fidelity contract).
    REPRO: Sequence: mark1 bid 2.10 (engine raises → no row stored, live peak_bid=2.10 > entry_ask 1.88 → live d2_armed=True), mark2 bid 1.85 → live sells d2_costbasis_backstop (1.85 <= 1.88); replay sees only mark2: peak_bid=1.85 < 1.88 → d2_armed=False → HOLDs. Same engine, same params, opposite decision.
    FIX: Move ledger.write_quote before the decide_exit try in _reval_positions - the snapshot depends only on the mark, not the decision. (Also lets the replay see exactly the marks live peaks saw.)
- [medium] REPLAY-LAB-4 exit_grid best_variant has no N floor and tie-breaks lexicographically - an all-tie cohort names 'stop-0.75_take2.0_trail25' best by alphabet
    CLAIM: run_overnight_lab.py:305-306 picks best = max by (net_worst_sum, key) over any nonempty variant. With short holds, most paths cross no threshold, every variant falls back to the actual exit bid, all sums tie, and the 'best' is simply the lexicographically greatest key. There is no minimum-N gate, so at n=1 a single trade names a nightly headline 'best=' (printed at :721), inviting a miscalibrated tweak - the platfor~
    EVIDENCE: scripts/run_overnight_lab.py:305-306 (max with string tie-break), :719-721 (summary prints best); grader law N>=25 (grade_options_shadow / memory: validation sample = N=25).
    REPRO: Computed with the real 18-variant grid, all sums = −16.0 (the actual IWM outcome, all thresholds uncrossed): best = 'stop-0.75_take2.0_trail25' - loosest stop, widest take, trail on - purely by string comparison ('7'>'5'>'2', 'take2.0'>'take1.0', 'trail25'>'off').
    FIX: best_variant = None unless (a) the top variant strictly beats ACTUAL and all ties, and (b) cohort n >= a registered floor (reuse N>=25). Report paired mean difference vs ACTUAL with sign counts instead of raw sums.
- [medium] REPLAY-LAB-11 Grid fills only at stored-mark granularity - systematically flatters take variants and penalizes stop variants vs continuous execution
    CLAIM: Marks exist at 300s (DTE>=1), 120s (0DTE afternoon), 60s (last 30 min) cadence (run_options_shadow.py:841-851). _replay_variant sells at the FIRST STORED row crossing the threshold (run_overnight_lab.py:246-253): on a trending path the stored stop crossing is deeper than the threshold (recorded loss > threshold loss, up to 5 minutes of adverse drift), while the stored take crossing is above the threshold (recorded g~
    EVIDENCE: scripts/run_options_shadow.py:841-851 (cadence); scripts/run_overnight_lab.py:246-253 (first stored crossing at that row's bid); the quote path IS the mark path (write_quote once per mark, run_options_shadow.py:1097).
    REPRO: A put mid decaying 0.5%/min against a -25% stop: threshold crossed at t+50min mid-move, but if the previous mark was at -24% the next stored row (300s later) records ≈ -27.5%; on a $1.88 entry mid that is ~$4.70/contract of extra recorded loss per event charged to the stop variant, while the symmetric take event credits the take variant ~
    FIX: Either mark positions at the 10s quote-poll cadence into the quote path (cheap: rows are small) or grade grid variants with an execution-lag correction (interpolate crossing between adjacent marks and haircut the fill), and state the residual bias in the report.
- [medium] REPLAY-LAB-12 variant_would_hold is a write-only flag: the overnight/EV counterfactual is counted, never graded
    CLAIM: The exit engine emits variant_would_hold (the unrestricted-overnight arm's divergence, exit_engine.py:349-350), the exit record stores it, and the grader merely increments overnight_divergences (grade_options_shadow.py:124). No lab stage grades the hold outcome to any horizon at any fills - and none can, because quote capture stops at exit (REPLAY-LAB-1). The registered promise that variants are 'measured in paired ~
    EVIDENCE: atlas/options/exit_engine.py:349-350; scripts/grade_options_shadow.py:120-124 (count only); no stage in run_overnight_lab.py touches variant_would_hold (grep over the lab: absent).
    REPRO: All-history overnight divergences: the one exit has variant_would_hold=false, so the counter has never even incremented - but had it, the report would still contain zero dollars of counterfactual outcome.
    FIX: With post-exit capture (REPLAY-LAB-1) add a lab stage: for each exit with variant_would_hold=true, grade the hold to the registered horizon (next-day close) at worst-ledger fills and report the paired delta - that is the actual evidence the overnight gates need.
- [medium] REPLAY-LAB-8 exit_efficiency mixes mid-based peaks with worst-ledger fills and has no robustness guard - and has never produced a number
    CLAIM: MFE capture = (exit worst fill − entry worst fill)/(peak mark mid − entry worst fill) (run_overnight_lab.py:313-345): the numerator is bid/ask fills while the denominator's peak is a HOLD-mark MID, so a perfect exit scores <1 by construction; the ratio is unbounded (tiny denominators explode it; losing exits produce large negative 'capture'); the mean (not median, no clipping) is dominated by one pathological trade.~
    EVIDENCE: scripts/run_overnight_lab.py:338-345; run_options_shadow.py:1119-1123 (marks written only on HOLD, so the exit row's own mid is never a peak candidate); runtime/overnight_lab_report.json exit_efficiency: {b_thesis_invalid: {n:1, n_with_peak:0, mean_mfe_capture:null}}.
    REPRO: Entry worst 1.88, one HOLD mark peak mid 1.90, exit worst 1.72: capture = (1.72−1.88)/(1.90−1.88) = −8.0 - a −$16 trade reports −800% capture and a mean over 3 normal trades (~0.6 each) flips to ≈ −1.7.
    FIX: Use peak realizable BID (matches worst-ledger grading), require peak − entry_fill above a materiality floor (e.g. one tick), clip capture to [−1, 1.5] or report the median, and count first-mark exits explicitly.
- [low] REPLAY-LAB-10 Guard/schedule interplay verified working (15:25 CT = 16:25 ET passes the 16:15 guard), but missed-start recovery can land inside RTH and silently lo~
    CLAIM: POSITIVE verification: trigger 15:25:00-05:00, DaysOfWeek=62 (Mon–Fri), machine TZ CST - always 16:25 ET (CT/ET shift DST together), 10 minutes past rth_blocked's 16:15 edge; 2026-07-15 ran with LastTaskResult 0, report generated 16:25:03-04:00, and the lab correctly ignores localgate (which brackets 15:40–17:10 ET to keep EXTERNAL users out of the lab's slot - gating on it would lock the lab out, run_overnight_lab.~
    EVIDENCE: Task ATLAS-OvernightLab: StartBoundary 2026-07-13T15:25:00-05:00, DaysOfWeek 62, StartWhenAvailable True, RestartCount 0, WakeToRun True, LastRunTime 7/15 15:25:25 CT, LastTaskResult 0; run_overnight_lab.py:107-112 (rth_blocked), :766-769 (exit 3 pre-heartbeat), :138-149 (_own_rth_allows rationale).
    REPRO: 15:25 CT + 1:00 = 16:25 ET; guard edge 16:15 ET; margin exactly 10 minutes. Recovery scenario: machine off Tue 15:25 CT, boots Wed 09:30 CT (10:30 ET) → task fires → rth_blocked(10:30, weekday)=True → exit 3, no retry until Wed 15:25 CT (which then covers Wed, but Tue's --day slice for questions/scorecard is never produced).
    FIX: In main(), on RTH refusal write a heartbeat with phase='rth_refused' (observability), and either sleep-until 16:16 ET when the refusal is a recovered missed start, or add a second daily trigger at ~20:00 CT as a catch-up (the stages are cumulative and idempotent except the day-scoped questions, whi~
- [low] REPLAY-LAB-9 Lab's self-declared write contract is false: job C writes to catalyst memory and runtime/lab/, beyond 'ONLY the report + lock/heartbeat'
    CLAIM: The module docstring promises 'writes ONLY runtime/overnight_lab_report.json (+ its own lock/heartbeat)' and 'Reads ONLY the shadow ledgers' (run_overnight_lab.py:4-9), but _job_catmem_append appends rows to the catalyst-memory story store and job files land under runtime/lab/ (:555-578, :424-435), and the stage reads news_stream.jsonl and hunt_list.json (:515, :523). The side effects are registered (sweep_ledger op~
    EVIDENCE: scripts/run_overnight_lab.py:4-9 (contract) vs :511 (import MEM_DIR/STORIES_PATH), :555-573 (append + fsync to STORIES_PATH), :424-435 (_write_job_file to runtime/lab/), :515 (news_stream read), :523 (hunt_list read); sweep_ledger.jsonl:82 (registration).
    FIX: Update the docstring to enumerate the real read/write surface (report, lock, heartbeat, runtime/lab/*, catalyst-memory stories append; reads news_stream + hunt_list), keeping 'no order path, never touches llama-swap lifecycle' as the hard invariants.

### premarket-crew - 14 findings
- [high] PREMARKET-CREW-1 No freshness check on runtime/hunt_list.json - lane 2 arms on a stale, possibly days-old AI watchlist [VERIFY-PENDING (refuters died on limit)]
    CLAIM: load_hunt_list (run_options_shadow.py:263-289) reads only the symbols/candidates array and ignores the session_date and generated_ts fields the crew explicitly writes (research_crew.py:370-371). _lane2_candidates consumes it once per day in _roll_day with no mtime/date validation, so if ATLAS-Premarket (05:15 CT) fails to fire, is killed by its 300s cap (see -2), or crashes before the atomic write, the 07:30 CT shad~
    EVIDENCE: run_options_shadow.py:263-289 (no date field read); runtime/hunt_list.json lines 3-4 carry "generated_ts": "2026-07-15T05:17:46-05:00", "session_date": "2026-07-15" - checked on disk 2026-07-16 01:36 CT, the file is yesterday's (mtime 7/15/2026 5:17:46 AM). Mechanism already observed live: runtime/options_shadow.log shows "day 2026-07-11~
    REPRO: date(session_date in artifact) = 2026-07-15; date(today) = 2026-07-16; delta = 1 trading day. Weekend case observed: 07-11 arm used a list generated 07-10 (~33h stale) and journaled stand-downs for its names.
    FIX: In load_hunt_list, parse session_date (fallback: file mtime) and compare to today's ET date; on mismatch return [] and journal {event: hunt_list_stale, session_date, today} so the day arms with an empty lane-2 universe LOUDLY instead of a dead-catalyst one. Also stamp the consumed session_date into~
- [high] PREMARKET-CREW-2 Crew worst-case runtime (gather + 5 sequential 90s provider calls) exceeds the 300s _run_crew kill window - timeout leaves the hunt list silently sta~ [VERIFY-PENDING (refuters died on limit)]
    CLAIM: build_day_briefing._run_crew caps the whole crew subprocess at CREW_TIMEOUT_S=300s (build_day_briefing.py:59,266) and subprocess.run KILLS the child on expiry. research_crew fans out to up to 5 providers SEQUENTIALLY at 90s timeout each (research_crew.py:319,347-355) after a gather phase whose movers scan is self-documented at ~2 minutes (research_crew.py:228). The crew writes hunt_list.json only at the END (atomic,~
    EVIDENCE: build_day_briefing.py:59 (CREW_TIMEOUT_S=300.0), :266-268 (subprocess.run(..., timeout=timeout), any failure -> 'failed', tolerated silently by design); research_crew.py:319 (--timeout default 90 per provider), :347-355 (sequential provider loop), :228 ("Full ~12.5k-file cache scan measured at ~2 min"), :304-308+390 (single atomic write ~
    REPRO: 4 hung providers x 90s = 360s > 300s with zero gather time. Realistic: measured gather+context path ~135-150s (task start 05:15:15 -> hunt write 05:17:46) leaves 165s < 2 x 90s - two hung providers suffice. Absolute worst case ~135 + 5x90 = 585s = 1.95x the budget.
    FIX: Budget-aware fan-out: pass each provider timeout = max(15, (deadline - now)/providers_remaining); or raise CREW_TIMEOUT_S to >= 600s; and/or write hunt_list.json incrementally (after gather with candidates=[], then rewrite after merge) so a kill can never strand yesterday's artifact. Pair with the ~
- [high] PREMARKET-CREW-3 Consensus ranking collapses to ALPHABETICAL ordering on single-model days, and the runner's 10-name cap consumes exactly that order [VERIFY-PENDING (refuters died on limit)]
    CLAIM: merge_consensus sorts by (-models_agree, -confidence, symbol) (consensus.py:256). Free models emit uncalibrated confidence (groq returned 1.0 for 9/10 names), so on a 1-model day every candidate ties at (agree=1, conf=1.0) and the rank becomes symbol-ascending - alphabetical. Downstream, _lane2_candidates sorts by abs(gap_pct) which is hardcoded 0.0 for all crew names (finding -7), so Python's stable sort preserves ~
    EVIDENCE: consensus.py:244-256 (tie-break = symbol asc); runtime/hunt_list.json: the 9 confidence-1.0 candidates appear exactly alphabetically - BLK, BNY, CTAS, ELV, FHN, JNJ, MS, MTB, PNC - then LEDS at 0.9; providers block shows answered=[groq] only. run_options_shadow.py:534-535 (sorted by abs(gap_pct) desc, then [:lane2_max_candidates]); load_~
    REPRO: 07-15 artifact: 9/10 rows conf=1.0, models_agree=1, sorted BLK<BNY<CTAS<ELV<FHN<JNJ<MS<MTB<PNC (pure lexicographic). Had groq returned 12 valid names, rows 11-12 by alphabet (e.g. any T-Z ticker) would be cut by the 10-cap regardless of catalyst quality.
    FIX: Break ties with deterministic packet features rather than symbol: e.g. mover _score (already computed in gather_movers), event magnitude, or |gap|. Journal a degraded-consensus flag when answered<2 so the day's lane-2 universe is known to be single-model. Ignore or shrink self-reported confidence (~
- [high] PREMARKET-CREW-5 Earnings gather is today-only - the packet structurally omits yesterday's after-close reporters, the richest morning-gap cohort [VERIFY-PENDING (refuters died on limit)]
    CLAIM: gather_earnings queries Finnhub with from=to=session (research_crew.py:104), so the packet contains only TODAY'S scheduled reporters. Names that reported YESTERDAY after the close - the classic cohort that gaps at today's open with first-5 RVOL >= 5, i.e. exactly what InPlayORB is built to trade - never appear in the earnings section. The movers section cannot recover them: it screens the PRIOR session's last daily ~
    EVIDENCE: research_crew.py:104 (params from=session, to=session), :121 (hour bmo/amc/dmh is captured but nothing fetches yesterday's amc set); gather_movers computes session-stale stats: research_crew.py:251 prev_close = close.iloc[-2], :257 gap = open/prev_close of the LAST CACHED session (pre-print for an amc reporter). 07-15 artifact confirms t~
    REPRO: For a stock reporting 07-14 amc: earnings section (07-15 calendar) = absent; movers section = 07-14 session bar, gap computed 07-14 open vs 07-13 close = pre-announcement noise; so P(in packet) ~= P(catalyst artifact caught the PR) only. The cohort with the highest expected first5_rvol has ~0 packet coverage on any day the 8-K feed is qu~
    FIX: Fetch two calendar days: from=prev_trading_day, to=session; keep prev-day rows only where hour=='amc' (and today's rows where hour in bmo/dmh), tagging each with reported_last_night=True so models can prioritize them. Cheap (same endpoint, one extra day) and directly widens the lane-2 funnel with i~
- [high] PREMARKET-CREW-8 The deterministic gap-scan is dead code (lane2_scan_symbols=[]) - five flaky free LLMs are the SOLE lane-2 universe source, with no deterministic fal~ [VERIFY-PENDING (refuters died on limit)]
    CLAIM: The scan branch in _lane2_candidates (run_options_shadow.py:500-513) only runs when cfg['lane2_scan_symbols'] is non-empty; the default is [] (run_options_shadow.py:78) and config/hunter.yaml has no options_shadow block (sweep_ledger opts-runner-defaults: 'hunter.yaml block absent -> DEFAULTS rule'), so it is unreachable in production and lane2_gap_min_pct=4.0 is inert config. Consequently the entire lane-2 universe~
    EVIDENCE: run_options_shadow.py:78 ("lane2_scan_symbols": []), :500-513 (guarded by `if scan and self.client`); hunter.yaml grep for options_shadow/lane2 returns nothing; hunt_list.json note "1/5 providers answered (failed: cerebras, gemini, openrouter, zai)"; research_crew.py:328-333 shows movers feed only the packet. The empty-hunt outcome is by~
    REPRO: P(lane-2 universe exists) = P(>=1 of 5 free providers answers AND names survive the allowlist). Observed 07-15: exactly 1/5 answered. If groq had also 429'd: candidates=[], lane2_cands=0, InPlayORB dead for the day while DAL-class earnings gappers trade without the shadow watching.
    FIX: Union the AI list with a deterministic floor: write gather_movers' top rows into the artifact as candidates with a source:'movers_screen' tag (they already pass symbol/liquidity checks), or set lane2_scan_symbols from the movers output. Keeps the crew as ranking/context enrichment rather than a sin~
- [medium] PREMARKET-CREW-4 Earnings packet truncation is alphabetical at 80 rows - heavy earnings days silently drop all late-alphabet reporters
    CLAIM: gather_earnings sorts rows by symbol and truncates to MAX_EARNINGS_ROWS=80 (research_crew.py:125-126). The Finnhub daily calendar routinely exceeds 80 rows in peak season (it includes micro-caps); the cut is lexicographic, not importance-based, so a peak-week packet keeps roughly A-through-J and the models can never nominate a reporter they never see (e.g. any S/T/V name on its own earnings day). This is a hidden, s~
    EVIDENCE: research_crew.py:68 (MAX_EARNINGS_ROWS=80), :125-126 (out.sort(key=symbol); return out[:MAX_EARNINGS_ROWS]). 07-15 had inputs.earnings=77 (hunt_list.json) - 3 rows of headroom on an ordinary mid-July day; peak weeks (late Jan/Apr/Jul/Oct) run 150-300+ rows on this endpoint.
    REPRO: With 200 calendar rows uniformly distributed over starting letters, the 80-row alpha cut retains symbols up to roughly the letter 'H'; P(a given T-name reporter enters the packet) ~= 0. On 2026-07-15 the cap was 96% consumed (77/80) with no earnings-season peak in effect.
    FIX: Rank before truncating: keep all bmo/dmh rows first, then by revenue_estimate (already fetched) descending; or simply raise the cap (the packet cost is trivial for 5 calls/day) and let MAX row length control size.
- [medium] PREMARKET-CREW-6 Movers screen's $5M dollar-volume floor multiplies price by IEX-scale volume - effective floor ~$150-250M consolidated, censoring the small/mid-cap i~
    CLAIM: gather_movers filters `close * volume < 5_000_000` (research_crew.py:255) where volume comes from runtime/harvest_daily_cache parquet built with Alpaca DataFeed.IEX (harvest_inplay_days.py:106) - single-venue volume the platform itself documents as ~2-3% of consolidated tape (lanes.py:34-39) and has a registered law against mixing with consolidated-scale thresholds (audit 2026-07-10). A $5M threshold applied to IEX ~
    EVIDENCE: research_crew.py:255 (`if close * volume < 5_000_000: continue`); harvest_inplay_days.py:8,82-84,106 (DAILY cache fetched with feed=DataFeed.IEX); lanes.py:34-39 + sweep_ledger opts-fix-lane2-rvol-scale-v1 (IEX ~2-3% of tape; consolidated-vs-IEX ~30-50x) - the same class of error this ledger row fixed in the lane gate persists in the cre~
    REPRO: $10 stock trading 20M consolidated shares ($200M true dollar volume): IEX volume ~= 0.025 x 20M = 500k -> close*volume = $5.0M - exactly at the cliff. Every genuine mover below ~$150M consolidated dollar volume is excluded from the packet's movers section.
    FIX: Scale the floor to the venue: use `close * volume < 5_000_000 * 0.025` (~$125k IEX-scale), or better, gate on vol_mult (already scale-free) plus a consolidated ADV floor from a source that has one. Document the cache's venue scale at the constant.
- [medium] PREMARKET-CREW-7 gap_pct=0.0 and catalyst=True hardcoded for every crew name - constant/wrong covariates written to the entry ledger and an inert priority sort
    CLAIM: load_hunt_list synthesizes gap_pct=0.0 (crew rows carry no gap field) and catalyst=True by default (run_options_shadow.py:277,283-284). Effects: (a) InPlayORB stamps notes {gap_pct: 0.0, catalyst: true} on every signal/entry - the journal shows gap_pct 0.0 on DAL's 07-10 earnings-gap morning - so the stage-2 covariates the platform intends to associate at N (opts-catalyst-kind-covariate-v1 discipline) are a constant~
    EVIDENCE: run_options_shadow.py:277,283-284 (defaults), :507 (gap math exists for scan names only), :519-527 (the quote pass that could compute it for hunt names), :534 (sort key); lanes.py:437 (catalyst -> notes only); runtime/options_shadow_journal.jsonl line 19: DAL inplay_orb signal on 2026-07-10 with "gap_pct":0.0,"catalyst":true (DAL gapped ~
    REPRO: Journal line 19: recorded gap_pct=0.0 for DAL on 07-10; the true open gap that day was on the order of +10%, i.e. the stored covariate is off by its entire value for every crew-sourced entry.
    FIX: In the existing average_volume quote pass, also set gap_pct=(q.last-q.prevclose)/q.prevclose*100 for hunt names (guard q.prevclose>0); record catalyst_kind (already retained) and drop the always-True bool from notes or rename it source='crew'. Fix or delete the misleading comment.
- [medium] PREMARKET-CREW-9 Observed first5_rvol values are numerically implausible (PFE 11.59, DAL 65.08) - the RVOL gate's effective scale needs verification before its >=5 th~
    CLAIM: rvol = first5_vol / (average_volume x FIRST5_UCURVE_FRAC=0.04) (lanes.py:387-388,408). Inverting the journal's values: PFE's 11.59 implies first-5-minute volume = 11.59 x 0.04 = 0.46 x its 90d ADV (~18M shares in 5 minutes for PFE) and DAL's 65.08 implies 2.6 x ADV in 5 minutes - both far beyond plausible even for heavy earnings opens (big days front-load ~8-15% of a 2-4x-ADV day into the first 5 minutes, i.e. expec~
    EVIDENCE: runtime/options_shadow_journal.jsonl lines 19-20 (2026-07-10): DAL first5_rvol=65.08, PFE first5_rvol=11.59; lanes.py:53 (FIRST5_UCURVE_FRAC=0.04), :387-388 (baseline = average_volume x 0.04), :399-408 (first5_vol = sum of the five 1-min bar volumes); feed.py:280-304 (backfill bars from Tradier timesales, per-interval volume). Registrati~
    REPRO: PFE: first5 = rvol x 0.04 x ADV = 11.59 x 0.04 = 0.4636 x ADV; with ADV ~= 40M -> ~18.5M shares in 5 min, ~8-12x a plausible busy open (~1.5-2.5M). DAL: 65.08 x 0.04 = 2.60 x ADV in 5 min - more volume in 5 minutes than DAL's entire average day, x2.6.
    FIX: One-morning read-only calibration: for SPY/QQQ plus two hunt names, log first5_vol, Tradier average_volume, and an independent consolidated first-5 reference; recompute the implied U-curve fraction. Then either correct the numerator source, the average_volume interpretation, or refit FIRST5_UCURVE_~
- [medium] PREMARKET-CREW-10 Single-shot average_volume backfill at day-roll - one transient quote failure stands the whole lane-2 universe down for the entire day
    CLAIM: _lane2_candidates runs once per day inside _roll_day (run_options_shadow.py:438). Hunt names arrive with average_volume=0.0 and get exactly ONE batched get_quotes backfill attempt (run_options_shadow.py:519-527); the except path passes silently, and every name still at <=0 is journaled lane2_rvol_baseline_missing and permanently stands down (baseline 0 -> rvol 0, lanes.py:387-389). A single Tradier hiccup at ~08:30 ~
    EVIDENCE: run_options_shadow.py:519-527 (one attempt, `except: pass`), :528-533 (stand-down journal), :438 (called once per _roll_day); lanes.py:387-389 (no baseline -> rvol 0 -> never fires); contrast run_options_shadow.py:95-100 (the registered bounded-retry fix for the analogous bar-backfill failure). Journal shows the stand-down path is live f~
    REPRO: P(day lost | one failed quotes call at arm time) = 1.0 for all crew names simultaneously, since `missing` = all 10 names on first arm and there is no second attempt.
    FIX: Mirror the backfill fix: retry the average_volume fetch on subsequent ticks up to N attempts (module constant, keeps config_hash), only journaling stand-down after the retries exhaust; per-name granularity so one bad symbol doesn't imply a bad batch.
- [medium] PREMARKET-CREW-11 day_briefing.json has zero consumers - the earnings enrichment, scorecard digest, calendar flags, and vix field are dead outputs
    CLAIM: Nothing in the platform reads runtime/day_briefing.json: a full-repo grep finds only the writer and its tests. The shadow runner re-derives session shape, events, and the hunt list itself (from hunt_list.json directly). Consequently the per-symbol Finnhub days-to-earnings calls (up to 25/day), the earnings_flag (built for the PreEarnings lane, which is a stub that never fires), the prior-scorecard digest, opex/witch~
    EVIDENCE: Grep for day_briefing across **/*.py: only scripts/build_day_briefing.py, tests/test_day_briefing.py, tests/test_keep_imports.py. run_options_shadow.py reads HUNT_LIST directly (:65,:263) and computes session/events itself (:401-429). build_day_briefing.py:285-307 (earnings_fn -> Finnhub per symbol), :239 (earnings_flag), :252 (vix null ~
    REPRO: Consumer count = 0 (writers/tests excluded). Daily dead work: up to 25 Finnhub lookups + 1 scorecard read + calendar math whose outputs no decision path touches.
    FIX: Either wire it in (the natural consumer: surface briefing notes + hunt staleness in the runner's day-arm journal row and/or the watch hub, and let the earnings_flag inform lane-2 covariates) or shrink build_day_briefing to the orchestration it actually performs and stop paying the enrichment cost. ~
- [low] PREMARKET-CREW-12 No packet-membership gate on crew symbols - hallucinated tickers can occupy hunt-list slots (the sibling news-flag classifier has the rule; the crew ~
    CLAIM: validate_allowlist enforces syntax (^[A-Z]{1,5}$), a closed kind enum, and sanitization (consensus.py:264-313) but never checks that a symbol was present in the gathered packet (earnings ∪ events ∪ movers). The system prompt merely asks models to name supported symbols (research_crew.py:83-84). A free model inventing a plausible ticker - or echoing one from its training prior - passes straight into hunt_list.json, o~
    EVIDENCE: consensus.py:264-313 (no membership check); research_crew.py:73-85 (prompt-level ask only); sweep_ledger.jsonl opts-svc-news-flag-tap-v1 (the stricter sibling rule); hunt_list.json providers.answered=[groq] (single-model day, so allowlist = the only filter). Downstream blast radius bounded by RVOL/price/selector gates but nonzero (slot d~
    REPRO: Cost of one minted name: 1/10 of the lane-2 universe + ~1 quote symbol all day + 1 IV-surface chain fetch. With MAX_CANDIDATES=25 -> 10-cap, a minted 'AAPL-prior' style name displaces the 10th-ranked real candidate with probability 1 when the list is full.
    FIX: Pass the gathered symbol set into validate_allowlist (or merge_consensus) and drop non-members unless models_agree >= 3; tag survivors source:'model_only' so the runner can deprioritize them in the 10-cap sort.
- [low] PREMARKET-CREW-13 Catalyst-event staleness edges: the 48 calendar-hour cliff kills Friday-night catalysts on Monday, while unparseable timestamps and the older-artifac~
    CLAIM: Three interacting edges in gather_catalyst_events/_events_from_artifact: (1) EVENT_MAX_AGE_H=48 counts calendar hours, so a Friday 17:00 ET 8-K is 61h old at Monday 06:15 and silently dropped - Monday packets systematically lose Friday-evening catalysts (weekend-blind units, the calendar-vs-trading-time class error); (2) an event whose timestamp fails fromisoformat is KEPT (fail-open, research_crew.py:146-147), exem~
    EVIDENCE: research_crew.py:71 (EVENT_MAX_AGE_H=48.0), :144 (timedelta(hours=...) vs now_utc - pure calendar hours), :146-147 (`except ValueError: pass # unparseable ts -> keep`), :197-206 (mtime-sorted glob over catalyst_*.json*, first artifact yielding events wins), :136 (packet section titled 'OVERNIGHT / RECENT CATALYST EVENTS'). Also :143 (`wh~
    REPRO: Friday 17:00 ET -> Monday 06:15 ET = 61.25h > 48h: dropped. Same event with a malformed ts: kept at any age. Weekend Saturday-06:15 run: Friday 17:00 is 13h -> kept, so the STALE weekend artifact (finding -1) can hold events the fresh Monday artifact lacks.
    FIX: Make the age window trading-aware (e.g. 48h + 48h weekend extension when now.weekday()==0, or 'since prior session close'); drop unparseable-ts events instead of keeping them (drop-never-repair, matching the module's own doctrine); restrict the glob to the live state file + the newest archive, or a~
- [low] PREMARKET-CREW-14 Stale comment: load_hunt_list claims 'lane-2 gating reads' the catalyst bool - nothing gates on it anywhere
    CLAIM: run_options_shadow.py:280-281 says the bool catalyst is kept because 'lane-2 gating reads it', and lanes.py:338 repeats 'the bool catalyst above still drives lane gating'. In fact InPlayORBLane consumes it exactly once, into the signal notes (lanes.py:437) - no gate, sort, or threshold reads it, and since every crew row defaults to True it is a constant anyway (finding -7). Under the 2026-07-16 authority change (OWNER~
    EVIDENCE: run_options_shadow.py:280-281 (comment), :284 (bool(r.get('catalyst', True))); lanes.py:336-338 (dataclass comment), :437 (sole read, into notes); grep for `.catalyst` across atlas/options returns only those note lines.
    REPRO: Reads of InPlayCandidate.catalyst outside notes-serialization = 0.
    FIX: Correct both comments to 'observability covariate only', or remove the field in favor of catalyst_kind (which the stage-2 covariate work actually uses).

### news-covariates - 9 findings
- [medium] NEWS-COVARIATES-1 Multi-source items advance NewsTap's single cursor, permanently skipping Benzinga headlines after any Benzinga lag/outage > 120s
    CLAIM: NewsTap's cursor rule ('advances only on SEEN items so a failed poll never skips the window forward') is broken by the multi-source seam: fresh Finnhub/GDELT items advance the SAME cursor that windows the Benzinga fetch, so during a Benzinga/Alpaca outage the cursor keeps marching to ~now on Finnhub's clock and the missed Benzinga window (minus the 120s overlap) is never re-queried. Lost headlines mean permanently l~
    EVIDENCE: atlas/collect/benzinga_news.py:287-296 (poll_once: window start = self.cursor - overlap passed to fetch; cursor = max ts over ALL fresh items); atlas/collect/multi_news_source.py:337-343 (__call__ passes NewsTap's start/end ONLY to the benzinga fetch; finnhub/gdelt use their own cursors, and their items flow back into the merged list New~
    REPRO: Alpaca outage 15:00:00-15:10:00 (fetch_news fail-open -> []). Cursor at outage start = 15:00:00. Finnhub polls deliver fresh stories ts 15:00:50, 15:04:40, 15:08:30 -> cursor = 15:08:30. Alpaca recovers 15:10:10: next Benzinga window = [15:08:30 - 120s, 15:10:10] = [15:06:30, 15:10:10]. Benzinga headlines published 15:00:00-15:06:30 (6.5~
    FIX: Track a per-source cursor for Benzinga inside MultiNewsFetcher (mirror the finnhub/gdelt pattern: advance only on Benzinga items / Benzinga success) and ignore NewsTap's start for the benzinga call, OR advance NewsTap.cursor only on items whose source == 'benzinga'. Either keeps the seam contract (~
- [medium] NEWS-COVARIATES-2 Process-local dedup + unwindowed Finnhub general + Benzinga updated_at re-serves put duplicate stories into the stream across restarts - 158 duplicat~
    CLAIM: NewsTap's fingerprint dedup (_seen) is in-memory only and fetch_finnhub_general ignores its time window entirely, so every daily launch (or intra-day restart with since_hours=1) re-appends any story still in Finnhub's latest-100 or re-served by Alpaca with a bumped updated_at. Duplicates that carry symbols get re-classified by news_flag_tap with headline_ts = the NEW serve time, making a days-old story read as a fre~
    EVIDENCE: PROVEN in live data: runtime/news_stream.jsonl has 126 duplicate-fingerprint groups / 158 extra rows of 3002 (5.3%): e.g. finnhub:8199917 at rows 448 and 1469 (identical ts, different launch days), and Benzinga id 60396668 appended 07-10 with ts 2026-07-10T14:06:25 and AGAIN 07-13 with ts 2026-07-13T09:08:45 (updated_at bumped -> passes ~
    REPRO: 158/3002 = 5.3% of stream rows are duplicate fingerprints. Benzinga case: same fingerprint d48274bdee6c4d53 with headline_ts 3 days apart - if that story had carried symbols, news_shock_15m at 2026-07-13 09:08-09:23 would report a 2026-07-10 story as fresh news.
    FIX: Either persist NewsTap's seen-fingerprint set to a sidecar (bounded, reload on start), or make news_flag_tap keep its own seen-fingerprint set and skip records whose fingerprint it has already classified (cheap: it already reads every stream row). The second also immunizes against any future upstre~
- [medium] NEWS-COVARIATES-3 Per-source health heartbeat counts raw fetched items, so a frozen/stale feed reads permanently healthy - the exact silent-failure class it was built ~
    CLAIM: _record() writes items_last_poll = len(items) and consecutive_zero based on PRE-dedup fetched counts. Finnhub general returns its latest ~100 stories every poll regardless of novelty, so items_last_poll=100 / consecutive_zero=0 forever - a feed frozen on yesterday's content (stale CDN cache, stuck API) is indistinguishable from a healthy one. The 2026-07-12 'silent-zero fix' only catches EMPTY feeds, not STALE feeds~
    EVIDENCE: atlas/collect/multi_news_source.py:304-317 (_record: zero = 0 if items else zero+1; items are the raw adapter output, upstream of NewsTap's fingerprint dedup which is the only place novelty is known); live runtime/news_sources_heartbeat.json shows finnhub items_last_poll=100 consecutive_zero=0 - by construction this will read identically~
    REPRO: Finnhub heartbeat: items_last_poll=100, consecutive_zero=0 on every poll since launch. Actual NEW items per poll (post NewsTap dedup) is typically 0-3 - a 30-100x gap between the reported and productive counts that the heartbeat cannot see.
    FIX: Record max item timestamp age (now - max ts of fetched items) and/or thread the post-dedup new-item count back into the sidecar (NewsTap knows it as `n` in poll_once). Alert on 'newest fetched item older than X minutes' instead of 'zero items'.
- [medium] NEWS-COVARIATES-4 Symbol-state poller hits the nasdaqtrader halt RSS every 45s, 1.33x its own documented <=1/min courtesy limit - throttle/ban risk silently fail-opens~
    CLAIM: poll_symbol_state defaults to --poll-seconds 45 and the launcher passes exactly that, while symbol_state.py's own header states 'nasdaqtrader asks <=1/min' (written when the consumer was the 120s equity cycle). Persistent over-polling risks an IP throttle/block; the breaker then opens (3 fails -> 900s cooldown), halt ts_epoch ages past the 180s freshness bound, and the gate goes silently inert - a missed-halt window~
    EVIDENCE: scripts/poll_symbol_state.py:46 (default=45.0) and scripts/launch_options_day.ps1:160 (--poll-seconds 45) vs atlas/collect/symbol_state.py:20 ('nasdaqtrader asks <=1/min; the ATLAS cycle is 120s'). Breaker: symbol_state.py:251-275 (3 failures -> 900s open); freshness bound: symbol_state.py:99 (<=180s); gate consumer: scripts/run_options_~
    REPRO: 86400/45 = 1920 requests/day vs 1440 at the documented 1/min ceiling (+33%). Freshness margin at 60s cadence would still be 3x the 180s bound (60s poll + write latency << 180s), so slowing down costs nothing.
    FIX: Set --poll-seconds 60 (or 90) in the launcher and the script default. The 180s freshness bound still holds with 2-3x margin; the courtesy limit is respected.
- [medium] NEWS-COVARIATES-5 parse_halt_rss equal-key tie-break (key >= prior) keeps the document-LAST row, which can resurrect a stale non-resumed halt row over the resumed one ~
    CLAIM: For two feed items with the identical (HaltDate, HaltTime) key - e.g. the updated resumed item plus a lingering copy of the original halt item - `key >= prior[0]` makes the LAST item in document order win. RSS feeds conventionally list newest-first, so the resumed (newer) row is iterated FIRST and then overwritten by the stale non-resumed duplicate; the symbol then reads 'halted' for up to _MAX_HALT_AGE_DAYS=7 days,~
    EVIDENCE: atlas/collect/symbol_state.py:183-184 (`if prior is None or key >= prior[0]: latest[sym] = (key, rec)` - document order iteration via root.iter(), no tie-break on resumed status); symbol_state.py:181 (resumed = bool(ResumptionTradeTime)); symbol_state.py:186-189 (only the surviving latest row's resumed flag decides); symbol_state.py:156 ~
    REPRO: parse_halt_rss(xml with items in order [(SYM, 07/16, 09:35:00, Resumption=09:40:00), (SYM, 07/16, 09:35:00, Resumption='')], today=2026-07-16) -> keys equal (739813, 34200.0); second item passes `key >= prior[0]` and overwrites -> output {SYM: {...}} i.e. HALTED, though the newest information says resumed. With the 45s poller re-stamping~
    FIX: On equal keys prefer the row WITH a ResumptionTradeTime (resumption is strictly newer information for the same halt event): replace `key >= prior[0]` with `key > prior[0] or (key == prior[0] and rec['resumed'] and not prior[1]['resumed'])`, and add a fixture test with duplicate-key items in both do~
- [low] NEWS-COVARIATES-6 news_flag_tap LLM join-back is last-record-wins while flag validation is first-row-wins - flag metadata can come from a different headline than the c~
    CLAIM: When one burst carries 2+ headlines for the same symbol, validate_flags keeps only the FIRST model row per symbol (seen-set dedup) but the join-back map keeps the LAST record carrying that symbol (by_sym[s] = rec overwrites in stream order), so the written flag can pair headline A's kind/direction/materiality with headline B's fingerprint/news_id/headline_ts. Downstream, news_kind_recent / headline_age_min mislabel ~
    EVIDENCE: scripts/news_flag_tap.py:236-239 (`for rec in symbol_batch: for s in ...: by_sym[s] = rec` - last wins; comment claims 'newest record carrying it' which is only true if stream order is strictly chronological, and finding 2 shows re-appended old rows break that) vs atlas/crew/news_flags.py:175+185-186 (validate_flags: `if sym ... in seen:~
    REPRO: Burst = [rec1(AAPL, 'FDA approves...', ts 10:00, fp A), rec2(AAPL, 'analyst reiterates', ts 10:03, fp B)]. Model returns [{AAPL, shock:true, kind:fda, ...}] for rec1; written row = {kind:fda, fingerprint:B, headline_ts:10:03} - the fda shock is attributed to the analyst note's identity and timestamped 3 minutes late.
    FIX: Have the model echo an index (build_packet already renders one row per record - number them) or emit one packet row per (record,symbol) pair and join on that index instead of symbol alone; at minimum join to the record whose headline matched, not the last one.
- [low] NEWS-COVARIATES-7 sec_suspended and ssr_active are dead in the live wiring - the poller hardcodes them off and nothing calls update_ssr, yet the entry path and selecto~
    CLAIM: The live poller is constructed with sec_suspensions=False, ssr_self_compute=False, resolver=None, and no live component ever calls update_ssr, so suspensions and ssr are permanently {} in runtime/symbol_state.json (confirmed live). The 'sec_suspended' arm of the entry gate and the ssr_active covariate/features_for path are unreachable code that reads as live coverage. Documented as v1 scope in the poller header, but~
    EVIDENCE: scripts/poll_symbol_state.py:38-39 (SymbolStateParams(... sec_suspensions=False, ssr_self_compute=False), resolver=None); grep shows no update_ssr caller outside attic/; live runtime/symbol_state.json: "suspensions": {}, "ssr": {}; dead consumers: scripts/run_options_shadow.py:733 (sec_suspended branch), atlas/collect/symbol_state.py:379~
    FIX: Either wire the EntityResolver follow-up and an update_ssr caller, or annotate the consumer sites (runner line 732-733, covariate comment 788-790) with 'suspensions/SSR permanently empty in v1' so the coverage gap is explicit at the point of consumption.
- [low] NEWS-COVARIATES-8 NewsFlagsCache doc contract drift: refreshed every ~10s tick (not 'once per reval sweep, never per tick') and queries are O(64) ring scans, not O(1)
    CLAIM: _reval_positions runs on every tick (~10s at the poll cadence) and calls _news_cache.update() unconditionally at its top, so the cache is refreshed per tick - the news_cache.py header's 'NEVER per tick' law is violated in letter (harmlessly in spirit: a no-new-data update is one stat() call). Similarly '_recent' linearly scans a 64-deep ring per query (5 queries per entry, 1 per position per sweep) - bounded, fine, ~
    EVIDENCE: scripts/run_options_shadow.py:1170 (tick() calls self._reval_positions(now) every tick) + 953-954 (update() at sweep top, before the positions loop, regardless of position count) vs atlas/options/news_cache.py:4-7 ('refreshed ... once per reval sweep / at entry - NEVER per tick'); atlas/options/news_cache.py:85-87 (_recent list-comprehen~
    REPRO: At poll_seconds=10 over a 6.5h session: ~2340 update() calls/day vs the implied ~1 per 300s mark sweep (~78/day). Each no-op update costs one os.stat - ~2KB/s of syscall noise, zero correctness impact.
    FIX: Update the docstring to match reality (per-tick stat-guarded refresh is fine), or hoist update() behind a min-interval guard if the letter of the law matters for a future config-hash audit.
- [low] NEWS-COVARIATES-9 news_count_60m has unstable semantics (tier-0 counts per-record, LLM counts per-burst-symbol, cross-provider same-story double-admits) - noisy covari~
    CLAIM: The covariate counts FLAG ROWS, but row production is engine-shaped: tier-0 emits one row per (record,symbol) while the LLM emits at most one row per symbol per burst, and the same story syndicated by Benzinga (tagged [AAPL,MSFT]) and Finnhub-company (tagged [AAPL]) survives CrossSourceDedup because the key includes the full symbol-set - so one story can contribute 1, 2, or 3 counts depending on wire shape and burst~
    EVIDENCE: atlas/options/news_cache.py:93-94 (count = len of flag rows in 60m); atlas/crew/news_flags.py:84-94 (tier-0: one flag per matching record per symbol) vs news_flags.py:175 (LLM: symbol-deduped per burst); atlas/collect/multi_news_source.py:249-252 (_key includes sorted symbol-set: 'AAPL,MSFT' != 'AAPL' -> both admitted); WS5 double-mark p~
    REPRO: Story X on the Benzinga wire tagged [AAPL,MSFT] at 10:00 and via Finnhub company-news [AAPL] at 10:02: CrossSourceDedup keys '<bucket>|<lede>|AAPL,MSFT' vs '<bucket>|<lede>|AAPL' -> both admitted -> 2 classified records -> news_count_60m(AAPL)=2 for one story (and up to 4 if a tier-0 pattern also matches both). An AAPL position gets re-m~
    FIX: For the covariate, count DISTINCT (5-min bucket, normalized lede) stories rather than flag rows (the CrossSourceDedup normalizer is importable and pure); for the accel, dedupe on the same content key instead of the raw fingerprint.

### tradier-feed (atlas/collect/tradier_data.py) - 10 findings
- [high] TRADIER-FEED-1 Retry loop covers only HTTP 429 - timeouts/5xx/connect errors are first-failure-fatal, and the chain-fetch caller kills the whole signal on one blip [VERIFY-PENDING (refuters died on limit)]
    CLAIM: _request's 'retries=2' applies exclusively to 429 responses: any httpx transport exception (ReadTimeout at the 5s scalar timeout, ConnectError) or 5xx raises immediately on the first attempt, and the runner's _enter treats any _chain_rows exception as terminal for the signal (journal chain_fetch_error, return, never re-attempted) - so a single transient network blip during the largest payloads the system fetches (gr~
    EVIDENCE: atlas/collect/tradier_data.py:302-317 (only status_code==429 triggers continue; resp.raise_for_status() at :315 raises 5xx on attempt 0; httpx exceptions from self._client.post/get at :306-308 are never caught); scripts/run_options_shadow.py:705-709 (except Exception -> chain_fetch_error -> return). Corroborating history: the 2026-07-10 ~
    REPRO: A signal costs 1 expirations + up to 3 chain requests = 4 first-failure-fatal calls. With a modest 2% per-request transient failure rate, P(signal killed) = 1-0.98^4 = 7.8%; at 0.14 fires/day each killed signal is ~1/0.14 = 7.1 days of expected funnel output. One retry drops the loss to 1-(1-0.02^2)^4 = 0.16%.
    FIX: Inside _request, retry transport exceptions and 5xx once or twice with a short capped backoff (e.g. 0.5s, 1s) before raising; independently, let _enter retry _chain_rows once ~2s later before abandoning a signal. Both are cheap against a 100/min budget used at ~6-16/min.
- [medium] TRADIER-FEED-2 Retry-After honored uncapped inside a single-threaded loop; HTTP-date form crashes float(); '0' header hammers
    CLAIM: On 429, wait = float(retry_after) has no ceiling - the min(30, 2^attempt) cap applies only to the missing-header fallback - so a vendor Retry-After of 120 (or a buggy large value) blocks the ONE runner thread for that long, stalling every position mark (60/120s cadences on 0DTE afternoons) and gapping every symbol's bars; an RFC-legal HTTP-date Retry-After ('Wed, 16 Jul 2026 14:00:00 GMT') raises an uncaught ValueEr~
    EVIDENCE: atlas/collect/tradier_data.py:309-314: `retry_after = resp.headers.get("Retry-After"); wait = float(retry_after) if retry_after else min(30.0, 2.0 ** attempt)`. The sleeping happens on the same thread that runs core.tick() (scripts/run_options_shadow.py:1210-1218 single loop; marks at :986-1006, bar folding at :1163-1169).
    REPRO: Retry-After: 120 -> two full 60s mark cycles missed for all open positions in the last-30-min window; a 0DTE held into 15:35 ET gets zero marks (and therefore zero exit-ladder evaluations) for 2 minutes exactly when gamma is highest. float('Wed, 16 Jul 2026 14:00:00 GMT') -> ValueError.
    FIX: wait = min(30.0, _f(retry_after, 2.0 ** attempt)) - reuse the module's own tolerant _f so date-form headers fall back to exponential backoff, floor it at ~0.5s, and cap it.
- [medium] TRADIER-FEED-3 Quote parse discards trade/bid/ask timestamps - stale quotes become fresh ticks, so a halted name manufactures flat bars indefinitely
    CLAIM: parse_quotes_batch keeps no trade_date/bid_date/ask_date (fields Tradier supplies as epoch-ms), so HunterFeed must stamp Tick.ts_epoch with POLL time; LiveBarBuilder then folds the SAME unchanged last into every subsequent 1-min bar. For a LULD-halted or simply non-printing lane-2 name, the system fabricates O=H=L=C flat bars (volume honestly 0 via the cum-vol delta) that are indistinguishable from a real quiet tape~
    EVIDENCE: atlas/collect/tradier_data.py:122-138 (no *_date fields parsed); atlas/hunter/feed.py:268-275 (ts_epoch=now=self.clock_fn(), i.e. poll time); atlas/hunter/feed.py:160-193 (on_tick folds any last>0 into the forming bar); scripts/run_options_shadow.py:1015-1025 (possible_halt: held single names, >=600s, journal-only).
    REPRO: 10s poll cadence -> 6 identical phantom ticks/min; a 5-minute halt produces 5 fictional flat bars; with mu_window_min of, say, 10 the trajectory estimator's return vector becomes all-zeros (mu_hat=0, t_stat=0) - feeding rules g/h a 'no drift, no evidence' picture manufactured by the feed, not the market.
    FIX: Parse trade_date into TQuote; in HunterFeed.poll drop (or flag) ticks whose trade_date did not advance since the previous poll for that symbol, and use trade_date (not poll time) as the tick timestamp so the bar bucket reflects when the trade actually happened.
- [medium] TRADIER-FEED-4 Vendor-null and true-zero are the same 0.0 - a genuinely zero-bid book reads as 'no quote', un-marking a collapsing position and under-recording the ~
    CLAIM: _f defaults missing fields to 0.0 and TQuote/TOption carry no presence markers, so downstream code cannot distinguish 'Tradier returned nothing' from 'the market bids 0.00'. _fresh_option_quote requires bid>0 on both the quotes and chain paths, so a collapsed long option (real book 0.00 x 0.05) yields None -> the position stops being marked (mark_no_quote streak), the exit ladder never evaluates the collapse, and th~
    EVIDENCE: atlas/collect/tradier_data.py:113-119 (_f default 0.0), :134 (bid=_f(q.get('bid'))); scripts/run_options_shadow.py:857-869 (both quote paths demand bid>0), :988-1003 (quote None -> journal + continue, no engine call), :903-906 (_force_exit degrades to last stored mark's bid).
    REPRO: 0DTE long put entered at 0.45; book collapses to 0.00 x 0.05 at 14:30 with the last good mark (bid 0.10) at 14:25. Forced flat later books sell-at-0.10 = -$35/contract on WORST instead of the true -$45 (22% of the loss unrecorded). On an N=25 lane verdict, one such trade shifts mean net by +$0.40/trade and inflates PF.
    FIX: Make TOption/TQuote bid/ask Optional (or add a has_nbbo flag distinguishing null from 0.0); treat a REAL 0.00 x ask book as a valid, markable quote (mid=max(bid,0) logic at runner:1006 already handles it) and reserve the no-quote path for genuinely absent NBBO.
- [medium] TRADIER-FEED-5 Backfill is RTH-only (session_filter='open') while live polling starts pre-open with no lower gate - restart vs continuous runs disagree on session V~
    CLAIM: get_timesales hardcodes session_filter='open', so a mid-day restart seeds LiveBarBuilder with 09:30+ bars only; but the live tick path has no lower session gate and the scheduled task starts 08:30 ET, so a from-open run commits premarket bars (SPY/QQQ/IWM trade premarket with real cum-volume deltas) into the VWAP/ATR accumulators. The same trading day therefore has two different session VWAPs depending on process hi~
    EVIDENCE: atlas/collect/tradier_data.py:335-336 (session_filter: 'open' hardcoded, no parameter); atlas/hunter/feed.py:287-289 (backfill start=f'{day} 09:30'); scripts/run_options_shadow.py:1159-1169 (only the close gates polling - nothing blocks pre-open ticks); atlas/hunter/feed.py:121-130 (premarket bars enter cum_pv/cum_v and the ATR arrays vi~
    REPRO: Gap-up day: 3M premarket shares at ~626.50 then 40M RTH shares at ~628.20 by 10:00. Continuous run svwap = (3x626.50+40x628.20)/43 = 628.08; restarted run svwap = 628.20. Any bar closing between 628.08 and 628.20 is 'above VWAP' to one process and 'below VWAP' to the other - a 12-cent decision-flipping band created purely by process hist~
    FIX: Pick one session definition: either gate builder input to minute>=570 in the runner (RTH-only everywhere, matching backfill) or expose session_filter and backfill 'all' from the premarket open. The RTH-only gate is the smaller, cohort-neutral change.
- [low] TRADIER-FEED-6 last<=0 fallback fabricates a price (close -> prevclose) with no provenance flag
    CLAIM: When Tradier returns a null/zero last, parse_quotes_batch substitutes close-else-prevclose into TQuote.last with nothing marking it synthetic, so one vendor hiccup can inject a fabricated-price tick (timestamped 'now' by HunterFeed) into the bar builder, and the lane-2 gap scan reads gap=0 for such names (silent candidate suppression - honest arithmetic, wrong premise).
    EVIDENCE: atlas/collect/tradier_data.py:130-132 (`if last <= 0: last = _f(q.get('close')) or _f(q.get('prevclose'))`); consumed at atlas/hunter/feed.py:272 (Tick.last) and scripts/run_options_shadow.py:507 (gap computed from q.last).
    REPRO: Vendor null on a name with prevclose=32.10 at 10:14 -> TQuote.last=32.10 -> one 10:14 bar O=H=L=C=32.10 regardless of the actual tape; if the real market was 30.80 the phantom bar prints a +4.2% one-minute spike-and-revert into ATR/HOD context.
    FIX: Keep the fallback for display/context uses but add a last_is_stale flag (or set last=0 and let on_tick's last<=0 guard reject it) so the bar builder never ingests a fabricated print.
- [low] TRADIER-FEED-7 Layered rate caps are incoherent: the documented 60/min shadow share only binds HunterFeed polls; direct calls ride a 100/min client cap, over-commit~
    CLAIM: The intent ('runner passes tradier_self_cap_per_min = 60 of the account's 120/min') is enforced only for poll/backfill via HunterFeed._budget_ok; chain fetches, expirations, and per-position mark quotes go straight to TradierData whose own throttle defaults to 100/min - so the shadow's true worst case is 100/min, which plus hunter.yaml's 30 and the owner's own scans exceeds 120 and invites account-level 429s exactly on t~
    EVIDENCE: atlas/collect/tradier_data.py:258 (MAX_PER_MINUTE=100), :292-300 (blocking sleep); scripts/run_options_shadow.py:210 (max_per_minute default 100), :1191-1192 (cap 60 applied only to HunterFeed); direct-call sites bypassing the 60 budget: :503, :522, :667, :683, :857, :863; docs/OPTIONS_SYSTEM_FLOW.md:237 states the 60-of-120 intent.
    REPRO: Steady state: 6 quote polls/min + 3 positions x 1 mark/min (60s cadence) + a signal burst of 4 chain requests = ~13/min - far under 100, so the blocking-sleep path is unreachable today; but the ENFORCED ceiling (100+30=130) exceeds the account's 120 by 8%, meaning the protection that exists cannot actually prevent account 429s in the wor~
    FIX: Pass the same 60 (or cfg value) as max_per_minute when constructing TradierData in tradier_from_yaml, or route all runner calls through one budget object; keep the account arithmetic in one place.
- [low] TRADIER-FEED-8 from_local_config violates its returns-None contract (only OSError caught) and reads utf-8 instead of utf-8-sig; the launcher preflight uses this str~
    CLAIM: A corrupted YAML raises yaml.YAMLError and a non-scalar timeout_seconds raises TypeError, both escaping from_local_config despite its 'or None (caller degrades)' contract; the file is read with plain utf-8 where the post-audit machine law is utf-8-sig on every config read. The runner sidesteps all of this with its own tradier_from_yaml, so the strict parser survives only in the launcher preflight - where a parse cra~
    EVIDENCE: atlas/collect/tradier_data.py:277-279 (except OSError only; read_text('utf-8')), :286-289 (except ValueError misses TypeError); scripts/run_options_shadow.py:193-212 (tradier_from_yaml: utf-8-sig, catches yaml.YAMLError, notes 'from_local_config handles neither'); scripts/launch_options_day.ps1:79-88 (preflight builds via from_local_conf~
    REPRO: echo 'token: [unterminated' > tradier_shadow.local.yaml -> yaml.scanner.ScannerError escapes from_local_config; the ps1 preflight exits nonzero and pages 'Tradier token WARN' while build_tradier_client in the runner returns a healthy client from tradier.local.yaml seconds later.
    FIX: Widen from_local_config's except to (OSError, yaml.YAMLError, TypeError, ValueError), read utf-8-sig, and point the preflight at tradier_from_yaml so one parser defines token validity.
- [low] TRADIER-FEED-9 TOption.mid falls back to a possibly hours-stale last; the unguarded consumer is the EOD IV snapshot's ATM solver
    CLAIM: mid returns 'last' whenever the book is not two-sided, so snapshot_iv's ATM fallback solver (used when ORATS IV is absent) can solve an implied vol from a stale print against the CURRENT underlying price, writing a fictional atm_iv row into the archive that later shifts entry-time iv_rank. The live selector is immune (its no_quote gate requires bid>0 and ask>=bid before mid is used), which correctly confines this to~
    EVIDENCE: atlas/collect/tradier_data.py:81-86 (mid fallback to last); scripts/snapshot_iv.py:59-64 (mid>0 -> implied_vol solve, no two-sided check); safe consumer: atlas/options/selector.py:150-155; archive feeds entry iv_rank at scripts/run_options_shadow.py:716-718.
    REPRO: ATM strike book 0 x 0 with last=3.10 printed at 10:02; underlying has since moved from 100.00 to 97.50 by the snapshot. Solving a 2-DTE call at S=97.50, K=100, price=3.10 forces IV toward ~1.9x the true level (the 3.10 print embodied ~2.50 of intrinsic+time at S=100 that no longer exists) - one poisoned tenor row in the rank window.
    FIX: In _atm_iv_for_expiration, only solve from o.mid when o.bid>0 and o.ask>=o.bid (mirror the selector's no_quote gate); otherwise return 0.0 and let the caller drop the row.
- [low] TRADIER-FEED-10 get_daily_history over-fetches its window and has no live options-path consumer (dead surface)
    CLAIM: The 1.6x+10 calendar-day window means days=60 returns ~72 trading bars (callers assuming exactly N bars would over-average), but no active shadow-path code calls it - only attic (archived equity) code does - so it is dead weight in the vendored client rather than a live defect.
    EVIDENCE: atlas/collect/tradier_data.py:363-368 (start = end - timedelta(days=int(days*1.6)+10), no truncation of the parsed list); repo grep: zero call sites outside atlas/collect/tradier_data.py itself, tests, and attic/.
    REPRO: days=60 -> window of int(60*1.6)+10 = 106 calendar days ≈ 72-73 trading sessions returned; a naive 'mean of returned bars' 60-day average would actually be a 72-day average (20% longer lookback than requested).
    FIX: Either truncate to the requested count (return parse_daily_history(...)[-days:]) or annotate/remove it as attic-only surface so a future consumer doesn't inherit the ~20% window overshoot silently.

## CRITIC
- (critic died on session limit)